anchor=1663829
---SNIPPET---
itle||'');\\n}\\nfunction dayOrderRC67(day){if(day==='R\\u00fcckstand')return -1;const order=['Montag','Dienstag','Mittwoch','Donnerstag','Freitag'];let i=order.indexOf(day||'');return i>=0?i:99}\\nfunction areaOrderRC67(area){\\n  const a=norm(area||'');\\n  const exact=['picken','abd erstellen','anmelden','auftrage kontrollieren','zusatzaufgaben'];\\n  let i=exact.indexOf(a);if(i>=0)return i;\\n  const rest=['manuelle auftrage','sendung','cmr','ladeliste','ups','zoll','sop','academy','vertretung','sonstiges'];\\n  i=rest.findIndex(x=>a.includes(x));\\n  return i>=0?20+i:80;\\n}\\nfunction areaLabelRC67(area){return area&&String(area).trim()?area:'Sonstige Aufgaben'}\\nfunction groupByRC67(arr,fn){\\n  const m=new Map();\\n  arr.forEach(x=>{const k=fn(x); if(!m.has(k))m.set(k,[]); m.get(k).push(x)});\\n  return [...m.entries()];\\n}\\nfunction renderTaskGroupsRC67(list,selectedDay){\\n  const single=selectedDay!=='Alle Tage';\\n  if(!single){\\n    const days=['R\\u00fcckstand','Montag','Dienstag','Mittwoch','Donnerstag','Freitag','Ohne Tag'];\\n    return days.map(d=>{\\n      const arr=d==='R\\u00fcckstand'\\n        ? list.filter(isTaskBacklog)\\n        : d==='Ohne Tag'\\n        ? list.filter(t=>!['Montag','Dienstag','Mittwoch','Donnerstag','Freitag','R\\u00fcckstand'].includes(t.day||''))\\n        : list.filter(t=>t.day===d && !isTaskBacklog(t));\\n      if(!arr.length)return '';\\n      const open=arr.filter(t=>t.status!=='erledigt').length;\\n      return `<section class=\\\"task-day-block ${d==='R\\u00fcckstand'?'backlog-block':''}\\\"><div class=\\\"task-day-title\\\"><h2>${esc(d)}</h2><div><span class=\\\"pill blue\\\">${arr.length} Aufgaben</span> <span class=\\\"pill orange\\\">${open} offen</span></div></div>${renderAreaGroupsRC67(arr,false)}</section>`;\\n    }).join('');\\n  }\\n  const label=selectedDay==='Heute'?germanWeekday():selectedDay;\\n  return `<section class=\\\"task-day-block ${label==='R\\u00fcckstand'?'backlog-block':''}\\\"><div class=\\\"task-day-title\\\"><h2>${esc(label)}</h2><span class=\\\"pill blue\\\">${list.length} Aufgaben</span></div>${renderAreaGroupsRC67(list,true)}</section>`;\\n}\\nfunction renderAreaGroupsRC67(arr,defaultOpen){\\n  return groupByRC67(arr,t=>taskGroupNameRC874(t)).sort((a,b)=>areaOrderRC67(a[0])-areaOrderRC67(b[0])||a[0].localeCompare(b[0])).map(([area,tasks])=>{\\n    const openCount=tasks.filter(t=>t.status!=='erledigt').length,isOpen=taskGroupOpenRC874(area,defaultOpen),cards=tasks.map(renderTask).join('');\\n    return `<details class=\\\"task-area-block task-area-details\\\" data-task-group=\\\"${esc(area)}\\\" ${isOpen?'open':''}><summary class=\\\"task-area-title\\\"><div class=\\\"task-area-summary-main\\\"><span class=\\\"task-area-chevron\\\">\\u203a</span><h3>${esc(area)}</h3></div><div><span class=\\\"pill gray\\\">${tasks.length} gesamt</span> <span class=\\\"pill orange\\\">${openCount} offen</span></div></summary><div class=\\\"task-group-body\\\">${isOpen?cards:''}</div>${isOpen?'':`<template data-task-group-template=\\\"1\\\">${cards}</template>`}</details>`;\\n  }).join('');\\n}\\nfunction setTaskDay(day){state.taskDay=day;render()}\\nfunction toggleCompletedTasksVisibility(){state.taskShowCompleted=false;return false}\\nfunction taskHasVisibleTime(t){\\n  const time=String(t&&t.time||'').trim();\\n  if(!time)return false;\\n  const title=norm((t.title||'')+' '+(t.linkedCustomer||''));\\n  const note=norm(t.note||'');\\n  const area=norm(t.area||'');\\n  if(t.manualTime===true || note.includes('manuell erstellt'))return true;\\n  if(note.includes('fixzeit') || note.includes('feste zeit') || note.includes('vorgegeben'))return true;\\n  if(time==='11:00' && (area.includes('abd') || title.includes('abd')))return true;\\n  if(time==='13:00' && /(esysco|neff|bsh|gaggenau)/.test(title))return true;\\n  return false;\\n}\\nfunction taskTimePill(t){\\n  return taskHasVisibleTime(t)?`<span class=\\\"pill blue\\\">${esc(t.time)}</span>`:'';\\n}\\nfunction renderTask(t){\\nif(!taskMeaningfulRC818(t)||taskLinkedTestShipmentRC818(t))return '';\\nlet linked=t.linkedCustomer?`<span class=\\\"pill yellow\\\">${esc(t.linkedCustomer)}</span>`:'';\\nlet done=taskCompletedRC777(t);\\nlet backlog=isTaskBacklog(t);\\nlet pr=t.priority==='high'?'red':t.priority==='low'?'green':'orange';\\nlet timePill=taskTimePill(t);\\nlet dayLabel=backlog?'R\\u00fcckstand':(t.day||'Ohne Tag');\\nlet backlogLine=backlog?`<p class=\\\"task-backlog-note\\\">Offen aus Vorwoche \\u00b7 urspr\\u00fcnglicher Tag: ${esc(t.originalDay||'unbekannt')}${t.originalWeekKey?' \\u00b7 '+esc(t.originalWeekKey):''}</p>`:'';\\nlet originalPill=backlog?`<span class=\\\"pill red\\\">Vorwoche offen</span>${t.originalDay?`<span class=\\\"pill gray\\\">${esc(t.originalDay)}</span>`:''}`:'';\\nlet recurringPill=!backlog&&isWeeklyRecurringTask(t)?`<span class=\\\"pill purple\\\">${esc(recurringDisplayLabelRC818(t))} \\u00b7 ${esc(t.weekKey||taskWeekKey())}</span>`:'';\\nreturn `<article class=\\\"task-card ${done?'done':''} ${backlog?'backlog':''} ${esc(t.priority||'medium')}\\\" onclick=\\\"toggleTaskCard(event,'${esc(t.id)}')\\\" title=\\\"Anklicken zum ${done?'Wieder \\u00f6ffnen':'Erledigen'}\\\">\\n  <div class=\\\"task-card-top\\\"><span class=\\\"pill ${pr}\\\">${t.priority==='high'?'Hoch':t.priority==='low'?'Niedrig':'Normal'}</span><span class=\\\"task-status ${done?'done':'open'}\\\">${done?'Erledigt':'Offen'}</span></div>\\n  <h3>${esc(t.title)}</h3>\\n  <div><span class=\\\"pill blue\\\">${esc(dayLabel)}</span><span class=\\\"pill gray\\\">${esc(t.area||'Aufgabe')}</span>${timePill}${linked}${originalPill}${recurringPill}</div>\\n  ${backlogLine}\\n  <div class=\\\"task-clickhint\\\">${done?'erneut klicken = \\u00f6ffnen':'klicken = erledigt'}</div>\\n</article>`}\\nfunction toggleTaskCard(ev,id){ev.preventDefault();ev.stopPropagation();toggleTask(id)}\\nfunction toggleTask(id){\\n  if(!requireEdit())return;\\n  const t=state.tasks.find(x=>String(x.id)===String(id));\\n  if(!t){alert('Aufgabe nicht gefunden.');return;}\\n  t.status=t.status==='erledigt'?'offen':'erledigt';\\n  t.done=t.status==='erledigt';t.completed=t.done;\\n  t.doneAt=t.status==='erledigt'?new Date().toISOString():'';\\n  t.completedAt=t.doneAt;\\n  t.doneBy=t.status==='erledigt'?(currentUser?.name||currentUser?.user||'Benutzer'):'';t.completedBy=t.doneBy;\\n  if(t.status==='erledigt'&&isWeeklyRecurringTask(t))t.completedWeekKey=taskWeekKey();else if(t.status!=='erledigt')t.completedWeekKey='';\\n  audit(t.status==='erledigt'?'Aufgabe erledigt':'Aufgabe wieder ge\\u00f6ffnet', t.title||id);\\n  save();\\n  render();\\n}\\nfunction showAllTasks(){state.taskFilter='Alle Aufgaben anzeigen';state.taskDay='Alle Tage';state.taskSearch='';render()}\\nfunction addManualTask(){if(!requireEdit())return;let title=prompt('Name der Aufgabe'); if(!title)return;let day=prompt('Wochentag', germanWeekday())||germanWeekday();let time=prompt('Uhrzeit optional', '')||'';let owner=prompt('Zust\\u00e4ndig', currentUser.name)||currentUser.name;state.tasks.unshift({id:uid('T'),day,time,title,owner,area:'Manuell',status:'offen',priority:'medium',note:'Manuell erstellt',manualTime:!!time});audit('Manuelle Aufgabe erstellt',title);save();render()}\\nfunction userNames(){return users.map(u=>u.name||u.user)}\\nfunction norm(v){return String(v??'').toLowerCase().normalize('NFD').replace(/[\\\\u0300-\\\\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}\\nfunction customerOption(c){if(!c)return '';return `${c.account||c.id} \\u00b7 ${c.name} \\u00b7 ${c.country||''}`}\\nfunction findCustomerBySearch(value){const n=norm(value);if(!n)return null;return state.customers.find(c=>norm(customerOption(c))===n)||state.customers.find(c=>{const all=norm([c.account,c.customerNumber,c.id,c.name,c.country,c.address,c.mail,c.mailLink,c.note,c.language,c.carrier].join(' '));return all.includes(n)||n.includes(norm(c.account||''))||n.includes(norm(c.name||''));})||null}\\nfunction shipmentAddressFromObject(o){if(!o||typeof o!=='object')return '';const street=[o.street||o.strasse,o.houseNumber||o.hausnummer].filter(Boolean).join(' ').trim();const city=[o.zip||o.postalCode||o.plz,o.city||o.ort].filter(Boolean).join(' ').trim();return String(o.formattedAddress||o.fullAddress||o.addressText||o.address||o.deliveryAddress||o.recipientAddress||[street,city,o.country||o.land].filter(Boolean).join('\\\\n')).trim()}\\nfunction shipmentRecipientAddress(s,c){s=s||{};c=c||{};const nested=[s.locationData,s.siteData,s.location].find(x=>x&&typeof x==='object')||{};const selected=String(s.locationId||s.selectedLocationId||s.siteId||s.destinationId||nested.id||nested.locationId||nested.selectedLocationId||nested.siteId||nested.destinationId||'').trim();const locations=[].concat(Array.isArray(c.locations)?c.locations:[],Array.isArray(c.sites)?c.sites:[],Array.isArray(c.standorte)?c.standorte:[],Array.isArray(c.deliveryLocations)?c.deliveryLocations:[],Array.isArray(c.addresses)?c.addresses:[]);const locId=l=>String(l&&(l.id||l.locationId||l.selectedLocationId||l.siteId||l.destinationId||l.code||l.number)||'').trim();const locName=l=>norm(l&&(l.name||l.locationName||l.siteName||l.standort||l.city||l.ort)||'');let selectedLocation=null;if(selected)selectedLocation=locations.find(l=>locId(l)===selected)||null;if(!selectedLocation&&s.locationName)selectedLocation=locations.find(l=>locName(l)===norm(s.locationName))||null;let address=String(s.recipientAddress||s.deliveryAddress||s.destinationAddress||s.formattedAddress||s.fullAddress||s.addressText||shipmentAddressFromObject(nested)||shipmentAddressFromObject(selectedLocation)||shipmentAddressFromObject(c)).trim();if(address){s.recipientAddress=address;s.deliveryAddress=address;s.destinationAddress=address;if(selectedLocation){const id=locId(selectedLocation);if(id){s.locationId=id;s.selectedLocationId=id;s.siteId=id;s.destinationId=id}s.locationData={...selectedLocation};s.siteData={...selectedLocation};s.location={...selectedLocation}}}return address}\\nfunction cleanShipmentRef(v){return String(v||'').toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6)}\\nfunction validShipmentRef(v){return /^[A-Z0-9]{6}$/.test(String(v||''))}\\nfunction randomRef6(){const chars='ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';let out='';const cryptoObj=window.crypto||window.msCrypto;if(cryptoObj&&cryptoObj.getRandomValues){const arr=new Uint32Array(6);cryptoObj.getRandomValues(arr);for(const n of arr)out+=chars[n%chars.length];}else{for(let i=0;i<6;i++)out+=chars[Math.floor(Math.random()*chars.length)];}return out}\\nfunction generateShipmentRef(c){let existing=[];try{existing=(state&&state.shipments||[]).map(x=>String(x.ref||'').toUpperCase())}catch(e){}for(let i=0;i<5000;i++){const ref=randomRef6();if(!existing.includes(ref))return ref}return randomRef6()}\\nfunction ensureShipmentReference(force=false){const c=selectedCustomer();const ref=cleanShipmentRef(state.shipment.ref);if(force||!validShipmentRef(ref)||state.shipment.refAuto!==false){state.shipment.ref=generateShipmentRef(c);state.shipment.refAuto=true}else{state.shipment.ref=ref}state.shipment.customerSearch=customerOption(c)}\\nfunction newShipment(){if(!requireEdit())return;if(!state.customers.length){alert('Bitte zuerst einen Kunden im Bereich Kunden & Dokumente anlegen.');window.ExportHUBRC325.route('customers','canonical-module');return;}const keepId=state.shipment.customerId||state.selectedCustomerId||state.customers[0]?.id;const c=state.customers.find(x=>x.id===keepId)||state.customers[0]||EMPTY_CUSTOMER;state.shipment=emptyShipment(c.id||'');state.shipment.ref=generateShipmentRef(c);state.shipment.refAuto=true;state.shipment.deliveryNotes='';state.shipment.deliveryFiles=[];state.shipment.carrier='';state.shipment.incoterm='';state.shipment.goodsDescription='';state.shipment.comments='';state.shipment.nonEuCheck=false;state.shipment.addressConfirmed=false;state.shipment.freigabe='offen';state.shipment.rows=[];state.activeShipmentId='current';audit('Neue Sendung gestartet','Alle Felder geleert, neue Referenz '+state.shipment.ref);render();}\\nfunction applyCustomerSearch(){const v=(document.getElementById('shipmentCustomerSearch')||{}).value||state.shipment.customerSearch||'';const c=findCustomerBySearch(v);if(!c)return alert('Kunde/Kundennummer nicht gefunden. Bitte Kundennummer, Name oder Land pr\\u00fcfen.');selectShipmentCustomer(c.id)}\\nfunction shipment(){\\nif(!state.customers.length){return head('\\ud83d\\ude9a Sendung erstellen','Kundennummer suchen, 6-stellige Referenz automatisch erstellen, Lieferscheine optional anh\\u00e4ngen, Outlook-Avisierungsmail je Kundennummer erstellen und Ladeliste/CMR erzeugen.')+`<div class=\\\"notice\\\"><b>Noch keine Kunden vorhanden.</b><br>Bitte zuerst unter <b>Kunden & Dokumente</b> einen echten Kunden anlegen. Danach kannst du Sendung, Ladeliste und CMR erstellen.</div><button class=\\\"btn\\\" onclick=\\\"window.ExportHUBRC325.route('customers','canonical-module')\\\">Kunden anlegen</button>`}\\nlet s=state.shipment;\\nlet c=selectedCustomer();if(s.__rc562Fresh!==true||s.customerId){if(s.customerId){c=(state.customers||[]).find(x=>String(x.id)===String(s.customerId))||c}if(!s.customerId&&c&&c.id){s.customerId=c.id}s.customerSearch=s.customerSearch||customerOption(c);if(!validShipmentRef(s.ref)){s.ref=generateShipmentRef(c);s.refAuto=true}}else{c=EMPTY_CUSTOMER}s.deliveryFiles=s.deliveryFiles||[]\\nconst refOk=validShipmentRef(s.ref);\\nconst recipientAddress=shipmentRecipientAddress(s,c);\\nconst missing=!recipientAddress;\\nconst completionMissing=shipmentCompletionMissing(s);\\nconst completionOk=!completionMissing.length;\\nconst saved=state.shipments.slice(0,8);return head('\\ud83d\\ude9a Sendung erstellen','Kundennummer suchen, 6-stellige Referenz automatisch erstellen, Lieferscheine optional anh\\u00e4ngen, Outlook-Avisierungsmail je Kundennummer erstellen und Ladeliste/CMR erzeugen.')+`<div class=\\\"grid2\\\"><div class=\\\"card\\\"><h3>Kunde & Grunddaten</h3><label class=\\\"field\\\">Kundennummer / Kunde suchen<input id=\\\"shipmentCustomerSearch\\\" list=\\\"customerList\\\" value=\\\"${esc(s.customerSearch)}\\\" oninput=\\\"setShipmentCustomerSearchAuto(this.value)\\\" onchange=\\\"applyCustomerSearch()\\\" onkeydown=\\\"if(event.key==='Enter'){event.preventDefault();applyCustomerSearch()}\\\"><datalist id=\\\"customerList\\\">${state.customers.map(cu=>`<option value=\\\"${esc(customerOption(cu))}\\\"></option>`).join('')}</datalist></label><div class=\\\"btn-row-spaced\\\"><button class=\\\"btn\\\" onclick=\\\"newShipment()\\\">+ Neue Sendung</button><button class=\\\"ghost\\\" onclick=\\\"applyCustomerSearch()\\\">Kunde \\u00fcbernehmen</button><button class=\\\"ghost\\\" onclick=\\\"ensureShipmentReference(true)\\\">Neue 6-Zeichen-Referenz</button><div class=\\\"new-shipment-note\\\">\\u201eNeue Sendung\\u201c leert Lieferscheine, Collis, Warenbeschreibung, Bemerkung und erzeugt eine neue 6-stellige Referenz.</div></div><label class=\\\"field\\\">Kunde<select onchange=\\\"selectShipmentCustomer(this.value)\\\">${state.customers.map(cu=>`<option value=\\\"${esc(cu.id)}\\\" ${cu.id===s.customerId?'selected':''}>${esc(cu.account||cu.id)} \\u00b7 ${esc(cu.name)} \\u00b7 ${esc(cu.country)}</option>`).join('')}</select></label><div class=\\\"${missing?'badbox':'okbox'}\\\"><b>Empf\\u00e4ngeradresse:</b><pre style=\\\"white-space:pre-wrap;margin:8px 0 0\\\">${esc(recipientAddress||'Adresse fehlt')}</pre></div><label class=\\\"checkrow\\\"><input type=\\\"checkbox\\\" ${s.addressConfirmed?'checked':''} onchange=\\\"state.shipment.addressConfirmed=this.checked\\\"><div>Adresse gepr\\u00fcft und best\\u00e4tigt</div></label><label class=\\\"field\\\">Sendungsreferenz / Referenznummer<input value=\\\"${esc(s.ref)}\\\" maxlength=\\\"6\\\" pattern=\\\"[A-Z0-9]{6}\\\" oninput=\\\"this.value=this.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6)\\\" onchange=\\\"setShipmentRef(this.value)\\\" placeholder=\\\"6 Zeichen A-Z/0-9\\\"></label><label class=\\\"field\\\">Lieferscheine / DNCs manuell (optional)<textarea onchange=\\\"state.shipment.deliveryNotes=this.value\\\" placeholder=\\\"Optional: eine DNC/Lieferscheinnummer je Zeile\\\">${esc(s.deliveryNotes)}</textarea></label><label class=\\\"field\\\">Lieferscheine hochladen / anh\\u00e4ngen (optional)<input type=\\\"file\\\" multiple onchange=\\\"addDeliveryNoteFiles(this.files)\\\" accept=\\\".pdf,.txt,.csv,.xlsx,.xls,.doc,.docx,.msg,.eml,image/*,application/pdf\\\"></label>${deliveryFileList(s)}<label class=\\\"field\\\">Verlader<input value=\\\"${esc(s.loader||'')}\\\" onchange=\\\"state.shipment.loader=this.value\\\" placeholder=\\\"Name Verlader\\\"></label><label class=\\\"checkrow\\\"><input type=\\\"checkbox\\\" ${s.nonEuCheck?'checked':''} onchange=\\\"state.shipment.nonEuCheck=this.checked\\\"><div>Drittland-/Zolldokumente gepr\\u00fcft, falls erforderlich</div></label>${missing?'<div class=\\\"badbox\\\">Adresse fehlt. Sendungserstellung blockiert.</div>':!s.addressConfirmed?'<div class=\\\"notice\\\">Adresse muss aktiv best\\u00e4tigt werden.</div>':!refOk?'<div class=\\\"badbox\\\">Referenznummer muss exakt 6 Zeichen A-Z/0-9 haben.</div>':!completionOk?shipmentCompletionBox(s):'<div class=\\\"okbox\\\">Sendung kann gespeichert werden. Ladeliste und CMR sind verf\\u00fcgbar.</div>'}</div><div class=\\\"card\\\"><h3>Collis, Ma\\u00dfe, Gewicht, Lademeter</h3><div id=\\\"rowsBox\\\">${s.rows.map(renderRow).join('')}</div><button class=\\\"ghost\\\" onclick=\\\"addShipmentRow()\\\">+ Zeile</button><div class=\\\"summaryGrid\\\">${shipmentSummaryBoxes(s)}</div>${shipmentCompletionBox(s)}<label class=\\\"field\\\">Warenbeschreibung f\\u00fcr CMR Feld 9<textarea onchange=\\\"state.shipment.goodsDescription=this.value\\\" placeholder=\\\"z. B. Waren laut Rechnung / Kunststoffteile / teile\\\">${esc(s.goodsDescription||'Waren laut Rechnung/Ladeliste')}</textarea></label><label class=\\\"field\\\">Bemerkung<textarea onchange=\\\"state.shipment.comments=this.value\\\">${esc(s.comments)}</textarea></label></div></div><div class=\\\"card\\\"><h3>Gespeicherte Sendungen</h3><div class=\\\"toolbar\\\"><button class=\\\"ghost\\\" onclick=\\\"window.ExportHUBRC325.route('shipmentoverview','canonical-module')\\\">Sendungs\\u00fcbersicht \\u00f6ffnen</button></div>${saved.length?`<table class=\\\"table\\\"><tr><th>Referenz</th><th>Kunde</th><th>Datum</th><th>Status</th><th>Aktion</th></tr>${saved.map(x=>`<tr><td><b>${esc(x.ref)}</b></td><td>${esc((x.customer&&x.customer.name)||'')}</td><td>${esc(x.created||'')}</td><td>${esc(x.freigabe||'offen')}</td><td><button class=\\\"ghost\\\" onclick=\\\"loadShipment('${esc(x.id)}')\\\">\\u00dcbernehmen</button> <button class=\\\"soft\\\" onclick=\\\"approveShipment('${esc(x.id)}')\\\">Freigeben</button> <button class=\\\"ghost\\\" onclick=\\\"showSavedCmr('${esc(x.id)}')\\\">Ladeliste/CMR</button></td></tr>`).join('')}</table>`:'<p class=\\\"muted\\\">Noch keine Sendung gespeichert.</p>'}</div>`}\\n\\nfunction shipmentEffectiveStatus(s){\\n  s=s||{};\\n  const raw=norm(s.status||s.freigabe||'offen');\\n  if(raw.includes('erledigt')||raw.includes('abgeschlossen')||raw.includes('freigegeben'))return 'erledigt';\\n  if(raw.includes('\\u00fcberf\\u00e4llig')||raw.includes('ueberfaellig'))return '\\u00fcberf\\u00e4llig';\\n  const d=String(s.pickupDate||'');\\n  if(d && d<todayISO())return '\\u00fcberf\\u00e4llig';\\n  return 'offen';\\n}\\nfunction shipmentStatusLabel(s){const st=shipmentEffectiveStatus(s);return st==='erledigt'?'Erledigt':st==='\\u00fcberf\\u00e4llig'?'\\u00dcberf\\u00e4llig':'Offen'}\\nfunction shipmentStatusClass(s){const st=shipmentEffectiveStatus(s);return st==='erledigt'?'done':st==='\\u00fcberf\\u00e4llig'?'overdue':'open'}\\nfunction shipmentStatusPill(s){return `<span class=\\\"shipment-status ${shipmentStatusClass(s)}\\\">${esc(shipmentStatusLabel(s))}</span>`}\\nfunction shipmentDocs(s){\\n  const notes=String(s&&s.deliveryNotes||'').split(/\\\\n|,|;/).map(x=>x.trim()).filter(Boolean);\\n  const files=((s&&s.deliveryFiles)||[]).map(f=>f.name||f.filename||'').filter(Boolean);\\n  return [...new Set([...notes,...files])];\\n}\\nfunction shipmentPods(s){return ((s&&s.podFiles)||[]).map(f=>f.name||f.filename||'').filter(Boolean)}\\nfunction shipmentSearchBlob(s){\\n  const c=s.customer||{};\\n  return norm([s.ref,s.created,s.pickupDate,s.freigabe,s.status,c.name,c.account,c.customerNumber,c.country,shipmentDocs(s).join(' '),shipmentPods(s).join(' '),s.comments,s.carrier,s.licensePlate,s.loader].join(' '));\\n}\\nfunction filteredShipments(){\\n  const query=norm(state.shipmentOverviewSearch||'');\\n  const filter=state.shipmentOverviewStatus||'Alle';\\n  let list=(state.shipments||[]).slice();\\n  if(query)list=list.filter(s=>shipmentSearchBlob(s).includes(query));\\n  if(filter&&filter!=='Alle')list=list.filter(s=>shipmentEffectiveStatus(s)===filter.toLowerCase());\\n  return list.sort((a,b)=>String(b.created||'').localeCompare(String(a.created||'')));\\n}\\nfunction podListHtml(s){\\n  const files=s.podFiles||[];\\n  if(!files.length)return '<span class=\\\"muted\\\">kein POD</span>';\\n  return `<div class=\\\"pod-list\\\">${files.map(f=>`<div class=\\\"pod-item\\\">\\ud83d\\udcce ${f.dataUrl?`<a href=\\\"${esc(f.dataUrl)}\\\" download=\\\"${esc(f.name||'POD')}\\\">${esc(f.name||'POD')}</a>`:esc(f.name||'POD')} <span class=\\\"muted\\\">${Math.round((f.size||0)/1024)} KB \\u00b7 ${esc(f.added||'')}</span> <button class=\\\"danger\\\" onclick=\\\"removePodFile('${esc(s.id)}','${esc(f.id)}')\\\">x</button></div>`).join('')}</div>`;\\n}\\nfunction readFileAsDataUrl(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file);});}\\nasync function addPodFiles(shipmentId,files){\\n  if(!requireEdit('shipmentoverview'))return;\\n  const s=(state.shipments||[]).find(x=>x.id===shipmentId);if(!s||!files||!files.length)return;\\n  s.podFiles=s.podFiles||[];\\n  let added=[];let tooLarge=[];\\n  for(const f of Array.from(files)){\\n    if(s.podFiles.some(x=>x.name===f.name&&x.size===f.size))continue;\\n    let item={id:uid('POD'),name:f.name,size:f.size,type:f.type||'Datei',added:new Date().toLocaleString('de-DE'),addedBy:currentUser?.name||currentUser?.user||'Benutzer'};\\n    if(f.size<=2.5*1024*1024){try{item.dataUrl=await readFileAsDataUrl(f);}catch(e){}}\\n    else tooLarge.push(f.name);\\n    s.podFiles.push(item);added.push(f.name);\\n  }\\n  if(added.length){s.podStatus='hochgeladen';s.podUploadedAt=new Date().toISOString();audit('POD hochgeladen',`${s.ref||''} \\u00b7 ${added.join(', ')}`);}\\n  save();render();\\n  if(tooLarge.length)alert('Diese POD-Dateien wurden nur als Dateiname gespeichert, weil sie f\\u00fcr den Browser-Speicher zu gro\\u00df sind:\\\\n- '+tooLarge.join('\\\\n- '));\\n}\\nfunction removePodFile(shipmentId,fileId){\\n  if(!requireEdit('shipmentoverview'))return;\\n  const s=(state.shipments||[]).find(x=>x.id===shipmentId);if(!s)return;\\n  s.podFiles=(s.podFiles||[]).filter(f=>f.id!==fileId);\\n  audit('POD entfernt',s.ref||shipmentId);save();render();\\n}\\nfunction markShipmentDone(id){\\n  if(!requireEdit('shipmentoverview'))return;\\n  const s=(state.shipments||[]).find(x=>x.id===id);if(!s)return;\\n  s.status='erledigt';s.freigabe='erledigt';s.completedAt=new Date().toISOString();s.completedBy=currentUser?.name||currentUser?.user||'Benutzer';\\n  audit('Sendung erledigt',s.ref||id);save();render();\\n}\\nfunction reopenShipment(id){\\n  if(!requireEdit('shipmentoverview'))return;\\n  const s=(state.shipments||[]).find(x=>x.id===id);if(!s)return;\\n  s.status='offen';s.freigabe='offen';s.completedAt='';s.completedBy='';\\n  audit('Sendung wieder ge\\u00f6ffnet',s.ref||id);save();render();\\n}\\n\\nfunction selectShipmentCustomer(id){state.shipment.customerId=id;state.selectedCustomerId=id;state.shipment.addressConfirmed=false;const c=selectedCustomer();state.shipment.customerSearch=customerOption(c);if(!validShipmentRef(state.shipment.ref)||state.shipment.refAuto!==false){state.shipment.ref=generateShipmentRef(c);state.shipment.refAuto=true}state.activeShipmentId='current';render()}\\nfunction deliveryFileList(s){const files=s.deliveryFiles||[];if(!files.length)return '<div class=\\\"notice\\\">Noch keine Lieferschein-Datei angeh\\u00e4ngt.</div>';return `<div class=\\\"okbox\\\"><b>Angeh\\u00e4ngte Lieferscheine:</b>${files.map(f=>`<div class=\\\"checkrow\\\"><div>\\ud83d\\udcce ${esc(f.name)} <span class=\\\"muted\\\">${Math.round((f.size||0)/1024)} KB \\u00b7 ${esc(f.added||'')}</span></div><button class=\\\"danger\\\" onclick=\\\"removeDeliveryFile('${esc(f.id)}')\\\">Entfernen</button></div>`).join('')}</div>`}\\nfunction totals(s){return (s.rows||[]).reduce((a,r)=>{a.count+=Number(r.count)||0;a.weight+=Number(r.weight)||0;a.ldm+=(Number(r.count)||0)*(Number(r.ldm)||0);return a},{count:0,weight:0,ldm:0})}\\nfunction shipmentSummaryBoxes(s){const t=totals(s);return `<div class=\\\"summaryBox\\\"><span>Collis</span><strong>${t.count}</strong></div><div class=\\\"summaryBox\\\"><span>Gewicht kg</span><strong>${t.weight.toFixed(1)}</strong></div><div class=\\\"summaryBox\\\"><span>Lademeter</span><strong>${t.ldm.toFixed(2)}</strong></div><div class=\\\"summaryBox\\\"><span>Zeilen</span><strong>${(s.rows||[]).length}</strong></div>`}\\n\\nfunction shipmentCompletionMissing(s){\\n  s=s||state.shipment||{};\\n  const copies=[s];\\n  const key=String(s.id||s.shipmentId||s.ref||s.reference||s.shipmentRef||'').toUpperCase();\\n  (state.shipments||[]).forEach(x=>{const xKey=String(x&&(x.id||x.shipmentId||x.ref||x.reference||x.shipmentRef)||'').toUpperCase();if(x&&x!==s&&key&&xKey===key)copies.push(x)});\\n  const num=v=>{const n=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(n)?n:0};\\n  const present=(obj,keys)=>keys.some(key=>obj&&obj[key]!==undefined&&obj[key]!==null&&String(obj[key]).trim()!=='');\\n  const firstPositive=(obj,keys)=>{for(const key of keys){if(obj&&obj[key]!==undefined&&obj[key]!==null&&String(obj[key]).trim()!==''){const n=num(obj[key]);if(n>0)return n}}return 0};\\n  const rowSources=[];\\n  copies.forEach(copy=>[copy.rows,copy.colli,copy.collis,copy.packages,copy.packagingRows,copy.shipmentRows,copy.colliRows,copy.items,copy.lines].forEach(list=>{if(Array.isArray(list)&&list.length)rowSources.push(list)}));\\n  const rowScore=list=>(list||[]).reduce((score,r)=>score+(r&&String(r.type||r.packaging||r.verpackung||r.packType||'').trim()?20:0)+(firstPositive(r,['count','qty','quantity','anzahl','colliCount','packages'])>0?16:0)+(firstPositive(r,['weight','gewicht','weightKg','totalWeight','totalWeightKg','grossWeight','bruttoGewicht','gesamtgewicht'])>0?12:0)+(present(r,['ldm','loadingMeter','lademeter','ldmPerUnit','ldmJeColli'])?8:0)+(present(r,['l','length','laenge','lengthCm'])?4:0)+(present(r,['w','width','breite','widthCm'])?4:0)+(present(r,['h','height','hoehe','heightCm'])?4:0),0)+(list||[]).length;\\n  rowSources.sort((a,b)=>rowScore(b)-rowScore(a));\\n  const rows=(rowSources[0]||[]).filter(Boolean);\\n  const masterFor=r=>{const name=norm(r&&(r.type||r.packaging||r.verpackung||r.packType)||'');return (SEED.colliTypes||[]).find(x=>norm(x&&x.name||'')===name)||null};\\n  const countOf=r=>firstPositive(r,['count','qty','quantity','anzahl','colliCount','packages']);\\n  const weightOf=r=>firstPositive(r,['weight','gewicht','weightKg','totalWeight','totalWeightKg','grossWeight','bruttoGewicht','gesamtgewicht']);\\n  const dimOf=(r,axis)=>{const keys=axis==='l'?['l','length','laenge','lengthCm']:axis==='w'?['w','width','breite','widthCm']:['h','height','hoehe','heightCm'];const direct=firstPositive(r,keys);if(direct>0)return direct;const master=masterFor(r);return master?num(master[axis]):0};\\n  const ldmOf=r=>{const keys=['ldm','loadingMeter','loadingMeters','lademeter','ldmPerUnit','ldmJeColli'];if(present(r,keys))return num(keys.map(k=>r&&r[k]).find(v=>v!==undefined&&v!==null&&String(v).trim()!==''));const master=masterFor(r);if(master&&master.ldm!==undefined&&master.ldm!==null)return num(master.ldm);const l=dimOf(r,'l'),w=dimOf(r,'w');return l>0&&w>0?(l*w/24000):0};\\n  const hasLdm=r=>present(r,['ldm','loadingMeter','loadingMeters','lademeter','ldmPerUnit','ldmJeColli'])||!!masterFor(r)||(dimOf(r,'l')>0&&dimOf(r,'w')>0);\\n  const rowCount=rows.reduce((sum,r)=>sum+countOf(r),0);\\n  const rowWeight=rows.reduce((sum,r)=>sum+weightOf(r),0);\\n  const rowLdm=rows.reduce((sum,r)=>sum+(countOf(r)*ldmOf(r)),0);\\n  const storedCount=Math.max(0,...copies.map(x=>firstPositive(x,['totalColli','totalCollis','colliCount','packageCount','count'])));\\n  const storedWeight=Math.max(0,...copies.map(x=>firstPositive(x,['totalWeight','totalWeightKg','weightKg','grossWeight','gesamtgewicht','weight'])));\\n  const storedLdm=Math.max(0,...copies.map(x=>firstPositive(x,['totalLdm','ldmTotal','loadingMeters','lademeter','ldm'])));\\n  const totalCount=rowCount||storedCount,totalWeight=rowWeight||storedWeight,totalLdm=rowLdm||storedLdm;\\n  if(rows.length&&rowScore(s.rows||[])<rowScore(rows)){s.rows=rows.map(r=>({...r}));s.colli=s.rows;s.collis=s.rows;s.packages=s.rows}\\n  if(totalCount>0){s.totalColli=totalCount;s.totalCollis=totalCount;s.colliCount=totalCount;s.packageCount=totalCount}\\n  if(totalWeight>0){s.totalWeight=totalWeight;s.totalWeightKg=totalWeight;s.weightKg=totalWeight;s.grossWeight=totalWeight}\\n  if(totalLdm>=0&&rows.length){s.totalLdm=totalLdm;s.ldmTotal=totalLdm;s.loadingMeters=totalLdm;s.lademeter=totalLdm}\\n  const missing=[];\\n  if(!rows.length)missing.push('mindestens eine Colli-/Packst\\u00fcck-Zeile');\\n  if(!(totalCount>0))missing.push('Anzahl der Collis');\\n  if(!(totalWeight>0))missing.push('Gesamtgewicht');\\n  if(rows.some(r=>!(dimOf(r,'l')>0)||!(dimOf(r,'w')>0)||!(dimOf(r,'h')>0)))missing.push('Ma\\u00dfe L\\u00e4nge/Breite/H\\u00f6he je Zeile');\\n  if(rows.some(r=>!(countOf(r)>0)))missing.push('Anzahl je Zeile');\\n  if(rows.some(r=>!(weightOf(r)>0))&&!(storedWeight>0))missing.push('Gewicht je Zeile');\\n  if(rows.some(r=>!hasLdm(r))&&!(storedLdm>0))missing.push('LDM je Colli je Zeile');\\n  return Array.from(new Set(missing));\\n}\\nfunction shipmentCompletionOk(s){return shipmentCompletionMissing(s).length===0}\\nfunction shipmentCompletionBox(s){const miss=shipmentCompletionMissing(s);return miss.length?'<div class=\\\"badbox\\\"><b>Sendungsdaten unvollst\\u00e4ndig.</b><br>'+miss.map(esc).join(', ')+'</div>':''}\\n\\nfunction taskMatchesShipment(t,s,c){if(!t||t.status==='erledigt')return false;const text=norm([t.title,t.area,t.note,t.linkedCustomer,t.linkedAccount].join(' '));const customerTerms=[c.name,c.account,c.customerNumber,c.id].filter(Boolean).map(norm).filter(x=>x&&x.length>2);const customerHit=customerTerms.some(term=>text.includes(term));const actionHit=/(anmeld|sendung|carrier|portal|cmr|ladeliste|abd|avis)/.test(text);return customerHit&&actionHit}\\nfunction autoCompleteShipmentTasks(s,c){const done=[];for(const t of state.tasks){if(taskMatchesShipment(t,s,c)){t.status='erledigt';t.completedBy='Sendung';t.completedAt=new Date().toLocaleString('de-DE');t.note=(t.note||'')+'\\\\nAutomatisch erledigt durch Sendung '+s.ref;done.push(t.title)}}return done}\\nfunction approveShipment(id){const old=state.shipments.find(x=>x.id===id);if(!old)return;if(!validShipmentRef(old.ref))old.ref=generateShipmentRef(old.customer||selectedCustomer());old.freigabe='erledigt';old.status='erledigt';old.completedAt=new Date().toISOString();old.completedBy=currentUser?.name||currentUser?.user||'Benutzer';audit('Sendung freigegeben/erledigt',old.ref);save();render()}\\nfunction activeCmrShipment(){if(state.activeShipmentId==='current')return state.shipment;return state.shipments.find(x=>x.id===state.activeShipmentId)||state.shipments[0]||state.shipment}\\nfunction cmr(){\\nlet s=activeCmrShipment();if(!validShipmentRef(s.ref)){s.ref=generateShip