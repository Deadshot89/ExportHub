'use strict';
const store=require('../shared/pickup-store');
module.exports=async function(context,req){try{const token=String(req.query&&req.query.token||'').toLowerCase(),got=await store.getRecord(token);if(got.record.suspended){context.res=store.json(410,{ok:false,code:'CANCELLED',message:'Diese Sendung ist storniert. Der QR-Code ist gesperrt.'});return}if(got.record.confirmedAt){context.res=store.json(410,{ok:false,code:'USED',message:'Dieser QR-Code wurde bereits verwendet und ist dauerhaft gesperrt.'});return}if(store.expired(got.record)){context.res=store.json(410,{ok:false,code:'EXPIRED',message:'Dieser QR-Code ist abgelaufen.'});return}const pallet=await store.getTeamPalletContext(got.record);got.record.palletOutgoingCount=pallet.outgoing;got.record.palletAccountType=pallet.partyType;got.record.palletAccountName=pallet.partyName;got.record.palletAccountKey=pallet.partyKey;context.res=store.json(200,store.publicRecord(got.record))}catch(e){context.res=store.json(e.status||500,{ok:false,code:e.code||'SERVER_ERROR',message:e.message||'Status konnte nicht gelesen werden.'})}};
