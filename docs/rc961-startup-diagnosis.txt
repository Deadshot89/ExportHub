TESTVERSION length: 5384284

=== 'Azure-Teamdaten werden geladen' hits=4 ===

--- hit 1 pos=88246 nearest_function=<none> ---
nach dem Deploy ohne Geheimnisse offenzulegen, ob user-policy, auth-store und @azure/storage-blob im Azure-Functions-Runtime geladen werden können.",
    "RC879 stabilisiert die Testservice-Anmeldung bei großen Azure-Teamständen: Die Benutzer-Policy dupliziert den kompletten state-Block nicht mehr per JSON-Serialisierung. Nur Benutzer und der flache State-Rahmen werden für die Normalisierung kopiert; Sendungen, Kunden, PODs und Dokumentdaten bleiben unverändert erhalten.",
    "RC879 prüft im Testservice vor der Anmeldung die echte exporthub-auth-Konfiguration mit bootstrap-status. Ein nicht erreichbarer oder falsch konfigurierter Auth-/Storage-Backendzustand wird bereits vor dem Login sichtbar und nicht mehr erst als unklarer HTTP-500-Loginfehler gemeldet.",
    "RC879 erweitert den Testservice-Deploy um eine verbindliche Auth-Backend-Prüfung. Ein Deploy darf nur noch grün werden, wenn /api/exporthub-auth bootstrap-status erfolgreich beantwortet und der Azure-Speicher erreichbar ist.",
    "RC878 korrigiert die Datenschutzmaskierung im Audit: reine Zahlenfolgen in Datei- und Lieferscheinnamen werden nicht mehr als Telefonnummer interpretiert. E-Mail- und echte Telefon-/Kontaktfelder bleiben weiterhin geschützt; bereits maskierte Lieferschein-Audits werden bei der Anzeige nur dann aus den noch vorhandenen Original-Dateinamen rekonstruiert, wenn die Zuordnung eindeutig ist.",
    "RC878 räumt den Aufgaben Planer weiter auf: Wochensteuerung, Status und Feiertagsvorwarnung stehen kompakt oben; Detailgruppen bleiben standardmäßig leichtgewichtig und werden erst beim tatsächlichen Aufklappen erzeugt. Dadurch sinkt die DOM-Last des Planers.",
    "RC878 erweitert die NRW-Feiertagsplanung um eine Vorwarnung bis 120 Tage im Voraus. Feiertage zeigen Wochentag, Resttage und ob ein regulärer Arbeitstag betroffen ist; ein Klick auf Feiertagswoche springt direkt in die passende Planungswoche. Die bestehende automatische ABD-/Anmeldungsverschiebung bleibt erhalten.",
    "RC877 repariert den Testservice-Start bei 8 Prozent: Der Teamstand-Read bekommt einen sichtbaren Wartefortschritt statt eines scheinbar eingefrorenen Ladebalkens und kann den Start nicht mehr unbegrenzt bei Azure-Teamdaten werden geladen stehen lassen.",
    "RC877 verwendet beim Testservice genau einen kontrollierten zweiten Teamstand-Read nur während des Starts, falls der erste Read durch Timeout, Failed to fetch oder einen vorübergehenden 5xx-Fehler scheitert. Es entsteht kein Retry-Sturm und es laufen nie zwei Start-Reads parallel.",
    "RC877 begrenzt den ersten Testservice-Teamstand-Read auf 14 Sekunden und den einmaligen Wiederholungsversuch auf 24 Sekunden. Schlägt auch dieser fehl, wird der Ladebildschirm beendet und der bestehende Testservice-Wiederholen-Dialog angezeigt statt dauerhaft bei 8 Prozent zu blockieren.",
    "RC876 führt für Azure-Teamstand und Diagnose einen gemeinsamen Störungsmodus mit wachsender Wartezeit ein. Bei Timeout, Failed to fetch oder Offline-Zustand werden keine schnellen Folge-Requests mehr gestartet; vorgemerkte Änderungen bleiben lokal aktiv und werden erst bei wieder stabiler Verbindung erneut übertragen.",
    "RC876 startet beim Ausblenden, pagehide, beforeunload oder freeze keinen neuen Azure-Save mehr und beendet einen noch laufenden Client-Request kontrolliert. Mobile Browser können einen 20-Sekunden-Timer im Hintergrund minutenlang anhalten; deshalb bleibt die Operation vorgemerkt und wird nach sichtbarer/online App mit derselben Änderungs-ID genau einmal weiter bestätigt.",
    "RC876 reduziert den TESTSERVICE-Start auf die notwendige Serverprüfung: Eine wiederhergestellte Global-Admin-Sitzung lädt direkt den serverseitig geschützten Teamstand und sendet nicht vorher noch eine zusätzliche Auth-Sitzungsprüfung. Bei transientem Ladefehler erfolgt kein automatischer zweiter Teamstand-Read; Wiederholen bleibt bewusst über denselben Login möglich.",
    "RC876 entfernt die vollständige Lager-/Location-Synchronisation vom Dashboard. Eine neu gespeicherte Sendung registriert gezielt nur ihre eigene Location-Metadatei; die komplette Synchronisation läuft ausschließlich im Lagerbereich. Automatische POD-Sicherung startet außerdem nur noch bei einer konkret vorhandenen Inline-Signatur oder echten Signatur-URL.",
    "RC875 repariert die Smartphone-Shell vollständig: App, Hauptbereich, Topbar und Dashboard verwenden auf kleinen Displays verbindlich die komplette Viewport-Breite. Desktop-Spaltenbreiten und Theme-Regeln können die mobile Darstellung nicht mehr überschreiben.",
    "RC875 ordnet die Smartphone-Kopfleiste neu: Menü-Taste bleibt kompakt, Suche und Testservice-Hinweis brechen sauber um und Design-/Aktionsfelder werden in bedienbaren mobilen Reihen dargestellt. Kein abgeschnittener Kopfbereich und kein ungenutzter rechter Seitenstreifen mehr.",
    "RC875 erzwingt für Dashboard-Kacheln und verschachtelte Kennzahlen mobile min-width/max-width-Regeln, verhindert horizontales Überlaufen und korrigiert die Testservice-Banderole für schmale Geräte. RC874-Aufgaben-, Feiertags- und Gruppierungslogik bleibt unverändert enthalten.",
    "RC874 gruppiert die Aufgabenansicht verbindlich nach Picken, ABD erstellen, Anmelden, Aufträge kontrollieren, Zusatzaufgaben und weiteren Aufgaben. Jede Gruppe kann einzeln ein- und ausgeklappt werden; zusätzlich stehen Alle Gruppen öffnen und Alle Gruppen schließen bereit. Der Klappzustand bleibt lokal auf dem Gerät erhalten und erzeugt keinen Azure-Save.",
    "RC874 entdoppelt offene Aufgaben in Aufgaben und Aufgaben Planer semantisch. Gleiche Wochen-/Tagesaufgaben mit anderem technischen Datensatz-ID werden nur einmal angezeigt; sendungsbezogene Aufgaben bleiben durch ihre Referenz getrennt. Der Masterplan entfernt zusätzlich echte doppelte Masterinstanzen derselben Serie/Woche, ohne manuelle Einzelaufgaben zusammenzufassen.",
    "RC874 macht Feiertage im Aufgaben Planer dauerhaft sichtbar: Die ausgewählte Woche markiert Feiertage direkt am Tag und ein eigener kompakter Bereich zeigt die nächsten gesetzlichen Feiertage in Nordrhein-Westfalen. Die bestehende ABD-/Anmeldelogik für Feiertagsverschiebungen bleibt erhalten.",
    "RC874 baut den Aufgaben Planer kompakter auf: oben steht eine 5-Tage-Wochenübersicht, darunter folgen einklappbare Aufgabengruppen. Eingeklappte Gruppen erzeugen ihre vielen Aufgabenkarten erst beim Öffnen; reine Wochen- und Gr

--- hit 2 pos=2232299 nearest_function=startStartupStateReadProgress ---
();evidence.forEach(function(item){var aliases=customerIdentityValues(item.data),key=customerRecoveryKey(item.data)||aliases[0]||lower(item.data.name||item.data.customerName);if(!key)return;var old=best.get(key);if(!old||item.master&&!old.master||item.master===old.master&&item.score>old.score)best.set(key,item)});var changed=false,restored=0,filled=0;
 function sameCustomer(a,b){var aa=customerIdentityValues(a),bb=new Set(customerIdentityValues(b));return aa.some(function(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
functi

--- hit 3 pos=2232525 nearest_function=startStartupStateReadProgress ---
master&&!old.master||item.master===old.master&&item.score>old.score)best.set(key,item)});var changed=false,restored=0,filled=0;
 function sameCustomer(a,b){var aa=customerIdentityValues(a),bb=new Set(customerIdentityValues(b));return aa.some(function(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
function rc524FinalFixes(){
 try{if(window.ExportHUBBuild){window.ExportHUBBuild.applyTitle();window.ExportHUBBuild.applyVersion()}}catch(_){}
 window.__EXPORTHUB_RC524__=true;
}

const SYNC_COLLECTION_KEYS={shipments:['id','ref'],t

--- hit 4 pos=2232767 nearest_function=loadState ---
function(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
function rc524FinalFixes(){
 try{if(window.ExportHUBBuild){window.ExportHUBBuild.applyTitle();window.ExportHUBBuild.applyVersion()}}catch(_){}
 window.__EXPORTHUB_RC524__=true;
}

const SYNC_COLLECTION_KEYS={shipments:['id','ref'],tasks:['id','_syncId'],customers:['id','account','customerNumber','name'],abdRequests:['id','ref'],palletAccount:['id','_syncId'],ideas:['id','_syncId'],customSops:['id','name','_syncId'],sops:['id','name','_syncId'],users:['id','user','login'

=== 'ExportHUB wird vorbereitet' hits=1 ===

--- hit 1 pos=859468 nearest_function=apply ---
ent(135deg,#fff7ed,#fffbeb)!important}
html[data-exporthub-testportal="1"] body::after{content:"";position:fixed;inset:0;pointer-events:none;z-index:2147482500;border:4px solid rgba(234,88,12,.72);box-sizing:border-box}
#rc561TestPortalBadge{position:fixed;top:0;left:50%;transform:translateX(-50%);z-index:2147483600;background:#c2410c;color:#fff;border:1px solid #9a3412;border-radius:0 0 12px 12px;padding:7px 18px;font:950 12px/1.15 system-ui,-apple-system,"Segoe UI",sans-serif;letter-spacing:.075em;box-shadow:0 6px 18px rgba(154,52,18,.3);pointer-events:none;white-space:nowrap}
#rc561TestRuntimeError{position:fixed;left:50%;bottom:12px;transform:translate(-50%,20px);z-index:2147483601;max-width:min(92vw,900px);background:#7f1d1d;color:#fff;border:1px solid #fecaca;border-radius:10px;padding:8px 12px;font:800 12px/1.35 system-ui,-apple-system,"Segoe UI",sans-serif;box-shadow:0 8px 24px rgba(127,29,29,.3);opacity:0;pointer-events:none;transition:.16s ease}
#rc561TestRuntimeError.show{opacity:1;transform:translate(-50%,0)}
@media(max-width:720px){html[data-exporthub-testportal="1"] body::after{border-width:3px}#rc561TestPortalBadge{padding:5px 8px;font-size:9px;border-radius:0 0 9px 9px;letter-spacing:.025em;max-width:calc(100vw - 18px)!important;white-space:normal!important;text-align:center!important;overflow-wrap:anywhere!important}#rc561TestRuntimeError{bottom:7px;font-size:10px;padding:6px 8px}}
</style>
<style id="rc725-render-containment">
/* RC725: Große, außerhalb des Viewports liegende Bereiche verursachen keine unnötige Layout-/Paint-Arbeit. */
#content > :is(section,.card,article,.rc524-page,.rc504-warehouse,#index218Planner),
#content .rc573-card,
#content .rc543-overview-group{
  content-visibility:auto;
  contain-intrinsic-size:auto 420px;
}
@media print{
  #content > :is(section,.card,article,.rc524-page,.rc504-warehouse,#index218Planner),
  #content .rc573-card,#content .rc543-overview-group{
    content-visibility:visible!important;contain:none!important;
  }
}
</style>
</head>
<body><div class="clean-load-panel hidden" id="cleanLoadPanel">
<div class="clean-load-card">
<div aria-hidden="true" class="clean-spinner"></div>
<h2 id="cleanLoadTitle">ExportHUB wird vorbereitet</h2>
<p id="cleanLoadText">Die Anwendungsmodule werden kontrolliert geladen.</p>
<div class="clean-progress"><span id="cleanProgressBar"></span></div>
<div class="clean-progress-label" id="cleanProgressLabel">0 %</div>
</div>
</div>
<div class="login" id="login">
<div class="login-card">
<div class="logo">EH</div>
<h1>ExportHUB</h1>
<div aria-label="Aktuelle Version RC583" class="clean-version-badge" id="cleanVersionBadge">Aktuelle Version RC583</div>
<div class="eh-login-mode-head" id="ehLoginModeHead"><div class="eh-login-mode-kicker" id="ehLoginKicker">PRODUKTION</div><h2 id="ehLoginModeTitle">ExportHUB anmelden</h2><p id="ehLoginModeText">Melde dich hier am produktiven ExportHUB an oder öffne den getrennten Testservice.</p></div>
<div class="eh-login-environment eh-login-switch" id="ehLoginEnvironment" aria-label="ExportHUB Umgebung wählen">
<button type="button" id="ehLoginProduction" data-environment="production"><strong>Produktion</strong><span>Produktives ExportHUB</span></button>
<button type="button" id="ehLoginTestservice" data-environment="testservice"><strong>Testservice</strong><span>Separate Testumgebung</span></button>
</div>
<button class="ghost hidden" id="ehBackProduction" type="button" aria-hidden="true" tabindex="-1">Zur Produktion</button>
<div class="eh-login-environment-note" id="ehLoginEnvironmentNote">Umgebung zuerst wählen. Zugangsdaten werden nur auf der geöffneten Umgebung eingegeben.</div>
<div class="login-lang">
<label class="field" id="loginLangLabel">Sprache
        <select autocomplete="off" id="loginLanguageSelect"><option value="de">Deutsch</option><option value="en">English</option></select>
</label>
<div class="lang-note hidden" id="loginLangNote"></div>
</div>
<div aria-live="polite" class="clean-login-status" id="cleanLoginStatus"></div>
<form id="loginFields" autocomplete="on" novalidate>
<label class="field">Benutzername<input autocapitalize="none" autocomplete="username" id="loginUser" name="username" spellcheck="false" value=""/></label>
<label class="field">Passwort<input autocomplete="current-password" id="loginPass" name="password" type="password"/></label>
<label class="rc119-login-remember" for="loginRemember"><input id="loginRemember" type="checkbox" autocomplete="off"/><span><strong>Login-Daten auf diesem Gerät speichern</strong><small id="loginRememberHint" style="display:block;font-weight:600;color:#64748b;margin-top:2px">Benutzername wird von ExportHUB gemerkt. Das Passwort verwaltet ausschließlich der Browser-Passwortmanager.</small></span></label>
<button class="btn" id="loginBtn" style="width:100%" type="submit">Anmelden</button>
<button class="ghost hidden" id="testserviceContinueBtn" type="button">Teamdaten erneut laden</button>
<button class="ghost" data-exporthub-ms-switch="1" id="loginMicrosoftAccountSwitchBtn" style="width:100%;margin-top:10px" type="button">Microsoft-Konto wechseln</button>
<button class="ghost" id="adminRecoveryBtn" style="width:100%;margin-top:10px" type="button">Admin-Zugang wiederherstellen</button>
</form>
<div class="hidden" id="pwChange">
<div class="notice">Erstanmeldung: Bitte neues Passwort setzen.</div>
<label class="field">Neues Passwort<input id="newPass1" type="password"/></label>
<label class="field">Passwort wiederholen<input id="newPass2" type="password"/></label>
<button class="btn" id="savePassBtn" style="width:100%">Passwort speichern</button>
</div>
</div>
</div>
<script id="exporthub-rc778-login-credentials">
(function(){
'use strict';
if(window.__EXPORTHUB_RC778_LOGIN_CREDENTIALS__)return;
window.__EXPORTHUB_RC778_LOGIN_CREDENTIALS__=true;
var PREF='exporthub_login_remember_v1',USER='exporthub_login_user_v1';
function by(id){return document.getElementById(id)}
function q(v){return String(v==null?'':v).trim()}
function pref(){try{return localStorage.getItem(PREF)==='1'}catch(_){return false}}
function hint(text,kind){var h=by('loginRememberHint');if(!h)return;h.textContent=text||'Benutzername wird von ExportHUB gemerkt. Das Passwort verwaltet ausschließlich der Browser-Passwortmanager.';h.style.color=kind==='ok'?'#166534':kind==='warn'?'#92400e':'#64748b'}
function setPref(on,user){try{if(on){l

=== 'Teamdaten werden geladen' hits=5 ===

--- hit 1 pos=88252 nearest_function=<none> ---
em Deploy ohne Geheimnisse offenzulegen, ob user-policy, auth-store und @azure/storage-blob im Azure-Functions-Runtime geladen werden können.",
    "RC879 stabilisiert die Testservice-Anmeldung bei großen Azure-Teamständen: Die Benutzer-Policy dupliziert den kompletten state-Block nicht mehr per JSON-Serialisierung. Nur Benutzer und der flache State-Rahmen werden für die Normalisierung kopiert; Sendungen, Kunden, PODs und Dokumentdaten bleiben unverändert erhalten.",
    "RC879 prüft im Testservice vor der Anmeldung die echte exporthub-auth-Konfiguration mit bootstrap-status. Ein nicht erreichbarer oder falsch konfigurierter Auth-/Storage-Backendzustand wird bereits vor dem Login sichtbar und nicht mehr erst als unklarer HTTP-500-Loginfehler gemeldet.",
    "RC879 erweitert den Testservice-Deploy um eine verbindliche Auth-Backend-Prüfung. Ein Deploy darf nur noch grün werden, wenn /api/exporthub-auth bootstrap-status erfolgreich beantwortet und der Azure-Speicher erreichbar ist.",
    "RC878 korrigiert die Datenschutzmaskierung im Audit: reine Zahlenfolgen in Datei- und Lieferscheinnamen werden nicht mehr als Telefonnummer interpretiert. E-Mail- und echte Telefon-/Kontaktfelder bleiben weiterhin geschützt; bereits maskierte Lieferschein-Audits werden bei der Anzeige nur dann aus den noch vorhandenen Original-Dateinamen rekonstruiert, wenn die Zuordnung eindeutig ist.",
    "RC878 räumt den Aufgaben Planer weiter auf: Wochensteuerung, Status und Feiertagsvorwarnung stehen kompakt oben; Detailgruppen bleiben standardmäßig leichtgewichtig und werden erst beim tatsächlichen Aufklappen erzeugt. Dadurch sinkt die DOM-Last des Planers.",
    "RC878 erweitert die NRW-Feiertagsplanung um eine Vorwarnung bis 120 Tage im Voraus. Feiertage zeigen Wochentag, Resttage und ob ein regulärer Arbeitstag betroffen ist; ein Klick auf Feiertagswoche springt direkt in die passende Planungswoche. Die bestehende automatische ABD-/Anmeldungsverschiebung bleibt erhalten.",
    "RC877 repariert den Testservice-Start bei 8 Prozent: Der Teamstand-Read bekommt einen sichtbaren Wartefortschritt statt eines scheinbar eingefrorenen Ladebalkens und kann den Start nicht mehr unbegrenzt bei Azure-Teamdaten werden geladen stehen lassen.",
    "RC877 verwendet beim Testservice genau einen kontrollierten zweiten Teamstand-Read nur während des Starts, falls der erste Read durch Timeout, Failed to fetch oder einen vorübergehenden 5xx-Fehler scheitert. Es entsteht kein Retry-Sturm und es laufen nie zwei Start-Reads parallel.",
    "RC877 begrenzt den ersten Testservice-Teamstand-Read auf 14 Sekunden und den einmaligen Wiederholungsversuch auf 24 Sekunden. Schlägt auch dieser fehl, wird der Ladebildschirm beendet und der bestehende Testservice-Wiederholen-Dialog angezeigt statt dauerhaft bei 8 Prozent zu blockieren.",
    "RC876 führt für Azure-Teamstand und Diagnose einen gemeinsamen Störungsmodus mit wachsender Wartezeit ein. Bei Timeout, Failed to fetch oder Offline-Zustand werden keine schnellen Folge-Requests mehr gestartet; vorgemerkte Änderungen bleiben lokal aktiv und werden erst bei wieder stabiler Verbindung erneut übertragen.",
    "RC876 startet beim Ausblenden, pagehide, beforeunload oder freeze keinen neuen Azure-Save mehr und beendet einen noch laufenden Client-Request kontrolliert. Mobile Browser können einen 20-Sekunden-Timer im Hintergrund minutenlang anhalten; deshalb bleibt die Operation vorgemerkt und wird nach sichtbarer/online App mit derselben Änderungs-ID genau einmal weiter bestätigt.",
    "RC876 reduziert den TESTSERVICE-Start auf die notwendige Serverprüfung: Eine wiederhergestellte Global-Admin-Sitzung lädt direkt den serverseitig geschützten Teamstand und sendet nicht vorher noch eine zusätzliche Auth-Sitzungsprüfung. Bei transientem Ladefehler erfolgt kein automatischer zweiter Teamstand-Read; Wiederholen bleibt bewusst über denselben Login möglich.",
    "RC876 entfernt die vollständige Lager-/Location-Synchronisation vom Dashboard. Eine neu gespeicherte Sendung registriert gezielt nur ihre eigene Location-Metadatei; die komplette Synchronisation läuft ausschließlich im Lagerbereich. Automatische POD-Sicherung startet außerdem nur noch bei einer konkret vorhandenen Inline-Signatur oder echten Signatur-URL.",
    "RC875 repariert die Smartphone-Shell vollständig: App, Hauptbereich, Topbar und Dashboard verwenden auf kleinen Displays verbindlich die komplette Viewport-Breite. Desktop-Spaltenbreiten und Theme-Regeln können die mobile Darstellung nicht mehr überschreiben.",
    "RC875 ordnet die Smartphone-Kopfleiste neu: Menü-Taste bleibt kompakt, Suche und Testservice-Hinweis brechen sauber um und Design-/Aktionsfelder werden in bedienbaren mobilen Reihen dargestellt. Kein abgeschnittener Kopfbereich und kein ungenutzter rechter Seitenstreifen mehr.",
    "RC875 erzwingt für Dashboard-Kacheln und verschachtelte Kennzahlen mobile min-width/max-width-Regeln, verhindert horizontales Überlaufen und korrigiert die Testservice-Banderole für schmale Geräte. RC874-Aufgaben-, Feiertags- und Gruppierungslogik bleibt unverändert enthalten.",
    "RC874 gruppiert die Aufgabenansicht verbindlich nach Picken, ABD erstellen, Anmelden, Aufträge kontrollieren, Zusatzaufgaben und weiteren Aufgaben. Jede Gruppe kann einzeln ein- und ausgeklappt werden; zusätzlich stehen Alle Gruppen öffnen und Alle Gruppen schließen bereit. Der Klappzustand bleibt lokal auf dem Gerät erhalten und erzeugt keinen Azure-Save.",
    "RC874 entdoppelt offene Aufgaben in Aufgaben und Aufgaben Planer semantisch. Gleiche Wochen-/Tagesaufgaben mit anderem technischen Datensatz-ID werden nur einmal angezeigt; sendungsbezogene Aufgaben bleiben durch ihre Referenz getrennt. Der Masterplan entfernt zusätzlich echte doppelte Masterinstanzen derselben Serie/Woche, ohne manuelle Einzelaufgaben zusammenzufassen.",
    "RC874 macht Feiertage im Aufgaben Planer dauerhaft sichtbar: Die ausgewählte Woche markiert Feiertage direkt am Tag und ein eigener kompakter Bereich zeigt die nächsten gesetzlichen Feiertage in Nordrhein-Westfalen. Die bestehende ABD-/Anmeldelogik für Feiertagsverschiebungen bleibt erhalten.",
    "RC874 baut den Aufgaben Planer kompakter auf: oben steht eine 5-Tage-Wochenübersicht, darunter folgen einklappbare Aufgabengruppen. Eingeklappte Gruppen erzeugen ihre vielen Aufgabenkarten erst beim Öffnen; reine Wochen- und Gruppena

--- hit 2 pos=2232305 nearest_function=startStartupStateReadProgress ---
dence.forEach(function(item){var aliases=customerIdentityValues(item.data),key=customerRecoveryKey(item.data)||aliases[0]||lower(item.data.name||item.data.customerName);if(!key)return;var old=best.get(key);if(!old||item.master&&!old.master||item.master===old.master&&item.score>old.score)best.set(key,item)});var changed=false,restored=0,filled=0;
 function sameCustomer(a,b){var aa=customerIdentityValues(a),bb=new Set(customerIdentityValues(b));return aa.some(function(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
function rc5

--- hit 3 pos=2232531 nearest_function=startStartupStateReadProgress ---
&&!old.master||item.master===old.master&&item.score>old.score)best.set(key,item)});var changed=false,restored=0,filled=0;
 function sameCustomer(a,b){var aa=customerIdentityValues(a),bb=new Set(customerIdentityValues(b));return aa.some(function(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
function rc524FinalFixes(){
 try{if(window.ExportHUBBuild){window.ExportHUBBuild.applyTitle();window.ExportHUBBuild.applyVersion()}}catch(_){}
 window.__EXPORTHUB_RC524__=true;
}

const SYNC_COLLECTION_KEYS={shipments:['id','ref'],tasks:[

--- hit 4 pos=2232773 nearest_function=loadState ---
on(v){return bb.has(v)})}
 remote.forEach(function(c){var matches=Array.from(best.values()).filter(function(item){return sameCustomer(c,item.data)}).sort(function(a,b){return Number(b.master)-Number(a.master)||b.score-a.score});if(matches.length&&customerRecoveryMergeBlank(c,matches[0].data)){filled++;changed=true}});
 Array.from(best.values()).filter(function(item){return item.master&&customerRecoveryKey(item.data)&&item.score>=34&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true});
 if(!restored){Array.from(best.values()).filter(function(item){return customerRecoveryKey(item.data)&&item.score>=60&&!customerRecoveryTombstoned(remoteState,item.data)}).sort(function(a,b){return b.score-a.score}).forEach(function(item){if(remote.some(function(c){return sameCustomer(c,item.data)}))return;remote.push(clone(item.data));restored++;changed=true})}
 if(changed){remoteState.customers=remote.filter(function(c){return customerRecoveryKey(c)||customerRecoveryScore(c)>=12});clearConflictingCustomerTombstones(remoteState,remoteState.customers)}return{changed:changed,restored:restored,filled:filled,sources:evidence.length}
}

let startupStateReadTimer=null,startupStateReadStartedAt=0,startupStateReadPromise=null;
function stopStartupStateReadProgress(){if(startupStateReadTimer){try{native.clearInterval(startupStateReadTimer)}catch(_){ }startupStateReadTimer=null}startupStateReadStartedAt=0}
function startStartupStateReadProgress(label){stopStartupStateReadProgress();startupStateReadStartedAt=Date.now();progress(8,label||'Azure-Teamdaten werden geladen …');startupStateReadTimer=native.setInterval(function(){var seconds=Math.max(0,Math.floor((Date.now()-startupStateReadStartedAt)/1000)),p=Math.min(15,8+Math.floor(seconds/3));progress(p,(label||'Azure-Teamdaten werden geladen …')+' · '+seconds+' s')},1000)}
async function loadState(requestOptions){
 if(startupStateReadPromise)return startupStateReadPromise;
 startupStateReadPromise=(async function(){
  startStartupStateReadProgress('Azure-Teamdaten werden geladen …');addSafeLoadNote();runtime.network.stateGets++;
  try{
   var d=await stateCall('read',{},requestOptions||{});
   stopStartupStateReadProgress();progress(18,'Teamdaten werden im Hintergrund verarbeitet …');
   if(!d||d.ok===false){var e=new Error((d&&d.message)||'Teamdaten ungültig');e.code=d&&d.code||'TEAMDATA_INVALID';throw e}
   runtime.shipmentRecoveryDisabled=true;runtime.shipmentRecoveryWarning=null;
   var __preLoadState=isObject(runtime.state)?clone(runtime.state):{};runtime.state=d.state||{};var __serverShipmentSnapshot=clone(runtime.state),__customerRecovery=recoverCustomerMasterData(runtime.state,__preLoadState),__ghostCleanup=sanitizeShipmentCollections(runtime.state,{removeEmptyGhosts:true});runtime.customerRecoveryPending=!!__customerRecovery.changed;runtime.customerRecoveryBase=runtime.customerRecoveryPending?__serverShipmentSnapshot:null;runtime.customerRecoveryResult=__customerRecovery;runtime.shipmentGhostCleanupPending=!!__ghostCleanup.changed;runtime.shipmentGhostCleanupRefs=__ghostCleanup.ghosts||[];runtime.shipmentGhostCleanupBase=runtime.shipmentGhostCleanupPending?__serverShipmentSnapshot:null;runtime.users=Array.isArray(d.users)?d.users:runtime.users;runtime.revision=Number(d.revision||0);var key=lower(runtime.user&&(runtime.user.user||runtime.user.login||runtime.user.name));runtime.user=runtime.users.find(function(u){return lower(u.user||u.login||u.name)===key})||runtime.user;restorePersistentFilterState(runtime.state);
   runtime.lastSnapshot=(runtime.shipmentGhostCleanupPending||runtime.customerRecoveryPending)?__serverShipmentSnapshot:clone(runtime.state);runtime.lastUsers=clone(runtime.users);runtime.dirty=false;
   window.__CLEAN_BOOT_STATE__=runtime.state;window.__CLEAN_BOOT_USERS__=runtime.users;return d
  }finally{stopStartupStateReadProgress()}
 })();
 try{return await startupStateReadPromise}finally{startupStateReadPromise=null}
}

function runOne(entry){return new Promise(function(resolve){native.setTimeout(function(){runtime.currentModuleId=Number(entry.id)||0;try{const s=document.createElement('script');s.dataset.canonicalModule=String(entry.id);s.text=entry.code+'\n//# sourceURL=exporthub-module-'+entry.id+'.js';document.head.appendChild(s);s.remove();if(Number(entry.id)===148){window.__RC524_CORE_SHOW_DOCUMENTS__=window.rc390ShowDocuments;window.__RC524_CORE_LOADLIST_PDF__=window.rc390DownloadLoadlistPdf;window.__RC524_CORE_LOADLIST_PAGE2_PDF__=window.rc390DownloadLoadlistPage2Pdf;window.__RC524_CORE_PRINT_DOCUMENTS__=window.rc390PrintDocuments}}catch(e){console.error('ExportHUB-Modul '+entry.id,e)}finally{runtime.currentModuleId=0}resolve()},0)})}
async function runScripts(entries){for(let i=0;i<entries.length;i++){await runOne(entries[i]);if(i%10===9)await new Promise(function(r){native.setTimeout(r,0)})}}
async function fetchScriptText(src){
 const url=src+(String(src).indexOf('?')>=0?'&':'?')+'v='+encodeURIComponent(CACHE);
 const res=await native.fetch(url,{cache:'force-cache'});
 if(!res.ok)throw new Error('Modul konnte nicht vorgeladen werden: '+src+' (HTTP '+res.status+')');
 return {src:src,code:await res.text()};
}
async function executeScriptText(src,code){
 const script=document.createElement('script');script.dataset.cleanGroup=src;script.text=code+'\n//# sourceURL='+src.replace(/[^A-Za-z0-9._/-]/g,'_');document.head.appendChild(script);script.remove();
}
async function loadScript(src){const item=await fetchScriptText(src);return executeScriptText(item.src,item.code)}
async function cleanYield(ms){var wait=Number(ms);if(!Number.isFinite(wait)||wait<0)wait=0;return new Promise(function(resolve){native.setTimeout(resolve,wait)})}

/* exporthub-rc902-bootstrap-performance */
function rc524FinalFixes(){
 try{if(window.ExportHUBBuild){window.ExportHUBBuild.applyTitle();window.ExportHUBBuild.applyVersion()}}catch(_){}
 window.__EXPORTHUB_RC524__=true;
}

const SYNC_COLLECTION_KEYS={shipments:['id','ref'],tasks:['id','_syncId'],customers:['id','account','customerNumber','name'],abdRequests:['id','ref'],palletAccount:['id','_syncId'],ideas:['id','_syncId'],customSops:['id','name','_syncId'],sops:['id','name','_syncId'],users:['id','user','login','user

--- hit 5 pos=2316193 nearest_function=finishAuthenticatedLogin ---
nst fresh=await authCall('login',{username:pending.username,password:pending.password,deviceId:pending.deviceId,recoveryRequested:false},'');
 if(!fresh||!text(fresh.token)||fresh.mustChange===true)return false;
 runtime.authToken=text(fresh.token);runtime.user=fresh.user||runtime.user;runtime.users=fresh.user?[fresh.user]:runtime.users;
 await verifyCurrentSession();
 return true
}
async function verifySessionForLoad(){
 var attempts=runtime.sessionRestored?3:1,lastError=null;
 for(var i=0;i<attempts;i++){
  try{return await verifyCurrentSession()}catch(error){lastError=error;if(!isSessionError(error)||i===attempts-1)throw error;await new Promise(function(resolve){native.setTimeout(resolve,350*(i+1))})}
 }
 throw lastError
}

function isTransientLoginLoadError(error){var code=String(error&&error.code||''),statusCode=Number(error&&error.status||0);return ['API_UNREACHABLE','API_TIMEOUT','STATE_API_UNREACHABLE','INVALID_SERVER_JSON','HTTP_408','HTTP_425','HTTP_429','HTTP_500','HTTP_502','HTTP_503','HTTP_504'].indexOf(code)>=0||[408,425,429,500,502,503,504].indexOf(statusCode)>=0}
async function loadStateAfterLogin(){
 if(isTestServiceOrigin()){
  try{return await loadState({timeoutMs:14000,maxAttempts:1})}
  catch(firstError){
   if(!isTransientLoginLoadError(firstError))throw firstError;
   status('TESTSERVICE-Anmeldung bestätigt · Azure-Teamdaten antworten verzögert. Ein kontrollierter Wiederholungsversuch läuft …','');
   progress(12,'Azure-Teamdaten antworten verzögert · einmaliger Wiederholungsversuch …');
   await new Promise(function(resolve){native.setTimeout(resolve,1200)});
   return loadState({timeoutMs:24000,maxAttempts:1})
  }
 }
 try{return await loadState({timeoutMs:25000,maxAttempts:1})}catch(firstError){if(isSessionError(firstError)&&await renewSessionOnce())return loadState({timeoutMs:25000,maxAttempts:1});throw firstError}
}
async function finishAuthenticatedLogin(){
 stopLoginProgress();progress(runtime.sessionRestored?5:6,runtime.sessionRestored?'Gespeicherte Sitzung wird geprüft …':'Anmeldung bestätigt · Teamdaten werden vorbereitet …');status(runtime.sessionRestored?'Sitzung wird nach dem Neuladen wiederhergestellt …':'Anmeldung bestätigt. Teamdaten werden geladen …','ok');
 if(isTestServiceOrigin()&&runtime.sessionRestored){try{await rejectTestserviceNonGlobal(runtime.user,runtime.authToken)}catch(e){hideProgress();status((e&&e.message)||'TESTSERVICE-Zugang verweigert.','bad');var f=by('loginFields'),l=by('login'),a=by('app');if(f)f.classList.remove('hidden');if(l)l.classList.remove('hidden');if(a)a.classList.add('hidden');setLoginEnabled(true);return false}}
 else if(isTestServiceOrigin()){try{await rejectTestserviceNonGlobal(runtime.user,runtime.authToken)}catch(e){hideProgress();status((e&&e.message)||'TESTSERVICE-Zugang verweigert.','bad');var f2=by('loginFields'),l2=by('login'),a2=by('app');if(f2)f2.classList.remove('hidden');if(l2)l2.classList.remove('hidden');if(a2)a2.classList.add('hidden');setLoginEnabled(true);return false}}
 try{var loginBox=by('login'),appBox=by('app'),contentBox=by('content');if(loginBox)loginBox.classList.add('hidden');if(appBox)appBox.classList.remove('hidden');if(contentBox&&!text(contentBox.textContent))contentBox.innerHTML='<section class="card" id="rc654LoadState"><h2>ExportHUB wird geladen …</h2><p>Teamdaten und Funktionen werden synchronisiert.</p></section>'}catch(_){}
 try{if(runtime.sessionRestored&&!isTestServiceOrigin())await verifySessionForLoad();await loadStateAfterLogin();runtime.sessionRefreshAttempted=false;await loadCanonicalModules()}
 catch(e){stopLoginProgress();stopStartupStateReadProgress();hideProgress();runtime.loading=false;runtime.loaded=false;var testService=isTestServiceOrigin();if(isSessionError(e)&&!testService){clearTabSession();runtime.authToken='';runtime.user=null;runtime.users=[];runtime.state=null}if(!testService)runtime.loginRetryCredentials=null;runtime.sessionRefreshAttempted=false;const code=e&&e.code?'\nFehlercode: '+e.code:'';const prefix=isSessionError(e)?'ExportHUB-Sitzung konnte nicht bestätigt werden: ':'ExportHUB konnte nicht geladen werden: ';status(prefix+(e.message||'Unbekannter Fehler')+code+(testService?'\nDie Anmeldung wird nicht erneut gesendet. Du kannst die Teamdaten mit derselben Sitzung erneut laden.':''),'bad');const fields=by('loginFields'),change=by('pwChange'),loginBox=by('login'),appBox=by('app'),retry=by('testserviceContinueBtn');if(fields)fields.classList.remove('hidden');if(change)change.classList.add('hidden');if(loginBox)loginBox.classList.remove('hidden');if(appBox)appBox.classList.add('hidden');if(retry)retry.classList.toggle('hidden',!(testService&&text(runtime.authToken)));setLoginEnabled(true);console.error(e)}
}
function showPasswordChange(message){
 const fields=by('loginFields'),change=by('pwChange');
 if(fields)fields.classList.add('hidden');if(change)change.classList.remove('hidden');
 status(message||'Bitte das automatisch erzeugte Startpasswort ändern.','');
}
async function login(manual){
 if(runtime.loading||runtime.loginBusy)return false;if(logoutSignalPresent()&&manual!==true){showLoggedOutScreen();return false}if(manual===true){clearLogoutSignalForManualLogin();runtime.sessionExpiredHandled=false}
 const name=by('loginUser')&&by('loginUser').value,pass=by('loginPass')&&by('loginPass').value;if(!text(name)||!text(pass)){status('Benutzername und Passwort sind erforderlich.','bad');return false}
 var testLogin=isTestServiceOrigin(),retry=by('testserviceContinueBtn');if(retry)retry.classList.add('hidden');runtime.sessionRestored=false;runtime.loginBusy=true;setLoginEnabled(false);status(testLogin?'TESTSERVICE-Anmeldung wird einmalig geprüft …':'ExportHUB-Anmeldung wird geprüft …','');startLoginProgress();
 try{const d=testLogin?await testserviceLoginOnce(name,pass,runtime.adminRecoveryRequested===true):await authCall('login',{username:name,password:pass,deviceId:runtime.deviceId,recoveryRequested:runtime.adminRecoveryRequested===true});if(testLogin)await rejectTestserviceNonGlobal(d.user,d.token);stopLoginProgress();progress(6,'Anmeldung bestätigt · Teamdaten werden vorbereitet …');runtime.authToken=text(d.token);runtime.passwordChangeTicket=d.passwordChangeTicket||'';runtime.user=d.user||null;runtime.users=d.user?[d.user]:[];runtime.loginRetryCredentials=testLogin?null:{username:text(name),password:String(pass||''),deviceId:runtime.deviceId};ru

=== 'exporthub-state' hits=21 ===

--- hit 1 pos=1898 nearest_function=perfThreshold ---
<!DOCTYPE html>

<html lang="de">
<head>
<meta charset="utf-8"/>
<meta name="referrer" content="no-referrer"/>
<meta name="robots" content="noindex,nofollow,noarchive,nosnippet"/>
<script id="exporthub-rc863-diagnostics-bootstrap">
(function(){
'use strict';
if(window.__EXPORTHUB_RC863_DIAGNOSTICS_BOOT__)return;
window.__EXPORTHUB_RC863_DIAGNOSTICS_BOOT__=true;
var KEY='exporthub:rc864:diagnostics',MAX=180,DIAG_COALESCE_MS=5000,records=[],seq=0,persistTimer=0,uiTimer=0,lastSignal={name:'',at:0,detail:null},renderBurst=Object.create(null),nativeConsole={error:console.error&&console.error.bind(console),warn:console.warn&&console.warn.bind(console),info:console.info&&console.info.bind(console)},nativeAlert=typeof window.alert==='function'?window.alert.bind(window):null,nativeFetch=typeof window.fetch==='function'?window.fetch.bind(window):null;
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function now(){return new Date().toISOString()}
function currentView(){try{var s=window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.ExportHUBClean&&window.ExportHUBClean.runtime&&window.ExportHUBClean.runtime.state)||window.state||{};return q(s.view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):'')||'start')}catch(_){return q(document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):'')||'start')}}
function safeUrl(value){try{var u=new URL(String(value||''),location.href),keep=[];['action','mode','full','ack','environment'].forEach(function(k){if(u.searchParams.has(k))keep.push(k+'='+encodeURIComponent(u.searchParams.get(k)||''))});return u.pathname+(keep.length?'?'+keep.join('&'):'')}catch(_){return q(String(value||'').split('?')[0]).slice(0,300)}}
function perfThreshold(url){url=low(url);if(/exporthub-state.*mode=meta|diagnostics-(?:append|read)/.test(url))return 8000;if(/exporthub-auth/.test(url))return 5000;if(/location-booking/.test(url))return 4000;if(/pickup-status/.test(url))return 3500;return 1500}
function expectedProbe404(url,status){return Number(status)===404&&/\/api\/pickup-(?:status|pod)(?:$|\?)/i.test(String(url||''))}
function serverPhaseDetails(res){var out={};try{var h=res&&res.headers;if(!h||typeof h.get!=='function')return out;[['serverMs','x-exporthub-server-ms'],['authMs','x-exporthub-auth-ms'],['teamMs','x-exporthub-team-ms'],['mergeMs','x-exporthub-merge-ms'],['uploadMs','x-exporthub-upload-ms'],['retryReadMs','x-exporthub-retry-read-ms']].forEach(function(pair){var v=h.get(pair[1]);if(v!==null&&v!==''){var n=Number(v);if(Number.isFinite(n))out[pair[0]]=n}});var cache=h.get('x-exporthub-team-cache');if(cache)out.teamCache=q(cache)}catch(_){}return out}
/* exporthub-rc902-diagnostics-security */
function cleanText(v){var text='';try{if(v instanceof Error)text=(v.name? v.name+': ':'')+(v.message||'')+(v.stack?' | '+v.stack:'');else if(typeof v==='object'&&v!==null)text=JSON.stringify(v,function(k,val){return /token|authorization|password|passwort|session|signature|base64|dataurl/i.test(k)?'[geschützt]':val});else text=String(v==null?'':v)}catch(_){text=String(v==null?'':v)}return q(text).replace(/(bearer\s+)[a-z0-9._~+\/-]+/ig,'$1[geschützt]').replace(/([?&](?:password|passwort|username|user|token|session|auth|code)=)[^&\s)]+/ig,'$1[geschützt]').replace(/((?:password|passwort|username|user)\s*[:=]\s*)[^\s,;|]+/ig,'$1[geschützt]').slice(0,1800)}
function sanitizeDetails(d){if(!d||typeof d!=='object')return d==null?null:cleanText(d);var out={};Object.keys(d).slice(0,24).forEach(function(k){if(/token|authorization|password|passwort|session|signature|base64|dataurl|filedata|content/i.test(k)){out[k]='[geschützt]';return}var v=d[k];if(typeof v==='string')out[k]=cleanText(v).slice(0,500);else if(typeof v==='number'||typeof v==='boolean'||v==null)out[k]=v;else out[k]=cleanText(v).slice(0,500)});return out}
function load(){var raw='';try{raw=localStorage.getItem(KEY)||''}catch(_){}if(!raw)try{raw=sessionStorage.getItem(KEY)||''}catch(_){}try{var a=JSON.parse(raw||'[]');if(Array.isArray(a)){records=a.slice(-MAX);records.forEach(function(x){seq=Math.max(seq,Number(x&&x.seq||0))})}}catch(_){} }
function persistSoon(){if(persistTimer)return;persistTimer=setTimeout(function(){persistTimer=0;var raw=JSON.stringify(records.slice(-MAX));try{localStorage.setItem(KEY,raw)}catch(_){try{sessionStorage.setItem(KEY,raw)}catch(__){}}},4000)}
function scheduleUi(){if(uiTimer)return;uiTimer=setTimeout(function(){uiTimer=0;try{var d=window.ExportHUBDiagnostics863;if(d&&typeof d.refreshLocal==='function')d.refreshLocal();else if(d&&typeof d.refresh==='function')d.refresh(false)}catch(_){}},700)}
function fingerprint(x){return [x.level,x.category,x.area,x.message].join('|')}
function add(category,level,message,details,area){message=cleanText(message)||'Unbekanntes Ereignis';var rec={seq:++seq,at:now(),lastAt:now(),level:level||'info',category:category||'system',area:q(area||currentView()||'unbekannt'),message:message,details:sanitizeDetails(details),count:1},prev=records[records.length-1];if(prev&&fingerprint(prev)===fingerprint(rec)&&Date.now()-Date.parse(prev.lastAt||prev.at)<DIAG_COALESCE_MS){prev.count=Number(prev.count||1)+1;prev.lastAt=rec.at;if(rec.details)prev.details=rec.details}else{records.push(rec);if(records.length>MAX)records.splice(0,records.length-MAX)}persistSoon();scheduleUi();try{window.dispatchEvent(new CustomEvent('exporthub:diagnostic',{detail:{category:rec.category,level:rec.level,area:rec.area}}))}catch(_){}return rec}
function argsText(args){return Array.prototype.slice.call(args||[]).map(cleanText).filter(Boolean).join(' · ')}
load();
window.addEventListener('error',function(e){var target=e&&e.target;if(target&&target!==window&&target.tagName){add('resource','error','Ressource konnte nicht geladen werden: '+q(target.tagName),{url:safeUrl(target.src||target.href||''),tag:q(target.tagName)},currentView());return}add('javascript','error',e&&e.message||'JavaScript-Fehler',{file:safeUrl(e&&e.file

--- hit 2 pos=13717 nearest_function=watchContent ---
t:0,added:0,removed:0,areas:Object.create(null)};if(!areas.length)return;if(mutationCount>=120)add('performance','warning','Großer Render-Vorgang: '+label,detail,view);if(mutationCount<15)return;var key=low(view+'|'+label),a=renderBurst[key]||(renderBurst[key]=[]),t=Date.now();a.push(t);while(a.length&&t-a[0]>3500)a.shift();if(a.length>=4){add('performance','warning','Häufiges Neu-Rendern erkannt: '+label,{rendersIn3_5s:a.length,view:view,cause:cause},view);a.length=0}}
function watchContent(){var content=document.getElementById('content');if(!content||content.__ehDiag863Observed)return false;content.__ehDiag863Observed=true;var mo=new MutationObserver(function(list){var view=currentView();if(view==='diagnostics')return;var count=0,added=0,removed=0;for(var i=0;i<list.length;i++){var m=list[i];if(!m||m.type!=='childList')continue;var target=m.target&&m.target.nodeType===1?m.target:null;if(target&&target.closest&&(target.closest('#rc863Diagnostics')||target.closest('[data-rc871-diagnostics-view]')))continue;count++;added+=m.addedNodes?m.addedNodes.length:0;removed+=m.removedNodes?m.removedNodes.length:0}if(!count)return;pending.count+=count;pending.added+=added;pending.removed+=removed;pending.areas['Gesamtbereich '+(view||'unbekannt')]=1;if(renderTimer)clearTimeout(renderTimer);renderTimer=setTimeout(flushRender,320)});mo.observe(content,{childList:true,subtree:true});return true}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',watchContent,{once:true});else watchContent();
window.addEventListener('exporthub:ready',watchContent);window.addEventListener('exporthub:rendered',function(){setTimeout(watchContent,0)});window.addEventListener('exporthub:viewchange',function(){setTimeout(watchContent,0)});
window.__EXPORTHUB_DIAG863_STORE__={records:records,add:add,clear:function(){records.splice(0,records.length);persistSoon();scheduleUi()},safeUrl:safeUrl,currentView:currentView,max:MAX,uiQuietUntil:0,rawFetch:nativeFetch};
})();
</script>
<script id="exporthub-rc864-diagnostics-cloud">
(function(){
'use strict';
if(window.__EXPORTHUB_RC864_DIAGNOSTICS_CLOUD__)return;window.__EXPORTHUB_RC864_DIAGNOSTICS_CLOUD__=true;
var endpoint='/api/exporthub-state',remoteRecords=[],remoteMeta={status:'Warte auf Anmeldung',lastSyncAt:'',lastError:'',revision:0,total:0},uploadTimer=0,refreshTimer=0,busyUpload=false,busyRead=false,lastReadAt=0,uploadFailures=0,uploadRetryAt=0;
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function store(){return window.__EXPORTHUB_DIAG863_STORE__||null}
function rawFetch(){var s=store();return s&&typeof s.rawFetch==='function'?s.rawFetch:(typeof window.fetch==='function'?window.fetch.bind(window):null)}
function environment(){return /-testservice\./i.test(location.hostname)?'testservice':'production'}
function runtime(){return window.ExportHUBClean&&window.ExportHUBClean.runtime||{}}
function token(){var rt=runtime();return q(rt.authToken||rt.sessionToken||window.__EXPORTHUB_AUTH_TOKEN__||'')}
function user(){try{return (typeof window.__EXPORTHUB_GET_CURRENT_USER__==='function'&&window.__EXPORTHUB_GET_CURRENT_USER__())||runtime().user||window.currentUser||{}}catch(_){return window.currentUser||{}}}
function isGlobal(){var u=user(),role=low(u.role||u.rolle||u.level||u.type),name=low(u.user||u.username||u.login||u.name||u.displayName),p=Array.isArray(u.permissions)?u.permissions:[];return !!(u.globalAdmin===true||u.isGlobalAdmin===true||u.isAdmin===true||u.admin===true||p.indexOf('*')>=0||/global.?admin|administrator|vollzugriff/.test(role)||name==='tobias'||name==='t.limberg'||name==='tobias limberg'||name==='tobias.limberg')}
function deviceId(){var rt=runtime(),id=q(rt.deviceId);if(id)return id;var key='exporthub:diagnostics-device-id',saved='';try{saved=q(localStorage.getItem(key))}catch(_){}if(saved)return saved;try{var a=new Uint8Array(12);crypto.getRandomValues(a);saved='diag-'+Array.prototype.map.call(a,function(x){return x.toString(16).padStart(2,'0')}).join('')}catch(_){saved='diag-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,10)}try{localStorage.setItem(key,saved)}catch(_){}return saved}
function headers(){var t=token(),h={'Accept':'application/json','Content-Type':'application/json','Cache-Control':'no-cache','X-ExportHUB-Environment':environment()};if(t){h['X-ExportHUB-Token']=t;h['X-ExportHUB-Session']=t;h.Authorization='Bearer '+t}return h}
function clientVersion(){return q(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version||'RC864')}
function eventId(r){return deviceId()+'|'+String(Number(r&&r.seq||0))+'|'+q(r&&r.at||r&&r.lastAt)}
function localRecords(){var s=store();return s&&Array.isArray(s.records)?s.records:[]}
function lastSeqKey(){return'exporthub:rc864:diagnostics:last-seq:'+environment()+':'+deviceId()}
function lastSentSeq(){try{return Number(sessionStorage.getItem(lastSeqKey())||0)||0}catch(_){return 0}}
function setLastSentSeq(n){try{sessionStorage.setItem(lastSeqKey(),String(Number(n)||0))}catch(_){}}
function notify(){try{window.dispatchEvent(new CustomEvent('exporthub:diagnostics-cloud-updated',{detail:{status:remoteMeta.status,total:remoteMeta.total}}))}catch(_){}}
function setStatus(status,error){remoteMeta.status=status;remoteMeta.lastError=q(error||'');notify()}
async function request(mode,opt){var f=rawFetch(),t=token();if(!f)throw new Error('Fetch ist nicht verfügbar.');if(!t)throw new Error('Keine angemeldete ExportHUB-Sitzung.');if(navigator.onLine===false){var offline=new Error('Offline');offline.code='OFFLINE';throw offline}opt=opt||{};var method=opt.method||'GET',url=endpoint+'?mode='+encodeURIComponent(mode)+'&environment='+encodeURIComponent(environment());if(opt.limit)url+='&limit='+encodeURIComponent(opt.limit);var controller=typeof AbortController==='function'?new AbortController():null,timer=controller?setTimeout(function(){try{controller.abort()}catch(_){}},45000):0,init={method:method,credentials:'same-origin',cache:'no-store',headers:headers()};if(controller)init.signal=controller.signal;if(opt.body)init.body=JSON.stringify(opt.body);var r,raw='';try{r=await f(url,init)}finally{if(timer)clearTimeout(timer)}try{raw=await r.text()}catch(_){}var data={};try{data=raw?JSON.parse(raw):{}}catch(_){data={}}if(!r.ok||data.ok===false){var e=new Error(q(data.mess

--- hit 3 pos=68832 nearest_function=tokenFromUrl ---
6 vereinheitlicht den Neu-Sendungs-Reset: selectedLocationId, locationName sowie Empfänger-, Liefer- und Zieladresse werden gemeinsam mit den übrigen Standortfeldern zurückgesetzt.',
    'RC936 vereinheitlicht den bestehenden Speicher-/Wiederherstellungspfad: destinationId sowie Empfänger-, Liefer- und Zieladresse werden gemeinsam gelesen und wiederhergestellt, damit kein Standort-Alias mehr auseinanderläuft.',
    'RC935 beseitigt den letzten Standort-Alias-Drift beim Speichern und Synchronisieren: locationId, selectedLocationId, siteId und destinationId sowie Empfänger-, Liefer- und Zieladresse werden in allen Wiederherstellungspfaden gemeinsam gesetzt.',
    'RC935 entfernt beim Zurücksetzen eines Standorts auch destinationId sowie Empfänger- und Zielland, damit kein alter Zielstandort unsichtbar in CMR, Ladeliste oder Mailbereich erhalten bleibt.',
    'RC934 hält den gewählten Kundenstandort nach Speichern und erneutem Öffnen vollständig synchron: Location-ID, Destination-ID, Empfänger-, Liefer- und Zieladresse werden gemeinsam wiederhergestellt oder gemeinsam zurückgesetzt.',
    'RC933 übernimmt den gewählten Kundenstandort vollständig in die aktive Sendung: Location-ID sowie Empfänger-, Liefer- und Zieladresse bleiben für CMR, Ladeliste, Mailbereich und Speichern konsistent.',
    'RC932 korrigiert die Kundenauswahl in Sendung erstellen: Eine Auswahl aus Kundennummer/Kundensuche wird wieder sofort in den Sendungsentwurf übernommen und bleibt nicht nur sichtbar im Eingabefeld stehen.',
    'RC932 vereinheitlicht Standort auswählen: Alle hinterlegten Standort-/Adresslisten werden angezeigt; bei nur einer Kundenadresse wird Hauptadresse als einzelner Standort angezeigt und automatisch übernommen.',
     "RC931 misst State-API-Performance phasenweise: Authentifizierung, Teamstand/Cache, Merge, Azure-Upload und Server-Gesamtzeit werden getrennt in der Fehlerdiagnose ausgewiesen.",
     "RC931 ändert dabei keine Fach-, Rechte- oder Merge-Logik; die Messwerte dienen dazu, den verbleibenden Azure-Engpass aus RC930 eindeutig zu lokalisieren.",
    'RC930 verhindert einen stillen Testservice-Mischstand: Für read, meta und save akzeptiert der Browser nur noch eine exporthub-state-API ab RC925. Eine ältere oder nicht versionsgekennzeichnete State-API wird mit STATE_API_OUTDATED klar blockiert statt weiter mit den alten langsamen Speicherpfaden zu arbeiten.',
    'RC930 liefert die bereits in RC925 entwickelte schnelle exporthub-state-API jetzt zusätzlich als explizite Testservice-Datei aus. Dadurch können Warm-Cache, metadata-only Revisionsprüfung, optimistischer ETag-Save und kleiner ACK-Pfad auf dem echten Testservice tatsächlich aktiv werden.',
    'RC930 ändert keine fachlichen Teamdaten, Sendungsregeln oder Benutzerrechte. Bei Paralleländerungen bleibt If-Match aktiv; nach Konflikt wird der aktuelle Teamstand neu gelesen und erneut autorisiert.',
    'RC929 vergrößert auf der Ladeliste/POD-Ausgabe die Werte und Beschriftungen für Fahrername, Datum/Uhrzeit, Kennzeichen, Verlader und Europaletten. Das Unterschriftsfeld und die Signaturgröße bleiben unverändert.',
    'RC929 stellt Kennzeichen in der Ladelisten-/POD-Ausgabe konsequent in Großbuchstaben dar, ohne den gespeicherten Originalwert der Sendung zu verändern.',
    'RC929 verhindert den POD-Cloud-Abbruch allein wegen fehlendem EXPORTHUB_POD_DRIVE_USER: api/pod-backup verwendet dann dieselbe bereits projektweit festgelegte Standard-Zielablage; explizite App-Settings bleiben vorrangig.',
    'RC928 repariert den QR-Location-End-to-End-Pfad: Ein Scan darf den registrierten Location-Datensatz mit dem geheimen 48-stelligen Token laden, ohne eine interne ExportHUB-Anmeldung auf dem Fahrer-/Verladergerät zu benötigen.',
    'RC928 prüft jede öffentliche Location-Umbuchung wieder serverseitig mit dem persönlichen vierstelligen Verlader-PIN. Falsche PINs, nicht abholbereite oder bereits abgeholte Sendungen werden ohne Lageränderung abgewiesen.',
    'RC928 bindet die korrigierte api/location-booking/index.js zusätzlich als SHA-256-geprüften Produktions-Fallback ein, damit das Release-Center keinen älteren Location-API-Stand übernehmen kann.',
    'RC927 wartet vor Druck/PDF einer aktiven Sendung auf die serverseitige Registrierung des QR-Location-Datensatzes. Ein Deckblatt kann damit nicht mehr gedruckt werden, während der gelbe QR-Code zwar sichtbar, serverseitig aber noch nicht registriert ist.',
    'RC927 begrenzt diese QR-Location-Vorprüfung auf acht Sekunden und bricht die Ausgabe mit einem klaren Hinweis ab, statt einen nicht funktionsfähigen Location-QR auszugeben. Bereits abgeholte, abgeschlossene, archivierte oder stornierte Sendungen werden nicht erneut registriert.',
  'RC926 stellt den zweiten QR-Code auf dem Deckblatt wieder dauerhaft her: QR-Location wird nicht mehr allein wegen des Sendungsstatus Abgeholt/POD ausgeblendet. Solange ein gültiger Location-/Pickup-Token vorhanden und die Sendung nicht storniert ist, werden QR-Location links und QR-Abholung rechts gemeinsam dargestellt; die serverseitige Location-Sperre nach Abholung bleibt unverändert.',
  'RC925 entlastet Eingaben und schnelle Bedienaktionen: queueSave führt den teuren vollständigen Vergleich des gesamten Teamstands nicht mehr synchron bei jedem Tastendruck/Klick aus. Die Prüfung auf echte persistente Änderungen bleibt im entprellten flushSave erhalten, bevor ein Azure-Aufruf gestartet wird.',
  'RC925 verhindert damit UI-Hänger bei großen Teamständen, ohne zusätzliche Azure-Speicherungen zu erzeugen: unveränderte bzw. rein lokale UI-Zustände werden weiterhin vor dem Netzwerkzugriff als No-Change erkannt.',
  'RC925 reduziert im bestätigten Speicherpfad weitere vollständige Teamstand-Kopien: derselbe bereits getrennte Sendesnapshot wird für die laufende Speicherung wiederverwendet; der bestätigte Azure-Snapshot wird ohne erneutes JSON-Klonen als letzte Vergleichsbasis übernommen.',
  'RC925 hält den Azure-Teamstand innerhalb derselben warmen Function-Instanz als getrennten Snapshot im Speicher. Wiederholte Reads prüfen nur den Blob-ETag; unveränderte Teamdaten werden nicht erneut vollständig heruntergeladen.',
  'RC925 kann wiederholte Saves auf einer warmen Function-Instanz direkt mit dem zuletzt bestätigten ETag schreiben. Bei einer Paralleländerung erzwingt Azure per If-Match einen Konflikt; danach wird der aktuelle Teamstand neu gelesen, Benutzerre

--- hit 4 pos=69090 nearest_function=tokenFromUrl ---
: destinationId sowie Empfänger-, Liefer- und Zieladresse werden gemeinsam gelesen und wiederhergestellt, damit kein Standort-Alias mehr auseinanderläuft.',
    'RC935 beseitigt den letzten Standort-Alias-Drift beim Speichern und Synchronisieren: locationId, selectedLocationId, siteId und destinationId sowie Empfänger-, Liefer- und Zieladresse werden in allen Wiederherstellungspfaden gemeinsam gesetzt.',
    'RC935 entfernt beim Zurücksetzen eines Standorts auch destinationId sowie Empfänger- und Zielland, damit kein alter Zielstandort unsichtbar in CMR, Ladeliste oder Mailbereich erhalten bleibt.',
    'RC934 hält den gewählten Kundenstandort nach Speichern und erneutem Öffnen vollständig synchron: Location-ID, Destination-ID, Empfänger-, Liefer- und Zieladresse werden gemeinsam wiederhergestellt oder gemeinsam zurückgesetzt.',
    'RC933 übernimmt den gewählten Kundenstandort vollständig in die aktive Sendung: Location-ID sowie Empfänger-, Liefer- und Zieladresse bleiben für CMR, Ladeliste, Mailbereich und Speichern konsistent.',
    'RC932 korrigiert die Kundenauswahl in Sendung erstellen: Eine Auswahl aus Kundennummer/Kundensuche wird wieder sofort in den Sendungsentwurf übernommen und bleibt nicht nur sichtbar im Eingabefeld stehen.',
    'RC932 vereinheitlicht Standort auswählen: Alle hinterlegten Standort-/Adresslisten werden angezeigt; bei nur einer Kundenadresse wird Hauptadresse als einzelner Standort angezeigt und automatisch übernommen.',
     "RC931 misst State-API-Performance phasenweise: Authentifizierung, Teamstand/Cache, Merge, Azure-Upload und Server-Gesamtzeit werden getrennt in der Fehlerdiagnose ausgewiesen.",
     "RC931 ändert dabei keine Fach-, Rechte- oder Merge-Logik; die Messwerte dienen dazu, den verbleibenden Azure-Engpass aus RC930 eindeutig zu lokalisieren.",
    'RC930 verhindert einen stillen Testservice-Mischstand: Für read, meta und save akzeptiert der Browser nur noch eine exporthub-state-API ab RC925. Eine ältere oder nicht versionsgekennzeichnete State-API wird mit STATE_API_OUTDATED klar blockiert statt weiter mit den alten langsamen Speicherpfaden zu arbeiten.',
    'RC930 liefert die bereits in RC925 entwickelte schnelle exporthub-state-API jetzt zusätzlich als explizite Testservice-Datei aus. Dadurch können Warm-Cache, metadata-only Revisionsprüfung, optimistischer ETag-Save und kleiner ACK-Pfad auf dem echten Testservice tatsächlich aktiv werden.',
    'RC930 ändert keine fachlichen Teamdaten, Sendungsregeln oder Benutzerrechte. Bei Paralleländerungen bleibt If-Match aktiv; nach Konflikt wird der aktuelle Teamstand neu gelesen und erneut autorisiert.',
    'RC929 vergrößert auf der Ladeliste/POD-Ausgabe die Werte und Beschriftungen für Fahrername, Datum/Uhrzeit, Kennzeichen, Verlader und Europaletten. Das Unterschriftsfeld und die Signaturgröße bleiben unverändert.',
    'RC929 stellt Kennzeichen in der Ladelisten-/POD-Ausgabe konsequent in Großbuchstaben dar, ohne den gespeicherten Originalwert der Sendung zu verändern.',
    'RC929 verhindert den POD-Cloud-Abbruch allein wegen fehlendem EXPORTHUB_POD_DRIVE_USER: api/pod-backup verwendet dann dieselbe bereits projektweit festgelegte Standard-Zielablage; explizite App-Settings bleiben vorrangig.',
    'RC928 repariert den QR-Location-End-to-End-Pfad: Ein Scan darf den registrierten Location-Datensatz mit dem geheimen 48-stelligen Token laden, ohne eine interne ExportHUB-Anmeldung auf dem Fahrer-/Verladergerät zu benötigen.',
    'RC928 prüft jede öffentliche Location-Umbuchung wieder serverseitig mit dem persönlichen vierstelligen Verlader-PIN. Falsche PINs, nicht abholbereite oder bereits abgeholte Sendungen werden ohne Lageränderung abgewiesen.',
    'RC928 bindet die korrigierte api/location-booking/index.js zusätzlich als SHA-256-geprüften Produktions-Fallback ein, damit das Release-Center keinen älteren Location-API-Stand übernehmen kann.',
    'RC927 wartet vor Druck/PDF einer aktiven Sendung auf die serverseitige Registrierung des QR-Location-Datensatzes. Ein Deckblatt kann damit nicht mehr gedruckt werden, während der gelbe QR-Code zwar sichtbar, serverseitig aber noch nicht registriert ist.',
    'RC927 begrenzt diese QR-Location-Vorprüfung auf acht Sekunden und bricht die Ausgabe mit einem klaren Hinweis ab, statt einen nicht funktionsfähigen Location-QR auszugeben. Bereits abgeholte, abgeschlossene, archivierte oder stornierte Sendungen werden nicht erneut registriert.',
  'RC926 stellt den zweiten QR-Code auf dem Deckblatt wieder dauerhaft her: QR-Location wird nicht mehr allein wegen des Sendungsstatus Abgeholt/POD ausgeblendet. Solange ein gültiger Location-/Pickup-Token vorhanden und die Sendung nicht storniert ist, werden QR-Location links und QR-Abholung rechts gemeinsam dargestellt; die serverseitige Location-Sperre nach Abholung bleibt unverändert.',
  'RC925 entlastet Eingaben und schnelle Bedienaktionen: queueSave führt den teuren vollständigen Vergleich des gesamten Teamstands nicht mehr synchron bei jedem Tastendruck/Klick aus. Die Prüfung auf echte persistente Änderungen bleibt im entprellten flushSave erhalten, bevor ein Azure-Aufruf gestartet wird.',
  'RC925 verhindert damit UI-Hänger bei großen Teamständen, ohne zusätzliche Azure-Speicherungen zu erzeugen: unveränderte bzw. rein lokale UI-Zustände werden weiterhin vor dem Netzwerkzugriff als No-Change erkannt.',
  'RC925 reduziert im bestätigten Speicherpfad weitere vollständige Teamstand-Kopien: derselbe bereits getrennte Sendesnapshot wird für die laufende Speicherung wiederverwendet; der bestätigte Azure-Snapshot wird ohne erneutes JSON-Klonen als letzte Vergleichsbasis übernommen.',
  'RC925 hält den Azure-Teamstand innerhalb derselben warmen Function-Instanz als getrennten Snapshot im Speicher. Wiederholte Reads prüfen nur den Blob-ETag; unveränderte Teamdaten werden nicht erneut vollständig heruntergeladen.',
  'RC925 kann wiederholte Saves auf einer warmen Function-Instanz direkt mit dem zuletzt bestätigten ETag schreiben. Bei einer Paralleländerung erzwingt Azure per If-Match einen Konflikt; danach wird der aktuelle Teamstand neu gelesen, Benutzerrechte werden erneut geprüft und erst dann zusammengeführt.',
  'RC925 beendet eine ACK-Bestätigung ohne Paralleländerung ohne zweiten Vollklon und ohne zweiten Vollvergleich des Teamstands. Die Sendungssperre wird außerdem nur bei tatsächlichen Sendungsänderu

--- hit 5 pos=107853 nearest_function=<none> ---
n für Speditionsmail/Eigene Info-Mail und keinen EML-Mailpaket-Button mehr; die normalen Mailvorlagen und Outlook-mailto-Funktion bleiben erhalten.",
     "RC861 vereinheitlicht den Lieferavis-Status über alle Kopien derselben Sendung. Maßgeblich ist das zeitlich jüngste echte Avis-Ereignis (Aktivierung oder Deaktivierung) statt eines zufällig neueren, aber fachlich veralteten Sendungsobjekts.",
     "RC861 stellt den Lieferavis-Link nach erfolgreicher Aktivierung wieder zuverlässig in der Kundenmail dar. Eine alte deaktivierte Sendungskopie darf den aktuellen aktiven Avis weder im Mailtext noch im Kundenportal überdecken.",
     "RC861 macht die Customer-Avis-API token-konsistent: Ein aktiver aktueller Token bleibt gültig, alte Tokens nach erneuter Aktivierung bleiben ungültig und eine spätere manuelle/automatische Deaktivierung gewinnt weiterhin eindeutig.",

     "RC860 entfernt den kompletten Bereich Dateien für Kundenmail. Kundenmails benötigen keinen separaten EML-/Anhang-Workflow mehr; bei aktivem Lieferavis ruft der Kunde seine Lieferscheine direkt im Avis-Portal ab.",
     "RC860 zeigt im Kunden-Avis einen eigenen Bereich Lieferscheine. Dort erscheinen ausschließlich tatsächlich abrufbare Lieferschein-Dateien mit Öffnen/Download; reine Metadaten werden niemals als Kundendownload angeboten.",
     "RC860 lässt die Dateilogik für Speditionsmail und Eigene Info-Mail unverändert bestehen. POD bleibt im Avis weiterhin als eigener Downloadbereich erhalten.",

     "RC859 speichert neu zugewiesene Lieferschein-PDFs mit echtem Dateiinhalt statt nur Dateiname und Größe. Dadurch können sie im Mailbereich geöffnet, heruntergeladen und als echter EML-Anhang verwendet werden.",
     "RC859 verhindert irreführende Pflichtauswahl: Dateien ohne erreichbaren Inhalt werden nicht mehr automatisch angehakt und zählen nicht als ausgewählter Anhang.",
     "RC859 zeigt beim EML-Mailpaket einen konkreten Sperrgrund, solange kein echter Anhang verfügbar/ausgewählt ist. Vorhandene alte Metadaten-Dateien bleiben sichtbar, müssen aber einmal neu zugewiesen werden, falls der Ref-Ordner sie nicht liefern kann.",

     "RC858 beendet den großen Rückkanal nach jedem Azure-Save: /api/exporthub-state antwortet nach erfolgreichem Schreiben nur noch mit einer kleinen ACK-Bestätigung aus ok, Umgebung, Blob und Revision statt den vollständigen Teamstand zurückzusenden.",
     "RC858 beseitigt damit die Hauptursache für Failed to fetch beim Speichern großer ExportHUB-Bestände. Der Browser muss nach einer Änderung nicht mehr den kompletten Azure-Teamstand als Save-Antwort empfangen.",
     "RC858 behandelt die serverseitige ACK-Bestätigung unmittelbar als dauerhaft gespeichert. Sendung und Kunden-Avis lösen nach erfolgreichem ACK keinen zweiten Vollstand-Read mehr aus und dürfen den lokalen Zustand deshalb nicht wegen eines nachgelagerten Netzwerkfehlers zurückrollen.",
     "RC858 hält die RC855-Testservice-Isolation unverändert aufrecht: Auch die kleine Save-Antwort muss weiterhin exakt die erwartete Umgebung production beziehungsweise testservice und den dazu passenden Blob bestätigen.",
     "RC857 repariert den Testservice-Start nach der harten Datenumgebungs-Trennung. Azure-Proxy-Hosts dürfen die sichtbare Testservice-Origin nicht mehr fälschlich als Produktionsaufruf einstufen; Browser-Origin/Referer und der explizite Environment-Header entscheiden verbindlich.",
     "RC857 Lieferavis ersetzt Ansprechpartner / Abholer vollständig durch ein optionales Kennzeichen und eine verpflichtende Sendungsnummer. Die Sendungsnummer wird ohne Überschreiben vorhandener Inhalte als eigener Lieferavis-Eintrag in der internen Sendungsnotiz gespeichert.",
     "RC857 zeigt Kennzeichen und Sendungsnummer in der internen Avis-Rückmeldung sowie nach erneutem Öffnen der Sendung an. Wiederholte Avis-Meldungen aktualisieren denselben Sendungsnummer-Eintrag statt Duplikate zu erzeugen.",
     "RC857 trennt auch die öffentliche customer-avis API zwischen Testservice und Produktion. Ein Avis aus dem Testservice liest und schreibt ausschließlich testservice/team-state.json; Produktions-Avis bleiben ausschließlich im Produktionsbestand.",
     "RC856 wertet eine erfolgreich beantwortete Azure-Speicheranforderung mit ok=true, bestätigter Datenumgebung und Server-Revision als verbindliche Dauer-Speicherbestätigung. Ein verzögerter Kontroll-Read darf diesen bereits bestätigten Save nicht mehr fälschlich in 'nicht dauerhaft gespeichert' zurückstufen.",
     "RC856 verhindert beim Kunden-Avis das Zurückrollen eines bereits serverseitig gespeicherten Aktivieren/Deaktivieren nur wegen eines späteren, veralteten Kontroll-Snapshots. Netzwerk- oder API-Fehler vor der Serverbestätigung bleiben weiterhin echte Fehler.",
     "RC856 behandelt einen Save ohne noch offene Änderungen als erfolgreich. Dadurch erzeugen idempotente Benutzeraktionen keine falsche Azure-Fehlermeldung mehr, wenn der gewünschte Stand bereits gespeichert ist.",
     "RC856 protokolliert die letzte serverseitig bestätigte Save-Generation, Revision, Operation-ID und Datenumgebung. Sendung speichern und Lieferavis können diese Bestätigung gemeinsam verwenden, statt voneinander abweichende Erfolgskriterien zu haben.",
     "RC856 behält die RC855-Trennung von Testservice und Produktion unverändert bei; die stärkere Save-Bestätigung gilt nur innerhalb der vom Server bestätigten richtigen Datenumgebung.",

     "RC855 trennt Testservice und Produktion auf Azure-Speicherebene. Die Produktionsdomain verwendet weiterhin team-state.json; die echte -testservice-Domain verwendet ausschließlich testservice/team-state.json beziehungsweise EXPORTHUB_TEST_STORAGE_BLOB.",
     "RC855 verlangt bei jedem Teamdaten-Read, Meta-Check und Save eine bestätigte Server-Umgebung. Der Testservice verweigert den Start und jede Speicherung, wenn die State-API die isolierte testservice-Umgebung nicht ausdrücklich bestätigt. Damit kann eine alte gemeinsame State-API keine Testdaten mehr unbemerkt in Produktion schreiben.",
     "RC855 initialisiert den isolierten Testservice-Bestand beim ersten Start einmalig aus dem aktuellen Produktionsstand, damit reale Ausgangsdaten zum Testen verfügbar bleiben. Ab diesem Zeitpunkt laufen Revisionen, Saves, Recovery-Versionen und Backups des Testservice ausschließlich im Testservice-Namensraum.",
     "RC855 hält die Produktionsfreigabe konsistent: Die geprüfte ap

=== 'Promise.race' hits=1 ===

--- hit 1 pos=4348259 nearest_function=createPdf ---
 hit?(hit.textContent||''):'';
}
async function renderer(targetWindow){
 var w=targetWindow||window;
 if(typeof w.html2canvas==='function')return w.html2canvas;
 if(w!==window){
  var source=bundledHtml2CanvasSource();
  if(source){var script=w.document.createElement('script');script.textContent=source;w.document.head.appendChild(script);if(typeof w.html2canvas==='function')return w.html2canvas}
 }
 if(typeof window.html2canvas==='function')return window.html2canvas;
 var ensure=window.ExportHUBLazy&&window.ExportHUBLazy.ensureHtml2Canvas,fn=ensure?await ensure():null;
 if(typeof fn!=='function')throw new Error('Der PDF-Renderer ist nicht verfügbar.');
 return fn;
}
/* exporthub-rc913-pdf-without-qr */
function stripQrForPdf(root){
 if(!root||!root.querySelectorAll)return root;
 var nodes=Array.prototype.slice.call(root.querySelectorAll('.rc390-cover-qr,.rc390-qrslot,.index351-qr-box,.index352-qr-box,[data-index352-qr="1"],[data-index240-qr="1"],[data-index240-cover-qr="1"],[data-index240-cover-location-qr="1"],[data-index240-cover-pickup-qr="1"]'));
 nodes.forEach(function(node){
  if(!node)return;
  if(node.matches&&node.matches('.rc390-qrslot,.index351-qr-box,.index352-qr-box,[data-index352-qr="1"],[data-index240-qr="1"],[data-index240-cover-qr="1"],[data-index240-cover-location-qr="1"],[data-index240-cover-pickup-qr="1"]')){
   node.innerHTML='';
   if(node.classList)node.classList.add('empty')
  }
  if(node.style)node.style.setProperty('visibility','hidden','important');
  if(node.setAttribute)node.setAttribute('data-rc913-pdf-qr-hidden','1')
 });
 return root
}

async function createPdf(mode,sh){var outputMode=low(mode)==='pod'?'pod':'normal';await ensureOutputReady(sh,outputMode);var frame=null,root=null,source=document.createElement('div');source.innerHTML=documentHtml(sh);decorateDocument(sh,source,outputMode);try{frame=documentWorkFrame('pdf');var doc=resetDocumentWorkFrame(frame,'PDF '+(sref(sh)||sid(sh)));root=clonePages(mode,doc,source);root.className='index352-pdf-root';root.setAttribute('data-index352-output-kind',outputMode);doc.body.appendChild(root);decorateDocument(sh,doc,outputMode);stripQrForPdf(root);try{if(doc.fonts&&doc.fonts.ready)await Promise.race([doc.fonts.ready,new Promise(function(resolve){later(resolve,500)})])}catch(_){}await waitImages(root,outputMode==='pod'?3000:1800);var h2c=await timed(renderer(frame.contentWindow),15000,'Der PDF-Renderer konnte nicht geladen werden.'),nodes=Array.prototype.slice.call(root.children),images=[],renderScale=outputMode==='pod'?2:1.5,jpegQuality=outputMode==='pod'?.96:.92;if(!nodes.length)throw new Error('Die ausgewählten Dokumentseiten wurden nicht gefunden.');for(var i=0;i<nodes.length;i++){var node=nodes[i],canvas=await timed(h2c(node,{scale:renderScale,backgroundColor:'#ffffff',useCORS:false,allowTaint:false,logging:false,scrollX:0,scrollY:0,windowWidth:Math.max(900,node.scrollWidth||900),windowHeight:Math.max(1250,node.scrollHeight||1250)}),45000,'PDF-Seite '+(i+1)+' konnte nicht innerhalb von 45 Sekunden erstellt werden.');images.push({bytes:bytesFromDataUrl(canvas.toDataURL('image/jpeg',jpegQuality)),width:canvas.width,height:canvas.height})}return{blob:multiPagePdf(images),pages:images.length,signed:outputMode==='pod',renderScale:renderScale}}catch(error){if(frame&&frame===pdfFrameCache&&/Arbeitsbereich|Renderer/i.test(q(error&&error.message||error))){try{frame.remove()}catch(_){}pdfFrameCache=null}throw error}finally{if(frame&&frame.contentDocument&&frame.contentDocument.body)try{frame.contentDocument.body.replaceChildren()}catch(_){}}}
function fileName(mode,sh){var ref=sref(sh)||sid(sh)||'Sendung';return mode==='all'?'Gesamtausgabe_'+ref+'.pdf':mode==='load1'?'Ladeliste_1_'+ref+'.pdf':mode==='load2'?'Ladeliste_2_'+ref+'.pdf':'POD_'+ref+'_Ladeliste_mit_Unterschrift.pdf'}
async function downloadDocument(mode,button){if(mode==='load2'){alert('Ladeliste Seite 2 wurde entfernt. Bitte Ladeliste Seite 1 verwenden.');return false}if(pdfBusy)return false;var sh=currentSaved();if(!sh){alert('Bitte zuerst eine gespeicherte Sendung auswählen.');return false}pdfBusy=true;var old=q(button&&button.textContent),label=mode==='all'?'Gesamtausgabe':mode==='load1'?'Ladeliste 1':'Ladeliste 2',opToken=docWorkStart(label+' als PDF wird vorbereitet …');if(button){button.disabled=true;button.textContent=label+' wird erstellt …'}try{docWorkStep(opToken,'PDF-Dokumentseiten werden vorbereitet …');var out=await createPdf(mode||'all',sh);docWorkStep(opToken,'PDF wird zum Download bereitgestellt …');downloadBlob(out.blob,fileName(mode||'all',sh));window.__INDEX352_LAST_PDF__={ok:true,mode:mode||'all',pages:out.pages,bytes:out.blob.size,ref:sref(sh)||sid(sh),owner:VERSION};docWorkDone(opToken,label+' als PDF erstellt')}catch(e){console.error('RC352 PDF',e);var detail=q(e&&e.message||e);window.__INDEX352_LAST_PDF__={ok:false,mode:mode||'all',error:detail,owner:VERSION};docWorkFail(opToken,label+' konnte nicht erstellt werden');message(label+' konnte nicht erstellt werden: '+detail,'bad');alert(label+' konnte nicht heruntergeladen werden.\n\n'+detail)}finally{pdfBusy=false;if(button){button.disabled=false;button.textContent=old}}return false}
async function syncPod(button){var sh=currentSaved(),api=window.ExportHUBPickupPOD;if(!sh||!api)return false;var old=q(button&&button.textContent);if(button){button.disabled=true;button.textContent='QR/POD wird geprüft …'}try{await api.syncOne(sh,true);if(api.hasSignature(sh))await hydrateSignature(sh);decorateDocument(sh,document,'normal');message(api.hasSignature(sh)?'POD vorhanden: Die Unterschrift wird ausschließlich in der separaten POD-PDF verwendet. Die normale Ladeliste bleibt ohne Unterschrift.':'Abholung noch nicht vollständig mit Unterschrift bestätigt.',api.hasSignature(sh)?'ok':'info')}catch(e){message('QR/POD-Status konnte nicht vollständig geladen werden: '+q(e&&e.message||e),'bad')}finally{if(button){button.disabled=false;button.textContent=old}}return false}
async function downloadPod(button){var sh=currentSaved(),api=window.ExportHUBPickupPOD;if(!sh||!api)return false;try{await api.syncOne(sh,true)}catch(_){}if(!api.hasSignature(sh)){alert('Es liegt noch kein echter POD mit digitaler Unterschrift vor.');return false}if(pdfBusy)return false;pdfBusy=true;var old=q(button&&button.textContent);if(button){button.disabled=true;button.textContent='Unterschriebene Ladeliste wird erstellt 

=== 'AbortController' hits=22 ===

--- hit 1 pos=17383 nearest_function=request ---
e==='tobias'||name==='t.limberg'||name==='tobias limberg'||name==='tobias.limberg')}
function deviceId(){var rt=runtime(),id=q(rt.deviceId);if(id)return id;var key='exporthub:diagnostics-device-id',saved='';try{saved=q(localStorage.getItem(key))}catch(_){}if(saved)return saved;try{var a=new Uint8Array(12);crypto.getRandomValues(a);saved='diag-'+Array.prototype.map.call(a,function(x){return x.toString(16).padStart(2,'0')}).join('')}catch(_){saved='diag-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,10)}try{localStorage.setItem(key,saved)}catch(_){}return saved}
function headers(){var t=token(),h={'Accept':'application/json','Content-Type':'application/json','Cache-Control':'no-cache','X-ExportHUB-Environment':environment()};if(t){h['X-ExportHUB-Token']=t;h['X-ExportHUB-Session']=t;h.Authorization='Bearer '+t}return h}
function clientVersion(){return q(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version||'RC864')}
function eventId(r){return deviceId()+'|'+String(Number(r&&r.seq||0))+'|'+q(r&&r.at||r&&r.lastAt)}
function localRecords(){var s=store();return s&&Array.isArray(s.records)?s.records:[]}
function lastSeqKey(){return'exporthub:rc864:diagnostics:last-seq:'+environment()+':'+deviceId()}
function lastSentSeq(){try{return Number(sessionStorage.getItem(lastSeqKey())||0)||0}catch(_){return 0}}
function setLastSentSeq(n){try{sessionStorage.setItem(lastSeqKey(),String(Number(n)||0))}catch(_){}}
function notify(){try{window.dispatchEvent(new CustomEvent('exporthub:diagnostics-cloud-updated',{detail:{status:remoteMeta.status,total:remoteMeta.total}}))}catch(_){}}
function setStatus(status,error){remoteMeta.status=status;remoteMeta.lastError=q(error||'');notify()}
async function request(mode,opt){var f=rawFetch(),t=token();if(!f)throw new Error('Fetch ist nicht verfügbar.');if(!t)throw new Error('Keine angemeldete ExportHUB-Sitzung.');if(navigator.onLine===false){var offline=new Error('Offline');offline.code='OFFLINE';throw offline}opt=opt||{};var method=opt.method||'GET',url=endpoint+'?mode='+encodeURIComponent(mode)+'&environment='+encodeURIComponent(environment());if(opt.limit)url+='&limit='+encodeURIComponent(opt.limit);var controller=typeof AbortController==='function'?new AbortController():null,timer=controller?setTimeout(function(){try{controller.abort()}catch(_){}},45000):0,init={method:method,credentials:'same-origin',cache:'no-store',headers:headers()};if(controller)init.signal=controller.signal;if(opt.body)init.body=JSON.stringify(opt.body);var r,raw='';try{r=await f(url,init)}finally{if(timer)clearTimeout(timer)}try{raw=await r.text()}catch(_){}var data={};try{data=raw?JSON.parse(raw):{}}catch(_){data={}}if(!r.ok||data.ok===false){var e=new Error(q(data.message)||('HTTP '+r.status));e.status=r.status;e.code=q(data.code);throw e}return data}
async function uploadPending(force){if(busyUpload)return false;var t=token();if(!t){setStatus('Warte auf Anmeldung');return false}if(!force&&(document.hidden||navigator.onLine===false||Date.now()<uploadRetryAt)){setStatus(navigator.onLine===false?'Diagnose lokal · offline':'Azure-Sync ausstehend',remoteMeta.lastError);return false}var rows=localRecords(),last=lastSentSeq(),pending=rows.filter(function(r){return Number(r&&r.seq||0)>last});if(!pending.length){if(remoteMeta.status==='Warte auf Anmeldung')setStatus('Azure bereit');return false}var batch=pending.slice(0,50).map(function(r){return{id:eventId(r),seq:Number(r.seq||0),at:r.at,lastAt:r.lastAt,level:r.level,category:r.category,area:r.area,message:r.message,details:r.details,count:r.count}});busyUpload=true;try{setStatus('Synchronisiert Diagnose …');var data=await request('diagnostics-append',{method:'POST',body:{action:'diagnostics-append',environment:environment(),deviceId:deviceId(),clientVersion:clientVersion(),records:batch}});var maxSeq=Math.max.apply(null,batch.map(function(r){return Number(r.seq||0)}));setLastSentSeq(maxSeq);remoteMeta.lastSyncAt=new Date().toISOString();remoteMeta.revision=Number(data.revision||remoteMeta.revision||0);remoteMeta.total=Number(data.records||remoteMeta.total||0);uploadFailures=0;uploadRetryAt=0;setStatus('Azure synchronisiert');if(pending.length>batch.length)scheduleUpload(1200);return true}catch(e){uploadFailures=Math.min(5,uploadFailures+1);var wait=Math.min(300000,30000*Math.pow(2,uploadFailures-1));uploadRetryAt=Date.now()+wait;setStatus('Azure-Sync ausstehend',e&&e.message||e);if(!document.hidden&&navigator.onLine!==false)scheduleUpload(wait);return false}finally{busyUpload=false}}
function scheduleUpload(delay){if(uploadTimer)clearTimeout(uploadTimer);if(document.hidden||navigator.onLine===false)return false;var wait=Math.max(500,Number(delay)||2200,uploadRetryAt>Date.now()?uploadRetryAt-Date.now():0);uploadTimer=setTimeout(function(){uploadTimer=0;uploadPending(false)},wait);return true}
async function refresh(force){if(!isGlobal())return false;if(busyRead)return false;if(!token()){setStatus('Warte auf Anmeldung');return false}if(!force&&Date.now()-lastReadAt<12000)return false;busyRead=true;try{var data=await request('diagnostics-read',{limit:5000});remoteRecords=Array.isArray(data.records)?data.records:[];remoteMeta.lastSyncAt=new Date().toISOString();remoteMeta.revision=Number(data.revision||0);remoteMeta.total=Number(data.total||remoteRecords.length);remoteMeta.maxRecords=Number(data.maxRecords||5000);remoteMeta.retentionDays=Number(data.retentionDays||30);lastReadAt=Date.now();setStatus('Azure synchronisiert');return true}catch(e){setStatus('Azure lesen fehlgeschlagen',e&&e.message||e);return false}finally{busyRead=false}}
function combinedRecords(){var out=remoteRecords.slice(),seen=Object.create(null);out.forEach(function(r){if(r&&r.id)seen[r.id]=1});localRecords().forEach(function(r){var id=eventId(r);if(seen[id])return;var u=user();out.push(Object.assign({},r,{id:id,user:q(u.name||u.user||u.username)||'Dieses Gerät',deviceId:deviceId(),clientVersion:clientVersion(),environment:environment(),localPending:true}));seen[id]=1});out.sort(function(a,b){return Date.parse(q(a.lastAt||a.at)) - Date.parse(q(b.lastAt||b.at))});return out}
function recentRecords(limit){var n=Math.max(1,Math.min(1000,Number(limit)||250)),out=remoteRecords.slice(-n),seen=Object.create(null),u=user();out.forEach(function(r){if(r&&r.id)seen[r.id]=1});localRecords().slice(-n).forEach(function(r){var id=eventId(r);if(seen[id])return;out.pus

--- hit 2 pos=17416 nearest_function=request ---
|name==='tobias limberg'||name==='tobias.limberg')}
function deviceId(){var rt=runtime(),id=q(rt.deviceId);if(id)return id;var key='exporthub:diagnostics-device-id',saved='';try{saved=q(localStorage.getItem(key))}catch(_){}if(saved)return saved;try{var a=new Uint8Array(12);crypto.getRandomValues(a);saved='diag-'+Array.prototype.map.call(a,function(x){return x.toString(16).padStart(2,'0')}).join('')}catch(_){saved='diag-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,10)}try{localStorage.setItem(key,saved)}catch(_){}return saved}
function headers(){var t=token(),h={'Accept':'application/json','Content-Type':'application/json','Cache-Control':'no-cache','X-ExportHUB-Environment':environment()};if(t){h['X-ExportHUB-Token']=t;h['X-ExportHUB-Session']=t;h.Authorization='Bearer '+t}return h}
function clientVersion(){return q(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version||'RC864')}
function eventId(r){return deviceId()+'|'+String(Number(r&&r.seq||0))+'|'+q(r&&r.at||r&&r.lastAt)}
function localRecords(){var s=store();return s&&Array.isArray(s.records)?s.records:[]}
function lastSeqKey(){return'exporthub:rc864:diagnostics:last-seq:'+environment()+':'+deviceId()}
function lastSentSeq(){try{return Number(sessionStorage.getItem(lastSeqKey())||0)||0}catch(_){return 0}}
function setLastSentSeq(n){try{sessionStorage.setItem(lastSeqKey(),String(Number(n)||0))}catch(_){}}
function notify(){try{window.dispatchEvent(new CustomEvent('exporthub:diagnostics-cloud-updated',{detail:{status:remoteMeta.status,total:remoteMeta.total}}))}catch(_){}}
function setStatus(status,error){remoteMeta.status=status;remoteMeta.lastError=q(error||'');notify()}
async function request(mode,opt){var f=rawFetch(),t=token();if(!f)throw new Error('Fetch ist nicht verfügbar.');if(!t)throw new Error('Keine angemeldete ExportHUB-Sitzung.');if(navigator.onLine===false){var offline=new Error('Offline');offline.code='OFFLINE';throw offline}opt=opt||{};var method=opt.method||'GET',url=endpoint+'?mode='+encodeURIComponent(mode)+'&environment='+encodeURIComponent(environment());if(opt.limit)url+='&limit='+encodeURIComponent(opt.limit);var controller=typeof AbortController==='function'?new AbortController():null,timer=controller?setTimeout(function(){try{controller.abort()}catch(_){}},45000):0,init={method:method,credentials:'same-origin',cache:'no-store',headers:headers()};if(controller)init.signal=controller.signal;if(opt.body)init.body=JSON.stringify(opt.body);var r,raw='';try{r=await f(url,init)}finally{if(timer)clearTimeout(timer)}try{raw=await r.text()}catch(_){}var data={};try{data=raw?JSON.parse(raw):{}}catch(_){data={}}if(!r.ok||data.ok===false){var e=new Error(q(data.message)||('HTTP '+r.status));e.status=r.status;e.code=q(data.code);throw e}return data}
async function uploadPending(force){if(busyUpload)return false;var t=token();if(!t){setStatus('Warte auf Anmeldung');return false}if(!force&&(document.hidden||navigator.onLine===false||Date.now()<uploadRetryAt)){setStatus(navigator.onLine===false?'Diagnose lokal · offline':'Azure-Sync ausstehend',remoteMeta.lastError);return false}var rows=localRecords(),last=lastSentSeq(),pending=rows.filter(function(r){return Number(r&&r.seq||0)>last});if(!pending.length){if(remoteMeta.status==='Warte auf Anmeldung')setStatus('Azure bereit');return false}var batch=pending.slice(0,50).map(function(r){return{id:eventId(r),seq:Number(r.seq||0),at:r.at,lastAt:r.lastAt,level:r.level,category:r.category,area:r.area,message:r.message,details:r.details,count:r.count}});busyUpload=true;try{setStatus('Synchronisiert Diagnose …');var data=await request('diagnostics-append',{method:'POST',body:{action:'diagnostics-append',environment:environment(),deviceId:deviceId(),clientVersion:clientVersion(),records:batch}});var maxSeq=Math.max.apply(null,batch.map(function(r){return Number(r.seq||0)}));setLastSentSeq(maxSeq);remoteMeta.lastSyncAt=new Date().toISOString();remoteMeta.revision=Number(data.revision||remoteMeta.revision||0);remoteMeta.total=Number(data.records||remoteMeta.total||0);uploadFailures=0;uploadRetryAt=0;setStatus('Azure synchronisiert');if(pending.length>batch.length)scheduleUpload(1200);return true}catch(e){uploadFailures=Math.min(5,uploadFailures+1);var wait=Math.min(300000,30000*Math.pow(2,uploadFailures-1));uploadRetryAt=Date.now()+wait;setStatus('Azure-Sync ausstehend',e&&e.message||e);if(!document.hidden&&navigator.onLine!==false)scheduleUpload(wait);return false}finally{busyUpload=false}}
function scheduleUpload(delay){if(uploadTimer)clearTimeout(uploadTimer);if(document.hidden||navigator.onLine===false)return false;var wait=Math.max(500,Number(delay)||2200,uploadRetryAt>Date.now()?uploadRetryAt-Date.now():0);uploadTimer=setTimeout(function(){uploadTimer=0;uploadPending(false)},wait);return true}
async function refresh(force){if(!isGlobal())return false;if(busyRead)return false;if(!token()){setStatus('Warte auf Anmeldung');return false}if(!force&&Date.now()-lastReadAt<12000)return false;busyRead=true;try{var data=await request('diagnostics-read',{limit:5000});remoteRecords=Array.isArray(data.records)?data.records:[];remoteMeta.lastSyncAt=new Date().toISOString();remoteMeta.revision=Number(data.revision||0);remoteMeta.total=Number(data.total||remoteRecords.length);remoteMeta.maxRecords=Number(data.maxRecords||5000);remoteMeta.retentionDays=Number(data.retentionDays||30);lastReadAt=Date.now();setStatus('Azure synchronisiert');return true}catch(e){setStatus('Azure lesen fehlgeschlagen',e&&e.message||e);return false}finally{busyRead=false}}
function combinedRecords(){var out=remoteRecords.slice(),seen=Object.create(null);out.forEach(function(r){if(r&&r.id)seen[r.id]=1});localRecords().forEach(function(r){var id=eventId(r);if(seen[id])return;var u=user();out.push(Object.assign({},r,{id:id,user:q(u.name||u.user||u.username)||'Dieses Gerät',deviceId:deviceId(),clientVersion:clientVersion(),environment:environment(),localPending:true}));seen[id]=1});out.sort(function(a,b){return Date.parse(q(a.lastAt||a.at)) - Date.parse(q(b.lastAt||b.at))});return out}
function recentRecords(limit){var n=Math.max(1,Math.min(1000,Number(limit)||250)),out=remoteRecords.slice(-n),seen=Object.create(null),u=user();out.forEach(function(r){if(r&&r.id)seen[r.id]=1});localRecords().slice(-n).forEach(function(r){var id=eventId(r);if(seen[id])return;out.push(Object.assign({},r,{id:id,user:

--- hit 3 pos=421558 nearest_function=request ---
495PickupColliResult.ok{border-color:#86efac!important;background:#f0fdf4!important;color:#166534!important}
#index495PickupColliResult.bad{border-color:#fecaca!important;background:#fff1f2!important;color:#991b1b!important}
.index491-colli-test{display:block;color:#64748b;font-size:11px;font-weight:800}
@media(max-width:620px){#index491PickupColliSection .index495-colli-check{grid-template-columns:1fr!important}}
@media(max-width:620px){.index225-pickup-grid,.index209-pickup-info{grid-template-columns:1fr!important}.index225-pickup-section{padding:13px}.index209-pickup-card{border-radius:22px!important}.index209-signature-trigger{min-height:110px!important}}
</style>
<script id="exporthub-rc352-pickup-confirmation-controller">(function(){
'use strict';
var current;try{current=new URL(window.location.href)}catch(_){return}
function q(v){return String(v==null?'':v).trim()}
function tokenFromUrl(u){var hash='';try{hash=decodeURIComponent(String(u.hash||'').replace(/^#/,''))}catch(_){hash=String(u.hash||'').replace(/^#/,'')}var hm=hash.match(/(?:^|[\/?&])pickup(?:=|\/)([A-Za-z0-9_-]{6,128})(?:$|[&\/?])/i)||hash.match(/^pickup=([A-Za-z0-9_-]{6,128})$/i),pm=String(u.pathname||'').match(/\/pickup(?:\.html)?\/([A-Za-z0-9_-]{6,128})\/?$/i),legacy=/^pickup$/i.test(q(u.searchParams.get('ehcmd'))),list=[u.searchParams.get('pickup'),u.searchParams.get('token'),legacy&&u.searchParams.get('ref'),pm&&pm[1],hm&&hm[1]];for(var i=0;i<list.length;i++)if(/^[A-Za-z0-9_-]{6,128}$/.test(q(list[i])))return q(list[i]);return''}
var token=tokenFromUrl(current)||q(window.__EXPORTHUB_PICKUP_ROUTE_TOKEN__);if(!token)return;
window.__EXPORTHUB_PICKUP_MODE__=true;window.__EXPORTHUB_PICKUP_CONFIRMATION_OWNER__='RC352';document.documentElement.setAttribute('data-index209-pickup','1');
var signatureSaved='',canvas=null,ctx=null,drawing=false,drawn=false,submitting=false,expectedPickupCollis=0,colliCountConfirmed=false,currentCarrierName='';
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
function request(path,opt){opt=opt||{};var timeout=Math.max(5000,Number(opt.timeout||30000)),controller=typeof AbortController==='function'?new AbortController():null,timer=null;delete opt.timeout;opt.credentials='same-origin';opt.cache='no-store';opt.headers=Object.assign({'Accept':'application/json','Content-Type':'application/json'},opt.headers||{});if(controller)opt.signal=controller.signal;if(controller)timer=setTimeout(function(){controller.abort()},timeout);return fetch('/api/'+String(path||'').replace(/^\//,''),opt).then(async function(r){var text='',body={};try{text=await r.text();body=text?JSON.parse(text):{}}catch(_){body={}}if(!r.ok){var e=new Error(body.message||body.error||('HTTP '+r.status));e.status=r.status;e.code=body.code||'';e.retryAfter=Math.max(0,Number(r.headers.get('retry-after')||body.retryAfter||body.retryAfterSeconds||0)||0);throw e}return body}).catch(function(error){if(error&&error.name==='AbortError'){var e=new Error('Der Server hat innerhalb der zulässigen Zeit nicht geantwortet.');e.code='REQUEST_TIMEOUT';e.timeout=true;throw e}throw error}).finally(function(){if(timer)clearTimeout(timer)})}
function valueDeep(data,names){var seen=[];function walk(x,d){if(!x||typeof x!=='object'||d>7||seen.indexOf(x)>=0)return'';seen.push(x);for(var i=0;i<names.length;i++){var v=x[names[i]];if(v!==undefined&&v!==null&&q(v)!=='')return v}for(var k in x){if(Object.prototype.hasOwnProperty.call(x,k)&&x[k]&&typeof x[k]==='object'){var hit=walk(x[k],d+1);if(hit!==''&&hit!=null)return hit}}return''}return walk(data,0)}
function confirmed(data){return !!(data&&(data.confirmedAt||data.pickupConfirmedAt||data.completedAt||data.pickedUpAt||data.used||data.confirmed||/confirmed|completed|abgeholt|used/i.test(q(data.status))))}
function signatureAvailable(data){return !!(data&&(data.signatureAvailable||data.driverSignatureAvailable||data.driverSignatureUrl||data.pickupSignatureUrl||data.signatureUrl||valueDeep(data,['driverSignature','pickupDriverSignature','signatureDataUrl','pickupSignature','signature','qrPickupSignature'])||Array.isArray(data.podFiles)&&data.podFiles.length))}
function disabled(data){return !!(data&&(data.disabled||/disabled|cancelled|storniert/i.test(q(data.status))))}
function status(text,kind){var n=document.getElementById('index209PickupStatus');if(!n)return;n.className='index209-pickup-status '+(kind||'info');n.textContent=text}
function shell(){var old=document.getElementById('index209PickupApp');if(old)return old;var app=document.createElement('main');app.id='index209PickupApp';app.innerHTML='<section class="index209-pickup-card"><div class="index209-pickup-brand"><div class="index209-pickup-logo">EH</div><div><h1>Abholung bestätigen</h1><p>ExportHUB · digitaler Abliefernachweis</p></div></div><div id="index209PickupStatus" class="index209-pickup-status info">Sendungsdaten werden geladen …</div><div id="index209PickupDetails"></div><form id="index209PickupForm" hidden><section class="index225-pickup-section"><h2>Fahrer und Fahrzeug</h2><div class="index225-pickup-grid"><label>Name des Fahrers <span class="small">(optional)</span><input id="index209PickupDriver" autocomplete="name" autocapitalize="words" lang="de"></label><label>Kennzeichen <span class="small">(Pflicht)</span><input id="index209PickupPlate" autocomplete="off" inputmode="text" maxlength="40" required></label></div><label>Speditionsname <span class="small">(Pflicht)</span><input id="index209PickupCarrier" autocomplete="organization" maxlength="180" required></label><label id="index209PickupReturnLabel" hidden>Zurückgegebene Europaletten<input id="index209PickupReturn" type="number" min="0" step="1" value="0"></label></section><section id="index491PickupColliSection" class="index225-pickup-section" hidden><h2>Colli-Prüfung <span class="small">(Pflicht)</span></h2><div class="index209-pickup-box" id="index675PickupColliTotalBox"><span>Gesamtanzahl der Sendung</span><b id="index675PickupExpectedTotal">—</b></div><p class="index491-colli-test">Es gilt die komplette Colli-Gesamtmenge der Sendung, unabhängig von der Anzahl der Colli-Zeilen.</p><div class="index495-colli-check"><label>Gezählte Gesamt-Colli<input id="index495PickupColliInput" type="number" min="1" step="1" inputmode="numeric" autocomplete="off"></label><button id="index495Pickup

--- hit 4 pos=421591 nearest_function=request ---
lor:#86efac!important;background:#f0fdf4!important;color:#166534!important}
#index495PickupColliResult.bad{border-color:#fecaca!important;background:#fff1f2!important;color:#991b1b!important}
.index491-colli-test{display:block;color:#64748b;font-size:11px;font-weight:800}
@media(max-width:620px){#index491PickupColliSection .index495-colli-check{grid-template-columns:1fr!important}}
@media(max-width:620px){.index225-pickup-grid,.index209-pickup-info{grid-template-columns:1fr!important}.index225-pickup-section{padding:13px}.index209-pickup-card{border-radius:22px!important}.index209-signature-trigger{min-height:110px!important}}
</style>
<script id="exporthub-rc352-pickup-confirmation-controller">(function(){
'use strict';
var current;try{current=new URL(window.location.href)}catch(_){return}
function q(v){return String(v==null?'':v).trim()}
function tokenFromUrl(u){var hash='';try{hash=decodeURIComponent(String(u.hash||'').replace(/^#/,''))}catch(_){hash=String(u.hash||'').replace(/^#/,'')}var hm=hash.match(/(?:^|[\/?&])pickup(?:=|\/)([A-Za-z0-9_-]{6,128})(?:$|[&\/?])/i)||hash.match(/^pickup=([A-Za-z0-9_-]{6,128})$/i),pm=String(u.pathname||'').match(/\/pickup(?:\.html)?\/([A-Za-z0-9_-]{6,128})\/?$/i),legacy=/^pickup$/i.test(q(u.searchParams.get('ehcmd'))),list=[u.searchParams.get('pickup'),u.searchParams.get('token'),legacy&&u.searchParams.get('ref'),pm&&pm[1],hm&&hm[1]];for(var i=0;i<list.length;i++)if(/^[A-Za-z0-9_-]{6,128}$/.test(q(list[i])))return q(list[i]);return''}
var token=tokenFromUrl(current)||q(window.__EXPORTHUB_PICKUP_ROUTE_TOKEN__);if(!token)return;
window.__EXPORTHUB_PICKUP_MODE__=true;window.__EXPORTHUB_PICKUP_CONFIRMATION_OWNER__='RC352';document.documentElement.setAttribute('data-index209-pickup','1');
var signatureSaved='',canvas=null,ctx=null,drawing=false,drawn=false,submitting=false,expectedPickupCollis=0,colliCountConfirmed=false,currentCarrierName='';
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
function request(path,opt){opt=opt||{};var timeout=Math.max(5000,Number(opt.timeout||30000)),controller=typeof AbortController==='function'?new AbortController():null,timer=null;delete opt.timeout;opt.credentials='same-origin';opt.cache='no-store';opt.headers=Object.assign({'Accept':'application/json','Content-Type':'application/json'},opt.headers||{});if(controller)opt.signal=controller.signal;if(controller)timer=setTimeout(function(){controller.abort()},timeout);return fetch('/api/'+String(path||'').replace(/^\//,''),opt).then(async function(r){var text='',body={};try{text=await r.text();body=text?JSON.parse(text):{}}catch(_){body={}}if(!r.ok){var e=new Error(body.message||body.error||('HTTP '+r.status));e.status=r.status;e.code=body.code||'';e.retryAfter=Math.max(0,Number(r.headers.get('retry-after')||body.retryAfter||body.retryAfterSeconds||0)||0);throw e}return body}).catch(function(error){if(error&&error.name==='AbortError'){var e=new Error('Der Server hat innerhalb der zulässigen Zeit nicht geantwortet.');e.code='REQUEST_TIMEOUT';e.timeout=true;throw e}throw error}).finally(function(){if(timer)clearTimeout(timer)})}
function valueDeep(data,names){var seen=[];function walk(x,d){if(!x||typeof x!=='object'||d>7||seen.indexOf(x)>=0)return'';seen.push(x);for(var i=0;i<names.length;i++){var v=x[names[i]];if(v!==undefined&&v!==null&&q(v)!=='')return v}for(var k in x){if(Object.prototype.hasOwnProperty.call(x,k)&&x[k]&&typeof x[k]==='object'){var hit=walk(x[k],d+1);if(hit!==''&&hit!=null)return hit}}return''}return walk(data,0)}
function confirmed(data){return !!(data&&(data.confirmedAt||data.pickupConfirmedAt||data.completedAt||data.pickedUpAt||data.used||data.confirmed||/confirmed|completed|abgeholt|used/i.test(q(data.status))))}
function signatureAvailable(data){return !!(data&&(data.signatureAvailable||data.driverSignatureAvailable||data.driverSignatureUrl||data.pickupSignatureUrl||data.signatureUrl||valueDeep(data,['driverSignature','pickupDriverSignature','signatureDataUrl','pickupSignature','signature','qrPickupSignature'])||Array.isArray(data.podFiles)&&data.podFiles.length))}
function disabled(data){return !!(data&&(data.disabled||/disabled|cancelled|storniert/i.test(q(data.status))))}
function status(text,kind){var n=document.getElementById('index209PickupStatus');if(!n)return;n.className='index209-pickup-status '+(kind||'info');n.textContent=text}
function shell(){var old=document.getElementById('index209PickupApp');if(old)return old;var app=document.createElement('main');app.id='index209PickupApp';app.innerHTML='<section class="index209-pickup-card"><div class="index209-pickup-brand"><div class="index209-pickup-logo">EH</div><div><h1>Abholung bestätigen</h1><p>ExportHUB · digitaler Abliefernachweis</p></div></div><div id="index209PickupStatus" class="index209-pickup-status info">Sendungsdaten werden geladen …</div><div id="index209PickupDetails"></div><form id="index209PickupForm" hidden><section class="index225-pickup-section"><h2>Fahrer und Fahrzeug</h2><div class="index225-pickup-grid"><label>Name des Fahrers <span class="small">(optional)</span><input id="index209PickupDriver" autocomplete="name" autocapitalize="words" lang="de"></label><label>Kennzeichen <span class="small">(Pflicht)</span><input id="index209PickupPlate" autocomplete="off" inputmode="text" maxlength="40" required></label></div><label>Speditionsname <span class="small">(Pflicht)</span><input id="index209PickupCarrier" autocomplete="organization" maxlength="180" required></label><label id="index209PickupReturnLabel" hidden>Zurückgegebene Europaletten<input id="index209PickupReturn" type="number" min="0" step="1" value="0"></label></section><section id="index491PickupColliSection" class="index225-pickup-section" hidden><h2>Colli-Prüfung <span class="small">(Pflicht)</span></h2><div class="index209-pickup-box" id="index675PickupColliTotalBox"><span>Gesamtanzahl der Sendung</span><b id="index675PickupExpectedTotal">—</b></div><p class="index491-colli-test">Es gilt die komplette Colli-Gesamtmenge der Sendung, unabhängig von der Anzahl der Colli-Zeilen.</p><div class="index495-colli-check"><label>Gezählte Gesamt-Colli<input id="index495PickupColliInput" type="number" min="1" step="1" inputmode="numeric" autocomplete="off"></label><button id="index495PickupColliCheck" type="button">Gesamtm

--- hit 5 pos=2195852 nearest_function=fetchWithTimeout ---
(){
 const token=text(runtime.authToken);resetClientSessionForMicrosoftSwitch();if(!token)return;
 try{const headers={'Content-Type':'application/json','X-ExportHUB-Token':token,'X-ExportHUB-Session':token,Authorization:'Bearer '+token};await native.fetch(AUTH_API,{method:'POST',headers:headers,body:JSON.stringify({action:'logout',sessionToken:token}),credentials:'same-origin',cache:'no-store',keepalive:true})}catch(_){}
}
async function switchMicrosoftAccount(){
 if(runtime.msSwitchBusy)return false;runtime.msSwitchBusy=true;
 try{
  await settleAzureBeforeMicrosoftSwitch();
  const hint=await readMicrosoftLogoutHint();
  await endExportHubSessionForMicrosoftSwitch();
  markLogoutSignal();microsoftSwitchStore(MS_SWITCH_FLOW_KEY,String(Date.now()));if(hint)microsoftSwitchStore(MS_SWITCH_HINT_KEY,hint);
  const back=cleanReturnUrl({msAccountSwitch:'provider'});
  navigateMicrosoft(appAuthUrl('/.auth/logout',{post_logout_redirect_uri:back.href}));
 }catch(e){runtime.msSwitchBusy=false;status('Microsoft-Konto konnte nicht gewechselt werden: '+text(e&&e.message||e),'bad')}
 return false
}
function handleMicrosoftSwitchStage(){
 let u;try{u=new URL(window.location.href)}catch(_){return false}
 const changed=text(u.searchParams.get('msAccountChanged'));
 if(changed==='1'){
  resetClientSessionForMicrosoftSwitch();clearMicrosoftSwitchState();markLogoutSignal();
  try{u.searchParams.delete('msAccountChanged');history.replaceState(null,'',u.href)}catch(_){}
  return false
 }
 const stage=text(u.searchParams.get(MS_SWITCH_PARAM));if(!stage)return false;
 resetClientSessionForMicrosoftSwitch();markLogoutSignal();microsoftSwitchStore(MS_SWITCH_FLOW_KEY,microsoftSwitchRead(MS_SWITCH_FLOW_KEY)||String(Date.now()));
 if(stage==='provider'){
  const next=cleanReturnUrl({msAccountSwitch:'login'}).href,hint=microsoftSwitchRead(MS_SWITCH_HINT_KEY);
  navigateMicrosoft(microsoftProviderLogoutUrl(next,hint));return true
 }
 if(stage==='login'){
  const done=cleanReturnUrl({msAccountChanged:'1'});navigateMicrosoft(microsoftAccountLoginUrl(done));return true
 }
 clearMicrosoftSwitchState();return false
}
async function fetchWithTimeout(url,options,timeoutMs){
 const controller=typeof AbortController!=='undefined'?new AbortController():null;
 const timer=controller?native.setTimeout(function(){controller.abort()},Math.max(1000,Number(timeoutMs)||5000)):null;
 try{
  return await native.fetch(url,Object.assign({},options||{},controller?{signal:controller.signal}:{}));
 }finally{if(timer)native.clearTimeout(timer)}
}

function addSafeLoadNote(){
 const panel=by('cleanLoadPanel'); if(!panel||by('cleanSafeLoadNote'))return;
 const n=document.createElement('div'); n.id='cleanSafeLoadNote'; n.className='clean-safe-note';
 n.textContent='ExportHUB wird stabil geladen. Hintergrundmodule starten erst nach der sichtbaren Bereitschaft.';
 panel.appendChild(n);
}

function setVersion(){
 try{const d=Object.getOwnPropertyDescriptor(document,'title');if(d&&d.configurable)delete document.title}catch(_){ }
 try{window.ExportHUBBuild.applyTitle();}catch(_){ }
 const title=document.querySelector('title');if(title)window.ExportHUBBuild.applyTitle();
 window.ExportHUBBuild.applyVersion();
 document.querySelectorAll('[id*=version i],[class*=version i],[data-exporthub-version-label]').forEach(function(e){
  const value=text(e.textContent);
  if(/(?:ExportHUB\s+Private\s+|Private\s+)?RC\d+/i.test(value))window.ExportHUBBuild.applyVersion();
 });
 const login=document.querySelector('.login-card');if(login&&!by('cleanVersionBadge')){const d=document.createElement('div');d.id='cleanVersionBadge';d.className='clean-version-badge';d.textContent='Aktuelle Version '+window.ExportHUBBuild.getBuild().version;d.setAttribute('aria-label',d.textContent);const heading=login.querySelector('h1');if(heading)heading.insertAdjacentElement('afterend',d);else login.insertBefore(d,login.firstChild);window.ExportHUBBuild.applyVersion()}
 try{ensureUserDisplay()}catch(_){ }
}


// Browser-Speicher bleibt unverändert; ExportHUB verwendet keine globalen Storage-Prototyp-Overrides.

// Azure-Teamdaten werden ausschließlich über stateCall()/jsonFetch() verwaltet; window.fetch bleibt unverändert.

// Browser-Timer, MutationObserver und requestAnimationFrame bleiben native Browser-APIs.

function apiEndpointLabel(url){
 try{return new URL(String(url||''),location.href).pathname}catch(_){return String(url||'API')}
}
async function resilientFetch(url,options){
 if(location.protocol==='file:'){
  const localError=new Error('ExportHUB wurde als lokale Datei geöffnet. Azure-Speicherung und Anmeldung funktionieren nur über die veröffentlichte Azure-Webadresse.');
  localError.code='LOCAL_FILE_NO_API';throw localError;
 }
 const base=Object.assign({credentials:'same-origin',cache:'no-store'},options||{}),requestedTimeout=Number(base.timeoutMs),timeoutMs=Math.max(3000,requestedTimeout>0?requestedTimeout:30000),maxAttempts=Math.max(1,Math.min(4,Number(base.maxAttempts)||4)),externalSignal=base.signal||null;
 delete base.timeoutMs;delete base.maxAttempts;delete base.signal;
 let lastError=null;
 for(let attempt=1;attempt<=maxAttempts;attempt++){
  const controller=typeof AbortController==='function'?new AbortController():null;
  var externalAbort=null;
  if(controller&&externalSignal){if(externalSignal.aborted){try{controller.abort()}catch(_){ }}else{externalAbort=function(){try{controller.abort()}catch(_){ }};try{externalSignal.addEventListener('abort',externalAbort,{once:true})}catch(_){ }}}
  const timer=controller?native.setTimeout(function(){try{controller.abort()}catch(_){ }},timeoutMs):null;
  const opts=Object.assign({},base);if(controller)opts.signal=controller.signal;else if(externalSignal)opts.signal=externalSignal;
  try{
   const res=await native.fetch(url,opts);
   if(timer)native.clearTimeout(timer);if(externalAbort&&externalSignal)try{externalSignal.removeEventListener('abort',externalAbort)}catch(_){ }
   return res;
  }catch(e){
   if(timer)native.clearTimeout(timer);if(externalAbort&&externalSignal)try{externalSignal.removeEventListener('abort',externalAbort)}catch(_){ }
   lastError=e;
   if(externalSignal&&externalSignal.aborted)break;
   if(attempt<maxAttempts)await new Promise(function(resolve){native.setTimeout(resolve,Math.min(1800,attempt*450))});
  }
 }
 const endpoint=apiEndpointLabel(url),timedOut=!!(lastError&&String(lastError.name||'

=== RELEVANT FUNCTION INVENTORY ===
3967: load
18002: uploadPending
19712: scheduleUpload
26192: workStart
26826: state
34636: setViewState
50245: historyPayload
50380: initHistory
51935: handlePopState
58903: boot
335770: boot
337594: publicLoader
354348: viewState
355482: stateLabel
364368: getState
387214: stateObj
439987: load
447351: boot
447841: getState
880600: state
991473: syncApi
1106041: hydrateShipmentView
1128368: reloadDetectedTestVersion
1130913: changeChecklistState
1131139: changeResultState
1131356: failureReportState
1143692: checklistState
1149027: downloadFailureReport
1164990: downloadBlob
1180358: releaseDownloadStarted
1181887: downloadPreparedRelease
1182492: reloadTestService
1606388: defaultState
1607894: loadJSON
1608028: loadFirstJSON
1608146: migrateState
1627823: syncShipmentTaskAutomation
1639084: taskGroupStateRC874
1639584: taskGroupHydrateRC874
1707644: loadListTextForMail
1858431: teamMeta
1858961: teamAreas
1859059: getCheckedTeamAreas
1859209: selectedTeamAreas
1859279: teamLabel
1859437: teamCounts
1859667: buildTeamPayload
1860620: exportTeamMaster
1861104: exportTeamChangePackage
1861840: openTeamImport
1862129: handleTeamImport
1862502: importTeamMaster
1865506: importTeamChanges
1866655: teamBaselineAreas
1866706: stripTeamMetaRecord
1866867: teamRecordChecksum
1867000: buildTeamBaseline
1867293: buildTeamSeedBaseline
1867914: ensureTeamBaseline
1868242: teamChangedRecords
1868514: teamChangedCounts
1868689: resetTeamJournal
1869154: exportTeamCollectedPackage
1870687: exportTeamMergedPackage
1872410: exportTeamFullBackup
1873106: teamfile
1881924: download
1907875: mergePayload97
2000890: selectCustomerState
2033567: isLoadButton
2055172: syncAcademyProfession
2127138: boot
2141793: download
2144723: boot
2175123: boot
2187419: startLoginProgress
2190736: clearMicrosoftSwitchState
2192838: settleAzureBeforeMicrosoftSwitch
2196190: addSafeLoadNote
2203214: loadMicrosoft
2203294: loadUsers
2205837: checkBootstrapStatus
2208564: verifyStateEnvironment
2209476: stateCall
2219460: forensicCollectStateEvidence
2227372: customerRecoveryCollectState
2231985: stopStartupStateReadProgress
2232166: startStartupStateReadProgress
2232588: loadState
2236170: loadScript
2239518: isLocalOnlyStateKey
2240007: isPersistentFilterStateKey
2240416: collectPersistentFilterState
2243511: savePersistentFilterState
2243947: restorePersistentFilterState
2251888: buildAzureStateDelta
2253224: persistentStateShapeComplete
2253959: syncRecordSameIdentity
2254198: syncFieldSame
2254650: syncRecordContainsDelta
2255615: syncedCollectionContainsDelta
2256144: syncedCollectionsContainDelta
2256590: mergeTeamSyncMeta
2257354: applyLocalDeltaToState
2258594: rebaseLiveStateFromRemote
2269721: shipmentSyncAliases
2270587: shipmentPrimarySyncKey
2278856: startRevisionPolling
2282290: bindCanonicalState
2287740: loadCanonicalModules
2294731: authoritativeShipmentSyncReason
2296227: shouldPersistFilterStateForSave
2296469: persistentStateEqual
2297801: azureNetworkFailure
2298055: noteAzureNetworkFailure
2298416: noteAzureNetworkSuccess
2314379: verifySessionForLoad
2314730: isTransientLoginLoadError
2315089: loadStateAfterLogin
2321900: continueTestserviceLoad
2331544: startNoIdleKeepAlive
2346323: sync
2391543: flushDocumentUpload
2392454: verifyAbdUploadAfterAzure
2393860: overrideUploads
2408938: state
2424883: hasLoadlist2
2429347: syncPrintStatus
2452665: syncAbd
2468114: syncGlobalQrPin
2481299: storedGroupDefinitions
2483174: initGroupDraft
2484512: persistGroupDefinition
2489293: syncUsers
2490398: initDraft
2490971: loading
2500903: load
2711954: state
2725806: rc819SyncCcField
2738356: syncLegacyState
2746855: downloadLoadlist1
2747070: downloadLoadlist2
2750509: syncRows
2752807: shipmentAzureSnapshotMatches
2753452: confirmShipmentAzureSave
2774677: syncField
2814903: reloadLatest
2822254: state
2830484: download
2831463: loadDetails
2868689: states
2902700: syncSnapshot
2904334: syncOne
2905979: syncAll
2906673: downloadUrl
2907722: syncVisible
2908409: scheduleVisibleSync
2908679: boot
2927238: getState
2986085: state
2989923: uploadLabel
2994190: installUploadFunctions
2996313: buildLoadingRows
3003101: state
3007915: state
3016561: state
3027443: state
3046677: state
3047655: initialize
3070412: state
3087494: costStateRoots
3095399: shipmentLoad
3098095: costState
3099393: syncCostFromShipment
3119653: loadCostFromShipment
3145995: inspectionState
3146041: ihkState
3152683: ihkStartExam
3158445: ihkStartTimer
3173833: ihkLoadPdfLib
3199706: ihkDownload
3206455: abdState
3216496: uploadCompletedAbd
4070990: state
4079048: downloadOfficialPod
4095097: stateObj
4101439: stateObjects
4101743: sync
4106581: state
4126439: state
4205374: weekStart
4207536: groupState
4207939: hydrateGroup
4237862: boot
4239287: state
4249685: printLoad2
4249786: downloadLoad2
4250193: boot
4262454: loadLocalNotificationCenter
4263217: centerState
4272923: reminderRecipientState
4285806: boot
4287908: docWorkStart
4288866: addState
4288975: states
4314802: loadHtml
4330386: hydrateSignature
4345598: downloadBlob
4349876: downloadDocument
4351242: syncPod
4352028: downloadPod
4355649: setDocumentViewState
4358978: boot
4374262: states
4383812: boot
4389772: state
4394716: boot
4397309: state
4409179: boot
4411494: states
4425765: hasLoad2
4430688: syncStatus
4435347: boot
4440306: states
4466612: boot
4472567: states
4479216: syncRemote
4479799: boot
4490650: state
4492796: boot
4493878: state
4496497: sync
4605155: ensureLoadingBar
4607454: loadingTask
4607519: activeLoadingTask
4607750: loadingBarPaintActive
4608458: loadingBarStart
4609149: loadingBarStep
4609325: loadingBarDone
4610557: loadingBarFail
4610664: navigationLoadingDone
4611120: operationStart
4613351: commandLoadingAllowed
4614246: genericCommandStart
4616951: state
4626359: needsLockSync
4630665: syncReferenceField
4631961: startFreshShipment
4650884: syncRows
4742792: writeFieldToAllStates
4745158: syncVisibleShipmentFields
4768272: setSelectedCustomerState
4770836: stateRoots
4791584: installExactLoadShipment
4792259: syncRemoteVisibleFields
4858524: syncManualCloudUploads
4859277: syncServer
4863319: load
4866598: downloadAll
4873746: boot
4888745: states
4892493: warehouseLoadSummary
4899111: warehouseStateSignature
4902052: sync
5008952: syncLoginMask
5025529: state
5040948: loadReferenceDocs
5059296: downloadDocObject
5060232: downloadDoc
5062182: viewerLoad
5065100: viewerDownload