'use strict';
const { BlobServiceClient } = require('@azure/storage-blob');
let pins=null; try{pins=require('../shared/loader-pin-store')}catch(_){}
const CONTAINER=process.env.EXPORTHUB_LOCATION_CONTAINER||'exporthub-location';
const BLOB=process.env.EXPORTHUB_LOCATION_BLOB||'warehouse-locations.json';
const LOCATIONS={0:'Eingang',1:'Lane 1',2:'Lane 2',3:'Lane 3',4:'Lane 4',5:'Lane 5',6:'Regal',7:'Vor dem Regal',8:'DAF'};
const MAX_RETRIES=6;
function text(v){return String(v==null?'':v).replace(/[\u0000-\u001f\u007f]/g,' ').replace(/\s+/g,' ').trim()}
function now(){return new Date().toISOString()}
function conn(){return process.env.EXPORTHUB_STORAGE_CONNECTION_STRING||process.env.EXPORTHUB_STORAGE_CONNECTION||process.env.EXPORTHUB_AZURE_STORAGE_CONNECTION_STRING||process.env.AzureWebJobsStorage||''}
function json(status,body){return{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store, no-cache, must-revalidate','X-Content-Type-Options':'nosniff'},body:JSON.stringify(body)}}
function err(code,message,status=400){const e=new Error(message);e.code=code;e.status=status;return e}
function header(req,name){const h=req&&req.headers||{};return text(h[name.toLowerCase()]||h[name]||'')}
function appAuth(req){return !!(header(req,'x-exporthub-token')||header(req,'x-exporthub-session')||/^Bearer\s+\S+/i.test(header(req,'authorization')))}
function validToken(v){return /^[A-Za-z0-9_-]{6,128}$/.test(text(v))}
function empty(){return{schemaVersion:2,updatedAt:now(),records:[]}}
async function blob(){const cs=conn();if(!cs)throw err('STORAGE_NOT_CONFIGURED','Azure Storage ist nicht konfiguriert.',503);const service=BlobServiceClient.fromConnectionString(cs),container=service.getContainerClient(CONTAINER);await container.createIfNotExists();return container.getBlockBlobClient(BLOB)}
async function read(){const b=await blob();try{const d=await b.download(0),chunks=[];for await(const c of d.readableStreamBody)chunks.push(Buffer.from(c));const raw=Buffer.concat(chunks).toString('utf8');return{blob:b,etag:d.etag||null,doc:raw?JSON.parse(raw):empty()}}catch(e){if(e&&e.statusCode===404)return{blob:b,etag:null,doc:empty()};throw e}}
async function write(b,doc,etag){doc.updatedAt=now();const raw=JSON.stringify(doc),conditions=etag?{ifMatch:etag}:{ifNoneMatch:'*'};return b.upload(raw,Buffer.byteLength(raw),{blobHTTPHeaders:{blobContentType:'application/json; charset=utf-8',blobCacheControl:'no-store'},conditions})}
function normalize(doc){doc=doc&&typeof doc==='object'?doc:empty();doc.records=Array.isArray(doc.records)?doc.records:[];return doc}
function pub(r){return{token:r.token,shipmentId:r.shipmentId||'',reference:r.reference||'',customer:r.customer||'',locationCode:Number(r.locationCode||0),currentLocation:LOCATIONS[Number(r.locationCode||0)]||'Eingang',prepared:r.prepared===true,ready:r.ready===true,palletCount:Number(r.palletCount||0),packageCount:Number(r.packageCount||0),colliCount:Number(r.colliCount||0),loaderName:r.loaderName||'',bookedAt:r.bookedAt||null,collected:r.collected===true,collectedAt:r.collectedAt||null,history:Array.isArray(r.history)?r.history:[],version:'RC869'}}
async function mutate(token,fn){for(let i=0;i<MAX_RETRIES;i++){const got=await read(),doc=normalize(got.doc),idx=doc.records.findIndex(x=>text(x.token)===token),rec=idx>=0?Object.assign({},doc.records[idx]):{token,createdAt:now(),locationCode:0,prepared:false,history:[]},next=await fn(rec);if(idx>=0)doc.records[idx]=next;else doc.records.push(next);try{await write(got.blob,doc,got.etag);return next}catch(e){if(e&&e.statusCode===412&&i<MAX_RETRIES-1)continue;throw e}}throw err('CONFLICT','Lagerstatus konnte wegen eines Speicherkonflikts nicht aktualisiert werden.',409)}
async function loader(req,b){if(appAuth(req))return{name:'ExportHUB'};if(!pins)throw err('PIN_SERVICE_UNAVAILABLE','Verlader-PIN-Prüfung ist auf dem Server nicht verfügbar.',503);const pin=text((b&&b.pin)||(b&&b.loaderPin)||(b&&b.personalLoaderPin));if(!pins.validPin(pin))throw err('INVALID_PIN','Bitte die vierstellige Verlader-PIN eingeben.',400);const row=await pins.findByPin(pin);if(!row)throw err('INVALID_PIN','Verlader-PIN ist nicht korrekt oder deaktiviert.',401);return row}
module.exports=async function(context,req){if(req.method==='OPTIONS'){context.res={status:204,headers:{Allow:'GET, POST, OPTIONS','Cache-Control':'no-store'},body:''};return}try{const action=text(req.query&&req.query.action||'list').toLowerCase(),b=req.body&&typeof req.body==='object'?req.body:{},token=text((req.query&&req.query.token)||b.token).toLowerCase();if(action==='list'){if(!appAuth(req))throw err('UNAUTHORIZED','ExportHUB-Sitzung erforderlich.',401);const got=await read();context.res=json(200,{ok:true,records:normalize(got.doc).records.map(pub),version:'RC869'});return}if(!validToken(token))throw err('INVALID_TOKEN','Location-Token fehlt oder ist ungültig.',400);if(action==='get'){if(!appAuth(req))throw err('UNAUTHORIZED','ExportHUB-Sitzung erforderlich.',401);const got=await read(),r=normalize(got.doc).records.find(x=>text(x.token)===token);context.res=r?json(200,Object.assign({ok:true},pub(r))):json(404,{ok:false,code:'NOT_FOUND',message:'Location-Datensatz nicht gefunden.'});return}if(req.method!=='POST')throw err('METHOD_NOT_ALLOWED','Nur POST ist für Änderungen erlaubt.',405);if(action==='register'){if(!appAuth(req))throw err('UNAUTHORIZED','ExportHUB-Sitzung erforderlich.',401);const r=await mutate(token,async rec=>{rec.shipmentId=text(b.shipmentId||rec.shipmentId);rec.reference=text(b.reference||rec.reference).toUpperCase();rec.customer=text(b.customer||rec.customer);rec.palletCount=Math.max(0,Math.round(Number(b.palletCount)||0));rec.packageCount=Math.max(0,Math.round(Number(b.packageCount)||0));rec.colliCount=Math.max(0,Math.round(Number(b.colliCount||b.totalColli||b.expectedColliCount)||0));rec.ready=b.ready===true;rec.status=text(b.status||rec.status);rec.updatedAt=now();return rec});context.res=json(200,Object.assign({ok:true},pub(r)));return}if(action==='move'){if(!appAuth(req))throw err('UNAUTHORIZED','ExportHUB-Sitzung erforderlich.',401);const code=Math.round(Number(b.locationCode));if(!(code in LOCATIONS))throw err('INVALID_LOCATION','Ungültiger Lagerplatz.',400);const r=await mutate(token,async rec=>{const from=Number(rec.locationCode||0),stamp=now();rec.locationCode=code;rec.prepared=code!==0;rec.bookedAt=stamp;rec.loaderName=text(b.loaderName||b.loadedBy||b.loader||b.verlader||'ExportHUB');rec.history=Array.isArray(rec.history)?rec.history:[];rec.history.push({at:stamp,from,to:code,fromName:LOCATIONS[from]||'Eingang',toName:LOCATIONS[code],by:rec.loaderName});rec.history=rec.history.slice(-80);return rec});context.res=json(200,Object.assign({ok:true},pub(r)));return}if(action==='collect'){const who=await loader(req,b);const r=await mutate(token,async rec=>{if(rec.collected===true)return rec;const stamp=now(),from=Number(rec.locationCode||0);rec.locationCode=0;rec.prepared=false;rec.collected=true;rec.collectedAt=stamp;rec.loaderName=text(who&&who.name)||text(b.loaderName||b.loadedBy||'QR-Abholung');rec.history=Array.isArray(rec.history)?rec.history:[];rec.history.push({at:stamp,from,to:0,fromName:LOCATIONS[from]||'Eingang',toName:'Abgeholt',by:rec.loaderName,action:'collect'});rec.history=rec.history.slice(-80);return rec});context.res=json(200,Object.assign({ok:true},pub(r)));return}throw err('UNKNOWN_ACTION','Unbekannte Lageraktion.',400)}catch(e){context.log&&context.log.error&&context.log.error('location-booking RC869',e&&e.code,e&&e.message);context.res=json(e.status||e.statusCode||500,{ok:false,code:e.code||'SERVER_ERROR',message:e.message||'Lageraktion fehlgeschlagen.'})}}
