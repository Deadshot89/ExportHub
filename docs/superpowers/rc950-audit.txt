# RC950 Active Path Audit
file_bytes=5395547
file_lines=27063
build=version:'RC946',cache:'946',loginReturn:'/TESTVERSION.html?v=946'

## renderView
needle='function renderView'
count=2

### line~21382 pos=4279506
```text
(t))+'</h3><p>'+esc(taskOwner(t)||'Alle')+(d?' · '+esc(fmtDate(d)):' · ohne Fälligkeitsdatum')+(ref?' · '+esc(ref):'')+'</p></div><span class="pill '+(late?'red':'orange')+'">'+(late?'Überfällig':'Offen')+'</span></div><div class="actions"><button class="btn" data-index236-action="open-task" data-id="'+esc(id)+'">Aufgabe öffnen</button><button class="ghost" data-index236-action="complete-task" data-id="'+esc(id)+'">Erledigt</button>'+(ref?'<button class="soft" data-index236-action="open-shipment" data-id="'+esc(ref)+'">Sendung öffnen</button>':'')+'</div></article>'}
function shipmentCard(sh){var id=shipmentId(sh),ref=shipmentRef(sh)||id,d=shipmentPickup(sh),days=dayDiff(d,localISO());return '<article class="index236-item is-overdue"><div class="index236-item-head"><div><h3>'+esc(ref)+'</h3><p>'+esc(shipmentCustomer(sh)||'Ohne Kunde')+' · Abholung '+esc(fmtDate(d))+'</p></div><span class="pill red">'+days+' Tag'+(days===1?'':'e')+' überfällig</span></div><div class="index236-date-row"><input type="date" value="'+esc(addDays(localISO(),1))+'" data-index236-pickup-date="'+esc(id)+'"><button class="ghost" data-index236-action="set-pickup" data-id="'+esc(id)+'">Neues Abholdatum</button></div><div class="actions"><button class="btn" data-index236-action="open-shipment" data-id="'+esc(id)+'">Sendung öffnen</button></div>'+reminderButtons(sh)+'</article>'}
function historyHtml(){var h=centerState().history.slice().reverse().slice(0,12);if(!h.length)return'<div class="index236-empty">Noch keine geplante Erinnerung protokolliert.</div>';return h.map(function(x){return'<div class="index236-history"><b>'+esc(new Date(x.at).toLocaleString('de-DE'))+'</b><span>'+esc(x.label||x.slot)+' · '+Number(x.tasks||0)+' Aufgaben · '+Number(x.shipments||0)+' Sendungen</span></div>'}).join('')}
function renderView(force){if(low(main().view)!=='notifications'&&low((document.body?document.body.getAttribute('data-exporthub-view'):''))!=='notifications')return false;var r=root();if(!r)return false;var tasks=openTasks(),late=overdueTasks(),next=nextCheck(new Date()),perm=permissionText();var html='<div id="index236NotificationCenter" class="index236-center"><div class="page-head"><div><span class="index236-kicker">PERSÖNLICHE ERINNERUNGEN</span><h2>Benachrichtigungen</h2><p>Hier erscheinen ausschließlich deine offenen Aufgaben. ExportHUB erinnert an Arbeitstagen um 09:00, 12:00 und 15:00 Uhr. Operative Probleme an Sendungen werden getrennt im Warncenter angezeigt.</p></div><div class="actions"><button class="btn" data-index236-action="check-now">Jetzt prüfen</button><button class="ghost" data-index236-action="enable-browser">Browser-Benachrichtigungen aktivieren</button></div></div><div class="index236-statusbar"><div><span>Browser-Benachrichtigung</span><strong>'+esc(perm)+'</strong></div><div><span>Nächste Prüfung</span><strong>'+(next?esc(next.toLocaleString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})):'—')+'</strong></div><div><span>Signalton</span><strong>Deaktiviert</strong></div></div><div class="metrics index236-metrics"><div class="metric kpi-orange"><span>Offene Aufgaben</span><strong>'+tasks.length+'</strong></div><div class="metric kpi-red"><span>Überfällige Aufgaben</span><strong>'+late.length+'</strong></div><div class="metric kpi-blue"><span>Prüfzeiten</span><strong>3</strong><small>09:00 · 12:00 · 15:00</small></div></div><section class="card"><div class="index236-section-head"><div><h3>Offene Aufgaben</h3><p>Bis zur Erledigung bei jeder Prüfzeit. Sendungswarnungen werden hier bewusst nicht doppelt angeze
```

### line~25021 pos=5059010
```text
d=q(v.shipmentId||v.reference);try{sessionStorage.removeItem('exporthub_document_viewer_v1')}catch(_){}viewerMemory=null}if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentview','document-viewer-back');s.view='shipmentview';return render()}
function viewerCurrentDoc(){var v=viewerRead();if(!v)return null;return v.doc||v.persist&&v.persist.doc||v.persist||v}
async function viewerLoad(){var seq=++viewerLoadSeq,v=viewerPersist(),d=viewerCurrentDoc(),stage=document.getElementById('rc786DocumentStage');if(!v||!d||!stage)return false;try{var blob=await fetchDocBlob(Object.assign({},d,{reference:v.reference}));if(seq!==viewerLoadSeq||low(state().view)!=='documentviewer')return false;if(blob){viewerClearUrl();viewerObjectUrl=URL.createObjectURL(blob);stage.innerHTML='<iframe id="rc786DocumentFrame" title="'+esc(v.name||'Dokument')+'"></iframe>';var frame=document.getElementById('rc786DocumentFrame');frame.onload=function(){if(v.autoPrint===true){v.autoPrint=false;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){}setTimeout(function(){try{frame.contentWindow.focus();frame.contentWindow.print()}catch(e){console.warn('RC808 Drucken',e)}},180)}};frame.src=viewerObjectUrl;return false}if(d.url){stage.innerHTML='<iframe id="rc786DocumentFrame" src="'+esc(d.url)+'" title="'+esc(v.name||'Dokument')+'"></iframe>';return false}throw new Error('Für diese Datei ist kein vorhandener Dateiinhalt erreichbar.')}catch(e){if(seq!==viewerLoadSeq)return false;stage.innerHTML='<div class="rc786-viewer-error"><b>Datei konnte nicht geöffnet werden.</b><span>'+esc(q(e&&e.message||e))+'</span><button type="button" class="ghost" data-rc786-open-ref="1">Ref-Ordner öffnen</button></div>';return false}}
function renderViewer(){var v=viewerPersist(),d=viewerCurrentDoc(),root=document.getElementById('content');if(!root)return false;root.className='content rc786-document-viewer';root.setAttribute('data-exporthub-rendered-view','documentviewer');if(!v){root.innerHTML='<div class="rc786-viewer-shell"><div class="rc786-viewer-error"><b>Kein Dokument ausgewählt.</b><button type="button" class="btn" data-rc786-viewer-back="1">Zurück zur Sendung</button></div></div>';return false}var origin=v.source==='referenceFolder'?'Originaldatei aus Ref-Ordner':'Gespeicherte Datei aus der Sendung',meta=[q(v.documentType)||docType(v.name,''),origin,v.createdAt?fmtDate(v.createdAt):'',fmtSize(v.size)].filter(Boolean).join(' · ');root.innerHTML='<div class="rc786-viewer-shell"><header class="rc786-viewer-head"><div><button type="button" class="rc786-viewer-back" data-rc786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" data-rc786-viewer-print="1">Drucken</button>':'')+'<button type="button" class="ghost" data-rc786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.conte
```

## generic render()
needle='function render('
count=6

### line~2564 pos=400639
```text
');
  (document.head||document.documentElement).appendChild(s);
})();

function avisHost(view){view=view||visibleShipmentView();var host=null;if(view==='shipmentview'){host=document.getElementById('rc845AvisReadHost')||document.getElementById('rc844AvisHost')||document.querySelector('#content.rc776-shipment-view .rc844-avis-host');if(!host){var page=document.querySelector('#content.rc776-shipment-view .rc776-page');if(page){host=document.createElement('div');host.id='rc845AvisReadHost';host.className='rc844-avis-host';host.setAttribute('data-rc845-avis-host','readonly');var grids=page.querySelectorAll(':scope > .rc776-grid');if(grids.length&&grids[0].nextSibling)page.insertBefore(host,grids[0].nextSibling);else page.appendChild(host)}}return host||null}if(view==='shipment'){host=document.getElementById('rc845AvisEditHost');if(!host){var body=document.querySelector('#rc363BlockShipment .rc363-process-body')||document.getElementById('rc363BlockShipment')||document.getElementById('rc573ShipmentShell');if(body){host=document.createElement('div');host.id='rc845AvisEditHost';host.className='rc845-avis-host';host.setAttribute('data-rc845-avis-host','edit');body.appendChild(host)}}return host||null}return null}
function render(){return false}
async function toggle(on){var sh=current();if(!sh)return false;if(on&&pickupStamp(sh)){alert('Der Lieferavis ist nach der Abholung geschlossen und kann nicht erneut aktiviert werden.');return false}if(on&&!ref(sh)){alert('Die Sendung benötigt zuerst eine Referenznummer.');return false}var stamp=new Date().toISOString(),values;if(on){var newToken=makeToken();values={customerAvisEnabled:true,avisEnabled:true,customerAvisToken:newToken,avisToken:newToken,customerAvisAccessCode:'',avisAccessCode:'',customerAvisSecurityVersion:SECURITY_VERSION,avisSecurityVersion:SECURITY_VERSION,customerAvisEnabledAt:stamp,avisEnabledAt:stamp,customerAvisExpiresAt:'',avisExpiresAt:'',customerAvisUsedAt:'',avisUsedAt:'',customerAvisDisabledAt:'',avisDisabledAt:'',customerAvisResponseStatus:'offen',avisResponseStatus:'offen',customerAvisAutoDisabledAt:'',avisAutoDisabledAt:'',customerAvisAutoDisabledReason:'',avisAutoDisabledReason:''}}else{values={customerAvisEnabled:false,avisEnabled:false,customerAvisToken:'',avisToken:'',customerAvisAccessCode:'',avisAccessCode:'',customerAvisSecurityVersion:0,avisSecurityVersion:0,customerAvisExpiresAt:'',avisExpiresAt:'',customerAvisUsedAt:'',avisUsedAt:'',customerAvisDisabledAt:stamp,avisDisabledAt:stamp,customerAvisAutoDisabledAt:'',avisAutoDisabledAt:'',customerAvisAutoDisabledReason:'',avisAutoDisabledReason:''}}var targets=copies(sh),keys=Object.keys(values),before=targets.map(function(x){var v={};keys.forEach(function(k){v[k]=x[k]});return{x:x,v:v}});patchCopies(sh,values);lastSig='';render();try{await persist(on?'Kunden-Avis sicher aktiviert':'Kunden-Avis deaktiviert');window.dispatchEvent(new CustomEvent('exporthub:customer-avis-updated',{detail:{reference:ref(sh),enabled:on}}));try{if(window.ExportHUBMailStatus37
```

### line~15132 pos=2494531
```text
'+E(id)+'</small></td><td><div class="rc544-rights rc771-group-rights" data-group-right-id="'+E(id)+'" data-selected-level="'+E(lv)+'">'+[['none','Kein Zugriff'],['view','Nur ansehen'],['edit','Bearbeiten'],['admin','Funktions-Admin']].map(function(x){return '<button type="button" data-level="'+x[0]+'" aria-pressed="'+(lv===x[0]?'true':'false')+'" class="rc544-level rc544-level-'+x[0]+' '+(lv===x[0]?'active':'')+'" onclick="return rc771SetGroupRight(\''+E(id)+'\',\''+x[0]+'\',this)">'+x[1]+'</button>'}).join('')+'</div></td></tr>'}).join('')+'</tbody></table><div class="toolbar"><button class="btn" id="rc771SaveGroupRights" onclick="return rc771SaveGroupRights()">Gruppenrechte speichern & Mitglieder aktualisieren</button><button class="ghost" onclick="return rc771ResetGroupDraft()">Standard in Entwurf laden</button></div><div class="notice" id="rc771GroupSaveStatus">Noch keine Änderung gespeichert.</div></section>'
}
function rightsSummaryHtml(u,modules){
 if(!u)return'';
 var counts={none:0,view:0,edit:0,admin:0};
 (modules||[]).forEach(function(id){var lv=draftRights[id]||level(serverRight(u,id));counts[lv]=Number(counts[lv]||0)+1});
 return '<div class="rc544-right-summary"><div class="rc544-right-summary-head"><b>Aktuelle Rechte dieses Benutzers</b><span>✓ markiert die ausgewählte Rechtestufe je Funktion</span></div><div class="rc544-right-summary-grid"><span class="rc544-summary-none"><b>'+counts.none+'</b> Kein Zugriff</span><span class="rc544-summary-view"><b>'+counts.view+'</b> Nur ansehen</span><span class="rc544-summary-edit"><b>'+counts.edit+'</b> Bearbeiten</span><span class="rc544-summary-admin"><b>'+counts.admin+'</b> Funktions-Admin</span></div><small>Änderungen sind erst nach „Benutzer und Rechte speichern“ dauerhaft in Azure bestätigt.</small></div>'
}
function render(){
 var r=root();if(!r||currentView()!=='rights'||!cache)return;
 var u=userById();if(u&&!selectedId)selectedId=u.id;if(u&&!Object.keys(draftRights).length)initDraft(u);if(!Object.keys(groupDraftRights).length)initGroupDraft(groupEditId);
 var users=cache.users||[],modules=cache.modules||[],sessions=cache.sessions||[];
 var list=users.map(function(x){var gid=detectGroup(x),g=groupById(gid);return '<button class="rc544-user '+(u&&x.id===u.id?'active':'')+'" onclick="return rc544SelectUser(\''+E(x.id)+'\')"><span><b>'+E(x.name||x.user)+'</b><small>'+E(x.user)+'</small><span class="rc544-user-group">'+E(g?g.label:'Individuell')+'</span></span><span>'+(x.active?'<span class="pill green">Aktiv</span>':'<span class="pill red">Inaktiv</span>')+'</span></button>'}).join('');
 var rows=u?modules.map(function(id){var lv=draftRights[id]||level(serverRight(u,id));return '<tr data-rc544-level="'+E(lv)+'"><td><b>'+E(LABELS[id]||id)+'</b><small>'+E(id)+'</small></td><td><div class="rc544-rights" data-right-id="'+E(id)+'" data-selected-level="'+E(lv)+'">'+[['none','Kein Zugriff'],['view','Nur ansehen'],['edit','Bearbeiten'],['admin','Funktions-Admin']].map(function(x){return '<button type="button" data-level="'+x[0]+'" aria-pressed="'+(lv===x[0]?'true':'false')+'" class="rc544-level rc544-level-'+x[0]+' '+(lv===x[0]?'active':'')+'" onclick="return rc544SetDraftRight(\''+E(id)+'\',\''+x[0]+'\',this)">'+x[1]+'</button>'}).join('')+'</div></td></tr>'}).join(''):'';
 var userSessions=u?sessions.filter(function(s){return s.userId===u.id}):[];
 var sessionRows=userSessions.length?userSessions.map(function(s){return '<tr><td>'+E(s.deviceId||'Unbekanntes Gerät')+'</td><td>'+E(new Date(s.createdAt).toLocaleString('de-DE'))+'</td><td><button class="danger" onclick="return rc544Te
```

### line~15769 pos=2827899
```text
/b></div>'}
function eventHtml(x,i){var key=eventKey(x,i),count=Number(x.count||1),sub=[categoryLabel(x.category),count>1?'×'+count:'',x.localPending?'lokal':''].filter(Boolean).join(' · ');return '<details class="rc871-diag-event '+esc(x.level||'info')+'" data-rc871-diag-key="'+esc(key)+'"><summary><div class="rc871-diag-time">'+esc(fmtTime(x.lastAt||x.at))+'</div><div><span class="rc871-diag-level '+esc(x.level||'info')+'">'+esc(levelLabel(x.level))+'</span></div><div class="rc871-diag-area">'+esc(x.area||'–')+'</div><div class="rc871-diag-message-wrap"><span class="rc871-diag-message">'+esc(x.message||'–')+'</span><span class="rc871-diag-sub">'+esc(sub)+'</span></div></summary><div class="rc871-diag-details" data-rc871-diag-details="1">Details werden beim Öffnen geladen.</div></details>'}
function renderList(){var list=document.getElementById('rc871DiagList'),meta=document.getElementById('rc871DiagMeta'),pager=document.getElementById('rc871DiagPager');if(!list||!meta||!pager)return;var rows=filtered().slice().reverse(),pages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));if(page>=pages)page=pages-1;if(page<0)page=0;var start=page*PAGE_SIZE,shown=rows.slice(start,start+PAGE_SIZE);list.innerHTML=shown.length?shown.map(eventHtml).join(''):'<div class="rc871-diag-empty">Keine passenden Diagnoseereignisse vorhanden.</div>';meta.textContent=rows.length+' Treffer · '+snapshot.length+' Ereignisse · '+PAGE_SIZE+' pro Seite';pager.innerHTML='<button type="button" class="ghost" data-rc871-diag-page="prev" '+(page<=0?'disabled':'')+'>← Zurück</button><span>Seite '+(page+1)+' / '+pages+'</span><button type="button" class="ghost" data-rc871-diag-page="next" '+(page>=pages-1?'disabled':'')+'>Weiter →</button>'}
function refreshVisible(){renderStats();renderList();updateStatusOnly()}
function render(){var root=document.getElementById('content');if(!root)return false;if(!isGlobal()){root.innerHTML='<div class="badbox">Die Fehlerdiagnose ist nur für globale Administratoren freigegeben.</div>';return false}root.className='content rc871-diagnostics-view';root.setAttribute('data-exporthub-rendered-view','diagnostics');root.setAttribute('data-rc871-diagnostics-view','1');root.innerHTML='<div class="rc871-diag-page" data-rc871-diagnostics-view="1"><div class="page-head rc871-diag-head"><div><span class="rc871-diag-kicker">GLOBAL ADMIN · '+(isTestservice()?'TESTSERVICE':'PRODUKTION')+'</span><h2>Fehlerdiagnose</h2><p>Statischer Diagnose-Snapshot. Neue Hintergrundereignisse verändern diese Ansicht nicht; neue Cloud-Daten werden ausschließlich über „Aktualisieren“ übernommen.</p></div><div id="rc871DiagStatus" class="rc871-diag-status">'+esc(statusText())+'</div></div><section class="card"><div id="rc871DiagSummary" class="rc871-diag-summary"></div></section><section class="card rc871-diag-controls"><div class="rc871-diag-toolbar"><label class="rc871-diag-field">Anzeige<select id="rc871DiagFilter"><option value="problems">Fehler + Warnungen</option><option value="errors">Nur Fehler</option><option value="warnings">Nur Warnungen</option><option value="network">Netzwerk/API</option><option value="performance">Leistung</option><option value="render">Neu-Render</option><option value="all">Alle Ereignisse</option></select></label><label class="rc871-diag-field">Suche<input id="rc871DiagSearch" type="search" autocomplete="off" placeholder="Meldung, Bereich oder Benutzer"></label><div class="rc871-diag-actions"><button type="button" class="btn" data-rc871-diag="refresh">Aktualisieren</button><button type="button" class="ghost" data-rc871-diag="export">JSON herunterl
```

### line~21140 pos=4230794
```text
8-overview-chip">'+esc(g.replace('Aufträge kontrollieren','Kontrolle').replace('Zusatzaufgaben','Zusatz'))+' '+n+'</span>':''}).join('');return '<article class="i218-overview-day '+(h?'is-holiday':'')+'"><h4>'+esc(day)+'</h4><span class="i218-overview-date">'+esc(dateDe(date))+'</span>'+(h?'<span class="i218-holiday">'+esc(h)+'</span>':'')+'<strong class="i218-overview-count">'+dayTasks.length+'</strong><span class="muted">offene Aufgaben</span><div class="i218-overview-chips">'+chips+'</div></article>'}).join('')+'</div>'}
function setWeek(ws){var cur=weekStart(new Date());ws=weekStart(ws);if(ws<cur)ws=cur;st().taskPlannerWeekStart=ws;try{localStorage.setItem('exporthub:planner:week:rc874',ws)}catch(_){}render();return false}
function shiftWeek(n){return setWeek(localISO(addDays(mondayOf(selectedWeek()),n*7)))}
function navigateTasks(){if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('tasks','task-planner');try{if(typeof window.setView==='function')window.setView('tasks')}catch(_){}return false}
/* exporthub-rc923-planner-source-signature */
function plannerSourceSignatureRC923(ws){var s=st(),rows=[];(Array.isArray(s.tasks)?s.tasks:[]).forEach(function(t){if(!t||isTest(t))return;rows.push([q(t.id||t.taskId),q(t.status),q(t.title||t.name),q(t.area||t.category||t.section||t.type),q(t.owner||t.assignee),q(t.day),q(t.originalDay),q(t.plannedDate||t.dueDate||t.date||t.targetDate),q(t.originalDate||t.baseDate),q(t.linkedShipmentRef||t.shipmentRef||t.reference||t.ref),q(t.linkedCustomer||t.customerName),q(t.linkedAccount||t.customerAccount),t.manualMoved===true?'1':'0',t.holidayAutoMoved===true?'1':'0'].join('|'))});rows.sort();return ws+'§'+rows.join('~')+'§'+JSON.stringify(Array.isArray(s.holidays)?s.holidays:[])}
function render(){if(rendering||view()!=='planning')return false;ensureRC895PlannerStyle();var r=root();if(!r)return false;rendering=true;try{var ws=selectedWeek();try{if(window.ExportHUBTaskMasterRC848)window.ExportHUBTaskMasterRC848.ensureWeek(ws,true)}catch(_){}var sourceSignature=plannerSourceSignatureRC923(ws),mounted=!!r.querySelector(':scope>#index218Planner');if(mounted&&sourceSignature===lastSourceSignature)return false;lastSourceSignature=sourceSignature;var tasks=openTasksForWeek(ws),hcount=0;DAYS.forEach(function(d){if(holidayName(dayDate(ws,d)))hcount++});var moved=tasks.filter(function(t){return t.manualMoved||t.holidayAutoMoved}).length,conflicts=tasks.filter(function(t){var d=effectiveDate(t);return d&&!isWorkday(d)&&groupName(t)!=='ABD erstellen'&&groupName(t)!=='Anmelden'}).length,forecast=holidayListFrom(ws),nextImpact=forecast.find(function(h){var d=parseDate(h.date);return d&&d.getDay()!==0&&d.getDay()!==6}),nextLabel=nextImpact?(nextImpact.name+' · in '+daysUntil(nextImpact.date)+' Tagen'):'kein Arbeitstags-Feiertag in 120 Tagen';var html='<div id="index218Planner"><div class="i218-head"><div><h2>🗓️ Aufgaben Planer</h2><p>Woche planen, Aufgaben nach Bereich öffnen und Feiertage frühzeitig berücksichtigen.</p></div><div class="actions"><button class="ghost" data-i218-action="tasks">Aufgaben öffnen</button></div></div><div class="i218-toolbar"><button class="ghost" data-i218-action="prev" '+(ws<=weekStart(new Date())?'disabled':'')+'>← Vorwoche</button><label>Woche<input type="date" min="'+esc(weekStart(new Date()))+'" data-i218-week value="'+esc(ws)+'"></label><button class="ghost" data-i218-action="next">Nächste Woche →</button><button class="soft" data-i218-action="holidays">Feiertage automatisch planen</button></div><div class="i218-statusbar"><b
```

### line~24425 pos=4911783
```text
tion stopWarehouseDragScroll(){dragScrollSpeed=0;if(dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function warehouseDragScrollTick(){dragScrollFrame=0;if(!dragKey||!dragScrollSpeed)return;window.scrollBy(0,dragScrollSpeed);dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick)}
function updateWarehouseDragScroll(e){if(!dragKey||!e)return stopWarehouseDragScroll();var h=Math.max(1,window.innerHeight||document.documentElement.clientHeight||1),edge=Math.max(80,Math.min(150,Math.round(h*.16))),y=Number(e.clientY),speed=0;if(Number.isFinite(y)){if(y<edge)speed=-Math.max(5,Math.round((edge-y)/edge*28));else if(y>h-edge)speed=Math.max(5,Math.round((y-(h-edge))/edge*28))}dragScrollSpeed=speed;if(speed&&!dragScrollFrame)dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick);if(!speed&&dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function paintNavIcon(){var b=document.querySelector('#nav>button[data-index321-view="warehouse"],#nav button[data-view="warehouse"]');if(!b)return false;var icon=b.querySelector('.index308-nav-icon');if(icon){icon.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 21h18M4 21V8l7-4v17M11 5l9 3v13"></path><rect x="6" y="11" width="2" height="2"></rect><rect x="6" y="15" width="2" height="2"></rect><rect x="14" y="10" width="2" height="2"></rect><rect x="18" y="10" width="2" height="2"></rect><rect x="14" y="14" width="2" height="2"></rect><rect x="18" y="14" width="2" height="2"></rect></svg>';icon.style.fontSize='';var svg=icon.querySelector('svg');if(svg){svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('stroke-linecap','round');svg.setAttribute('stroke-linejoin','round')}}return true}
function render(doSync){var root=document.getElementById('content');if(!root)return false;root.className='content rc504-warehouse-view';var activeList=all().filter(active),pallets=activeList.reduce(function(n,sh){return n+palletCount(sh)},0),prepared=activeList.filter(function(sh){return sh.warehousePrepared===true}).length,eingang=zoneData('Eingang').list.length;root.innerHTML='<div class="rc504-warehouse"><div class="rc504-warehouse-head"><div><h2>🏭 Lager & Locations</h2><p>Aktuelle Position aller noch nicht abgeholten Sendungen. Ausgangslocations zeigen direkt, welche Sendungen dort stehen. Admins können Sendungen zusätzlich manuell verschieben.</p></div><div class="rc504-warehouse-actions"><button class="ghost" onclick="return rc504WarehouseRefresh()">Aktualisieren</button>'+(isAdmin()?'<button class="ghost" onclick="return rc504WarehouseOpenFirst()">Manuell verschieben</button>':'')+'</div></div><div class="rc504-warehouse-summary"><div class="rc504-warehouse-kpi"><span>Sendungen im Lager</span><strong>'+activeList.length+'</strong></div><div class="rc504-warehouse-kpi"><span>Belegte Locations</span><strong>'+occupiedCount()+'</strong></div><div class="rc504-warehouse-kpi"><span>Im Eingang</span><strong>'+eingang+'</strong></div><div class="rc504-warehouse-kpi"><span>Vorbereitet</span><strong>'+prepared+'</strong></div></div><div class="rc504-layout">'+LOCS.map(function(x){return zone(x[0],x[1],x[2])}).join('')+'</div><p class="rc504-sync">Location-Codes: 0 Eingang · 1–5 Lane 1–5 · 6 Regal · 7 Vor dem Regal · 8 DAF</p></div>'+moveModal();bindWarehouseDnD(root);if(doSync===true)setTimeout(function(){sync(false)},650);paintNavIcon();return false}
function locationUrl(sh){var tok=token(sh);if(!tok)return'';try{var u=new URL('/location.html',location.origin);u.searchPa
```

### line~24996 pos=5042860
```text
textContent=results.length+' Treffer';list.innerHTML=results.length?results.map(shipmentSearchResultHtml).join(''):'<div class="rc807-empty">Keine passende Sendung gefunden.</div>';return false}
function renderSearch(){var root=document.getElementById('content');if(!root)return false;root.className='content rc807-shipment-search';root.setAttribute('data-exporthub-rendered-view','shipmentsearch');root.innerHTML='<div class="rc807-search-page"><div class="rc807-search-head"><div><span class="rc776-kicker">Sendungsansicht · nur lesen</span><h1>Sendungsansicht</h1><p>Gespeicherte Sendungen nach Kunde, Referenz, Kundennummer oder hinterlegten Anhängen und Dateinamen suchen.</p></div></div><div class="rc807-search-box"><input id="rc807ShipmentSearch" type="search" autocomplete="off" placeholder="Kunde, Referenz, Kundennummer oder Anhang suchen …" value="'+esc(shipmentSearchQuery)+'" aria-label="Sendungen durchsuchen"><span class="rc807-search-count" id="rc807ShipmentCount"></span><button type="button" class="ghost rc911-search-clear" data-rc911-search-clear="1">Leeren</button></div><div class="rc807-result-list" id="rc807ShipmentResults"></div></div>';renderSearchResults(shipmentSearchQuery);return false}

function shipmentViewCreator(sh){var direct=q(sh&&(sh.createdByName||sh.createdBy||sh.creatorName||sh.creator||sh.owner));if(direct)return direct;var hist=arr(sh&&sh.history).slice().sort(function(a,b){return (Date.parse(q(a&&a.at||a&&a.date||a&&a.createdAt))||0)-(Date.parse(q(b&&b.at||b&&b.date||b&&b.createdAt))||0)});for(var i=0;i<hist.length;i++){var h=hist[i]||{},label=low(h.action||h.type||h.status||h.label||'');if(/erstellt|created|neue sendung/.test(label)){var who=q(h.user||h.by||h.actor||h.createdBy||h.name);if(who)return who}}return'Nicht gespeichert (Altdaten)'}
function render(){var s=state(),sh=find(s.shipmentViewId||s.selectedShipmentId||s.activeShipmentId),root=document.getElementById('content');if(!root)return false;root.className='content rc776-shipment-view';root.setAttribute('data-exporthub-rendered-view','shipmentview');if(!sh){root.innerHTML='<div class="rc776-page"><section class="rc776-card"><h2>Sendung nicht gefunden</h2><p class="muted">Die ausgewählte Sendung ist im aktuellen Teamstand nicht mehr vorhanden.</p><button type="button" class="btn" data-rc776-back="1">Zurück</button></section></div>';return false}var c=customer(sh),t=totals(sh),rs=rows(sh),fallback=collectDocs(sh).map(normalizeFallback),id=idOf(sh)||refOf(sh),ref=refOf(sh)||id,st=status(sh),location=obj(sh.locationData)?sh.locationData:obj(sh.location)?sh.location:{},address=q(sh.recipientAddress||sh.destinationAddress||sh.deliveryAddress||location.address||c.address),actual=q(sh.actualPickupDate||sh.pickedUpAtDate||sh.pickedUpAt||sh.pickupConfirmedAt),avis=sh.customerAvisEnabled===true||sh.avisEnabled===true?'Aktiv':'Aus',abd=q(sh.abdStatus||sh.customsStatus),pod=podEvidence(sh),stow=obj(sh.stowPlan)&&arr(sh.stowPlan.placements).length?'Vorhanden':'–',reminderHtml=window.ExportHUBIndex236&&typeof window.ExportHUBIndex236.reminderButtons==='function'?window.ExportHUBIndex236.reminderButtons(sh):'';window.__EXPORTHUB_RC776_VIEW_DOCS__=fallback;window.__EXPORTHUB_RC776_VIEW_SHIPMENT__=sh;
var rowHtml=rs.length?'<div class="rc776-table-wrap"><table class="rc776-table"><thead><tr><th>Verpackung</th><th>Anzahl</th><th>Maße L × B × H</th><th>Gewicht</th><th>LDM</th></tr></thead><tbody>'+rs.map(function(r){var count=num(r.count||r.qty||r.anzahl),dims=[r.l||r.length,r.w||r.width,r.h||r.height].map(q).filter(Boolean).join(' × '),ldm=num(r.ldm||r.loadingMeter);
```

## ResizeObserver
needle='ResizeObserver'
count=3

### line~365 pos=75977
```text
edundante Normalisierung und Vollkopie derselben Prüfungsfragen beim ersten Öffnen. Der 50-Fragen-Aufbau wird aus einem einmal normalisierten Fragenpool erzeugt; die resultierenden 30 Prüfungen und Fragen bleiben inhaltlich identisch.',
  'RC923 macht die regelmäßige Revisionsprüfung wirklich metadata-only: mode=meta validiert die Sitzung über den Auth-Blob und liest anschließend nur Azure-Blob-Metadaten statt den vollständigen Teamstand herunterzuladen. Ein anschließender Voll-Read erfolgt weiterhin nur bei einer tatsächlich neueren Revision.',
  'RC923 beschleunigt Save-Antworten mit ack=1 zusätzlich auf der State-API: Nach dem erfolgreichen Merge wird für die reine Bestätigung nicht mehr der komplette Teamstand nochmals für eine Vollantwort geklont und sanitisiert.',
  'RC923 begrenzt die sichtbare Fehlerdiagnose auf die 250 neuesten relevanten Einträge, ohne den vollständigen JSON-Export zu beschneiden. Dadurch muss die Diagnoseansicht nicht mehr bis zu 5000 Cloud-Einträge bei jedem Snapshot zusammenführen und sortieren.',
  'RC923 protokolliert die erwartete POD-Pause bei fehlender abrufbarer Fahrerunterschrift nur noch als Information. Die automatische 30-Minuten-Pause bleibt unverändert, erzeugt aber keine wiederkehrende Warnung in der Fehlerdiagnose.',
  'RC923 beseitigt den doppelten Azure-Teamstand-Lesezugriff im Testservice: der Teststand wird nur noch bei tatsächlich fehlendem Blob initialisiert und pro normalem Lese-/Speicheraufruf nur einmal geladen.',
  'RC923 trennt Diagnose-Uploads vom vollständigen Teamstand, verlängert die Speicher-Abbruchgrenze für langsame Azure-Antworten und reduziert Diagnose- sowie Render-Stürme.',
  'RC923 reduziert Browserlast in Dashboard, Aufgaben-Planer, Sendungsübersicht und Lager durch stabile Signaturen, wiederverwendete ResizeObserver und aktivierte Offscreen-Virtualisierung.',
    "RC922 stabilisiert + Zeile im Colli-Bereich: Beim Hinzufügen bleibt die aktuelle Scrollposition erhalten und der Browser fokussiert den sichtbaren Verpackungsbutton statt des unsichtbaren nativen Select-Felds. Dadurch springt die Sendungsansicht beim Anlegen weiterer Colli-Zeilen nicht mehr unnötig.",
    "RC922 ändert weder Colli-Datenmodell noch Maße-, LDM-, Gewichts-, Verpackungs- oder Speicherlogik. Die neue Zeile wird weiterhin lokal angelegt und erst über die vorhandenen fachlichen Speicherwege dauerhaft übernommen.",
    "RC921 entfernt den veralteten festen Cache-Schlüssel v=554 von der POD-Notfallseite. Öffnen und Kopieren der Notfallseite verwenden jetzt automatisch die Cache-Version des aktuell geladenen ExportHUB-Builds.",
    "RC921 stellt damit sicher, dass POD_Notfall_Start.html im verbundenen Notfallordner aus derselben aktuellen Version stammt wie die laufende Testversion und nicht aus einer alten Browser-/CDN-Zwischenspeicherung.",
    "RC921 ändert weder POD-PDF-Inhalte noch SharePoint-, Vault- oder Signaturdaten; korrigiert wird ausschließlich die Versionsbindung der Notfallseite.",
    "RC920 berechnet die Dashboard-Masonry-Höhe bei jeder Größenänderung aus der natürlichen aktuellen Kachelhöhe neu. Alte Grid-Row-Spans werden vor der Messung entfernt, damit geschrumpfte Kacheln keine leeren Flächen zurücklassen.",
    "RC920 beobachtet für die Masonry-Aktualisierung die tatsächlichen Kachelinhalte statt den durch das Raster künstlich gestreckten äußeren Grid-Eintrag. Dadurch entstehen bei Status-, Aufgaben- und Datenänderungen weniger unnötige Resize-Nachläufe.",
    "RC920 bestätigt die Smartphone-Darstellung von Dashboard und Sendung erstellen bei 320 bis 768 Pixel Breite ohne horizont
```

### line~20716 pos=4164429
```text
etSettlements).map(function(x){return[q(x.id),q(x.status),q(x.partyKey),q(x.reconciliationDate||x.checkDate||x.to),q(x.systemBalance),q(x.actualBalance),q(x.difference),q(x.adjustmentBookingId)].join('|')}).sort()
 })
}
/* exporthub-rc920-dashboard-dynamic-masonry */
function dashboardMasonryMobile(){return !!(window.matchMedia&&window.matchMedia('(max-width:760px)').matches)}
function dashboardMasonryMeasure(grid){
 if(!grid||!grid.isConnected)return false;
 var items=Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]'));if(!items.length)return false;
 items.forEach(function(item){item.style.removeProperty('grid-row-end')});
 if(dashboardMasonryMobile())return true;
 void grid.offsetHeight;
 var cs=window.getComputedStyle(grid),row=parseFloat(cs.gridAutoRows)||4,gap=parseFloat(cs.rowGap)||10,unit=row+gap;
 items.forEach(function(item){var height=Math.max(1,Math.ceil(item.scrollHeight||item.getBoundingClientRect().height||1)),span=Math.max(1,Math.ceil((height+gap)/unit));item.style.gridRowEnd='span '+span});
 return true
}
function dashboardMasonrySchedule(grid){
 if(grid)dashboardMasonryGrid=grid;if(!dashboardMasonryGrid)return false;
 if(dashboardMasonryFrame){try{(window.cancelAnimationFrame||clearTimeout)(dashboardMasonryFrame)}catch(_){}}
 var raf=window.requestAnimationFrame||function(fn){return setTimeout(fn,16)};dashboardMasonryFrame=raf(function(){dashboardMasonryFrame=0;dashboardMasonryMeasure(dashboardMasonryGrid)});return true
}
function dashboardMasonryBind(grid){
 if(!grid)return false;
 if(dashboardMasonryGrid!==grid){if(dashboardMasonryObserver){try{dashboardMasonryObserver.disconnect()}catch(_){}}dashboardMasonryObserver=null;dashboardMasonryTargets=new Set();dashboardMasonryGrid=grid}
 if(typeof window.ResizeObserver==='function'&&!dashboardMasonryObserver)dashboardMasonryObserver=new ResizeObserver(function(){dashboardMasonrySchedule(dashboardMasonryGrid)});
 if(dashboardMasonryObserver){
  var nextTargets=new Set();
  Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]')).forEach(function(item){
   var targets=Array.prototype.slice.call(item.children).filter(function(node){return node&&node.nodeType===1});
   if(!targets.length)targets=[item];
   targets.forEach(function(target){nextTargets.add(target);if(!dashboardMasonryTargets.has(target))try{dashboardMasonryObserver.observe(target)}catch(_){}})
  });
  dashboardMasonryTargets.forEach(function(target){if(!nextTargets.has(target))try{dashboardMasonryObserver.unobserve(target)}catch(_){}});dashboardMasonryTargets=nextTargets
 }
 dashboardMasonrySchedule(grid);return true
}
window.addEventListener('resize',function(){if(dashboardMasonryResizeTimer)clearTimeout(dashboardMasonryResizeTimer);dashboardMasonryResizeTimer=setTimeout(function(){dashboardMasonryResizeTimer=0;dashboardMasonrySchedule(dashboardMasonryGrid)},80)},{passive:true});
function dashboardMarkupHash(value){var s=String(value==null?'':value),h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(36)}
function dashboardElementFromHtml(html){var t=document.createElement('template');t.innerHTML=String(html||'').trim();return t.content.firstElementChild||null}
function dashboardTagGrid(grid,cards){if(!grid)return;cards.forEach(function(card){var node=grid.querySelector(':scope > [data-dashboard-widget="'+CSS.escape(card.id)+'"]');if(node)node.setAttribute('data-rc205-render-signature',card.signature)});dashboardMasonryBind(grid)}
function dashboardPatchGrid
```

### line~20716 pos=4164513
```text
iationDate||x.checkDate||x.to),q(x.systemBalance),q(x.actualBalance),q(x.difference),q(x.adjustmentBookingId)].join('|')}).sort()
 })
}
/* exporthub-rc920-dashboard-dynamic-masonry */
function dashboardMasonryMobile(){return !!(window.matchMedia&&window.matchMedia('(max-width:760px)').matches)}
function dashboardMasonryMeasure(grid){
 if(!grid||!grid.isConnected)return false;
 var items=Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]'));if(!items.length)return false;
 items.forEach(function(item){item.style.removeProperty('grid-row-end')});
 if(dashboardMasonryMobile())return true;
 void grid.offsetHeight;
 var cs=window.getComputedStyle(grid),row=parseFloat(cs.gridAutoRows)||4,gap=parseFloat(cs.rowGap)||10,unit=row+gap;
 items.forEach(function(item){var height=Math.max(1,Math.ceil(item.scrollHeight||item.getBoundingClientRect().height||1)),span=Math.max(1,Math.ceil((height+gap)/unit));item.style.gridRowEnd='span '+span});
 return true
}
function dashboardMasonrySchedule(grid){
 if(grid)dashboardMasonryGrid=grid;if(!dashboardMasonryGrid)return false;
 if(dashboardMasonryFrame){try{(window.cancelAnimationFrame||clearTimeout)(dashboardMasonryFrame)}catch(_){}}
 var raf=window.requestAnimationFrame||function(fn){return setTimeout(fn,16)};dashboardMasonryFrame=raf(function(){dashboardMasonryFrame=0;dashboardMasonryMeasure(dashboardMasonryGrid)});return true
}
function dashboardMasonryBind(grid){
 if(!grid)return false;
 if(dashboardMasonryGrid!==grid){if(dashboardMasonryObserver){try{dashboardMasonryObserver.disconnect()}catch(_){}}dashboardMasonryObserver=null;dashboardMasonryTargets=new Set();dashboardMasonryGrid=grid}
 if(typeof window.ResizeObserver==='function'&&!dashboardMasonryObserver)dashboardMasonryObserver=new ResizeObserver(function(){dashboardMasonrySchedule(dashboardMasonryGrid)});
 if(dashboardMasonryObserver){
  var nextTargets=new Set();
  Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]')).forEach(function(item){
   var targets=Array.prototype.slice.call(item.children).filter(function(node){return node&&node.nodeType===1});
   if(!targets.length)targets=[item];
   targets.forEach(function(target){nextTargets.add(target);if(!dashboardMasonryTargets.has(target))try{dashboardMasonryObserver.observe(target)}catch(_){}})
  });
  dashboardMasonryTargets.forEach(function(target){if(!nextTargets.has(target))try{dashboardMasonryObserver.unobserve(target)}catch(_){}});dashboardMasonryTargets=nextTargets
 }
 dashboardMasonrySchedule(grid);return true
}
window.addEventListener('resize',function(){if(dashboardMasonryResizeTimer)clearTimeout(dashboardMasonryResizeTimer);dashboardMasonryResizeTimer=setTimeout(function(){dashboardMasonryResizeTimer=0;dashboardMasonrySchedule(dashboardMasonryGrid)},80)},{passive:true});
function dashboardMarkupHash(value){var s=String(value==null?'':value),h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(36)}
function dashboardElementFromHtml(html){var t=document.createElement('template');t.innerHTML=String(html||'').trim();return t.content.firstElementChild||null}
function dashboardTagGrid(grid,cards){if(!grid)return;cards.forEach(function(card){var node=grid.querySelector(':scope > [data-dashboard-widget="'+CSS.escape(card.id)+'"]');if(node)node.setAttribute('data-rc205-render-signature',card.signature)});dashboardMasonryBind(grid)}
function dashboardPatchGrid(grid,cards){
 if(!grid)return false;var changed=false,wanted={};
 cards.forEach(fun
```

## resize listeners
needle="addEventListener('resize'"
count=2

### line~15801 pos=2837420
```text
en');btn.setAttribute('aria-controls','nav');top.insertBefore(btn,top.firstChild)}btn.type='button';btn.setAttribute('aria-controls','nav');btn.setAttribute('aria-expanded',document.body.classList.contains('eh-sidebar-open')&&mobile()?'true':'false');if(btn.dataset.ehMenuBound!=='1'){btn.dataset.ehMenuBound='1';btn.addEventListener('pointerdown',function(e){if(!mobile())return;e.preventDefault();e.stopPropagation();btn.dataset.ehSkipClick='1';setOpen(!document.body.classList.contains('eh-sidebar-open'))})}if(nav){nav.setAttribute('role','navigation');nav.setAttribute('aria-label','ExportHUB Funktionen');nav.querySelectorAll(':scope>button[data-index321-view]').forEach(function(b){b.type='button';b.style.pointerEvents='auto';b.removeAttribute('onclick')});if(mobile()){nav.style.scrollBehavior='auto';nav.tabIndex=0}else{nav.removeAttribute('tabindex');nav.style.removeProperty('scroll-behavior')}}if(!mobile())setOpen(false);return true}
function schedule(){if(scheduled)return;scheduled=true;setTimeout(function(){scheduled=false;ensure()},0)}
document.addEventListener('click',function(e){var toggle=e.target&&e.target.closest&&e.target.closest('#ehMenuBtn');if(toggle){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();if(toggle.dataset.ehSkipClick==='1'){delete toggle.dataset.ehSkipClick;return}setOpen(!document.body.classList.contains('eh-sidebar-open'));return}var item=e.target&&e.target.closest&&e.target.closest('#nav>button[data-index321-view]');if(mobile()&&item){setTimeout(function(){setOpen(false)},0);return}if(mobile()&&document.body.classList.contains('eh-sidebar-open')&&!(e.target&&e.target.closest&&e.target.closest('.sidebar')))setOpen(false)},true);
document.addEventListener('keydown',function(e){if(e.key==='Escape')setOpen(false)},true);
window.addEventListener('resize',schedule);['exporthub:ready','exporthub:rendered','exporthub:viewchange'].forEach(function(n){window.addEventListener(n,schedule)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ensure,{once:true});else ensure();
window.ExportHUBMobileMenu=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',install:ensure,open:function(){return setOpen(true)},close:function(){return setOpen(false)},isOpen:function(){return document.body.classList.contains('eh-sidebar-open')&&mobile()},selfTest:function(){return{mobile:mobile(),menuButtons:document.querySelectorAll('#ehMenuBtn').length,legacyCollapse:document.querySelectorAll('#ehCollapseBtn,.eh-collapse-btn').length,open:document.body.classList.contains('eh-sidebar-open')}}});
})();
</script>
<!-- RC569 LOCAL QR GENERATOR START -->
<script>
/*
 * QRCode for JavaScript, Copyright (c) 2009 Kazuhiko Arase.
 * MIT licensed. Embedded locally for offline/print-safe QR generation.
 */
(function(){
'use strict';
if(window.RC569QrSvg)return;
var modules={"QRMode":function(module,exports,require){
module.exports = {
    MODE_NUMBER :       1 << 0,
    MODE_ALPHA_NUM :    1 << 1,
    MODE_8BIT_BYTE :    1 << 2,
    MODE_KANJI :        1 << 3
};

},"QRErrorCorrectLevel":function(module,exports,require){
module.exports = {
	L : 1,
	M : 0,
	Q : 3,
	H : 2
};


},"QRMaskPattern":function(module,exports,require){
module.exports = {
	PATTERN000 : 0,
	PATTERN001 : 1,
	PATTERN010 : 2,
	PATTERN011 : 3,
	PATTERN100 : 4,
	PATTERN101 : 5,
	PATTERN110 : 6,
	PATTERN111 : 7
};

},"QRMath":function(module,exports,require){
var QRMath = {

	glog : function(n) {
	
		if (n < 1) {
			throw new Error("glog(" + n + ")");
		}
		
		return QRMath.LOG_TABLE[n];
	},
	
	gexp :
```

### line~20728 pos=4165325
```text
){var height=Math.max(1,Math.ceil(item.scrollHeight||item.getBoundingClientRect().height||1)),span=Math.max(1,Math.ceil((height+gap)/unit));item.style.gridRowEnd='span '+span});
 return true
}
function dashboardMasonrySchedule(grid){
 if(grid)dashboardMasonryGrid=grid;if(!dashboardMasonryGrid)return false;
 if(dashboardMasonryFrame){try{(window.cancelAnimationFrame||clearTimeout)(dashboardMasonryFrame)}catch(_){}}
 var raf=window.requestAnimationFrame||function(fn){return setTimeout(fn,16)};dashboardMasonryFrame=raf(function(){dashboardMasonryFrame=0;dashboardMasonryMeasure(dashboardMasonryGrid)});return true
}
function dashboardMasonryBind(grid){
 if(!grid)return false;
 if(dashboardMasonryGrid!==grid){if(dashboardMasonryObserver){try{dashboardMasonryObserver.disconnect()}catch(_){}}dashboardMasonryObserver=null;dashboardMasonryTargets=new Set();dashboardMasonryGrid=grid}
 if(typeof window.ResizeObserver==='function'&&!dashboardMasonryObserver)dashboardMasonryObserver=new ResizeObserver(function(){dashboardMasonrySchedule(dashboardMasonryGrid)});
 if(dashboardMasonryObserver){
  var nextTargets=new Set();
  Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]')).forEach(function(item){
   var targets=Array.prototype.slice.call(item.children).filter(function(node){return node&&node.nodeType===1});
   if(!targets.length)targets=[item];
   targets.forEach(function(target){nextTargets.add(target);if(!dashboardMasonryTargets.has(target))try{dashboardMasonryObserver.observe(target)}catch(_){}})
  });
  dashboardMasonryTargets.forEach(function(target){if(!nextTargets.has(target))try{dashboardMasonryObserver.unobserve(target)}catch(_){}});dashboardMasonryTargets=nextTargets
 }
 dashboardMasonrySchedule(grid);return true
}
window.addEventListener('resize',function(){if(dashboardMasonryResizeTimer)clearTimeout(dashboardMasonryResizeTimer);dashboardMasonryResizeTimer=setTimeout(function(){dashboardMasonryResizeTimer=0;dashboardMasonrySchedule(dashboardMasonryGrid)},80)},{passive:true});
function dashboardMarkupHash(value){var s=String(value==null?'':value),h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(36)}
function dashboardElementFromHtml(html){var t=document.createElement('template');t.innerHTML=String(html||'').trim();return t.content.firstElementChild||null}
function dashboardTagGrid(grid,cards){if(!grid)return;cards.forEach(function(card){var node=grid.querySelector(':scope > [data-dashboard-widget="'+CSS.escape(card.id)+'"]');if(node)node.setAttribute('data-rc205-render-signature',card.signature)});dashboardMasonryBind(grid)}
function dashboardPatchGrid(grid,cards){
 if(!grid)return false;var changed=false,wanted={};
 cards.forEach(function(card,index){
  wanted[card.id]=true;
  var selector=':scope > [data-dashboard-widget="'+CSS.escape(card.id)+'"]',node=grid.querySelector(selector),sig=node&&q(node.getAttribute('data-rc205-render-signature'));
  if(!node||sig!==card.signature){var fresh=dashboardElementFromHtml(card.markup);if(fresh){fresh.setAttribute('data-rc205-render-signature',card.signature);if(node)node.replaceWith(fresh);else grid.appendChild(fresh);node=fresh;changed=true}}
  if(node){var at=grid.children[index]||null;if(at!==node){grid.insertBefore(node,at);changed=true}}
 });
 Array.prototype.slice.call(grid.querySelectorAll(':scope > [data-dashboard-widget]')).forEach(function(node){var id=q(node.getAttribute('data-dashboard-widget'));if(!wanted[id]){node.remove();changed=true}});
 dashboardMasonryBind(grid);
 return
```

## requestAnimationFrame
needle='requestAnimationFrame'
count=14

### line~13692 pos=2197893
```text
{},controller?{signal:controller.signal}:{}));
 }finally{if(timer)native.clearTimeout(timer)}
}

function addSafeLoadNote(){
 const panel=by('cleanLoadPanel'); if(!panel||by('cleanSafeLoadNote'))return;
 const n=document.createElement('div'); n.id='cleanSafeLoadNote'; n.className='clean-safe-note';
 n.textContent='ExportHUB wird stabil geladen. Hintergrundmodule starten erst nach der sichtbaren Bereitschaft.';
 panel.appendChild(n);
}

function setVersion(){
 try{const d=Object.getOwnPropertyDescriptor(document,'title');if(d&&d.configurable)delete document.title}catch(_){ }
 try{window.ExportHUBBuild.applyTitle();}catch(_){ }
 const title=document.querySelector('title');if(title)window.ExportHUBBuild.applyTitle();
 window.ExportHUBBuild.applyVersion();
 document.querySelectorAll('[id*=version i],[class*=version i],[data-exporthub-version-label]').forEach(function(e){
  const value=text(e.textContent);
  if(/(?:ExportHUB\s+Private\s+|Private\s+)?RC\d+/i.test(value))window.ExportHUBBuild.applyVersion();
 });
 const login=document.querySelector('.login-card');if(login&&!by('cleanVersionBadge')){const d=document.createElement('div');d.id='cleanVersionBadge';d.className='clean-version-badge';d.textContent='Aktuelle Version '+window.ExportHUBBuild.getBuild().version;d.setAttribute('aria-label',d.textContent);const heading=login.querySelector('h1');if(heading)heading.insertAdjacentElement('afterend',d);else login.insertBefore(d,login.firstChild);window.ExportHUBBuild.applyVersion()}
 try{ensureUserDisplay()}catch(_){ }
}


// Browser-Speicher bleibt unverändert; ExportHUB verwendet keine globalen Storage-Prototyp-Overrides.

// Azure-Teamdaten werden ausschließlich über stateCall()/jsonFetch() verwaltet; window.fetch bleibt unverändert.

// Browser-Timer, MutationObserver und requestAnimationFrame bleiben native Browser-APIs.

function apiEndpointLabel(url){
 try{return new URL(String(url||''),location.href).pathname}catch(_){return String(url||'API')}
}
async function resilientFetch(url,options){
 if(location.protocol==='file:'){
  const localError=new Error('ExportHUB wurde als lokale Datei geöffnet. Azure-Speicherung und Anmeldung funktionieren nur über die veröffentlichte Azure-Webadresse.');
  localError.code='LOCAL_FILE_NO_API';throw localError;
 }
 const base=Object.assign({credentials:'same-origin',cache:'no-store'},options||{}),requestedTimeout=Number(base.timeoutMs),timeoutMs=Math.max(3000,requestedTimeout>0?requestedTimeout:30000),maxAttempts=Math.max(1,Math.min(4,Number(base.maxAttempts)||4)),externalSignal=base.signal||null;
 delete base.timeoutMs;delete base.maxAttempts;delete base.signal;
 let lastError=null;
 for(let attempt=1;attempt<=maxAttempts;attempt++){
  const controller=typeof AbortController==='function'?new AbortController():null;
  var externalAbort=null;
  if(controller&&externalSignal){if(externalSignal.aborted){try{controller.abort()}catch(_){ }}else{externalAbort=function(){try{controller.abort()}catch(_){ }};try{externalSignal.addEventListener('abort',externalAbort,{once:true})}catch(_){ }}}
  const timer=controller?native.setTimeout(function(){try{controller.abort()}catch(_){ }},timeoutMs):null;
  const opts=Object.assign({},base);if(controller)opts.signal=controller.signal;else if(externalSignal)opts.signal=externalSignal;
  try{
   const res=await native.fetch(url,opts);
   if(timer)native.clearTimeout(timer);if(externalAbort&&externalSignal)try{externalSignal.removeEventListener('abort',externalAbort)}catch(_){ }
   return res;
  }catch(e){
   if(timer)native.clearTimeout(timer);if(externalAbort&&externalS
```

### line~14251 pos=2282725
```text
 · '+fill+' Kundenordner ergänzbar · Wiederherstellung nur unter Einstellungen'}}catch(e){const code=String(e&&e.code||'');if(code!=='UNKNOWN_STATE_ACTION'&&code!=='ADMIN_REQUIRED')console.warn('Kunden-Historienprüfung nicht abgeschlossen',code,e&&e.message)}},250);return true
}
function ensureUserDisplay(){
 const name=text(runtime.user&&(runtime.user.name||runtime.user.user||runtime.user.login));
 const brand=by('brandUserLine');
 if(brand)brand.textContent='ExportHUB-Benutzer: '+(name||'nicht angemeldet');
 const profileName=by('profileName');
 if(profileName)profileName.textContent=name||'Nicht angemeldet';
 const profileRole=by('profileRole');
 if(profileRole)profileRole.textContent=name?text(runtime.user.role||'ExportHUB-Benutzer'):'';
 ['brandMicrosoftLine','rc448MicrosoftAccount'].forEach(function(id){const el=by(id);if(el){el.textContent='';el.style.display='none';el.setAttribute('aria-hidden','true')}});
 const profile=document.querySelector('.profile');
 if(profile&&name){
  let strong=profile.querySelector('strong');
  if(strong)strong.textContent=name;
  profile.querySelectorAll('*').forEach(function(node){
   if(/Microsoft(?:-Konto)?\s*:\s*nicht angemeldet|nicht intern angemeldet/i.test(text(node.textContent))&&node!==strong)node.style.display='none';
  });
 }
 document.documentElement.setAttribute('data-exporthub-user',name);
}
function bindCanonicalState(){let current=null;try{if(typeof window.__EXPORTHUB_GET_STATE__==='function')current=window.__EXPORTHUB_GET_STATE__()}catch(_){current=null}if(!isObject(current))current=isObject(runtime.state)?runtime.state:{};runtime.state=current;window.state=current;window.__CLEAN_BOOT_STATE__=current;window.__EXPORTHUB_STATE__=current;return current}
function nextFrame(){return new Promise(function(resolve){(native.requestAnimationFrame||native.setTimeout)(function(){resolve()},16)})}
async function signalReady(){
 const wasRestored=runtime.sessionRestored===true;
 bindCanonicalState();progress(99,'Oberfläche wird abschließend geprüft …');ensureUserDisplay();setVersion();await nextFrame();await nextFrame();
 const app=by('app'),content=by('content');if(!app||app.classList.contains('hidden')||!content||!content.children.length)throw new Error('Die Oberfläche ist nach dem Erst-Render noch nicht bedienbar.');
 const info=by('saveInfo');if(info)info.textContent=(DATA_ENVIRONMENT==='testservice'?'TESTSERVICE-Daten':'Produktionsdaten')+' geladen · '+new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit',second:'2-digit'});
 if(runtime.saveTimer)native.clearTimeout(runtime.saveTimer);runtime.saveTimer=null;runtime.pendingSave=false;runtime.dirty=false;
 var __cleanupBase=runtime.customerRecoveryPending&&runtime.customerRecoveryBase?runtime.customerRecoveryBase:(runtime.shipmentGhostCleanupPending&&runtime.shipmentGhostCleanupBase?runtime.shipmentGhostCleanupBase:null);runtime.lastSnapshot=__cleanupBase||clone(window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():runtime.state||{});runtime.lastUsers=clone(window.__EXPORTHUB_GET_USERS__?window.__EXPORTHUB_GET_USERS__():runtime.users||[]);
 runtime.ready=true;runtime.loaded=true;runtime.loading=false;runtime.applyingRemote=false;runtime.acceptWritesAt=0;var readyPass=by('loginPass');if(readyPass)readyPass.value='';var readyRetry=by('testserviceContinueBtn');if(readyRetry)readyRetry.classList.add('hidden');if(isTestServiceOrigin()){try{var readyUser=by('loginUser'),remember=localStorage.getItem('exporthub_login_remember_v1')==='1';if(remember&&readyUser&&text(readyUser.value))localStorage.setItem('exporthub_login_us
```

### line~20711 pos=4163959
```text
el(x)]});
 return JSON.stringify({
  day:todayISO(),u:userName(),
  p:{widgets:arr(p.widgets).slice(),quick:arr(p.quickLinks).slice(),entities:entities,layout:layout,config:!!p.configOpen},
  t:arr(s.tasks).filter(taskMeaningfulDashboardRC818).filter(function(t){return !dashboardTaskIsTestRC818(t)}).map(taskSig).sort(),
  s:arr(s.shipments).map(shipmentSig).sort(),
  a:arr(s.abdRequests).map(abdSig).sort(),
  b:arr(s.palletAccount).map(palSig).sort(),
  r:arr(s.palletSettlements).map(function(x){return[q(x.id),q(x.status),q(x.partyKey),q(x.reconciliationDate||x.checkDate||x.to),q(x.systemBalance),q(x.actualBalance),q(x.difference),q(x.adjustmentBookingId)].join('|')}).sort()
 })
}
/* exporthub-rc920-dashboard-dynamic-masonry */
function dashboardMasonryMobile(){return !!(window.matchMedia&&window.matchMedia('(max-width:760px)').matches)}
function dashboardMasonryMeasure(grid){
 if(!grid||!grid.isConnected)return false;
 var items=Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]'));if(!items.length)return false;
 items.forEach(function(item){item.style.removeProperty('grid-row-end')});
 if(dashboardMasonryMobile())return true;
 void grid.offsetHeight;
 var cs=window.getComputedStyle(grid),row=parseFloat(cs.gridAutoRows)||4,gap=parseFloat(cs.rowGap)||10,unit=row+gap;
 items.forEach(function(item){var height=Math.max(1,Math.ceil(item.scrollHeight||item.getBoundingClientRect().height||1)),span=Math.max(1,Math.ceil((height+gap)/unit));item.style.gridRowEnd='span '+span});
 return true
}
function dashboardMasonrySchedule(grid){
 if(grid)dashboardMasonryGrid=grid;if(!dashboardMasonryGrid)return false;
 if(dashboardMasonryFrame){try{(window.cancelAnimationFrame||clearTimeout)(dashboardMasonryFrame)}catch(_){}}
 var raf=window.requestAnimationFrame||function(fn){return setTimeout(fn,16)};dashboardMasonryFrame=raf(function(){dashboardMasonryFrame=0;dashboardMasonryMeasure(dashboardMasonryGrid)});return true
}
function dashboardMasonryBind(grid){
 if(!grid)return false;
 if(dashboardMasonryGrid!==grid){if(dashboardMasonryObserver){try{dashboardMasonryObserver.disconnect()}catch(_){}}dashboardMasonryObserver=null;dashboardMasonryTargets=new Set();dashboardMasonryGrid=grid}
 if(typeof window.ResizeObserver==='function'&&!dashboardMasonryObserver)dashboardMasonryObserver=new ResizeObserver(function(){dashboardMasonrySchedule(dashboardMasonryGrid)});
 if(dashboardMasonryObserver){
  var nextTargets=new Set();
  Array.prototype.slice.call(grid.querySelectorAll(':scope > .rc205-layout-item[data-dashboard-widget]')).forEach(function(item){
   var targets=Array.prototype.slice.call(item.children).filter(function(node){return node&&node.nodeType===1});
   if(!targets.length)targets=[item];
   targets.forEach(function(target){nextTargets.add(target);if(!dashboardMasonryTargets.has(target))try{dashboardMasonryObserver.observe(target)}catch(_){}})
  });
  dashboardMasonryTargets.forEach(function(target){if(!nextTargets.has(target))try{dashboardMasonryObserver.unobserve(target)}catch(_){}});dashboardMasonryTargets=nextTargets
 }
 dashboardMasonrySchedule(grid);return true
}
window.addEventListener('resize',function(){if(dashboardMasonryResizeTimer)clearTimeout(dashboardMasonryResizeTimer);dashboardMasonryResizeTimer=setTimeout(function(){dashboardMasonryResizeTimer=0;dashboardMasonrySchedule(dashboardMasonryGrid)},80)},{passive:true});
function dashboardMarkupHash(value){var s=String(value==null?'':value),h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toStr
```

### line~23512 pos=4727145
```text
action="toggle">Lieferavis aktivieren</button><button type="button" class="ghost" data-rc897-avis-action="copy">Link kopieren</button><button type="button" class="ghost" data-rc897-avis-action="open">Vorschau öffnen</button></div><div class="rc897-avis-help">Der Lieferavis ist Teil der Sendung. Der Kundenlink wird nur bei aktivem Lieferavis erzeugt und drei Arbeitstage nach der tatsächlichen Abholung automatisch deaktiviert.</div>';
 }
 if(panel.parentElement!==body)body.appendChild(panel);
 if(!window.__EXPORTHUB_RC897_AVIS_EVENTS__){
   window.__EXPORTHUB_RC897_AVIS_EVENTS__=true;
   document.addEventListener('click',function(ev){
     var btn=ev.target&&ev.target.closest&&ev.target.closest('#rc897LieferavisPanel [data-rc897-avis-action]');if(!btn)return;
     ev.preventDefault();ev.stopPropagation();
     var action=btn.getAttribute('data-rc897-avis-action'),api=rc897AvisApi(),sh=rc897AvisShipment();if(!api||!sh)return;
     if(action==='toggle'){
       var active=false;try{active=!!api.enabled(sh)}catch(_){}
       btn.disabled=true;
       Promise.resolve(api.toggle(!active)).catch(function(e){console.error('RC897 Lieferavis toggle',e)}).finally(function(){btn.disabled=false;updateRC897LieferavisPanel()});
       return
     }
     var url='';try{url=q(api.link(sh))}catch(_){}
     if(action==='copy'&&url){if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(url).catch(function(){prompt('Lieferavis-Link',url)});else prompt('Lieferavis-Link',url);return}
     if(action==='open'&&url){window.open(url,'_blank','noopener,noreferrer');return}
   },true);
   ['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:shipment-saved','exporthub:customer-avis-updated'].forEach(function(name){window.addEventListener(name,function(){requestAnimationFrame(updateRC897LieferavisPanel)})});
 }
 updateRC897LieferavisPanel();return true
}
function ensureRC894LieferavisButton(){var old=document.getElementById('lieferAvisBtn');if(old)old.remove();return !!document.getElementById('rc897LieferavisPanel')}
/* exporthub-rc899-no-voids-layout */
function applyRC899DocumentsAbd(documentsBody,abdDecisionBlock){
 if(!documentsBody||!abdDecisionBlock)return false;
 var main=documentsBody.querySelector(':scope > .rc899-doc-main');if(!main){main=document.createElement('div');main.className='rc899-doc-main';documentsBody.insertBefore(main,documentsBody.firstChild)}
 Array.from(documentsBody.children).forEach(function(node){if(node===main||node===abdDecisionBlock)return;main.appendChild(node)});
 if(abdDecisionBlock.parentElement!==documentsBody)documentsBody.appendChild(abdDecisionBlock);
 documentsBody.setAttribute('data-rc899-doc-abd','1');
 return true
}
function applyFixedBlocks(){
 var r=root(),shell=document.getElementById('rc573ShipmentShell')||r;if(!r||!shell)return 0;
 var layout=document.getElementById('rc363FixedShipmentLayout');
 if(!layout){layout=document.createElement('div');layout.id='rc363FixedShipmentLayout';var first=Array.from(r.children).find(function(el){return el.classList&&el.classList.contains('page-head')});if(first&&first.nextSibling)r.insertBefore(layout,first.nextSibling);else r.appendChild(layout)}
 function box(id,cls,parent){var el=document.getElementById(id);if(!el){el=document.createElement('div');el.id=id;el.className=cls}if(el.className!==cls)el.className=cls;if(el.parentElement!==parent)parent.appendChild(el);return el}
 var top=box('rc383TopPair','rc383-layout-pair',layout),left=box('rc383LeftColumn','rc383-column-stack',top),right=box('rc383RightColumn','rc383-column-stack',top);
```

### line~24183 pos=4868365
```text
on Optik'],
 ['current','Blau Grau Optik']
];
var currentUser='';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function slug(v){return q(v).toLowerCase().replace(/[^a-z0-9äöüß_-]+/g,'-')||'default'}
function valid(v){v=q(v);return THEMES.some(function(x){return x[0]===v})?v:'current'}
function userName(){
 var p=document.getElementById('profileName'),b=document.getElementById('brandUserLine');
 var value=q((p&&p.textContent)||(b&&b.textContent)).replace(/^ExportHUB-Benutzer:\s*/i,'');
 return !value||/nicht angemeldet/i.test(value)?'':value;
}
function key(user){return'exporthub-theme-choice::'+slug(user)}
function read(user){try{return valid(localStorage.getItem(key(user))||localStorage.getItem('exporthub-theme-choice:last')||'current')}catch(_){return'current'}}
function save(user,theme){try{localStorage.setItem(key(user),theme);localStorage.setItem('exporthub-theme-choice:last',theme)}catch(_){}return theme}
function apply(theme,persist){
 theme=valid(theme);
 var root=document.documentElement,body=document.body;
 if(!body)return theme;
 body.classList.remove('eh-dark');
 if(body.getAttribute('data-eh-theme')===theme){
  var existing=document.getElementById('ehThemeSelect');
  if(existing&&existing.value!==theme)existing.value=theme;
  if(persist)save(userName(),theme);
  return theme;
 }
 root.classList.add('eh-theme-changing');
 root.setAttribute('data-eh-theme',theme);
 body.setAttribute('data-eh-theme',theme);
 var app=document.getElementById('app'),content=document.getElementById('content');
 if(app)app.setAttribute('data-eh-theme',theme);
 if(content)content.setAttribute('data-eh-theme',theme);
 var select=document.getElementById('ehThemeSelect');
 if(select&&select.value!==theme)select.value=theme;
 if(persist)save(userName(),theme);
 requestAnimationFrame(function(){requestAnimationFrame(function(){root.classList.remove('eh-theme-changing')})});
 return theme;
}
function ensure(){
 var top=document.querySelector('.topbar');if(!top)return false;
 var wrap=document.getElementById('ehThemeSwitch');
 if(!wrap){
  wrap=document.createElement('div');wrap.id='ehThemeSwitch';wrap.className='eh-theme-switch ghost';wrap.title='Design auswählen';
  wrap.innerHTML='<span>Design</span><select id="ehThemeSelect" aria-label="Design auswählen"></select>';
  var lang=document.getElementById('languageSelect');
  if(lang&&lang.nextSibling)top.insertBefore(wrap,lang.nextSibling);else top.appendChild(wrap);
 }
 var select=document.getElementById('ehThemeSelect');
 if(select&&!select.dataset.rc446){
  select.innerHTML=THEMES.map(function(x){return'<option value="'+x[0]+'">'+x[1]+'</option>'}).join('');
  select.dataset.rc446='1';
  select.addEventListener('change',function(event){event.stopPropagation();apply(this.value,true)});
 }
 return true;
}
function refresh(){
 ensure();
 var user=userName();
 if(user!==currentUser||!document.body.getAttribute('data-eh-theme')){currentUser=user;apply(read(user),false)}
 var content=document.getElementById('content');if(content)content.setAttribute('data-eh-theme',valid(document.body.getAttribute('data-eh-theme')));
}
function boot(){refresh()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,refresh)});
window.addEventListener('storage',function(e){if(e&&e.key&&e.key.indexOf('exporthub-theme-choice')===0)refresh()});
window.ExportHUBThemeSwitcher=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||
```

### line~24183 pos=4868398
```text
u Optik']
];
var currentUser='';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function slug(v){return q(v).toLowerCase().replace(/[^a-z0-9äöüß_-]+/g,'-')||'default'}
function valid(v){v=q(v);return THEMES.some(function(x){return x[0]===v})?v:'current'}
function userName(){
 var p=document.getElementById('profileName'),b=document.getElementById('brandUserLine');
 var value=q((p&&p.textContent)||(b&&b.textContent)).replace(/^ExportHUB-Benutzer:\s*/i,'');
 return !value||/nicht angemeldet/i.test(value)?'':value;
}
function key(user){return'exporthub-theme-choice::'+slug(user)}
function read(user){try{return valid(localStorage.getItem(key(user))||localStorage.getItem('exporthub-theme-choice:last')||'current')}catch(_){return'current'}}
function save(user,theme){try{localStorage.setItem(key(user),theme);localStorage.setItem('exporthub-theme-choice:last',theme)}catch(_){}return theme}
function apply(theme,persist){
 theme=valid(theme);
 var root=document.documentElement,body=document.body;
 if(!body)return theme;
 body.classList.remove('eh-dark');
 if(body.getAttribute('data-eh-theme')===theme){
  var existing=document.getElementById('ehThemeSelect');
  if(existing&&existing.value!==theme)existing.value=theme;
  if(persist)save(userName(),theme);
  return theme;
 }
 root.classList.add('eh-theme-changing');
 root.setAttribute('data-eh-theme',theme);
 body.setAttribute('data-eh-theme',theme);
 var app=document.getElementById('app'),content=document.getElementById('content');
 if(app)app.setAttribute('data-eh-theme',theme);
 if(content)content.setAttribute('data-eh-theme',theme);
 var select=document.getElementById('ehThemeSelect');
 if(select&&select.value!==theme)select.value=theme;
 if(persist)save(userName(),theme);
 requestAnimationFrame(function(){requestAnimationFrame(function(){root.classList.remove('eh-theme-changing')})});
 return theme;
}
function ensure(){
 var top=document.querySelector('.topbar');if(!top)return false;
 var wrap=document.getElementById('ehThemeSwitch');
 if(!wrap){
  wrap=document.createElement('div');wrap.id='ehThemeSwitch';wrap.className='eh-theme-switch ghost';wrap.title='Design auswählen';
  wrap.innerHTML='<span>Design</span><select id="ehThemeSelect" aria-label="Design auswählen"></select>';
  var lang=document.getElementById('languageSelect');
  if(lang&&lang.nextSibling)top.insertBefore(wrap,lang.nextSibling);else top.appendChild(wrap);
 }
 var select=document.getElementById('ehThemeSelect');
 if(select&&!select.dataset.rc446){
  select.innerHTML=THEMES.map(function(x){return'<option value="'+x[0]+'">'+x[1]+'</option>'}).join('');
  select.dataset.rc446='1';
  select.addEventListener('change',function(event){event.stopPropagation();apply(this.value,true)});
 }
 return true;
}
function refresh(){
 ensure();
 var user=userName();
 if(user!==currentUser||!document.body.getAttribute('data-eh-theme')){currentUser=user;apply(read(user),false)}
 var content=document.getElementById('content');if(content)content.setAttribute('data-eh-theme',valid(document.body.getAttribute('data-eh-theme')));
}
function boot(){refresh()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,refresh)});
window.addEventListener('storage',function(e){if(e&&e.key&&e.key.indexOf('exporthub-theme-choice')===0)refresh()});
window.ExportHUBThemeSwitcher=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',themes:THEMES.map(fun
```

### line~24422 pos=4910251
```text
rn;var card=e.target&&e.target.closest&&e.target.closest('[data-warehouse-drag-key]');if(!card||!isAdmin())return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var key=q(card.getAttribute('data-warehouse-drag-key'));if(!key)return;dragPointer={pointerId:e.pointerId,key:key,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}});root.addEventListener('pointermove',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;dragKey=p.key;p.card.classList.add('rc513-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();updateWarehouseDragScroll(e);var zone=rc946WarehousePointerZone(e,root);root.querySelectorAll('.rc513-drop-target').forEach(function(x){if(x!==zone)x.classList.remove('rc513-drop-target')});if(zone)zone.classList.add('rc513-drop-target')},{passive:false});root.addEventListener('pointerup',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,key=p.key,zone=active?rc946WarehousePointerZone(e,root):null;if(active)e.preventDefault();rc946WarehouseClearPointer(root);if(active&&key&&zone)moveShipmentKey(key,Number(zone.getAttribute('data-warehouse-drop-code')||0))});root.addEventListener('pointercancel',function(e){var p=dragPointer;if(p&&e.pointerId===p.pointerId)rc946WarehouseClearPointer(root)})}
function stopWarehouseDragScroll(){dragScrollSpeed=0;if(dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function warehouseDragScrollTick(){dragScrollFrame=0;if(!dragKey||!dragScrollSpeed)return;window.scrollBy(0,dragScrollSpeed);dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick)}
function updateWarehouseDragScroll(e){if(!dragKey||!e)return stopWarehouseDragScroll();var h=Math.max(1,window.innerHeight||document.documentElement.clientHeight||1),edge=Math.max(80,Math.min(150,Math.round(h*.16))),y=Number(e.clientY),speed=0;if(Number.isFinite(y)){if(y<edge)speed=-Math.max(5,Math.round((edge-y)/edge*28));else if(y>h-edge)speed=Math.max(5,Math.round((y-(h-edge))/edge*28))}dragScrollSpeed=speed;if(speed&&!dragScrollFrame)dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick);if(!speed&&dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function paintNavIcon(){var b=document.querySelector('#nav>button[data-index321-view="warehouse"],#nav button[data-view="warehouse"]');if(!b)return false;var icon=b.querySelector('.index308-nav-icon');if(icon){icon.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 21h18M4 21V8l7-4v17M11 5l9 3v13"></path><rect x="6" y="11" width="2" height="2"></rect><rect x="6" y="15" width="2" height="2"></rect><rect x="14" y="10" width="2" height="2"></rect><rect x="18" y="10" width="2" height="2"></rect><rect x="14" y="14" width="2" height="2"></rect><rect x="18" y="14" width="2" height="2"></rect></svg>';icon.style.fontSize='';var svg=icon.querySelector('svg');if(svg){svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('stroke-linecap','round');svg.setAttribute('stroke-linejoin','round')}}return true}
function render(doSync){var root=document.getElementById('content');if(!root)return false;root.className='content rc504-warehouse-view';var activeList=all().filter(active),pallets=activeList.reduce(function(n,sh){return n+palletCount(sh)},0),prepared=activeList.filter
```

### line~24423 pos=4910757
```text
er(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;dragKey=p.key;p.card.classList.add('rc513-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();updateWarehouseDragScroll(e);var zone=rc946WarehousePointerZone(e,root);root.querySelectorAll('.rc513-drop-target').forEach(function(x){if(x!==zone)x.classList.remove('rc513-drop-target')});if(zone)zone.classList.add('rc513-drop-target')},{passive:false});root.addEventListener('pointerup',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,key=p.key,zone=active?rc946WarehousePointerZone(e,root):null;if(active)e.preventDefault();rc946WarehouseClearPointer(root);if(active&&key&&zone)moveShipmentKey(key,Number(zone.getAttribute('data-warehouse-drop-code')||0))});root.addEventListener('pointercancel',function(e){var p=dragPointer;if(p&&e.pointerId===p.pointerId)rc946WarehouseClearPointer(root)})}
function stopWarehouseDragScroll(){dragScrollSpeed=0;if(dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function warehouseDragScrollTick(){dragScrollFrame=0;if(!dragKey||!dragScrollSpeed)return;window.scrollBy(0,dragScrollSpeed);dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick)}
function updateWarehouseDragScroll(e){if(!dragKey||!e)return stopWarehouseDragScroll();var h=Math.max(1,window.innerHeight||document.documentElement.clientHeight||1),edge=Math.max(80,Math.min(150,Math.round(h*.16))),y=Number(e.clientY),speed=0;if(Number.isFinite(y)){if(y<edge)speed=-Math.max(5,Math.round((edge-y)/edge*28));else if(y>h-edge)speed=Math.max(5,Math.round((y-(h-edge))/edge*28))}dragScrollSpeed=speed;if(speed&&!dragScrollFrame)dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick);if(!speed&&dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function paintNavIcon(){var b=document.querySelector('#nav>button[data-index321-view="warehouse"],#nav button[data-view="warehouse"]');if(!b)return false;var icon=b.querySelector('.index308-nav-icon');if(icon){icon.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 21h18M4 21V8l7-4v17M11 5l9 3v13"></path><rect x="6" y="11" width="2" height="2"></rect><rect x="6" y="15" width="2" height="2"></rect><rect x="14" y="10" width="2" height="2"></rect><rect x="18" y="10" width="2" height="2"></rect><rect x="14" y="14" width="2" height="2"></rect><rect x="18" y="14" width="2" height="2"></rect></svg>';icon.style.fontSize='';var svg=icon.querySelector('svg');if(svg){svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('stroke-linecap','round');svg.setAttribute('stroke-linejoin','round')}}return true}
function render(doSync){var root=document.getElementById('content');if(!root)return false;root.className='content rc504-warehouse-view';var activeList=all().filter(active),pallets=activeList.reduce(function(n,sh){return n+palletCount(sh)},0),prepared=activeList.filter(function(sh){return sh.warehousePrepared===true}).length,eingang=zoneData('Eingang').list.length;root.innerHTML='<div class="rc504-warehouse"><div class="rc504-warehouse-head"><div><h2>🏭 Lager & Locations</h2><p>Aktuelle Position aller noch nicht abgeholten Sendungen. Ausgangslocations zeigen direkt, welche Sendungen dort stehen. Admins können Sendungen zusätzlich manuell verschieben.</p></div><div class="rc504-warehouse-actions"><button class="ghost" onclick="return rc504WarehouseRefresh()">Aktualis
```

## persist()
needle='function persist('
count=9

### line~19425 pos=3070588
```text
json-legacy{margin-top:14px}.ihk-json-legacy summary{cursor:pointer;font-weight:900;color:#334155}@media(max-width:850px){.ihk-pdf-upload,.ihk-editor-head,.ihk-qedit-grid{grid-template-columns:1fr}}
</style>
<script id="rc626-shipping-inspections-abd-controller" type="text/plain" data-exporthub-lazy="rc626">
(function(){
'use strict';
if(window.__EXPORTHUB_RC627_FUNCTIONS__)return;window.__EXPORTHUB_RC627_FUNCTIONS__=true;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',timer=0,observer=null,busy=false;
var nativeApi=window.ExportHUBClean&&window.ExportHUBClean.native;
var setT=nativeApi&&nativeApi.setTimeout?nativeApi.setTimeout:window.setTimeout.bind(window);
var NativeMO=nativeApi&&nativeApi.MutationObserver?nativeApi.MutationObserver:window.MutationObserver;
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
function num(v){var n=Number(String(v==null?'':v).replace(',','.').replace(/[^0-9.\-]/g,''));return isFinite(n)?n:0}
function money(v){return num(v).toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2})+' €'}
function arr(v){return Array.isArray(v)?v:[]}
function clone(v){try{return structuredClone(v)}catch(_){try{return JSON.parse(JSON.stringify(v))}catch(__){return v}}}
function uid(prefix){return(prefix||'ID')+'-'+Math.random().toString(36).slice(2,8)+'-'+Date.now().toString(36)}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.state||(window.state={}))}catch(_){return window.state||(window.state={})}}
function persist(reason){try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function'){window.ExportHUBClean.queueSave(reason||'RC627 Änderung');return}}catch(_){}try{if(typeof window.save==='function')window.save()}catch(_){}}
function content(){return document.getElementById('content')}
function currentView(){return low(state().view||(document.body?document.body.getAttribute('data-exporthub-view'):'')||'')}
function customerName(c){return q(c&&(c.name||c.customerName||c.company||c.kunde))}
function customerNumber(c){return q(c&&(c.account||c.customerNumber||c.kundennummer||c.customerNo||c.id))}
function customerCountry(c){return q(c&&(c.country||c.land||c.countryName||c.iso))}
function shipmentRecipientCountry(sh,c){sh=sh||{};c=c||{};var nested=[sh.locationData,sh.siteData,sh.location,sh.recipient,sh.destination,sh.deliveryLocation,sh.shipTo];for(var i=0;i<nested.length;i++){var n=nested[i];if(!n||typeof n!=='object'||Array.isArray(n))continue;var v=q(n.country||n.land||n.countryName||n.recipientCountry||n.destinationCountry||n.deliveryCountry||n.shipToCountry||n.countryCode||n.isoCountry||n.iso);if(v)return v}var wanted=q(sh.locationId||sh.selectedLocationId||sh.siteId||sh.destinationId),wantedName=low(sh.locationName||sh.site||sh.standort||sh.destinationName),locations=[].concat(arr(c.locations),arr(c.sites),arr(c.standorte),arr(c.deliveryLocations),arr(c.shipToLocations));for(var j=0;j<locations.length;j++){var loc=locations[j]||{},locId=q(loc.id||loc.locationId||loc.selectedLocationId||loc.siteId||loc.destinationId||loc.code||loc.number),locName=low(loc.name||loc.locationName||loc.siteName);if((wanted&&locId===wanted)||(wantedName&&locName===wantedName)){var lc=q(loc.country||loc.land||loc.countryName||loc.recipientCountry||loc.destinationCoun
```

### line~19931 pos=4073996
```text
ect'?[s.selectedShipment]:[]),map={},order=[];all.forEach(function(sh){var key=low(sref(sh)||sid(sh));if(!key)return;if(!map[key]){map[key]=sh;order.push(key)}else if(shipmentScore(sh)>shipmentScore(map[key]))map[key]=sh});return order.map(function(key){return map[key]}).filter(Boolean)}
function findShipment(id){var api=window.ExportHUBPickupPOD;if(api&&typeof api.find==='function')try{var found=api.find(id);if(found)return found}catch(_){}var key=q(id),up=key.toUpperCase();return shipments().filter(function(sh){return sid(sh)===key||sref(sh)===up}).sort(function(a,b){return shipmentScore(b)-shipmentScore(a)})[0]||null}
function currentUser(){try{var r=window.ExportHUBClean&&window.ExportHUBClean.runtime;return (r&&r.user)||window.currentUser||null}catch(_){return window.currentUser||null}}
function userKey(u){return low(u&&(u.id||u.userId||u.user||u.login||u.username||u.email||u.name))}
function isGlobal(u){var role=low(u&&(u.role||u.rolle)),key=userKey(u);return !!u&&(u.globalAdmin===true||u.isAdmin===true||u.admin===true||key==='tobias'||key==='t.limberg'||key==='tobias limberg'||key==='tobias.limberg'||role==='admin'||/global.?admin|administrator|vollzugriff/.test(role)||arr(u.permissions).indexOf('*')>=0)}
function isCreator(sh,u){var uk=userKey(u);if(!uk)return false;var vals=[sh.createdById,sh.createdByUserId,sh.createdBy,sh.creatorId,sh.creator,sh.ownerId,sh.owner,sh.createdByName].map(low).filter(Boolean);return vals.indexOf(uk)>=0}
function canReset(sh){var u=currentUser();return isGlobal(u)||isCreator(sh,u)}
function statusAtOrAfterPickup(sh){var raw=low(sh&&(sh.status||sh.pickupStatus||sh.processStatus));return !!(sh&&(sh.pickupQrUsed||sh.pickedUpAt||sh.actualPickupDate||/abgeholt|pod vorhanden|abgeschlossen|archiviert/.test(raw)))&&!/storniert/.test(raw)}
function persist(reason){try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function'){window.ExportHUBClean.queueSave(reason);return}}catch(_){}try{if(typeof window.save==='function')window.save(reason)}catch(_){}}
function refreshOverview(){var s=state();if(low(s.view)!=='shipmentoverview'&&!document.querySelector('#content .rc524-shipment-card'))return;setT(patchOverview,40)}
function fileList(sh,key){var list=sh&&sh[key];return Array.isArray(list)?list:[]}
function resolveFile(el){var card=el&&el.closest&&el.closest('[data-shipment]'),id=q((el&&el.dataset&&el.dataset.id)||(card&&card.getAttribute('data-shipment'))),key=q(el&&el.dataset&&el.dataset.key),index=Number(el&&el.dataset&&el.dataset.index),sh=findShipment(id),list=fileList(sh,key);return{card:card,id:id,key:key,index:index,shipment:sh,list:list,file:list[index]}}
function fileUrl(f){if(!f)return'';var url=q(f.data||f.dataUrl||f.url||f.downloadUrl||f.contentUrl||f.blobUrl);if(url)return url;var base=q(f.base64||f.content||f.fileData);if(!base)return'';if(/^data:/i.test(base))return base;var mime=q(f.type||f.mimeType)||'application/pdf';return'data:'+mime+';base64,'+base.replace(/^base64,/i,'')}
function officialPodShipment(el){var card=el&&el.closest&&el.closest('[data-shipment]'),id=q((el&&el.dataset&&el.dataset.id)||(card&&card.getAttribute('data-shipment')));return{shipment:findShipment(id),id:id}}
function officialPodName(sh,out){return q(out&&out.name)||('POD_'+(sref(sh)||sid(sh)||'Sendung')+'_Ladeliste_mit_Unterschrift.pdf')}
function realPodFile(sh){return arr(sh&&sh.podFiles).find(function(f){if(!f||f.placeholder||f.fallback||f.syntheticFallback||/deleted|geloscht|gelöscht|replaced|ersetzt/.test(low(f.status)))return false;var url=fileUrl(f),name=low([f.kind,f.source,f.name,f.fil
```

### line~20581 pos=4127904
```text
String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]})}
function arr(v){return Array.isArray(v)?v:[]}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.state||{})}catch(_){return window.state||{}}}
function currentUser(){try{var u=window.__EXPORTHUB_GET_CURRENT_USER__?window.__EXPORTHUB_GET_CURRENT_USER__():(window.currentUser||{});return u&&typeof u==='object'?u:{}}catch(_){return window.currentUser&&typeof window.currentUser==='object'?window.currentUser:{}}}
function userKey(){var u=currentUser();return low(u.user||u.login||u.username||u.name||'standard')||'standard'}
function userName(){var u=currentUser();return q(u.name||u.user||u.login||'Benutzer')||'Benutzer'}
function root(){return document.getElementById('content')}
function currentView(){return low(state().view||(document.body?document.body.getAttribute('data-exporthub-view'):'')||'')}
function localISO(d){d=d||new Date();var z=function(n){return String(n).padStart(2,'0')};return d.getFullYear()+'-'+z(d.getMonth()+1)+'-'+z(d.getDate())}
function todayISO(){return localISO(new Date())}
function weekday(iso){var d=iso?new Date(iso+'T12:00:00'):new Date();return ['Sonntag','Montag','Dienstag','Mittwoch','Donnerstag','Freitag','Samstag'][d.getDay()]}
function fmtDate(iso,withWeekday){if(!/^\d{4}-\d{2}-\d{2}$/.test(q(iso)))return'';try{return new Date(iso+'T12:00:00').toLocaleDateString('de-DE',withWeekday?{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'}:{day:'2-digit',month:'2-digit',year:'numeric'})}catch(_){return iso}}
function persist(reason){dashboardLocalChangeUntil=Date.now()+3500;try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')window.ExportHUBClean.queueSave(reason||'Dashboard/Aufgaben Planer gespeichert');else if(typeof window.save==='function')window.save()}catch(e){console.error('ExportHUB Dashboard/Planer speichern',e)}}
function accessRightId(id){id=low(id);if(id==='calculator')return'shippingcosts';if(id==='quiz')return'inspections';return id}
function canOpen(id){id=accessRightId(id);var u=currentUser();try{if(typeof window.canView==='function'&&typeof window.canRead==='function')return !!(window.canView(id)&&window.canRead(id)&&(id!=='rights'||typeof window.canManageRights!=='function'||window.canManageRights()))}catch(_){return false}var r=u&&u.rights&&u.rights[id]||{},level=low(r.level||r.access||'');if(/admin/.test(low(u&&u.role))||u&&u.globalAdmin)return true;return ['view','edit','admin'].indexOf(level)>=0||!!(r.read||r.visible||r.edit||r.admin||r.functionAdmin)}
function widgetAllowed(id){
 if(id==='tasks'||id==='shipments')return false;
 var right=WIDGET_RIGHTS[id];return !!right&&canOpen(right)
}
function availableModules(){return MODULES.filter(function(m){return canOpen(m.id)})}
function navigate(id){if(!canOpen(id)){alert('Dieser Bereich ist für deinen Benutzer nicht freigegeben.');return false}if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route(id,'dashboard');return false}
function directShipmentId(s){return q(s&&(s.id||s.shipmentId||s.ref||s.reference||s.shipmentRef))}
function directCustomerId(c){return q(c&&(c.id||c.customerId||c.account||c.customerNumber))}
function directCustomerNumber(c){return q(c&&(c.account||c.customerNumber||c.number))}
function directCustomer
```

### line~21076 pos=4217302
```text
k-integrity */
   var key=logicalTaskKeyRC896(t),ed=effectiveDate(t),score=0,order=Number(t&&t.masterOrder);
   if(sourceWeekMatches(t,ws))score+=8;
   if(q(t&&t.sourceWeekStart)===ws)score+=4;
   if(ed&&ed>=ws&&ed<=end)score+=2;
   if(q(t&&(t.weekKey||t.createdForWeek))===weekKey(ws))score+=1;
   if(!Number.isFinite(order))order=999999;
   var prev=map.get(key);
   if(!prev||score>prev.score||(score===prev.score&&order<prev.order)||(score===prev.score&&order===prev.order&&index<prev.index))map.set(key,{task:t,score:score,order:order,index:index})
 });
 return Array.from(map.values()).sort(function(a,b){return a.order-b.order||a.index-b.index}).map(function(x){return x.task})
}
function routeKey(t){var s=low(t&&(t.title||t.name||''));s=s.replace(/^abd\s+(fur|fuer)\s+/,'').replace(/^abd\s+/,'').replace(/\([^)]*(14.?tag|geplant)[^)]*\)/g,'').replace(/\b(nur|falls)\b.*$/,'').replace(/\s+/g,' ').trim();return s}
function setDateFields(t,date,kind,reason){if(!t||!date)return false;var old=effectiveDate(t)||originalDate(t);t.plannedDate=date;t.dueDate=date;t.date=date;t.targetDate=date;t.day=DAYS[(parseDate(date).getDay()||7)-1]||t.day;t.lastPlannedAt=new Date().toISOString();t.lastPlannedBy=q((st().currentUser&&st().currentUser.name)||st().currentUserName||'');if(kind==='manual'){t.manualMoved=true;t.holidayAutoMoved=false;t.plannerMoveReason=reason||'Manuell verschoben'}else if(kind==='holiday'){t.holidayAutoMoved=true;t.manualMoved=false;t.plannerMoveReason=reason||'Feiertag'}return old!==date}
function resetAutoMove(t){if(!t||!t.holidayAutoMoved||t.manualMoved)return false;var base=originalDate(t);if(!base)return false;t.plannedDate=base;t.dueDate=base;t.date=base;t.targetDate=base;t.day=q(t.originalDay||t.day);t.holidayAutoMoved=false;t.plannerMoveReason='';return true}
function persist(reason){try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')window.ExportHUBClean.queueSave(reason);else if(typeof window.save==='function')window.save()}catch(e){console.error('RC848 Planner speichern',e)}try{window.dispatchEvent(new CustomEvent('exporthub:tasks-updated',{detail:{version:VERSION,reason:reason}}))}catch(_){}return true}

/* exporthub-rc907-planner-optimistic-move */
function plannerUpdateDayCountRC907(section){
 if(!section)return false;var list=section.querySelector('.i218-list'),head=section.querySelector('.i218-day-head h4');if(!list||!head)return false;
 var count=list.querySelectorAll(':scope > .i218-task').length,day=q(section.getAttribute('data-i218-day-label')||head.textContent.split('·')[0]);
 head.textContent=day+' · '+count;
 if(count===0&&!list.querySelector('.i218-empty'))list.innerHTML='<p class="i218-empty">Keine offene Aufgabe</p>';
 return true
}
function plannerMoveDomRC907(id,date,targetSection){
 var card=document.querySelector('[data-i218-drag="'+CSS.escape(q(id))+'"]');if(!card)return false;
 var source=card.closest('[data-i218-drop-date]'),group=source&&source.closest('[data-i218-group]'),selector='[data-i218-drop-date="'+CSS.escape(q(date))+'"]',target=null;
 if(targetSection&&targetSection.matches&&targetSection.matches(selector))target=targetSection;
 else if(group)target=group.querySelector(selector);
 else target=document.querySelector(selector);
 if(!target)return false;
 var list=target.querySelector('.i218-list');if(!list)return false;
 var empty=list.querySelector('.i218-empty');if(empty)empty.remove();
 list.appendChild(card);card.classList.add('is-moved');
 var input=card.querySelector('[data-i218-date]');if(input)input.value=date;
 plannerUpdateDayCountRC907(source);planne
```

### line~21324 pos=4263504
```text
&c||'')}
function shipmentPickup(sh){return iso(sh&&(sh.plannedPickupDate||sh.pickupDate||sh.dueDate||sh.targetDate))}
function shipments(){try{if(window.ExportHUBIndex235&&typeof window.ExportHUBIndex235.shipments==='function')return window.ExportHUBIndex235.shipments()}catch(_){ }return arr(main().shipments)}
function overdueShipments(){var today=localISO();return shipments().filter(function(sh){if(!sh||isDone(sh)||sh.actualPickupDate||sh.pickedUpAt)return false;var d=shipmentPickup(sh);return !!d&&dayDiff(d,today)>0}).sort(function(a,b){return shipmentPickup(a).localeCompare(shipmentPickup(b))})}
var notificationLocalCache=null,notificationLocalKey='';
function notificationStoreKey(){return 'exporthub_notifications_local_v2:'+userKey()}
function loadLocalNotificationCenter(){var key=notificationStoreKey();if(notificationLocalCache&&notificationLocalKey===key)return notificationLocalCache;var data=null;try{data=JSON.parse(localStorage.getItem(key)||'null')}catch(_){data=null}if(!data||typeof data!=='object'){var legacy=main().notificationCenter&&typeof main().notificationCenter==='object'?main().notificationCenter:{},k=userKey();data={seen:{},history:arr(legacy.history).filter(function(x){return !x||!q(x.user)||low(x.user)===low(userName())}).slice(-80)};if(legacy.seen&&legacy.seen[k])data.seen[k]=clone(legacy.seen[k])}data.seen=data.seen&&typeof data.seen==='object'?data.seen:{};data.history=arr(data.history).slice(-80);notificationLocalKey=key;notificationLocalCache=data;return data}
function centerState(){return loadLocalNotificationCenter()}
function seenForToday(){var c=centerState(),k=userKey(),d=localISO();c.seen[k]=c.seen[k]&&typeof c.seen[k]==='object'?c.seen[k]:{};c.seen[k][d]=c.seen[k][d]&&typeof c.seen[k][d]==='object'?c.seen[k][d]:{};return c.seen[k][d]}
function persist(reason){var c=centerState();try{localStorage.setItem(notificationStoreKey(),JSON.stringify({seen:c.seen||{},history:arr(c.history).slice(-80),updatedAt:new Date().toISOString()}));return true}catch(e){console.warn('Lokaler Benachrichtigungsstatus konnte nicht gespeichert werden',e);return false}}
function addHistory(slot,label,counts){var c=centerState();c.history.push({at:new Date().toISOString(),slot:slot||'',label:label||'',tasks:counts.tasks||0,overdueTasks:counts.overdueTasks||0,shipments:counts.shipments||0,user:userName()});c.history=c.history.slice(-80)}
function counts(){return{tasks:openTasks().length,overdueTasks:overdueTasks().length}}
function slotsDue(now){now=now||new Date();if(!isWorkday(now))return[];var mins=now.getHours()*60+now.getMinutes(),seen=seenForToday();return SLOTS.filter(function(s){return mins>=s.minutes&&!seen[s.id]})}
function markDueSeen(due){var seen=seenForToday();due.forEach(function(s){seen[s.id]=new Date().toISOString()});persist('Benachrichtigungszeit gespeichert')}
function permissionText(){if(!('Notification' in window))return'Browser unterstützt keine Systembenachrichtigungen';if(Notification.permission==='granted')return'Aktiv';if(Notification.permission==='denied')return'Im Browser blockiert';return'Noch nicht aktiviert'}
function nextCheck(now){now=now||new Date();for(var offset=0;offset<15;offset++){var d=new Date(now);d.setDate(d.getDate()+offset);d.setHours(0,0,0,0);if(!isWorkday(d))continue;for(var i=0;i<SLOTS.length;i++){var x=new Date(d);x.setHours(Math.floor(SLOTS[i].minutes/60),SLOTS[i].minutes%60,0,0);if(x>now)return x}}return null}
function scheduleNext(){if(timer)clearTimeout(timer);var next=nextCheck(new Date()),delay=next?Math.max(1000,next-Date.now()+500):3600000;timer=setTimeout(function(){chec
```

### line~21893 pos=4423647
```text
o.join('; ');if(f.cc)f.cc.value=rec.cc.join('; ');if(f.subject)f.subject.value=subject;return{sh:sh,c:c,target:target,to:rec.to,cc:rec.cc,subject:subject,body:mailBody,fields:f}}
function mailtoUrl(data,includeBody){var to=data.to.map(encodeURIComponent).join(';'),params=[];params.push('subject='+encodeURIComponent(data.subject||''));if(data.cc.length)params.push('cc='+data.cc.map(encodeURIComponent).join(';'));if(includeBody!==false)params.push('body='+encodeURIComponent(data.body||''));return'mailto:'+to+'?'+params.join('&')}
function mailAnchor(url){var a=document.createElement('a');a.href=url;a.style.display='none';a.setAttribute('aria-hidden','true');document.body.appendChild(a);a.click();setTimeout(function(){try{a.remove()}catch(_){ }},250);return true}
function copyMailBody(body){try{var ta=document.createElement('textarea');ta.value=String(body||'');ta.setAttribute('readonly','');ta.style.position='fixed';ta.style.left='-9999px';ta.style.top='0';document.body.appendChild(ta);ta.select();ta.setSelectionRange(0,ta.value.length);var ok=document.execCommand&&document.execCommand('copy');ta.remove();return !!ok}catch(_){return false}}
function launchMail(data){var full=mailtoUrl(data,true);if(full.length<=1800)return mailAnchor(full);var copied=copyMailBody(data.body),shortUrl=mailtoUrl(data,false);mailAnchor(shortUrl);setTimeout(function(){alert(copied?'Outlook wurde mit Empfänger, CC und Betreff geöffnet. Der vollständige Mailtext war für einen direkten Browser-Aufruf zu lang und wurde deshalb in die Zwischenablage kopiert. Bitte fügen Sie ihn in Outlook mit Strg+V ein.':'Outlook wurde mit Empfänger, CC und Betreff geöffnet. Der Mailtext war für einen direkten Browser-Aufruf zu lang. Bitte kopieren Sie den Text aus ExportHUB in die Nachricht.')},180);return true}
function persist(reason){clearTimeout(saveTimer);saveTimer=setTimeout(function(){try{if(window.__EXPORTHUB_SHIPMENT_SAVE_TX__===true)return;if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')window.ExportHUBClean.queueSave(reason||'Mail und Status gespeichert')}catch(_){}},60)}
function setOpenedOnCopies(sh,target,record){shipmentCopies(sh).forEach(function(x){x.mailOpened=obj(x.mailOpened)?x.mailOpened:{};x.mailOpened[target]=Object.assign({},record);x.mailOpenedAt=record.openedAt})}
function refreshMailConfirmButtons(){
 var sh=currentShipment(),target=activeTarget(sh),opened=!!(sh&&shipmentCopies(sh).some(function(x){return !!(x.mailOpened&&x.mailOpened[target])}));
 document.querySelectorAll('#rc543MailArea button').forEach(function(b){
  if(!(/Mail als versendet bestätigen/i.test(q(b.textContent))||/rc543ConfirmMail|rc542ConfirmMail/i.test(q(b.getAttribute('onclick')))))return;
  var reason=low(b.getAttribute('data-lock-reason')),hard=/abd|empfanger|empfänger|mailvorlage/.test(reason),disabled=!opened||hard;
  b.disabled=disabled;if(disabled){b.setAttribute('disabled','');b.setAttribute('aria-disabled','true')}else{b.removeAttribute('disabled');b.removeAttribute('data-lock-reason');b.setAttribute('aria-disabled','false')}
 })
}
function openMail(){var d=prepareMailFields();if(!d)return false;if(!d.to.length){alert('Für den Kunden der geöffneten Sendung ist keine gültige Empfängeradresse hinterlegt. Bitte die Kundenmail im Kundenordner prüfen.');return false}var stamp=new Date().toISOString(),record={openedAt:stamp,to:d.to.join('; '),cc:d.cc.join('; '),subject:d.subject};setOpenedOnCopies(d.sh,d.target,record);refreshMailConfirmButtons();persist('Mail geöffnet – Empfänger, CC und Betreff getrennt');try{if(window.ExportHUBClean&&typeof wind
```

### line~22182 pos=4476999
```text
 Bearbeitung';if(/erstellt|created/.test(s))return'Erstellt';return'Entwurf'}
function effectiveStatus(sh){if(!sh)return'Entwurf';try{var api=window.ExportHUBRC543;if(api&&typeof api.computeShipmentStatus==='function'){var value=q(api.computeShipmentStatus(sh));if(value)return label(value)}}catch(e){console.error('RC645 kanonischer Status',e)}return label(sh.status||sh.processStatus)}
function statusFromCopies(sh){return effectiveStatus(sh)}
function classFor(status){return /storniert|nachbearbeitung/i.test(status)?'is-red':/entwurf|erstellt|in bearbeitung|wartet/i.test(status)?'is-orange':'is-green'}
function patchVisible(status){var fixed=document.getElementById('rc363BlockShipment'),shell=document.getElementById('rc573ShipmentShell')||fixed;if(!shell)return false;var box=document.getElementById('rc573ShipmentStatus');if(!box){var head=fixed&&fixed.querySelector('.rc363-process-head')||shell.querySelector('.rc573-page-head,.page-head,.rc363-process-head');if(!head)return false;box=document.createElement('div');box.id='rc573ShipmentStatus';box.setAttribute('aria-live','polite');box.setAttribute('role','status');head.appendChild(box)}var caption=box.querySelector('.rc573-status-label');if(!caption){caption=document.createElement('span');caption.className='rc573-status-label';caption.textContent='Aktueller Sendungsstatus';box.appendChild(caption)}var pill=box.querySelector('.rc573-status-pill');if(!pill){pill=document.createElement('span');pill.className='rc573-status-pill';box.appendChild(pill)}var changed=q(pill.textContent)!==status;if(changed)pill.textContent=status;pill.setAttribute('data-index297-status',status);pill.classList.remove('is-green','is-orange','is-red');pill.classList.add(classFor(status));box.setAttribute('data-current-status',status);return changed}
function persist(sh,status){copies(sh).forEach(function(x){x.status=status;x.processStatus=status})}
function refresh(reason){if(inRefresh)return lastVisible;var visibleShell=document.getElementById('rc573ShipmentShell')||document.getElementById('rc363FixedShipmentLayout'),activeState=states()[0]||{},activeView=low(activeState.view||(document.body?document.body.getAttribute('data-exporthub-view'):'')||'');if(!visibleShell&&activeView!=='shipment')return false;inRefresh=true;refreshRuns++;try{var sh=currentShipment();if(!sh)return false;var status=statusFromCopies(sh);var changed=patchVisible(status);if(status!==lastVisible||changed){lastVisible=status;statusChanges++;try{window.dispatchEvent(new CustomEvent('exporthub:shipment-status-visible',{detail:{shipmentId:sid(sh),reference:sref(sh),status:status,reason:q(reason)}}))}catch(_){}}return status}finally{inRefresh=false}}
function schedule(reason,delays){clearTimeout(timer);clearTimeout(lateTimer);var d=delays||[0,450];timer=setTimeout(function(){refresh(reason)},Math.max(0,d[0]||0));if(d.length>1)lateTimer=setTimeout(function(){refresh(reason)},Math.max(0,d[d.length-1]||0))}
function relevant(target){var b=target&&target.closest&&target.closest('button,a,label'),input=target&&target.closest&&target.closest('input[type="file"]'),t=low(q(b&&b.textContent)+' '+q(b&&b.id)+' '+q(b&&b.getAttribute&&b.getAttribute('onclick'))+' '+q(input&&input.getAttribute('onchange')));return !!input||/speichern|bestatig|versendet|abd|abhol|pickup|pod|druck|upload|abschlie|erledigt|storn|archiv/.test(t)}
document.addEventListener('click',function(e){if(relevant(e.target))schedule('Prozessschritt ausgeführt',[0,700])},true);document.addEventListener('change',function(e){if(relevant(e.target)||/status|erledigt|complete|confirmed/i.test(q(e.t
```

### line~24332 pos=4891134
```text
Shipment','selectedShipment'].forEach(function(n){var x=st[n];if(obj(x)&&skey(x)===key&&out.indexOf(x)<0)out.push(x)})});if(sh&&out.indexOf(sh)<0)out.push(sh);return out}
function token(sh){var api=window.ExportHUBPickupPOD,t='';try{if(api&&typeof api.ensure==='function')t=q(api.ensure(sh))}catch(_){}return t||q(sh&&(sh.pickupToken||sh.pickupQrToken||sh.qrToken))}
function customer(sh){if(!sh)return'';var direct=[sh.customerName,sh.customerDisplay,sh.customerSearch,obj(sh.customer)&&sh.customer.name,obj(sh.customerData)&&sh.customerData.name,obj(sh.selectedCustomer)&&sh.selectedCustomer.name].map(safeText).find(Boolean);if(direct)return direct;var wanted=safeText(sh.customerId||sh.customerAccount||sh.customerNumber||sh.kundennummer),found=null;states().some(function(st){found=arr(st.customers).find(function(c){var ids=[c&&c.id,c&&c.customerId,c&&c.account,c&&c.customerNumber,c&&c.kundennummer].map(safeText);return wanted&&ids.indexOf(wanted)>=0});return !!found});return safeText(found&&(found.name||found.customerName||found.company))}
function locationName(sh){if(!sh||picked(sh))return'';var code=Math.round(Number(sh.warehouseLocationCode||0)),found=LOCS.find(function(x){return x[2]===code});if(found)return found[0];var x=q(sh.warehouseLocation);if(/^regal(?:\s*6)?$/i.test(x))return'Regal';if(/^vor dem regal$/i.test(x))return'Vor dem Regal';if(/^daf$/i.test(x))return'DAF';return x||'Eingang'}
function defaultLocation(sh){if(!active(sh)||q(sh.warehouseLocation))return false;var changed=false;copies(sh).forEach(function(x){if(q(x.warehouseLocation)!=='Eingang'){x.warehouseLocation='Eingang';changed=true}if(Number(x.warehouseLocationCode||0)!==0){x.warehouseLocationCode=0;changed=true}if(x.warehousePrepared===true){x.warehousePrepared=false;changed=true}});return changed}
function persist(reason){try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')window.ExportHUBClean.queueSave(reason||'Lagerstatus aktualisiert')}catch(_){} }
function ready(sh){return sh&&((sh.readyForPickup===true)||/bereit zur abholung|vorbereitet/i.test(q(sh.status||sh.processStatus||sh.readinessStatus)))}
function currentUser(){try{return (typeof window.__EXPORTHUB_GET_CURRENT_USER__==='function'&&window.__EXPORTHUB_GET_CURRENT_USER__())||runtime().user||{}}catch(_){return runtime().user||{}}}
function isAdmin(){var u=currentUser(),role=low(u&&(u.role||u.rolle||u.level||u.type)),name=low(u&&(u.user||u.username||u.login||u.name||u.displayName));try{if(typeof window.canAdmin==='function'&&window.canAdmin('warehouse'))return true}catch(_){}try{if(typeof window.rc542RightLevel==='function'&&window.rc542RightLevel('warehouse',u)==='admin')return true}catch(_){}return !!(u&&(u.globalAdmin||u.isAdmin||u.admin||name==='tobias'||/admin|vollzugriff|administrator/.test(role)||(u.permissions||[]).indexOf('*')>=0))}
function fmt(ts){if(!q(ts))return '—';try{return new Date(ts).toLocaleString('de-DE',{day:'2-digit',month:'2-digit',year:'2-digit',hour:'2-digit',minute:'2-digit'})}catch(_){return q(ts)}}
function localDayKey(){var d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return y+'-'+m+'-'+day}
function plannedDayKey(sh){var v=q(sh&&(sh.plannedPickupDate||sh.pickupDate||sh.pickupPlannedDate||sh.plannedDate||sh.shippingDate));if(!v)return'';var m=v.match(/^(\d{4})-(\d{2})-(\d{2})/);if(m)return m[1]+'-'+m[2]+'-'+m[3];m=v.match(/^(\d{1,2})[.\/-](\d{1,2})[.\/-](\d{4})/);return m?m[3]+'-'+String(m[2]).padStart(2,'0')+'-'+String(m[1]).padStart(2,'0'):''}
function shipmentDateClass(sh){var
```

## save functions
needle='function save'
count=43

### line~23291 pos=4670330
```text
rt=round(total/count);for(var j=0;j<count;j++)stored.push(j===count-1?round(total-part*(count-1)):part)}var m=ensureWeightModal(),list=m.querySelector('#rc363WeightList');list.innerHTML='';for(var i=0;i<count;i++){var value=stored[i]||'',item=document.createElement('label');item.className='rc363-weight-entry';item.innerHTML='<b>'+(i+1)+'.</b><input type="number" min="0.01" step="0.01" inputmode="decimal" value="'+esc(value)+'"><span>kg</span>';list.appendChild(item)}m.querySelector('#rc363WeightCount').textContent=String(count);var target=m.querySelector('#rc363WeightTarget');if(target)target.value=total>0?String(round(total)):'';m.classList.add('open');document.body.style.overflow='hidden';updateModalTotals();setTimeout(function(){var first=list.querySelector('input');if(first){first.focus();first.select()}},20);return false}
function closeWeight(){var m=ensureWeightModal();m.classList.remove('open');document.body.style.overflow='';weightIndex=-1}function clearWeight(){modalInputs().forEach(function(i){i.value=''});var target=ensureWeightModal().querySelector('#rc363WeightTarget');if(target)target.value='';updateModalTotals()}function distributeWeight(){var inputs=modalInputs(),total=targetWeight();if(!(total>0)){var current=round(modalValues().reduce(function(a,b){return a+b},0));if(current>0)total=current}if(!(total>0)){alert('Bitte zuerst das Gesamtgewicht der Zeile eingeben.');var target=ensureWeightModal().querySelector('#rc363WeightTarget');if(target)target.focus();return false}if(!inputs.length)return false;var part=Math.floor((total/inputs.length)*100)/100,assigned=round(part*inputs.length),rest=round(total-assigned);inputs.forEach(function(input,i){var v=i===inputs.length-1?round(part+rest):part;input.value=v>0?String(v):''});updateModalTotals();return false}
function saveWeight(){var locked=lockedShipment();if(locked){lockShipmentForm(locked,true);return false}if(weightIndex<0)return false;localRowsDirty=true;var node=rowNodes()[weightIndex],row=shipment().rows[weightIndex]||{},count=Math.max(1,Math.round(num(row.count||row.qty||row.anzahl||field(node,'count')&&field(node,'count').value||1))),values=modalValues();if(values.length!==count){alert('Bitte alle '+count+' Einzelgewichte eingeben.');return false}var total=round(values.reduce(function(a,b){return a+b},0)),rows=arr(shipment().rows).slice();rows[weightIndex]=Object.assign({},rows[weightIndex]||{}, {weight:total,gewicht:total,weightInput:String(total),totalWeight:total,weightTotal:total,individualWeights:values.slice(),palletWeights:values.slice(),colliWeights:values.slice()});mirrorRows(rows);var input=field(node,'weight');if(input)input.value=String(total);updateSummary();queue('Einzelgewichte gespeichert');closeWeight();return false}
function clearChoosePrompts(){var r=root();if(!r)return 0;var changed=0;r.querySelectorAll('input,select,textarea').forEach(function(el){if(el.hasAttribute('placeholder')&&/bitte\s+(?:aus)?wählen/i.test(el.getAttribute('placeholder')||'')){el.setAttribute('placeholder','');changed++}if(el.tagName==='SELECT'){Array.from(el.options).forEach(function(o){if(q(o.value)===''&&/(?:bitte\s+(?:aus)?wählen|kunde wählen|standort wählen|verpackung wählen)/i.test(q(o.textContent))){o.textContent='—';changed++}})}});return changed}
function removeHints(){var r=root(),count=0;if(!r||!isShipmentView())return 0;var exact=/^(?:Anzahl\s*=\s*reale physische Anzahl|Referenzregel:|Dokumente:\s*Ladeliste wird als A4-Seite erzeugt|Ladeliste wird als A4-Seite erzeugt|Geplantes Datum steuert die Aufgabe [„\"]Abholtag[“\"]\. Tatsächliches Datum erledigt die Aufg
```

### line~23298 pos=4673633
```text
 r=root(),count=0;if(!r||!isShipmentView())return 0;var exact=/^(?:Anzahl\s*=\s*reale physische Anzahl|Referenzregel:|Dokumente:\s*Ladeliste wird als A4-Seite erzeugt|Ladeliste wird als A4-Seite erzeugt|Geplantes Datum steuert die Aufgabe [„\"]Abholtag[“\"]\. Tatsächliches Datum erledigt die Aufgabe\.?)/i;Array.from(r.querySelectorAll('.okbox,.badbox,.notice,.infobox,.rc344-note,.small,.hint,.help,[data-hint],div,p')).forEach(function(el){if(el.closest('#rc363ShipmentActions,#rc363WeightModal'))return;var text=q(el.textContent);if(!text||text.length>600||!exact.test(text))return;var target=el.closest('.okbox,.badbox,.notice,.infobox,.rc344-note,.hint,.help,[data-hint]')||el;if(target&&target!==r){target.remove();count++}});return count}
function processBlock(id,number,title,twoColumns){var el=document.getElementById(id);if(!el){el=document.createElement('section');el.id=id;el.className='rc363-process-block';el.innerHTML='<div class="rc363-process-head"><span class="rc363-process-number"></span><h2></h2></div><div class="rc363-process-body"></div>'}var head=el.querySelector('.rc363-process-head'),numEl=head&&head.querySelector('.rc363-process-number'),titleEl=head&&head.querySelector('h2'),body=el.querySelector('.rc363-process-body');if(numEl)numEl.textContent=String(number);if(titleEl)titleEl.textContent=title;if(body)body.classList.toggle('rc363-two-columns',!!twoColumns);return el}

function workflowButton(pattern){var r=root();return r&&Array.from(r.querySelectorAll('button')).find(function(b){return pattern.test(low(b.textContent))})||null}
function localActionHost(body,id){var h=document.getElementById(id);if(!h){h=document.createElement('div');h.id=id;h.className='rc380-local-action';body.appendChild(h)}else if(h.parentElement!==body)body.appendChild(h);return h}
function savedFlag(key,value,aliases){var sh=shipment(),saved=currentSaved();sh[key]=!!value;arr(aliases).forEach(function(k){sh[k]=!!value});if(saved){saved[key]=!!value;arr(aliases).forEach(function(k){saved[k]=!!value})}queue(key==='addressConfirmed'?'Adressprüfung geändert':'Zolldokumentprüfung geändert')}
function confirmationButton(host,id,label,key,aliases){var b=document.getElementById(id);if(!b){b=document.createElement('button');b.type='button';b.id=id;b.className='ghost rc386-confirm-button';b.setAttribute('data-rc386-confirm',key);b.textContent=label;host.appendChild(b)}else if(b.parentElement!==host)host.appendChild(b);if(key==='addressConfirmed'){var input=document.getElementById('rc386AddressState');if(!input){input=document.createElement('input');input.type='checkbox';input.id='rc386AddressState';input.hidden=true;input.setAttribute('aria-hidden','true');host.appendChild(input)}input.checked=!!shipment().addressConfirmed}var on=!!shipment()[key];if(!on&&arr(aliases).some(function(k){return !!shipment()[k]}))on=true;b.setAttribute('aria-pressed',on?'true':'false');b.title=on?'Bestätigung ist gespeichert – zum Zurücksetzen klicken':'Zum Bestätigen klicken';return b}
function customerForConfirmation(){var sh=shipment(),s=state(),id=q(sh.customerId||sh.customerAccount||sh.customerNumber),name=low(sh.customerName),list=arr(s.customers),found=list.find(function(c){return(id&&[c.id,c.customerId,c.account,c.customerNumber].some(function(v){return q(v)===id}))||(name&&low(c.name||c.customerName)===name)})||null;if(found)return found;var embedded=[sh.customer,sh.customerData,sh.selectedCustomer].find(function(c){return c&&typeof c==='object'&&(q(c.id||c.customerId||c.account||c.customerNumber)||q(c.name||c.customerName))});if(embedded)return embedded;if(id||name)r
```

### line~23599 pos=4752171
```text
='object'&&saved.siteData)||(saved.location&&typeof saved.location==='object'&&saved.location)||{},customer=resolveShipmentCustomer(saved,state())||{},address=q(sh.recipientAddress||sh.deliveryAddress||sh.destinationAddress||saved.recipientAddress||saved.deliveryAddress||saved.destinationAddress||nested.address||[nested.street,nested.zip||nested.postalCode,nested.city,nested.country].filter(Boolean).join(' ')||customer.address||customer.recipientAddress||customer.deliveryAddress||[customer.street,customer.zip||customer.postalCode,customer.city,customer.country].filter(Boolean).join(' ')),location=q(sh.locationId||sh.selectedLocationId||sh.siteId||sh.destinationId||saved.locationId||saved.selectedLocationId||saved.siteId||saved.destinationId||nested.id||nested.locationId||nested.siteId),country=q(sh.recipientCountry||sh.destinationCountry||saved.recipientCountry||saved.destinationCountry||nested.country||nested.land||customer.country||customer.land);if(address){sh.recipientAddress=address;sh.deliveryAddress=address;sh.destinationAddress=address}if(location){sh.locationId=location;sh.selectedLocationId=location;sh.siteId=location;sh.destinationId=location}if(country){sh.recipientCountry=country;sh.destinationCountry=country}if(saved.addressConfirmed||saved.addressChecked||q(address).length>=8){sh.addressConfirmed=!!(saved.addressConfirmed||saved.addressChecked||sh.addressConfirmed);sh.addressChecked=!!(saved.addressChecked||saved.addressConfirmed||sh.addressChecked)}if((!sh.customer||typeof sh.customer!=='object'||!Object.keys(sh.customer).length)&&saved.customer&&typeof saved.customer==='object'){sh.customer=clone(saved.customer);sh.customerData=clone(saved.customerData||saved.customer);sh.selectedCustomer=clone(saved.selectedCustomer||saved.customer)}return true}
async function saveAction(){window.__EXPORTHUB_SHIPMENT_SAVE_TX__=true;var beforeSaveRef=refOf(shipment()),button=document.getElementById('rc363SaveShipment'),oldText=button&&button.textContent,opToken=operationStart('Speichern wird vorbereitet …'),saveConfirmed=false;if(button){button.disabled=true;button.textContent='Speichern wird vorbereitet …'}cancelPendingEditSave();var cleanBefore=window.ExportHUBClean;if(cleanBefore&&cleanBefore.runtime&&cleanBefore.runtime.saveTimer){try{(cleanBefore.native&&cleanBefore.native.clearTimeout||window.clearTimeout)(cleanBefore.runtime.saveTimer)}catch(_){}cleanBefore.runtime.saveTimer=null}try{operationStep(opToken,'Sendungssperre wird geprüft …');if(button)button.textContent='Sendungssperre wird geprüft …';var locked=await refreshShipmentLock(true);if(locked){lockShipmentForm(locked,true);operationFail(opToken,'Sendung ist bereits gesperrt');return false}window.__EXPORTHUB_SHIPMENT_WRITE_GRANT_UNTIL__=Date.now()+60000;operationStep(opToken,'Sendungsdaten werden vorbereitet …');if(button)button.textContent='Sendungsdaten werden vorbereitet …';prepareCanonicalShipmentFields();restoreLocalFieldDraft();syncVisibleShipmentFields();syncRows();reconcileLatestSavedBeforeSave();locked=lockedShipment();if(locked){lockShipmentForm(locked,true);operationFail(opToken,'Sendung wurde während der Vorbereitung gesperrt');return false}restorePersistedCustomerForSave();var creatorStamp=currentCreator(),savingShipment=shipment();if(repairActive(savingShipment)){markRepairAudit(savingShipment);applyRepairEvidence(savingShipment)}if(isFreshDraft()&&!q(savingShipment.createdBy||savingShipment.createdByName)){savingShipment.createdBy=creatorStamp.name;savingShipment.createdByName=creatorStamp.name;savingShipment.createdById=creatorStamp.id}try{if(window.Export
```

### line~23679 pos=4771728
```text
tHUBClean.runtime;add(rt&&rt.state)}catch(_){ }return roots}
function findSavedExact(value){var wanted=q(value),upper=wanted.toUpperCase(),matches=[];if(!wanted)return null;stateRoots().forEach(function(rootState){arr(rootState.shipments).concat(arr(rootState.savedShipments),[rootState.shipment,rootState.currentShipment,rootState.selectedShipment]).forEach(function(x){if(!x||typeof x!=='object'||!((idOf(x)===wanted)||(refOf(x)===upper)))return;if(matches.indexOf(x)<0)matches.push(x)})});function score(x){if(!x||typeof x!=='object')return 0;var rowLists=[x.rows,x.colli,x.collis,x.packages,x.packagingRows,x.shipmentRows,x.colliRows,x.items,x.lines].filter(function(list){return arr(list).length}),rows=rowLists.sort(function(a,b){return rowDataScore(b)-rowDataScore(a)})[0]||[],address=q(x.recipientAddress||x.deliveryAddress||x.destinationAddress||(x.locationData&&x.locationData.address)||(x.siteData&&x.siteData.address)||(x.location&&x.location.address)),customer=q(x.customerId||x.customerAccount||x.customerNumber||x.customerName||(x.customer&&x.customer.name)),location=q(x.locationId||x.selectedLocationId||x.siteId),stamp=Date.parse(x.updatedAt||x._syncUpdatedAt||x.createdAt||x.created||0)||0,meaningful=Object.keys(x).reduce(function(total,key){var v=x[key];if(Array.isArray(v))return total+(v.length?6+Math.min(v.length,20):0);if(v&&typeof v==='object')return total+(Object.keys(v).length?4:0);return total+(q(v)!==''?2:0)},0);return meaningful+rowDataScore(rows)*20+arr(x.deliveryFiles).length*4+arr(x.podFiles).length*4+(address?300:0)+(customer?120:0)+(location?80:0)+(q(x.pickupDate||x.plannedPickupDate)?30:0)+(q(x.goodsDescription||x.description)?20:0)+Math.min(99,Math.floor(stamp/100000000000))}matches.sort(function(a,b){return score(b)-score(a)});return matches[0]||null}
function savedFromControl(control){if(!control)return null;var raw=q(control.getAttribute('data-rc410-open-shipment')||control.getAttribute('data-id')||control.getAttribute('data-shipment-id')||control.dataset&&control.dataset.id),onclick=q(control.getAttribute('onclick')),match=onclick.match(/loadShipment\(\s*['"]([^'"]+)['"]\s*\)/i);if(!raw&&match)raw=q(match[1]);if(raw){var direct=findSavedExact(raw);if(direct)return direct}var row=control.closest('tr,[data-shipment],.card,article,section'),refNode=row&&row.querySelector('td b,[data-ref],strong,b'),ref=q(refNode&&(refNode.getAttribute('data-ref')||refNode.textContent));return findSavedExact(ref)}
function bindEditingShipment(saved){
 if(!saved)return null;
 var rootState=state(),identity=customerIdentity(saved,rootState),location=shipmentLocationIdentity(saved,rootState),rowSources=[saved.rows,saved.colli,saved.collis,saved.packages,saved.packagingRows,saved.shipmentRows,saved.colliRows,saved.items,saved.lines].filter(function(list){return arr(list).length});
 if(identity.id||identity.account||identity.name)applyCustomerIdentity(saved,identity);
 applyShipmentLocationIdentity(saved,location);
 var bestRows=rowSources.sort(function(a,b){return rowDataScore(b)-rowDataScore(a)})[0]||[],canonicalRows=arr(bestRows).map(function(source){var row=normalizeRow(source||{});if(!q(row.type))row.type=q(source&&(source.packaging||source.verpackung||source.packagingType||source.packart));if(!(num(row.count)>0))row.count=num(source&&(source.count||source.qty||source.quantity||source.anzahl||source.colliCount));if(!(num(row.weight)>0))row.weight=num(source&&(source.weight||source.gewicht||source.weightKg||source.totalWeight||source.totalWeightKg||source.grossWeight||source.bruttoGewicht||source.gesamtgewicht));if(!(num(row.ldm)>0))ro
```

### line~23698 pos=4783165
```text
isabled=false;fs.removeAttribute('disabled');changed++});layout.querySelectorAll('input,select,textarea,button').forEach(function(el){if(el.type==='hidden'||el.type==='file')return;var key=shipmentFieldKey(el),inRow=!!el.closest('.rc363-owned-row'),customer=!!el.closest('#rc363BlockCustomer'),protectedField=(key==='ref')||el.hasAttribute('data-rc363-weight')||(/^rc543Mail(?:To|Cc|Subject)$/.test(el.id||''))||el.closest('#rc363BlockMail')&&el.readOnly;if((key||inRow||customer)&&!protectedField){if(el.disabled){el.disabled=false;el.removeAttribute('disabled');changed++}if(el.readOnly){el.readOnly=false;el.removeAttribute('readonly');el.removeAttribute('aria-readonly');changed++}}});var save=document.getElementById('rc363SaveShipment');if(save&&save.hasAttribute('data-rc420-text')){save.textContent=save.getAttribute('data-rc420-text')||'Sendung speichern';save.removeAttribute('data-rc420-text')}syncReferenceField(refOf(shipment()));protectRepairEvidenceFields();return changed}
function commitEditingToSaved(){var locked=lockedShipment();if(locked){lockShipmentForm(locked,false);return null}var sh=shipment(),snapshot=clone(sh),id=q(state().activeShipmentId||state().editingShipmentId||sh.__savedShipmentId||sh.savedShipmentId||idOf(sh)),ref=refOf(sh),updated=null;stateRoots().forEach(function(rootState){var list=arr(rootState.shipments),target=list.find(function(x){return(id&&idOf(x)===id)||(ref&&refOf(x)===ref)});if(!target)return;var keepId=q(target.id||target.shipmentId||id),keepRef=refOf(target)||ref;Object.assign(target,clone(snapshot));target.id=keepId;target.shipmentId=keepId;target.ref=keepRef;target.reference=keepRef;target.shipmentRef=keepRef;target.referenceNumber=keepRef;target.__savedShipmentId=keepId;target.savedShipmentId=keepId;updated=target});return updated}
function savedFieldValue(saved,key){if(!saved)return'';if(key==='ref')return refOf(saved);if(key==='pickupDate')return q(saved.plannedPickupDate||saved.pickupDate||saved.pickupPlannedDate);if(key==='actualPickupDate')return q(saved.actualPickupDate||saved.pickedUpAtDate||saved.pickedUpDate);if(key==='comments')return q(saved.comments||saved.remarks||saved.notes||saved.remark);if(key==='deliveryNotes')return q(saved.deliveryNotes);if(key==='carrier')return q(saved.carrier||saved.forwarder);if(key==='licensePlate')return q(saved.licensePlate||saved.plate||saved.kennzeichen);if(key==='loader')return q(saved.loader||saved.loadedBy||saved.verlader);if(key==='incoterm')return q(saved.incoterm);if(key==='goodsDescription')return q(saved.goodsDescription||saved.description);return q(saved[key])}
function paintEditingShipment(saved){
 var layout=document.getElementById('rc363FixedShipmentLayout');if(!layout||!saved)return false;enforceEditingCustomerLock();mergeRemote(saved);enforceEditingCustomerLock();prepareCanonicalShipmentFields();
 function displayDate(value){var v=q(value),m=v.match(/^(\d{4})-(\d{2})-(\d{2})/);if(m)return m[3]+'.'+m[2]+'.'+m[1];m=v.match(/^(\d{1,2})[.\/-](\d{1,2})[.\/-](\d{4})$/);return m?String(m[1]).padStart(2,'0')+'.'+String(m[2]).padStart(2,'0')+'.'+m[3]:v}
 Array.from(layout.querySelectorAll('input,select,textarea')).forEach(function(el){var key=shipmentFieldKey(el);if(!key||document.activeElement===el)return;var value=savedFieldValue(saved,key);if(key==='pickupDate'||key==='actualPickupDate'){var iso=q(value).slice(0,10);if(/^\d{4}-\d{2}-\d{2}$/.test(iso))el.dataset.dateIso=iso;value=displayDate(value)}if(String(el.value)!==String(value))el.value=value});
 var customerInput=document.getElementById('shipmentCustomerSearch'),account=q(saved.customerAc
```

### line~24111 pos=4862166
```text
||typeof api.createPodBlob!=='function')throw new Error('Lokale POD-Erstellung ist nicht verfügbar');
 var out=await api.createPodBlob(sh),bytes=new Uint8Array(await out.blob.arrayBuffer()),ext=fileFormat(bytes);
 if(ext!=='pdf')throw new Error('Lokal erzeugter POD ist keine gültige PDF-Datei');
 return{bytes:bytes,ext:'pdf',name:q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf')}
}
var crcTable=null;
function crc32(data){if(!crcTable){crcTable=new Uint32Array(256);for(var n=0;n<256;n++){var c=n;for(var k=0;k<8;k++)c=(c&1)?0xedb88320^(c>>>1):c>>>1;crcTable[n]=c>>>0}}var c=0xffffffff;for(var i=0;i<data.length;i++)c=crcTable[(c^data[i])&255]^(c>>>8);return(c^0xffffffff)>>>0}
function u16(n){return new Uint8Array([n&255,(n>>>8)&255])}
function u32(n){return new Uint8Array([n&255,(n>>>8)&255,(n>>>16)&255,(n>>>24)&255])}
function cat(parts){var len=parts.reduce(function(a,b){return a+b.length},0),out=new Uint8Array(len),p=0;parts.forEach(function(b){out.set(b,p);p+=b.length});return out}
function zip(entries){var local=[],central=[],offset=0,enc=new TextEncoder();entries.forEach(function(e){var name=enc.encode(e.name),data=e.data,crc=crc32(data),head=cat([u32(0x04034b50),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name]),block=cat([head,data]);local.push(block);central.push(cat([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]));offset+=block.length});var center=cat(central),body=cat(local),end=cat([u32(0x06054b50),u16(0),u16(0),u16(entries.length),u16(entries.length),u32(center.length),u32(body.length),u16(0)]);return new Blob([body,center,end],{type:'application/zip'})}
function saveBlob(blob,name){var u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u)},60000)}
function extension(src){var n=q(src.name),m=/\.(pdf|png|jpe?g|webp)(?:$|[?#])/i.exec(n||src.url),ext=m?m[1].toLowerCase():'pdf';return ext==='jpeg'?'jpg':ext}
async function downloadAll(){
 if(syncPromise)await syncPromise;
 await syncServer(false);
 var x=update(),m=modal(),btn=m.querySelector('#rc422PodDownload'),status=m.querySelector('#rc422PodStatus'),bar=m.querySelector('#rc422PodProgress');
 if(x.error||!x.list.length)return false;
 btn.disabled=true;bar.style.width='0%';var entries=[],fail=[];
 for(var i=0;i<x.jobs.length;i++){
  var job=x.jobs[i],sh=job.sh,src=job.src,isManual=src&&src.kind==='manual-uploaded-pod';status.className='rc422-pod-status';status.textContent=(isManual?'Hochgeladener POD ':'Unterschriebene Ladeliste ')+(i+1)+' von '+x.jobs.length+' wird gesichert: '+ref(sh);bar.style.width=Math.round((i/x.jobs.length)*100)+'%';
  try{if(isManual){var loaded=await load(src);if(loaded.ext!=='pdf')throw new Error('Der hochgeladene POD ist keine PDF-Datei');entries.push({name:src.name,data:loaded.bytes})}else{var generated=await localSignedPod(sh),base='POD_'+safe(ref(sh))+'_Ladeliste_mit_Unterschrift';entries.push({name:safe(podDate(sh)+'_'+base)+'.pdf',data:generated.bytes})}}
  catch(error){fail.push(ref(sh)+' · '+q(error&&error.message||error))}
 }
 bar.style.width='100%';
 if(!entries.length){status.className='rc422-pod-status bad';status.textContent='Keine vollständige unterschriebene Ladeliste konnte als PDF erzeugt werden. '+(fail[0]||'POD-Daten prüfen.');btn.disabled=false;return false}
 var period=(x.from?displayDate(x.from):'Anfa
```

### line~24162 pos=4867365
```text
.ExportHUBPODBackup=Object.freeze({version:VERSION,open:open,close:close,filter:filtered,refresh:function(){return syncServer(true)},download:downloadAll,selfTest:function(){var d=dateDefaults(),list=filtered(d.from,d.to),jobs=[];list.forEach(function(sh){jobs=jobs.concat(sourceList(sh))});return{periodSelection:true,mergedShipments:allShipments().length,signedShipments:list.length,files:jobs.length,azureSync:!!(window.ExportHUBPickupPOD&&typeof window.ExportHUBPickupPOD.syncAll==='function'),zip:true,manualUploadedPodSource:true,manualUploadedPodName:'POD_<REF>.pdf',dateFormat:'TT.MM.JJJJ',cloudBackup:!!window.ExportHUBPODCloudBackup,emergencyVault:!!window.ExportHUBPODVault,emergencyPage:'/pod-notfall.html'}}})
})();

</script>

<script id="rc430-canonical-theme-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_RC430_THEME__)return;
window.__EXPORTHUB_RC430_THEME__=true;
var THEMES=[
 ['boardroom-elite','Space Optik'],
 ['glass-workspace','Glass Optik'],
 ['neon-flow','Neon Optik'],
 ['current','Blau Grau Optik']
];
var currentUser='';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function slug(v){return q(v).toLowerCase().replace(/[^a-z0-9äöüß_-]+/g,'-')||'default'}
function valid(v){v=q(v);return THEMES.some(function(x){return x[0]===v})?v:'current'}
function userName(){
 var p=document.getElementById('profileName'),b=document.getElementById('brandUserLine');
 var value=q((p&&p.textContent)||(b&&b.textContent)).replace(/^ExportHUB-Benutzer:\s*/i,'');
 return !value||/nicht angemeldet/i.test(value)?'':value;
}
function key(user){return'exporthub-theme-choice::'+slug(user)}
function read(user){try{return valid(localStorage.getItem(key(user))||localStorage.getItem('exporthub-theme-choice:last')||'current')}catch(_){return'current'}}
function save(user,theme){try{localStorage.setItem(key(user),theme);localStorage.setItem('exporthub-theme-choice:last',theme)}catch(_){}return theme}
function apply(theme,persist){
 theme=valid(theme);
 var root=document.documentElement,body=document.body;
 if(!body)return theme;
 body.classList.remove('eh-dark');
 if(body.getAttribute('data-eh-theme')===theme){
  var existing=document.getElementById('ehThemeSelect');
  if(existing&&existing.value!==theme)existing.value=theme;
  if(persist)save(userName(),theme);
  return theme;
 }
 root.classList.add('eh-theme-changing');
 root.setAttribute('data-eh-theme',theme);
 body.setAttribute('data-eh-theme',theme);
 var app=document.getElementById('app'),content=document.getElementById('content');
 if(app)app.setAttribute('data-eh-theme',theme);
 if(content)content.setAttribute('data-eh-theme',theme);
 var select=document.getElementById('ehThemeSelect');
 if(select&&select.value!==theme)select.value=theme;
 if(persist)save(userName(),theme);
 requestAnimationFrame(function(){requestAnimationFrame(function(){root.classList.remove('eh-theme-changing')})});
 return theme;
}
function ensure(){
 var top=document.querySelector('.topbar');if(!top)return false;
 var wrap=document.getElementById('ehThemeSwitch');
 if(!wrap){
  wrap=document.createElement('div');wrap.id='ehThemeSwitch';wrap.className='eh-theme-switch ghost';wrap.title='Design auswählen';
  wrap.innerHTML='<span>Design</span><select id="ehThemeSelect" aria-label="Design auswählen"></select>';
  var lang=document.getElementById('languageSelect');
  if(lang&&lang.nextSibling)top.insertBefore(wrap,lang.nextSibling);else top.appendChild(wrap);
 }
 var select=document.getElementById('ehThemeSelect');
 if(select&&!select.dataset.rc446){
  select.innerHTML=THEMES.map(function(x)
```

### line~24936 pos=5025026
```text
wDomOk())return true;rc843ViewGuardBusy=true;try{s.shipmentViewId=wanted;if(!q(s.shipmentViewReturnView))s.shipmentViewReturnView=g.source||'shipmentoverview';s.view='shipmentview';try{if(typeof window.__EXPORTHUB_BIND_STATE__==='function')window.__EXPORTHUB_BIND_STATE__(s)}catch(_){}try{document.body.setAttribute('data-exporthub-view','shipmentview');document.body.setAttribute('data-current-view','shipmentview')}catch(_){}render();return true}finally{rc843ViewGuardBusy=false}}
function rc843ScheduleViewGuard(delay){if(!rc843ViewGuard)return false;if(rc843ViewGuardTimer)clearTimeout(rc843ViewGuardTimer);rc843ViewGuardTimer=setTimeout(function(){rc843ViewGuardTimer=0;rc843EnsureView()},Math.max(0,Number(delay)||0));return true}
window.__EXPORTHUB_RC843_RELEASE_SHIPMENT_VIEW__=rc843ReleaseViewGuard;
function score(sh){if(!obj(sh))return 0;var n=0;if(refOf(sh))n+=50;if(q(sh.customerName)||(obj(sh.customer)&&q(sh.customer.name)))n+=30;n+=Math.min(120,rows(sh).length*20);['deliveryFiles','podFiles','abdFiles','documents','generatedDocuments','files','attachments','deliveryNotesFiles','invoiceFiles','mailAttachments'].forEach(function(k){n+=arr(sh[k]).length*7});if(q(sh.updatedAt||sh._syncUpdatedAt))n+=5;return n}
function roots(){var out=[];function add(x){if(obj(x)&&out.indexOf(x)<0)out.push(x)}add(state());try{add(window.state)}catch(_){}try{add(window.__CLEAN_BOOT_STATE__)}catch(_){}try{add(window.ExportHUBClean&&window.ExportHUBClean.runtime&&window.ExportHUBClean.runtime.state)}catch(_){}return out}
function allShipments(){var list=[];roots().forEach(function(r){arr(r.shipments).concat(arr(r.savedShipments)).forEach(function(sh){if(obj(sh))list.push(sh)});['shipment','currentShipment','selectedShipment'].forEach(function(k){if(obj(r[k]))list.push(r[k])})});return list}
function savedShipments(){var map={};roots().forEach(function(r){arr(r.shipments).concat(arr(r.savedShipments)).forEach(function(sh){if(!obj(sh))return;var key=refOf(sh)||idOf(sh);if(!key)return;var prev=map[key],ss=score(sh);if(!prev||ss>prev.score)map[key]={shipment:sh,score:ss}})});return Object.keys(map).map(function(k){return map[k].shipment})}
function find(value){var key=typeof value==='object'&&value?(idOf(value)||refOf(value)):q(value),upper=key.toUpperCase(),best=null,bestScore=-1;allShipments().forEach(function(sh){if(idOf(sh)===key||idOf(sh).toUpperCase()===upper||refOf(sh)===upper){var ss=score(sh);if(ss>bestScore){best=sh;bestScore=ss}}});return best}
function rows(sh){var candidates=[sh&&sh.rows,sh&&sh.colli,sh&&sh.collis,sh&&sh.packages,sh&&sh.packagingRows,sh&&sh.shipmentRows],best=[];candidates.forEach(function(x){if(arr(x).length>best.length)best=arr(x)});return best}
function num(v){var n=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(n)?n:0}function fmt(v,max){return num(v).toLocaleString('de-DE',{maximumFractionDigits:max==null?2:max})}
function fmtDate(v){v=q(v);if(!v)return'–';var d=new Date(v);if(!Number.isNaN(d.getTime()))return d.toLocaleString('de-DE',{day:'2-digit',month:'2-digit',year:'numeric',hour:/T|\d:\d/.test(v)?'2-digit':undefined,minute:/T|\d:\d/.test(v)?'2-digit':undefined}).replace(', 00:00','');var m=v.match(/^(\d{4})-(\d{2})-(\d{2})/);return m?m[3]+'.'+m[2]+'.'+m[1]:v}
function customer(sh){if(obj(sh&&sh.customer))return sh.customer;if(obj(sh&&sh.customerData))return sh.customerData;var s=state(),id=q(sh&&(sh.customerId||sh.customerAccount||sh.customerNumber)),name=low(sh&&sh.customerName);return arr(s.customers).find(function(c){return id&&[c.id,c.customerId,c.account,c.customerNumber].some(function(v){retu
```

## await persist
needle='await persist'
count=8

### line~2565 pos=402216
```text
ipmentShell');if(body){host=document.createElement('div');host.id='rc845AvisEditHost';host.className='rc845-avis-host';host.setAttribute('data-rc845-avis-host','edit');body.appendChild(host)}}return host||null}return null}
function render(){return false}
async function toggle(on){var sh=current();if(!sh)return false;if(on&&pickupStamp(sh)){alert('Der Lieferavis ist nach der Abholung geschlossen und kann nicht erneut aktiviert werden.');return false}if(on&&!ref(sh)){alert('Die Sendung benötigt zuerst eine Referenznummer.');return false}var stamp=new Date().toISOString(),values;if(on){var newToken=makeToken();values={customerAvisEnabled:true,avisEnabled:true,customerAvisToken:newToken,avisToken:newToken,customerAvisAccessCode:'',avisAccessCode:'',customerAvisSecurityVersion:SECURITY_VERSION,avisSecurityVersion:SECURITY_VERSION,customerAvisEnabledAt:stamp,avisEnabledAt:stamp,customerAvisExpiresAt:'',avisExpiresAt:'',customerAvisUsedAt:'',avisUsedAt:'',customerAvisDisabledAt:'',avisDisabledAt:'',customerAvisResponseStatus:'offen',avisResponseStatus:'offen',customerAvisAutoDisabledAt:'',avisAutoDisabledAt:'',customerAvisAutoDisabledReason:'',avisAutoDisabledReason:''}}else{values={customerAvisEnabled:false,avisEnabled:false,customerAvisToken:'',avisToken:'',customerAvisAccessCode:'',avisAccessCode:'',customerAvisSecurityVersion:0,avisSecurityVersion:0,customerAvisExpiresAt:'',avisExpiresAt:'',customerAvisUsedAt:'',avisUsedAt:'',customerAvisDisabledAt:stamp,avisDisabledAt:stamp,customerAvisAutoDisabledAt:'',avisAutoDisabledAt:'',customerAvisAutoDisabledReason:'',avisAutoDisabledReason:''}}var targets=copies(sh),keys=Object.keys(values),before=targets.map(function(x){var v={};keys.forEach(function(k){v[k]=x[k]});return{x:x,v:v}});patchCopies(sh,values);lastSig='';render();try{await persist(on?'Kunden-Avis sicher aktiviert':'Kunden-Avis deaktiviert');window.dispatchEvent(new CustomEvent('exporthub:customer-avis-updated',{detail:{reference:ref(sh),enabled:on}}));try{if(window.ExportHUBMailStatus373&&typeof window.ExportHUBMailStatus373.patch==='function')window.ExportHUBMailStatus373.patch('Kunden-Avis geändert')}catch(_){}}catch(e){before.forEach(function(row){Object.keys(row.v).forEach(function(k){if(row.v[k]===undefined)delete row.x[k];else row.x[k]=row.v[k]})});lastSig='';render();alert('Kunden-Avis konnte nicht dauerhaft in Azure gespeichert werden. Der vorherige Zustand wurde wiederhergestellt.\n\n'+q(e&&e.message||e));return false}lastSig='';render();schedule();return false}
async function autoDisableIfDue(){if(avisAutoBusy)return false;var sh=current();if(!sh||!configured(sh)||!expired(sh))return false;avisAutoBusy=true;var stamp=new Date().toISOString(),until=autoExpiresOn(sh),values={customerAvisEnabled:false,avisEnabled:false,customerAvisToken:'',avisToken:'',customerAvisSecurityVersion:0,avisSecurityVersion:0,customerAvisExpiresAt:until,avisExpiresAt:until,customerAvisDisabledAt:stamp,avisDisabledAt:stamp,customerAvisAutoDisabledAt:stamp,avisAutoDisabledAt:stamp,customerAvisAutoDisabledReason:'3 Arbeitstage nach tatsächlicher Abholung',avisAutoDisabledReason:'3 business days after actual collection'};patchCopies(sh,values);lastSig='';render();try{await persist('Kunden-Avis automatisch nach 3 Arbeitstagen deaktiviert');window.dispatchEvent(new CustomEvent('exporthub:customer-avis-updated',{detail:{reference:ref(sh),enabled:false,automatic:true,expiresOn:until}}));try{if(window.ExportHUBMailStatus373&&typeof window.ExportHUBMailStatus373.patch==='function')window.ExportHUBMailStatus373.patch('Kunden-Avis automatisch deaktiviert')}cat
```

### line~2566 pos=403624
```text
'',customerAvisUsedAt:'',avisUsedAt:'',customerAvisDisabledAt:stamp,avisDisabledAt:stamp,customerAvisAutoDisabledAt:'',avisAutoDisabledAt:'',customerAvisAutoDisabledReason:'',avisAutoDisabledReason:''}}var targets=copies(sh),keys=Object.keys(values),before=targets.map(function(x){var v={};keys.forEach(function(k){v[k]=x[k]});return{x:x,v:v}});patchCopies(sh,values);lastSig='';render();try{await persist(on?'Kunden-Avis sicher aktiviert':'Kunden-Avis deaktiviert');window.dispatchEvent(new CustomEvent('exporthub:customer-avis-updated',{detail:{reference:ref(sh),enabled:on}}));try{if(window.ExportHUBMailStatus373&&typeof window.ExportHUBMailStatus373.patch==='function')window.ExportHUBMailStatus373.patch('Kunden-Avis geändert')}catch(_){}}catch(e){before.forEach(function(row){Object.keys(row.v).forEach(function(k){if(row.v[k]===undefined)delete row.x[k];else row.x[k]=row.v[k]})});lastSig='';render();alert('Kunden-Avis konnte nicht dauerhaft in Azure gespeichert werden. Der vorherige Zustand wurde wiederhergestellt.\n\n'+q(e&&e.message||e));return false}lastSig='';render();schedule();return false}
async function autoDisableIfDue(){if(avisAutoBusy)return false;var sh=current();if(!sh||!configured(sh)||!expired(sh))return false;avisAutoBusy=true;var stamp=new Date().toISOString(),until=autoExpiresOn(sh),values={customerAvisEnabled:false,avisEnabled:false,customerAvisToken:'',avisToken:'',customerAvisSecurityVersion:0,avisSecurityVersion:0,customerAvisExpiresAt:until,avisExpiresAt:until,customerAvisDisabledAt:stamp,avisDisabledAt:stamp,customerAvisAutoDisabledAt:stamp,avisAutoDisabledAt:stamp,customerAvisAutoDisabledReason:'3 Arbeitstage nach tatsächlicher Abholung',avisAutoDisabledReason:'3 business days after actual collection'};patchCopies(sh,values);lastSig='';render();try{await persist('Kunden-Avis automatisch nach 3 Arbeitstagen deaktiviert');window.dispatchEvent(new CustomEvent('exporthub:customer-avis-updated',{detail:{reference:ref(sh),enabled:false,automatic:true,expiresOn:until}}));try{if(window.ExportHUBMailStatus373&&typeof window.ExportHUBMailStatus373.patch==='function')window.ExportHUBMailStatus373.patch('Kunden-Avis automatisch deaktiviert')}catch(_){}}catch(e){console.error('Automatische Avis-Deaktivierung konnte noch nicht bestätigt werden',e)}finally{avisAutoBusy=false}return false}
function stripBlock(body){var text=String(body||'').replace(/\r\n/g,'\n');text=text.replace(/\n*--- ExportHUB Kunden-Avis ---[\s\S]*?\n---(?=\n|$)/g,'');text=text.replace(/\n*─{8,}\n(?:LIEFERAVIS \/ KUNDENPORTAL|COLLECTION NOTICE \/ CUSTOMER PORTAL|KUNDEN-AVIS – LIVE-ZUGANG|CUSTOMER COLLECTION NOTICE – LIVE AVIS)[\s\S]*?\n─{8,}(?=\n|$)/g,'');text=text.replace(/\n*-{20,}\n(?:KUNDEN-AVIS – DAUERHAFTER ZUGANG|CUSTOMER COLLECTION NOTICE – PERSISTENT ACCESS|KUNDEN-AVIS – LIVE-ZUGANG|CUSTOMER COLLECTION NOTICE – LIVE AVIS)[\s\S]*?\n-{20,}(?=\n|$)/g,'');return text.replace(/\n{3,}/g,'\n\n')}
function stripShipmentDetails(body){var lines=String(body||'').replace(/\r\n/g,'\n').split('\n'),out=[],i=0,heading=/^(Details zur Sendung|Sendungsdetails|Shipment details):\s*$/i,detail=/^(Referenz|Kunde|Abholdatum|Geplante Abholung|Spedition|Warenbeschreibung|Incoterm|Verpackung|Colli|Gesamtgewicht|LDM|Lademeter|Reference|Customer|Collection date|Planned collection|Carrier|Goods description|Description of goods|Packaging|Packages(?: \/ colli)?|Total weight|Loading meters):\s*/i;while(i<lines.length){if(!heading.test(lines[i].trim())){out.push(lines[i++]);continue}var j=i+1,seen=false;while(j<lines.length&&!lines[j].trim())j++;while(j<lines.length){var t=lines[j]
```

### line~15151 pos=2505161
```text
EditId);if(!g||g.id==='admin')return false;groupDraftRights=defaultGroupLevels(groupEditId,cache&&cache.modules||[]);refreshGroupEditorDom();var status=document.getElementById('rc771GroupSaveStatus');if(status)status.textContent='ExportHUB-Standard wurde nur in den Entwurf geladen. Zum Übernehmen bitte Gruppenrechte speichern.';return false};
window.rc771SaveGroupRights=async function(){if(busy||!cache)return false;var g=groupById(groupEditId);if(!g||g.id==='admin')return false;var modules=cache.modules||[],oldLevels=groupLevels(groupEditId,modules),newLevels={};modules.forEach(function(id){newLevels[id]=groupDraftRights[id]||'none'});var memberIds=groupMemberIds(groupEditId,oldLevels),previous={},status=document.getElementById('rc771GroupSaveStatus'),btn=document.getElementById('rc771SaveGroupRights');if(status)status.textContent='Gruppenrechte werden serverseitig aktualisiert …';if(btn)btn.disabled=true;busy=true;try{for(var i=0;i<memberIds.length;i++){var uid=memberIds[i],u=(cache.users||[]).find(function(x){return Q(x.id)===Q(uid)});if(!u)continue;previous[uid]={name:u.name||u.user,globalAdmin:!!u.globalAdmin,rights:clone(u.rights||{})};var rights={};modules.forEach(function(id){rights[serverRightId(id)]=rightObject(newLevels[id])});await call('admin-update-user',{userId:uid,name:u.name||u.user,globalAdmin:false,rights:rights})}cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);var failedMembers=memberIds.filter(function(uid){var u=(cache.users||[]).find(function(x){return Q(x.id)===Q(uid)});if(!u)return true;var cur={};modules.forEach(function(id){cur[id]=level(serverRight(u,id))});return !sameLevels(cur,newLevels,modules)});if(failedMembers.length)throw new Error('Die neuen Gruppenrechte wurden nicht bei allen Gruppenmitgliedern bestätigt.');try{await persistGroupDefinition(groupEditId,newLevels,memberIds)}catch(saveError){for(var uid in previous){var old=previous[uid];try{await call('admin-update-user',{userId:uid,name:old.name,globalAdmin:old.globalAdmin,rights:old.rights})}catch(_){}}cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);throw saveError}groupDraftRights=clone(newLevels);if(draftGroup===groupEditId&&userById()&&memberIds.indexOf(userById().id)>=0){draftRights=clone(newLevels);modules.forEach(refreshRightDom);refreshGroupDom()}refreshGroupEditorDom();if(status)status.textContent='Rechtegruppe '+g.label+' dauerhaft gespeichert · '+memberIds.length+' bestehende Gruppenmitglieder aktualisiert.'}catch(e){if(status){status.className='badbox';status.textContent='Gruppenrechte nicht vollständig gespeichert: '+Q(e&&e.message||e)}}finally{busy=false;if(btn)btn.disabled=false}return false};
window.rc544LoadAdmin=load;window.rc544RenderAdmin=render;
window.rc544SelectUser=function(id){selectedId=id;initDraft(userById());render();return false};
window.rc544ChooseUser=function(){var value=L((document.getElementById('rc544UserSearch')||{}).value),u=(cache.users||[]).find(function(x){return L(x.user)===value||L(x.name)===value});if(!u)return alert('Benutzer wurde nicht gefunden.');return window.rc544SelectUser(u.id)};
window.rc544SetDraftRight=function(id,v,button){draftRights[id]=v;draftGroup='custom';var wrap=button&&button.closest?button.closest('.rc544-rights'):null;if(wrap){wrap.setAttribute('data-selected-level',v);var row=wrap.closest('tr');if(row)row.setAttribute('data-rc544-level',v);wrap.querySelectorAll('button[data-level]').forEach(function(b){var on=b===button;b.classList.toggle('active',on);b.setAttribute('aria-pressed',on?'true':'false')})}else refreshRightDom(id);refreshGroupDom
```

### line~15159 pos=2509015
```text
etElementById('rc544NewAdmin');if(g&&box)box.checked=!!g.globalAdmin;return true};
window.rc544CreateUser=async function(){if(busy)return false;var login=Q((document.getElementById('rc544NewLogin')||{}).value),name=Q((document.getElementById('rc544NewName')||{}).value),groupId=Q((document.getElementById('rc544NewGroup')||{}).value),group=groupById(groupId),globalAdmin=group?!!group.globalAdmin:!!((document.getElementById('rc544NewAdmin')||{}).checked);if(!login)return alert('Bitte einen Benutzernamen eingeben.');if(!name)return alert('Bitte einen Anzeigenamen eingeben.');if(!validUsername(login))return alert('Der Benutzername darf Buchstaben einschließlich Ä, Ö, Ü und ß, Zahlen, Punkt, Unterstrich und Bindestrich enthalten.');busy=true;try{var d=await call('admin-create-user',{username:login,name:name,globalAdmin:globalAdmin});lastSecret=d.startPassword||d.initialPassword||'';cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);var created=d.user||cache.users.find(function(x){return L(x.user||x.login||x.username)===L(login)});if(!created)throw new Error('Der Benutzer wurde vom Server nicht zurückgegeben.');if(group){var levels=groupLevels(groupId,cache.modules),rights={};Object.keys(levels).forEach(function(id){rights[serverRightId(id)]=rightObject(levels[id])});await call('admin-update-user',{userId:created.id,name:name,globalAdmin:globalAdmin,rights:rights});cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);created=cache.users.find(function(x){return Q(x.id)===Q(created.id)})||created;var failed=(cache.modules||[]).filter(function(id){return level(serverRight(created,id))!==levels[id]});if(failed.length)throw new Error('Die Rechtegruppe wurde nicht vollständig übernommen: '+failed.map(function(id){return LABELS[id]||id}).join(', '));await persistUserGroupAssignment(created.id,groupId)}selectedId=created.id;initDraft(userById());render()}catch(e){alert((e.message||'Benutzer konnte nicht angelegt werden.')+(e.code?'\nFehlercode: '+e.code:''))}finally{busy=false}return false};
window.rc544SaveUser=async function(){if(busy||!userById())return false;var u=userById(),rights={},expected={},oldUser={name:u.name||u.user,globalAdmin:!!u.globalAdmin,rights:clone(u.rights||{})},globalAdmin=!!((document.getElementById('rc544EditAdmin')||{}).checked),desiredGroup=globalAdmin?'admin':(draftGroup&&draftGroup!=='custom'?draftGroup:'custom');Object.keys(draftRights).forEach(function(id){var canonical=canonicalRightId(id);rights[serverRightId(canonical)]=rightObject(draftRights[id]);expected[canonical]=draftRights[id]});busy=true;try{await call('admin-update-user',{userId:u.id,name:Q((document.getElementById('rc544EditName')||{}).value),globalAdmin:globalAdmin,rights:rights});cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);var saved=cache.users.find(function(x){return Q(x.id)===Q(u.id)});if(!saved)throw new Error('Der gespeicherte Benutzer wurde nicht erneut geladen.');var failed=Object.keys(expected).filter(function(id){return level(serverRight(saved,id))!==expected[id]});if(failed.length)throw new Error('Diese Rechte wurden vom Server nicht übernommen: '+failed.map(function(id){return LABELS[id]||id}).join(', '));try{await persistUserGroupAssignment(saved.id,desiredGroup)}catch(groupError){try{await call('admin-update-user',{userId:u.id,name:oldUser.name,globalAdmin:oldUser.globalAdmin,rights:oldUser.rights})}catch(_){}cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);throw groupError}selectedId=saved.id;initDraft(saved);render();alert('Benutzer, Rechte und Rechtegruppe wurde
```

### line~15160 pos=2510431
```text
he(await call('admin-list'));syncUsers(cache.users);created=cache.users.find(function(x){return Q(x.id)===Q(created.id)})||created;var failed=(cache.modules||[]).filter(function(id){return level(serverRight(created,id))!==levels[id]});if(failed.length)throw new Error('Die Rechtegruppe wurde nicht vollständig übernommen: '+failed.map(function(id){return LABELS[id]||id}).join(', '));await persistUserGroupAssignment(created.id,groupId)}selectedId=created.id;initDraft(userById());render()}catch(e){alert((e.message||'Benutzer konnte nicht angelegt werden.')+(e.code?'\nFehlercode: '+e.code:''))}finally{busy=false}return false};
window.rc544SaveUser=async function(){if(busy||!userById())return false;var u=userById(),rights={},expected={},oldUser={name:u.name||u.user,globalAdmin:!!u.globalAdmin,rights:clone(u.rights||{})},globalAdmin=!!((document.getElementById('rc544EditAdmin')||{}).checked),desiredGroup=globalAdmin?'admin':(draftGroup&&draftGroup!=='custom'?draftGroup:'custom');Object.keys(draftRights).forEach(function(id){var canonical=canonicalRightId(id);rights[serverRightId(canonical)]=rightObject(draftRights[id]);expected[canonical]=draftRights[id]});busy=true;try{await call('admin-update-user',{userId:u.id,name:Q((document.getElementById('rc544EditName')||{}).value),globalAdmin:globalAdmin,rights:rights});cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);var saved=cache.users.find(function(x){return Q(x.id)===Q(u.id)});if(!saved)throw new Error('Der gespeicherte Benutzer wurde nicht erneut geladen.');var failed=Object.keys(expected).filter(function(id){return level(serverRight(saved,id))!==expected[id]});if(failed.length)throw new Error('Diese Rechte wurden vom Server nicht übernommen: '+failed.map(function(id){return LABELS[id]||id}).join(', '));try{await persistUserGroupAssignment(saved.id,desiredGroup)}catch(groupError){try{await call('admin-update-user',{userId:u.id,name:oldUser.name,globalAdmin:oldUser.globalAdmin,rights:oldUser.rights})}catch(_){}cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);throw groupError}selectedId=saved.id;initDraft(saved);render();alert('Benutzer, Rechte und Rechtegruppe wurden gespeichert.')}catch(e){alert((e.message||'Rechte konnten nicht gespeichert werden.')+(e.code?'\nFehlercode: '+e.code:''))}finally{busy=false}return false};
window.rc544ResetPassword=async function(){if(busy||!userById())return false;var u=userById();if(!confirm('Passwort für '+u.name+' zurücksetzen? Alle Sitzungen werden beendet.'))return false;busy=true;try{var d=await call('admin-reset-password',{userId:u.id});lastSecret=d.startPassword||'';cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);render()}catch(e){alert(e.message)}finally{busy=false}return false};
window.rc544UnlockUser=async function(){if(busy||!userById())return false;busy=true;try{await call('admin-unlock',{userId:userById().id});cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);render()}catch(e){alert(e.message)}finally{busy=false}return false};window.rc544ToggleUser=async function(){if(busy||!userById())return false;var u=userById(),next=!u.active;if(!confirm('Benutzer '+(next?'aktivieren':'deaktivieren')+'?'))return false;busy=true;try{await call('admin-set-active',{userId:u.id,active:next});cache=sanitizeCache(await call('admin-list'));syncUsers(cache.users);initDraft(userById());render()}catch(e){alert(e.message)}finally{busy=false}return false};
window.rc544TerminateSession=async function(sessionId){if(busy)return false;busy=true;try{await call('admin-terminate-sessions',{sessionId:
```

### line~21629 pos=4381331
```text
leName:q(sh.podCloudBackupFileName),webUrl:q(sh.podCloudBackupWebUrl)};if(!retryAllowed(sh,options.force))return{saved:false,deferred:true};inflight[k]=(async function(){var blob=out.blob;await assertOfficialPdf(blob);if(blob.size>20*1024*1024)throw new Error('POD-PDF ist für die automatische Sicherung zu groß.');patch(sh,{podCloudBackupStatus:'uploading',podCloudBackupError:'',podCloudBackupAttemptAt:new Date().toISOString()});try{if(window.ExportHUBPODVault&&typeof window.ExportHUBPODVault.store==='function')await window.ExportHUBPODVault.store(sh,blob,q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf'),{pages:out.pages,documentKind:'signed-loadlist'})}catch(vaultError){console.warn('Lokale POD-Notfallsicherung',ref(sh),vaultError)}var pdfBase64=bytesBase64(await blob.arrayBuffer()),result=await post({token:token(sh),reference:ref(sh),shipmentId:sid(sh),confirmedAt:confirmedAt(sh),fileName:q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf'),pdfBase64:pdfBase64,source:q(options.source)||'automatic'});var values={podCloudBackupStatus:'saved',podCloudBackupAt:q(result.savedAt)||new Date().toISOString(),podCloudBackupFileName:q(result.fileName),podCloudBackupWebUrl:q(result.webUrl),podCloudBackupDriveItemId:q(result.driveItemId),podCloudBackupHash:q(result.hash),podCloudBackupError:'',podCloudBackupNextRetryAt:0};patch(sh,values);try{if(window.ExportHUBPODVault&&typeof window.ExportHUBPODVault.store==='function')await window.ExportHUBPODVault.store(sh,blob,q(out.name)||values.podCloudBackupFileName,{webUrl:values.podCloudBackupWebUrl,cloudFileName:values.podCloudBackupFileName,pages:out.pages,documentKind:'signed-loadlist'})}catch(_){}var persisted=save('POD automatisch in SharePoint gesichert');if(persisted&&typeof persisted.then==='function')try{await persisted}catch(_){}try{window.dispatchEvent(new CustomEvent('exporthub:pod-cloud-backup',{detail:{reference:ref(sh),saved:true,fileName:values.podCloudBackupFileName,webUrl:values.podCloudBackupWebUrl}}))}catch(_){}return Object.assign({saved:true},result)})().catch(async function(error){var automatic=!options.force,retry=Date.now()+(automatic?30*60*1000:5*60*1000);patch(sh,{podCloudBackupStatus:'error',podCloudBackupError:q(error&&error.message||error),podCloudBackupNextRetryAt:retry});if(automatic)pauseAutomatic(error);else{var persisted=save('POD SharePoint-Sicherung vorgemerkt');if(persisted&&typeof persisted.then==='function')try{await persisted}catch(_){}}throw error}).finally(function(){delete inflight[k]});return inflight[k]}
async function buildAndBackup(sh,force){if(!sh||!hasSignature(sh)||saved(sh)&&!force||!retryAllowed(sh,force)||(!force&&!automaticProofReady(sh)))return false;var docs=window.ExportHUBIndex240,pickup=window.ExportHUBPickupPOD;if(!docs||typeof docs.createPodBlob!=='function')return false;if(pickup&&typeof pickup.syncOne==='function')try{await pickup.syncOne(sh,true)}catch(_){}if(!hasSignature(sh))return false;var out=await docs.createPodBlob(sh);await backupBlob(sh,out,{force:!!force,source:'automatic-sync'});return true}
async function scan(force){if(scanBusy||document.hidden)return false;var cv=low((states()[0]||{}).view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):''));if(!force&&/^(?:diagnostics|release|update)$/.test(cv))return false;if(!force&&automaticPaused())return false;if(!force&&uiBusy()){schedule(3000,false);return false}scanBusy=true;try{var list=shipments().filter(function(sh){return (force?hasSignature(sh):automaticProofReady(sh))&&(!saved(sh)||force)&&retryAllowed(sh,force)}),done=0
```

### line~21629 pos=4381981
```text
Kind:'signed-loadlist'})}catch(vaultError){console.warn('Lokale POD-Notfallsicherung',ref(sh),vaultError)}var pdfBase64=bytesBase64(await blob.arrayBuffer()),result=await post({token:token(sh),reference:ref(sh),shipmentId:sid(sh),confirmedAt:confirmedAt(sh),fileName:q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf'),pdfBase64:pdfBase64,source:q(options.source)||'automatic'});var values={podCloudBackupStatus:'saved',podCloudBackupAt:q(result.savedAt)||new Date().toISOString(),podCloudBackupFileName:q(result.fileName),podCloudBackupWebUrl:q(result.webUrl),podCloudBackupDriveItemId:q(result.driveItemId),podCloudBackupHash:q(result.hash),podCloudBackupError:'',podCloudBackupNextRetryAt:0};patch(sh,values);try{if(window.ExportHUBPODVault&&typeof window.ExportHUBPODVault.store==='function')await window.ExportHUBPODVault.store(sh,blob,q(out.name)||values.podCloudBackupFileName,{webUrl:values.podCloudBackupWebUrl,cloudFileName:values.podCloudBackupFileName,pages:out.pages,documentKind:'signed-loadlist'})}catch(_){}var persisted=save('POD automatisch in SharePoint gesichert');if(persisted&&typeof persisted.then==='function')try{await persisted}catch(_){}try{window.dispatchEvent(new CustomEvent('exporthub:pod-cloud-backup',{detail:{reference:ref(sh),saved:true,fileName:values.podCloudBackupFileName,webUrl:values.podCloudBackupWebUrl}}))}catch(_){}return Object.assign({saved:true},result)})().catch(async function(error){var automatic=!options.force,retry=Date.now()+(automatic?30*60*1000:5*60*1000);patch(sh,{podCloudBackupStatus:'error',podCloudBackupError:q(error&&error.message||error),podCloudBackupNextRetryAt:retry});if(automatic)pauseAutomatic(error);else{var persisted=save('POD SharePoint-Sicherung vorgemerkt');if(persisted&&typeof persisted.then==='function')try{await persisted}catch(_){}}throw error}).finally(function(){delete inflight[k]});return inflight[k]}
async function buildAndBackup(sh,force){if(!sh||!hasSignature(sh)||saved(sh)&&!force||!retryAllowed(sh,force)||(!force&&!automaticProofReady(sh)))return false;var docs=window.ExportHUBIndex240,pickup=window.ExportHUBPickupPOD;if(!docs||typeof docs.createPodBlob!=='function')return false;if(pickup&&typeof pickup.syncOne==='function')try{await pickup.syncOne(sh,true)}catch(_){}if(!hasSignature(sh))return false;var out=await docs.createPodBlob(sh);await backupBlob(sh,out,{force:!!force,source:'automatic-sync'});return true}
async function scan(force){if(scanBusy||document.hidden)return false;var cv=low((states()[0]||{}).view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):''));if(!force&&/^(?:diagnostics|release|update)$/.test(cv))return false;if(!force&&automaticPaused())return false;if(!force&&uiBusy()){schedule(3000,false);return false}scanBusy=true;try{var list=shipments().filter(function(sh){return (force?hasSignature(sh):automaticProofReady(sh))&&(!saved(sh)||force)&&retryAllowed(sh,force)}),done=0,limit=force?25:automaticBatchLimit;for(var i=0;i<list.length&&done<limit;i++){try{if(await buildAndBackup(list[i],!!force))done++}catch(e){if(!force){pauseAutomatic(e);break}else console.error('Manuelle POD SharePoint-Sicherung',ref(list[i]),e)}}if(!force&&list.length>done&&!automaticPaused())schedule(60000,false);return done>0}finally{scanBusy=false}}
function schedule(delay,force){if(scanTimer)clearTimeout(scanTimer);scanTimer=setTimeout(function(){scanTimer=0;scan(!!force)},Math.max(500,Number(delay)||2000))}
function periodic(){if(periodicTimer)clearTimeout(periodicTimer);periodicTimer=setTimeout(function(){periodicTimer=0;schedule(1000,
```

### line~23599 pos=4754635
```text
ssperre wird geprüft …');if(button)button.textContent='Sendungssperre wird geprüft …';var locked=await refreshShipmentLock(true);if(locked){lockShipmentForm(locked,true);operationFail(opToken,'Sendung ist bereits gesperrt');return false}window.__EXPORTHUB_SHIPMENT_WRITE_GRANT_UNTIL__=Date.now()+60000;operationStep(opToken,'Sendungsdaten werden vorbereitet …');if(button)button.textContent='Sendungsdaten werden vorbereitet …';prepareCanonicalShipmentFields();restoreLocalFieldDraft();syncVisibleShipmentFields();syncRows();reconcileLatestSavedBeforeSave();locked=lockedShipment();if(locked){lockShipmentForm(locked,true);operationFail(opToken,'Sendung wurde während der Vorbereitung gesperrt');return false}restorePersistedCustomerForSave();var creatorStamp=currentCreator(),savingShipment=shipment();if(repairActive(savingShipment)){markRepairAudit(savingShipment);applyRepairEvidence(savingShipment)}if(isFreshDraft()&&!q(savingShipment.createdBy||savingShipment.createdByName)){savingShipment.createdBy=creatorStamp.name;savingShipment.createdByName=creatorStamp.name;savingShipment.createdById=creatorStamp.id}try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.recalculateShipmentStatus==='function')window.ExportHUBRC543.recalculateShipmentStatus(shipment())}catch(e){console.error('RC679 Status vor Speicherung',e)}if(repairActive(savingShipment))applyRepairEvidence(savingShipment);if(!persistenceSave)persistenceSave=window.ExportHUBRC565&&window.ExportHUBRC565.persistShipment;if(typeof persistenceSave!=='function'){operationFail(opToken,'Speicherfunktion ist nicht verfügbar');alert('Die Speicherfunktion ist nicht verfügbar.');return false}operationStep(opToken,'Sendung wird dauerhaft in Azure gespeichert …');if(button)button.textContent='Speicherung in Azure läuft …';var ok=await persistenceSave();if(ok!==true){operationFail(opToken,'Speichern nicht abgeschlossen');return false}saveConfirmed=true;clearLocalFieldDraft();var saved=currentSaved();if(saved&&repairActive(saved))applyRepairEvidence(saved);var wasRepair=!!(saved&&repairActive(saved));if(saved){try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.recalculateShipmentStatus==='function')window.ExportHUBRC543.recalculateShipmentStatus(saved)}catch(e){console.error('RC679 Status nach Speicherung',e)}if(wasRepair)applyRepairEvidence(saved);updateQr();if(wasRepair){operationDone(opToken,'Datenreparatur dauerhaft gespeichert · Abholung/POD unverändert');try{window.dispatchEvent(new CustomEvent('exporthub:shipment-saved',{detail:{shipmentId:idOf(saved),reference:refOf(saved),previousReference:beforeSaveRef,shipment:saved,qrReady:saved.pickupQrRegistered===true,status:q(saved.status||saved.processStatus),backgroundQr:false,dataRepair:true}}))}catch(_){}}else{operationDone(opToken,'Sendung dauerhaft gespeichert · QR-Freigabe läuft im Hintergrund');try{window.dispatchEvent(new CustomEvent('exporthub:shipment-saved',{detail:{shipmentId:idOf(saved),reference:refOf(saved),previousReference:beforeSaveRef,shipment:saved,qrReady:saved.pickupQrRegistered===true,status:q(saved.status||saved.processStatus),backgroundQr:true}}))}catch(_){}var backgroundQr=Promise.resolve().then(function(){return activateQr(saved)}).then(function(qrOk){try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.recalculateShipmentStatus==='function')window.ExportHUBRC543.recalculateShipmentStatus(saved)}catch(e){console.error('RC678 Status nach QR',e)}updateQr();try{var info=document.getElementById('saveInfo');if(info)info.textContent=qrOk===true?'Sendung gespeichert · QR-Code freigegeben':'Sendung gespeichert · Q
```

## window.print
needle='window.print('
count=3

### line~12869 pos=1058152
```text
}var c=e.customers[0]||{},b={id:'PAL-'+Date.now(),date:new Date().toISOString().slice(0,10),type:diff>0?'Eingang':'Ausgang',count:Math.abs(diff),customerId:c.id||'',customerAccount:c.account||c.customerNumber||'',customerName:c.name||'',partyType:e.party.type,partyName:e.party.name,note:Q((document.getElementById('rc524PalletNote')||{}).value)||'Palettenabgleich'};S().palletAccount=A(S().palletAccount);S().palletAccount.push(b);save('Palettenabgleich gespeichert');renderPallet();return false};
window.rc524PalletReport=function(){var ps=parties(),key=Q((document.getElementById('rc524PalletParty')||{}).value),e=ps.find(function(x){return x.party.key===key});if(!e)return false;var rows=e.bookings.map(function(b){return '<tr><td>'+E(fmtDate(b.date||b.createdAt))+'</td><td>'+E(b.shipmentRef||b.ref||'')+'</td><td>'+E(b.type||'')+'</td><td>'+E(palletBookingCount(b)||'')+'</td><td>'+E(b.note||'')+'</td></tr>'}).join(''),w=window.open('','_blank');if(!w){alert('Das Reportfenster wurde vom Browser blockiert.');return false}w.document.write('<!doctype html><html><head><meta charset="utf-8"><title>Palettenreport '+E(e.party.name)+'</title><style>body{font-family:Arial;padding:28px;color:#172033}h1{margin:0 0 6px}table{width:100%;border-collapse:collapse;margin-top:22px}th,td{border:1px solid #ccd3df;padding:8px;text-align:left}.summary{display:flex;gap:20px;margin-top:18px}.summary div{border:1px solid #ccd3df;padding:12px}</style></head><body><h1>Palettenreport</h1><p>'+E(e.party.label+': '+e.party.name)+'</p><div class="summary"><div>Saldo: <b>'+e.balance+'</b></div><div>Buchungen: <b>'+e.bookings.length+'</b></div></div><table><thead><tr><th>Datum</th><th>Referenz</th><th>Art</th><th>Anzahl</th><th>Bemerkung</th></tr></thead><tbody>'+rows+'</tbody></table><script>setTimeout(()=>window.print(),200)<\/script></body></html>');w.document.close();return false};

var RC824_SOP_DETAILS={"sop-sendung":{"purpose":"Eine Sendung vollständig, nachvollziehbar und versandbereit in ExportHUB anlegen. Alle Daten, Dokumente und Freigaben müssen vor der Abholung konsistent sein.","scope":"Für jede neu anzulegende Kunden- oder Speditionssendung – von der Auftragsprüfung bis zur Abholung/POD-Sicherung.","responsibility":"Versand / Export. Bei Drittland- oder Zollfragen zusätzlich Export/Zoll einbeziehen.","prerequisites":["Kunde und korrekter Empfänger/Standort sind bekannt.","Lieferscheine/DNCs liegen vollständig vor oder können eindeutig zugeordnet werden.","Packstücke, Maße, Gewicht und geplantes Abholdatum sind bekannt.","Spedition bzw. Versandweg und gegebenenfalls Zollanforderungen sind geklärt."],"steps":[{"title":"Auftrag und Versandbedarf prüfen","action":"Aufgabe, Kundenanfrage oder Versandauftrag öffnen. Kunde, Zielland, geplantes Abholdatum, Versandweg und besondere Hinweise vollständig lesen. Prüfen, ob es sich um EU, Drittland, Express, Stückgut oder Sondertransport handelt.","check":"Kunde, Zielland, Termin und Versandart sind eindeutig.","why":"Diese Angaben steuern Dokumente, Zollprüfung, Mailvorlage und weitere Prozessschritte.","warning":"Bei widersprüchlichen Angaben nicht mit Annahmen weiterarbeiten; zuerst klären."},{"title":"Kunde und Empfänger auswählen","action":"Im Bereich Kunde & Empfänger den Kunden über Kundennummer oder Namen auswählen. Danach den richtigen Standort wählen und die vollständige Empfängeradresse kontrollieren.","check":"Name, Straße, PLZ, Ort und Land entsprechen dem Versandauftrag.","why":"Die Empfängeradresse wird in Dokumenten, Anmeldung und Versandkommunikation verwendet.","warning":"Fehlt der Standort oder die Adr
```

### line~13240 pos=1881305
```text
state.view) || ['start','dashboard'].includes(state.view)) return;\\n  const banner=document.createElement('div');\\n  banner.className='rights-readonly-banner';\\n  banner.textContent='Lesemodus: F\\u00fcr diesen Bereich ist Lesen erlaubt, Bearbeiten aber nicht freigegeben.';\\n  content.prepend(banner);\\n  content.querySelectorAll('input,select,textarea').forEach(el=>{el.disabled=true;});\\n  content.querySelectorAll('button').forEach(btn=>{\\n    const t=(btn.textContent||'').toLowerCase();\\n    const allow=/drucken|pdf|kopieren|anzeigen|\\u00f6ffnen|zur\\u00fcck|weiter|suchen|filter|vorschau|cmr|ladeliste/.test(t);\\n    if(!allow) btn.disabled=true;\\n  });\\n}\\nfunction getLoginEl(id){return document.getElementById(id)}\\n\\n\\n/* PRIVATE RC52: feste DOM-Elementreferenzen statt Browser-ID-Globals */\\nvar login=getLoginEl('login');\\nvar app=getLoginEl('app');\\nvar content=getLoginEl('content');\\nvar nav=getLoginEl('nav');\\nvar loginUser=getLoginEl('loginUser');\\nvar loginPass=getLoginEl('loginPass');\\nvar logoutBtn=getLoginEl('logoutBtn');\\nvar printBtn=getLoginEl('printBtn');\\nvar backupBtn=getLoginEl('backupBtn');\\nvar importBtn=getLoginEl('importBtn');\\nvar importFile=getLoginEl('importFile');\\nvar globalSearch=getLoginEl('globalSearch');\\n\\n/* Benutzerrechte und Konten werden ausschlie\\u00dflich aus der autoritativen Server-Sitzung \\u00fcbernommen. */\\nconst langSel=document.getElementById('languageSelect'); if(langSel) langSel.onchange=e=>setLanguage(e.target.value);\\nconst loginLangSel=document.getElementById('loginLanguageSelect'); if(loginLangSel) loginLangSel.onchange=e=>setLanguage(e.target.value);\\n/* Login, Passwortwechsel und Logout werden ausschlie\\u00dflich durch clean-bootstrap/server-auth behandelt. */\\nprintBtn.onclick=()=>window.print();\\nbackupBtn.onclick=()=>download('exporthub_private_rc24_backup.json',JSON.stringify({state,users},null,2));\\nimportBtn.onclick=()=>importFile.click();\\nimportFile.onchange=async()=>{let f=importFile.files[0];if(!f)return;try{let b=JSON.parse(await f.text());if(b.state)state=b.state;if(b.users)users=b.users;save();saveUsers();render();alert('Import abgeschlossen.')}catch(e){alert('Import konnte nicht gelesen werden.')}};\\nglobalSearch.oninput=()=>{state.q=globalSearch.value;if(state.q.toLowerCase().includes('update exporthub'))state.view='update';render();restoreInputFocus('globalSearch')};\\nfunction download(name,text){const blob=new Blob([text],{type:'application/json;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;a.style.display='none';document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(url);a.remove();},1200)}\\n\\n/* PRIVATE RC46: SOP Editor / Upload / Create */\\nconst SOP_UPLOAD_STORE_KEY_RC46='exporthub_private_rc46_custom_sops';\\n\\nfunction rc46EnsureSopStore(){\\n  try{\\n    if(!state.customSops) state.customSops=[];\\n    const raw=localStorage.getItem(SOP_UPLOAD_STORE_KEY_RC46);\\n    const arr=raw?JSON.parse(raw):[];\\n    if(Array.isArray(arr)){\\n      for(const s of arr){\\n        if(s && s.id && !state.customSops.find(x=>x.id===s.id)) state.customSops.push(s);\\n      }\\n    }\\n  }catch(e){}\\n}\\nfunction rc46SaveSopStore(){\\n  try{\\n    localStorage.setItem(SOP_UPLOAD_STORE_KEY_RC46, JSON.stringify(state.customSops||[]));\\n  }catch(e){}\\n  save();\\n}\\nfunction rc46PortalUrl(p){\\n  if(!p) return '';\\n  if(typeof p==='string') return p;\\n  return p.url||p.link||p.href||'';\\n}\\nfunction rc46PortalName(p){\\n  if(!p) return 'Por
```

### line~14724 pos=2373073
```text
lletSettlements).find(function(s){return Q(s.id)===Q(id)});if(!x)return false;if(!confirm('Palettenabgleich stornieren und die zugehörige Korrekturbuchung deaktivieren?'))return false;x.status='Storniert';x.cancelledAt=new Date().toISOString();x.cancelledBy=Q((CU()||{}).name||(CU()||{}).user);if(x.adjustmentBookingId){var b=A(S().palletAccount).find(function(p){return Q(p.id)===Q(x.adjustmentBookingId)});if(b){b.status='Storniert';b.cancelledAt=x.cancelledAt;b.cancelledBy=x.cancelledBy}}save('Palettenabgleich storniert');renderPallet542(true);return false};
function rc542OpenReportWindow(title,body){var w=window.open('','_blank');if(!w){alert('Reportfenster wurde blockiert.');return null}w.document.write('<!doctype html><html lang="de"><head><meta charset="utf-8"><title>'+E(title)+'</title><style>@page{size:A4;margin:15mm}body{font-family:Segoe UI,Arial,sans-serif;color:#0f172a;padding:24px;max-width:1000px;margin:auto}h1{color:#08245d;margin-bottom:4px}.sub{color:#64748b;margin-bottom:20px}.summary{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:18px 0}.box{border:1px solid #cbd5e1;border-radius:12px;padding:12px}.box span{display:block;color:#64748b;font-size:12px;font-weight:700}.box strong{display:block;font-size:22px;margin-top:5px}table{width:100%;border-collapse:collapse;margin-top:18px}th,td{border:1px solid #94a3b8;padding:7px;text-align:left;font-size:12px}th{background:#eaf2ff;color:#08245d}.note{border-left:5px solid #2563eb;background:#eff6ff;padding:12px;margin:16px 0}.sign{display:grid;grid-template-columns:1fr 1fr;gap:40px;margin-top:50px}.line{border-top:1px solid #0f172a;padding-top:6px;font-size:12px}.no-print{margin-bottom:18px}@media print{body{padding:0}.no-print{display:none}}</style></head><body><button class="no-print" onclick="window.print()">Report drucken</button>'+body+'</body></html>');w.document.close();return w}
window.rc542PalletReport=function(){var e=parties().find(function(x){return x.party.key===Q(S().rc542PalletParty)});if(!e)return false;var b=balanceData(e),latest=A(S().palletSettlements).filter(function(x){return x.partyKey===e.party.key&&L(x.status)==='confirmed'}).sort(function(a,z){return String(z.reconciliationDate||z.confirmedAt).localeCompare(String(a.reconciliationDate||a.confirmedAt))})[0],rows=e.bookings.slice().sort(function(a,z){return String(isoDate(a.date||a.createdAt)).localeCompare(String(isoDate(z.date||z.createdAt)))}).map(function(x){return '<tr><td>'+E(deDate(x.date||x.createdAt))+'</td><td>'+E(x.shipmentRef||x.ref||'')+'</td><td>'+E(x.direction||x.type||'')+'</td><td>'+Math.abs(N(x.count||x.amount||x.quantity))+'</td><td>'+E(x.note||'')+'</td></tr>'}).join(''),latestHtml=latest?'<div class="note"><b>Letzter bestätigter Abgleich:</b> '+E(deDate(latest.reconciliationDate||latest.to))+' · Systemstand '+E(latest.systemBalance)+' · Ist-Stand '+E(latest.actualBalance)+' · Differenz '+E((Number(latest.difference)>0?'+':'')+latest.difference)+'</div>':'';rc542OpenReportWindow('Palettenkonto '+e.party.name,'<h1>Palettenkonto</h1><div class="sub">'+E(e.party.label+': '+e.party.name)+' · erstellt am '+E(new Date().toLocaleDateString('de-DE'))+'</div><div class="summary"><div class="box"><span>Eingang</span><strong>'+b.inTotal+'</strong></div><div class="box"><span>Ausgang</span><strong>'+b.outTotal+'</strong></div><div class="box"><span>Aktueller Systemstand</span><strong>'+b.net+'</strong></div></div><p><b>'+E(b.text)+'</b></p>'+latestHtml+'<table><thead><tr><th>Datum</th><th>Referenz</th><th>Richtung</th><th>Anzahl</th><th>Bemerkung</th></tr></thead><tbody>'+rows+'</
```

## print marker
needle='print('
count=22

### line~21216 pos=4249764
```text
{if(!c||!sh)return false;var map=templateMap(c),changed=false,id=customerId(c),types=['customer','carrier','own'],langs=['de','en'];types.forEach(function(type){langs.forEach(function(lang){var v=map[type][lang];if(!v)return;if(!sh.mailTemplates||typeof sh.mailTemplates!=='object'||Array.isArray(sh.mailTemplates)){sh.mailTemplates={};changed=true}if(!sh.mailTemplates[type]||typeof sh.mailTemplates[type]!=='object'||Array.isArray(sh.mailTemplates[type])){sh.mailTemplates[type]={};changed=true}if(q(sh.mailTemplates[type][lang])!==q(v)){sh.mailTemplates[type][lang]=v;changed=true}aliases[type][lang].forEach(function(key){if(q(sh[key])!==q(v)){sh[key]=v;changed=true}})})});var target=low(sh.rc543MailTarget||sh.rc542MailTarget||sh.rc524MailTarget||c.defaultMailTarget||c.mailDefaultTarget||c.standardMailTarget||c.mailRequirement||'customer');if(['customer','carrier','own'].indexOf(target)<0)target='customer';var lang=low(sh.rc543MailLang||sh.rc542MailLang||sh.rc524MailLang||c.defaultMailLanguage||c.mailDefaultLanguage||c.mailLang||'de')==='en'?'en':'de',body=q(map[target][lang])||q(map[target][lang==='de'?'en':'de'])||q(map.customer[lang]),linked=!!body;[['rc543MailTarget',target],['rc542MailTarget',target],['rc524MailTarget',target],['rc543MailLang',lang],['rc542MailLang',lang],['rc524MailLang',lang],['mailTemplateBody',body],['mailTemplateCustomerId',id],['mailProfileCustomerId',id],['mailProfileLinked',linked]].forEach(function(pair){if(sh[pair[0]]!==pair[1]){sh[pair[0]]=pair[1];changed=true}});return changed}
function applyMailLink(customer){var s=state(),c=customer||activeCustomer(),sh=s.shipment&&typeof s.shipment==='object'?s.shipment:null;if(!c||!sh)return false;return linkTemplates(c,sh)}
function printLoad2(){return window.ExportHUBIndex240?window.ExportHUBIndex240.print('load2'):false}
function downloadLoad2(){return window.ExportHUBIndex240?window.ExportHUBIndex240.download('load2'):false}
function patch(){tagColli();applyMailLink();return true}
window.addEventListener('exporthub:customer-selected',function(e){var d=e&&e.detail||{};try{if(d.customer)linkTemplates(d.customer,d.shipment||state().shipment||{});else applyMailLink()}catch(err){console.error('INDEX232 Mailvorlage',err)}});
function boot(){patch()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();

window.ExportHUBIndex232=Object.freeze({version:VERSION,patch:patch,tagColli:tagColli,linkMail:applyMailLink,printLoad2:printLoad2,downloadLoad2:downloadLoad2,selfTest:function(){var grid=document.querySelector('#rc573ColliCard .rc344-colli-grid[data-index232-grid="1"]'),fields=grid?grid.querySelectorAll('[data-index232-field]').length:0,c=activeCustomer(),m=c&&templateMap(c);return{version:VERSION,colliGrid:!!grid,colliFields:fields,packagingLeft:!!(grid&&grid.querySelector('[data-index232-field="type"]')),customer:customerId(c),mailTemplates:!!(m&&(m.customer.de||m.customer.en||m.carrier.de||m.carrier.en||m.own.de||m.own.en)),load2Actions:!!document.querySelector('.index232-load2-actions'),cache:window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.cache}}});
})();
</script>
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP END -->
<!-- INDEX 236 NOTIFICATION CENTER START -->
<style id="index236-notification-style">
#index236NotificationCenter{display:grid;gap:16px}
.index236-kicker{display:block;color:#075985;font-size:11px;font-weight:1000;letter-spacing:.08em;text-transform:uppercase;margin-bottom:6px}
.index236-statusbar{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}
.ind
```

### line~21454 pos=4302565
```text
f(!linkedShipment)return 0;if(d==='out')return euroPalletCount(linkedShipment);if(d==='in')return Math.max(0,Math.round(num(linkedShipment.returnedEuroPallets||linkedShipment.returnPallets||linkedShipment.palletIn||linkedShipment.euroPalletsIn)))}if(src==='shipment-pickup'||/^pal-(?:qr-)?(?:out|in)-/.test(bid))return 0;if(isManual)return explicit(b);if(d==='out')return 0;if(d==='adjust'){var delta=num(b&&(b.balanceChange||b.delta));if(delta)return Math.abs(Math.round(delta))}return explicit(b)}function sameParty(b){var key=low(b&&b.partyKey);if(key)return key===low(party.key);var carrier=low(b&&(b.partyName||b.carrier||b.spedition));if(party.key.indexOf('carrier:')===0&&carrier)return carrier===low(party.name);var values=[b&&b.customerId,b&&b.customerAccount,b&&b.customerNumber,b&&b.customerName,b&&b.customer].map(low).filter(Boolean);return values.some(function(v){return customerKeys.indexOf(v)>=0})}function sameShipment(b){return(id&&low(b&&b.shipmentId)===id)||(ref&&low(b&&(b.shipmentRef||b.ref))===ref)}palletEntries().forEach(function(b){var n=amount(b),d=direction(b);if(!n)return;if(sameParty(b)){if(d==='out')outs+=n;else if(d==='in')ins+=n}if(sameShipment(b)){if(d==='out')shipmentOut+=n;else if(d==='in')shipmentIn+=n}});var returned=Math.max(0,Math.round(num(sh&&(sh.returnedEuroPallets||sh.returnPallets||sh.palletIn||sh.euroPalletsIn)))),physical=euroPalletCount(sh),outgoing=Math.max(physical,shipmentOut),incoming=Math.max(returned,shipmentIn),net=outs-ins,abs=Math.abs(Math.round(net)),unit=abs===1?'Europalette':'Europaletten',wording=net>0?abs+' '+unit+' zugunsten Essentra':net<0?abs+' '+unit+' zugunsten '+party.label+' '+party.name:'0 Europaletten · ausgeglichen';return{incoming:incoming,outgoing:outgoing,balance:net,wording:wording,party:party}}
function fingerprint(sh){if(!sh)return'';return JSON.stringify({id:sid(sh),ref:sref(sh),customer:cname(sh),address:caddress(sh),country:country(sh),rows:rows(sh).map(function(r){return[rowType(r),rowCount(r),rowWeight(r),rowLdm(r),dimensions(r)]}),documents:documents(sh),carrier:q(sh.carrier||customerFor(sh).carrier||customerFor(sh).speditionName),goods:q(sh.goodsDescription),comments:q(sh.comments||sh.incoterm),pickup:q(sh.pickupDate||sh.plannedPickupDate||sh.createdAt)})}
function documents(sh){var out=[];function add(v){v=q(v);if(v&&!/^offen$/i.test(v)&&out.indexOf(v)<0)out.push(v)}arr(sh&&sh.deliveryFiles).concat(arr(sh&&sh.files)).forEach(function(f){add(f&&(f.name||f.fileName||f.filename)||f)});String(sh&&(sh.deliveryNotes||sh.dncs||sh.docs)||'').split(/[\n,;]+/).forEach(add);return out}
function dateDe(v){if(!q(v))return'';var d=new Date(v);if(!isNaN(d))return String(d.getDate()).padStart(2,'0')+'.'+String(d.getMonth()+1).padStart(2,'0')+'.'+d.getFullYear();return q(v)}
function logo(cls){return'<img class="'+cls+'" data-essentra-logo="1" src="data:image/png;base64,
```

### line~21466 pos=4322151
```text
576-cmr-extra rc601-cmr-extra')+'" data-shipment-ref="'+esc(sref(sh)||sid(sh))+'">'+html+'<div class="rc390-cmr-copy">CMR '+i+' / 3</div></div>'}return out}
function documentCacheKey(sh){var r=rows(sh),stamp=q(sh&&(sh.updatedAt||sh.modifiedAt||sh.lastModified||sh.createdAt)),parts=[sid(sh),sref(sh),stamp,cname(sh),caddress(sh),q(sh&&sh.goodsDescription),q(sh&&sh.pickupDate),q(sh&&sh.status),String(r.length),String(totals(sh).count),String(totals(sh).weight),String(totals(sh).ldm)];return parts.join('|')}
function documentHtml(sh){var key=documentCacheKey(sh),hit=docHtmlCache[key];if(hit)return hit;var html='<div class="rc390-docview" data-rc601-doc="load1" data-rc616-doc="load1"><div class="rc390-bundle">'+coverHtml(sh)+loadHtml(sh,true)+cmrHtml(sh)+'</div></div>';docHtmlCache=Object.create(null);docHtmlCache[key]=html;return html}
function options(selected){return'<option value="">Sendung auswählen</option>'+shipments().map(function(sh){var id=sid(sh)||sref(sh);return'<option value="'+esc(id)+'"'+(id===selected?' selected':'')+'>'+esc((sref(sh)||id)+' · '+(cname(sh)||'ohne Kunde')+' · '+status(sh))+'</option>'}).join('')}
function documentReturnContext(){try{var raw=sessionStorage.getItem('exporthub_document_return_v1'),ctx=raw&&JSON.parse(raw);return ctx&&low(ctx.view)==='shipmentview'?ctx:null}catch(_){return null}}
function hubHtml(selected,sh,deferDocument){var cmr=sh&&isForeign(sh),pod=sh&&window.ExportHUBPickupPOD&&window.ExportHUBPickupPOD.hasSignature(sh),returnCtx=documentReturnContext(),returnLabel=returnCtx&&q(returnCtx.reference)?'← Zurück zur Sendung '+q(returnCtx.reference):'Zur Sendung';return'<div id="rc576DocumentHub" data-index352-owner="'+VERSION+'" data-index352-shipment="'+esc(sh?(sid(sh)||sref(sh)):'')+'" data-index352-fingerprint="'+esc(sh?fingerprint(sh):'')+'"><section id="rc576DocumentHead"><div><h1>Ladeliste &amp; CMR</h1><p>Gespeicherte Sendung auswählen, drucken und als PDF herunterladen.</p><div id="index240DocumentStatus">'+shipments().length+' gespeicherte Sendung'+(shipments().length===1?'':'en')+' geladen</div></div><label class="field">Sendung auswählen<select id="rc576ShipmentSelect">'+options(selected||'')+'</select></label></section><div id="rc576DocumentSummary">'+(sh?'<div><span>Referenz</span><b>'+esc(sref(sh)||sid(sh))+'</b></div><div><span>Kunde</span><b>'+esc(cname(sh)||'–')+'</b></div><div><span>Status</span><b id="index352DocumentShipmentStatus">'+esc(status(sh))+'</b></div>':'')+'</div><div id="rc576DocumentActions"><button class="ghost" type="button" data-index352-action="shipment">'+esc(returnLabel)+'</button><button class="ghost" type="button" data-index352-action="overview">Zur Sendungsübersicht</button><button class="btn" type="button" data-index352-action="print-all">Gesamtausgabe drucken</button><button class="ghost" type="button" data-index352-action="download-all">Gesamtausgabe als PDF herunterladen</button><button class="ghost" type="button" data-index352-action="download-load1">Ladeliste 1 als PDF herunterladen</button><button class="ghost" type="button" data-index352-action="sync-pod">QR/POD-Status prüfen</button><button class="ghost" type="button" data-index352-action="download-pod"'+(pod?'':' disabled')+'>POD als PDF herunterladen</button></div><div id="index352DocumentMessage" role="status" aria-live="polite"></div><div id="rc576DocumentTabs"><button type="button" data-index352-doc="cover">Deckblatt</button><button type="button" data-index352-doc="load1" class="active">Ladeliste 1</button>'+(cmr?'<button type="button" data-index352-doc="cmr">CMR</button>':'')+'</div><div i
```

### line~21490 pos=4343108
```text
mise(function(resolve,reject){var settled=false,timer=later(function(){if(settled)return;settled=true;reject(new Error(message||'Zeitüberschreitung'))},Math.max(1000,Number(ms)||45000));Promise.resolve(promise).then(function(value){if(settled)return;settled=true;try{var clear=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.clearTimeout||window.clearTimeout;clear(timer)}catch(_){}resolve(value)},function(error){if(settled)return;settled=true;try{var clear=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.clearTimeout||window.clearTimeout;clear(timer)}catch(_){}reject(error)})})}
async function printDocuments(mode){if(mode==='load2'){alert('Ladeliste Seite 2 wurde entfernt. Bitte Ladeliste Seite 1 verwenden.');return false}if(printBusy)return false;var sh=currentSaved();if(!sh){alert('Bitte zuerst eine gespeicherte Sendung auswählen.');return false}printBusy=true;var frame=null,opToken=docWorkStart((mode==='all'?'Gesamtausgabe':'Ladeliste')+' wird vorbereitet …');try{docWorkStep(opToken,'Sendungsdaten werden geprüft …');await ensureOutputReady(sh,'normal');docWorkStep(opToken,'Druckseiten werden geöffnet …');var source=document.createElement('div');source.innerHTML=documentHtml(sh);decorateDocument(sh,source,'normal');frame=documentWorkFrame('print');var doc=resetDocumentWorkFrame(frame,(mode==='all'?'Gesamtausgabe':'Ladeliste')+' '+(sref(sh)||sid(sh))),root=clonePages(mode||'all',doc,source);doc.body.appendChild(root);decorateDocument(sh,doc,'normal');await waitImages(root,1500);window.__INDEX352_LAST_PRINT__={ok:true,mode:mode||'all',pages:root.children.length,ref:sref(sh)||sid(sh),owner:VERSION,signed:false};docWorkDone(opToken,'Druckdialog wird geöffnet');frame.contentWindow.focus();frame.contentWindow.print()}catch(e){console.error('RC679 Druck',e);window.__INDEX352_LAST_PRINT__={ok:false,mode:mode||'all',error:q(e&&e.message||e),owner:VERSION};docWorkFail(opToken,'Drucken konnte nicht gestartet werden');alert('Drucken konnte nicht gestartet werden.\n\n'+q(e&&e.message||e));if(frame&&frame===printFrameCache){try{frame.remove()}catch(_){}printFrameCache=null}}finally{printBusy=false}return false}

function bytesFromDataUrl(url){var raw=String(url).split(',')[1]||'',bin=atob(raw),out=new Uint8Array(bin.length);for(var i=0;i<bin.length;i++)out[i]=bin.charCodeAt(i);return out}
function enc(s){return new TextEncoder().encode(s)}
function concat(parts){var len=parts.reduce(function(sum,p){return sum+p.length},0),out=new Uint8Array(len),off=0;parts.forEach(function(p){out.set(p,off);off+=p.length});return out}
function multiPagePdf(images){var n=images.length,total=2+n*3,objs=new Array(total+1),kids=[];objs[1]=enc('<< /Type /Catalog /Pages 2 0 R >>');for(var i=0;i<n;i++){var page=3+i*3,content=page+1,image=page+2,k=images[i],pageW=595,pageH=842,margin=18,scale=Math.min((pageW-margin*2)/k.width,(pageH-margin*2)/k.height),dw=(k.width*scale).toFixed(2),dh=(k.height*scale).toFixed(2),x=((pageW-k.width*scale)/2).toFixed(2),y=((pageH-k.height*scale)/2).toFixed(2),stream='q '+dw+' 0 0 '+dh+' '+x+' '+y+' cm /Im0 Do Q\n';kids.push(page+' 0 R');objs[page]=enc('<< /Type /Page /Parent 2 0 R /MediaBox [0 0 '+pageW+' '+pageH+'] /Resources << /XObject << /Im0 '+image+' 0 R >> >> /Contents '+content+' 0 R >>');objs[content]=concat([enc('<< /Length '+stream.length+' >>\nstream\n'),enc(stream),enc('endstream')]);objs[image]=concat([enc('<< /Type /XObject /Subtype /Image /Width '+k.width+' /Height '+k.height+' /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length '+k.bytes.len
```

### line~23436 pos=4704242
```text
nd:#cbd5e1;color:#475569}.rc396-load-shape{display:grid;place-items:center;height:18px;width:100%}.rc396-pallet-shape{width:28px;height:12px;border-radius:3px;background:#a96832;box-shadow:0 3px 0 #7c4a22;position:relative}.rc396-pallet-shape:before,.rc396-pallet-shape:after{content:"";position:absolute;bottom:-3px;width:5px;height:3px;background:#7c4a22}.rc396-pallet-shape:before{left:5px}.rc396-pallet-shape:after{right:5px}.rc396-pallet-shape.high{height:18px;box-shadow:0 4px 0 #7c4a22}.rc396-box-shape{width:24px;height:14px;border:2px solid #0ea5e9;border-radius:3px;background:#cffafe;position:relative}.rc396-box-shape:after{content:"";position:absolute;left:50%;top:0;bottom:0;border-left:1px solid rgba(14,165,233,.35)}.rc396-slot-text{width:100%;text-align:center;font-size:7px;font-weight:950;line-height:1.05;color:#08245d;overflow:hidden;text-overflow:ellipsis}.rc396-slot-sub,.rc396-position{font-size:6px;font-weight:850;line-height:1.05;text-align:center}.rc396-position{color:#7c2d12;text-transform:uppercase}.rc396-form-badge{background:#dbeafe;color:#1d4ed8}.rc396-secure-badge{background:#dcfce7;color:#166534}.rc396-endnote{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px}.rc396-note{border:1px solid #fed7aa;border-radius:10px;background:#fff;padding:6px;font-size:8px;font-weight:900;color:#7c2d12;line-height:1.15}.rc396-note.center{text-align:center}.rc396-actions,button{display:none!important}</style></head><body><h1>Stauplan '+esc(refOf(shipment()))+'</h1>'+card.innerHTML+'</body></html>'


;var 
w=window.open('about:blank','_blank','width=1200,height=820');if(!w){alert('Das Druckfenster wurde vom Browser blockiert.');return false}try{w.opener=null;w.document.open();w.document.write(html);w.document.close();var run=function(){try{w.focus();w.print()}catch(e){console.error('Stauplan drucken',e)}};if(w.document.readyState==='complete')setTimeout(run,250);else w.addEventListener('load',function(){setTimeout(run,200)},{once:true});return false}catch(e){try{w.close()}catch(_){}alert('Der Stauplan konnte nicht geöffnet werden: '+q(e&&e.message||e));return false}}
function normalizeActionButtons(bar){var host=bar&&bar.querySelector('.rc363-actions-buttons');if(!host)return;['rc363SaveShipment','rc363OpenDocuments','rc363PrintAll'].forEach(function(id){var b=document.getElementById(id);if(b)host.appendChild(b)})}
function activateQr(saved){saved=saved||currentSaved();var api=window.ExportHUBPickupPOD;if(!saved||!api||typeof api.register!=='function'){updateQr();return Promise.resolve(false)}if(qrPromise)return qrPromise;function timeout(p,ms,label){return new Promise(function(resolve,reject){var done=false,t=setTimeout(function(){if(done)return;done=true;reject(new Error(label||'Zeitüberschreitung'))},ms);Promise.resolve(p).then(function(v){if(done)return;done=true;clearTimeout(t);resolve(v)},function(e){if(done)return;done=true;clearTimeout(t);reject(e)})})}updateQr();qrPromise=(async function(){try{if((saved.pickupToken||saved.pickupQrToken||saved.qrToken)&&typeof api.syncOne==='function'){try{await timeout(api.syncOne(saved,true),14000,'QR-Serverstatus konnte nicht rechtzeitig geprüft werden.')}catch(_){}if(saved.pickupQrRegistered===true)return true}var ok=await timeout(api.register(saved,false),28000,'QR-Aktivierung wurde nach 28 Sekunden beendet.');return !!ok}catch(e){try{saved.pickupQrActivating=false;saved.pickupQrActivationStartedAt='';saved.pickupQrError=q(e&&e.message||e)||'QR-Aktivierung nicht bestätigt.'}catch(_){}return false}})().then(function(ok){if(!ok){try{saved.pickupQrActivating=false;saved.pic
```

### line~23601 pos=4758239
```text
eigabe noch offen'}catch(_){}return qrOk===true}).catch(function(e){console.error('RC678 QR-Hintergrundfreigabe',e);try{var info=document.getElementById('saveInfo');if(info)info.textContent='Sendung gespeichert · QR-Freigabe wird später erneut versucht'}catch(_){}return false});window.__EXPORTHUB_BACKGROUND_QR_PROMISE__=backgroundQr}}else operationDone(opToken,'Sendung dauerhaft gespeichert');if(wasRepair){clearRepairMode();var lockedAfterRepair=currentSaved();if(lockedAfterRepair)lockShipmentForm(lockedAfterRepair,false)}else safePatchDuringEdit();return false}catch(e){console.error('RC679 Speichern',e);operationFail(opToken,saveConfirmed?'Sendung gespeichert · Nachbearbeitung läuft im Hintergrund':'Speichern fehlgeschlagen');throw e}finally{window.__EXPORTHUB_SHIPMENT_SAVE_TX__=false;window.__EXPORTHUB_SHIPMENT_WRITE_GRANT_UNTIL__=0;if(button){var finalLock=lockedShipment();if(finalLock){button.disabled=true;button.textContent='Bearbeitung gesperrt'}else{button.disabled=false;button.textContent=oldText||'Sendung speichern'}}}}
function selectSaved(){var s=state(),saved=currentSaved();if(!saved)return null;var id=idOf(saved);s.shipment=clone(saved);s.activeShipmentId=id;s.selectedShipmentId=id;s.documentShipmentId=id;s.currentShipmentId=id;return saved}
async function printAll(){syncRows();var saved=selectSaved();if(!saved){alert('Bitte die Sendung zuerst speichern.');return false}var api=window.ExportHUBIndex240;if(!api||typeof api.print!=='function'){alert('Die Gesamtdruck-Funktion ist nicht verfügbar.');return false}var button=document.getElementById('rc363PrintAll'),oldText=button&&button.textContent,opToken=operationStart('Gesamtausgabe wird vorbereitet …');if(button){button.disabled=true;button.textContent='Druck wird vorbereitet …'}try{await Promise.resolve(api.print('all'));var result=window.__INDEX352_LAST_PRINT__;if(result&&result.ok===false)operationFail(opToken,'Drucken konnte nicht gestartet werden');else if(!result)operationDone(opToken,'Druckauftrag verarbeitet');return false}catch(e){operationFail(opToken,'Drucken fehlgeschlagen');throw e}finally{if(button){button.disabled=false;button.textContent=oldText||'Gesamtausgabe drucken'}}}
async function openDocuments(){syncRows();var saved=selectSaved();if(!saved){alert('Bitte die Sendung zuerst speichern.');return false}var api=window.ExportHUBIndex240,nav=window.ExportHUBRC325;if((!nav||typeof nav.route!=='function')&&(!api||typeof api.openDocuments!=='function')){alert('Ladeliste & CMR ist nicht verfügbar.');return false}var button=document.getElementById('rc363OpenDocuments'),oldText=button&&button.textContent,opToken=operationStart('Ladeliste & CMR werden geladen …'),id=idOf(saved);if(button){button.disabled=true;button.textContent='Dokumente werden geladen …'}try{var st=state();st.documentShipmentId=id;st.rc565SelectedShipmentId=id;st.rc616SelectedShipmentId=id;if(nav&&typeof nav.route==='function')await Promise.resolve(nav.route('cmr','shipment-documents'));else await Promise.resolve(api.openDocuments(id));operationDone(opToken,'Ladeliste & CMR geöffnet');return false}catch(e){operationFail(opToken,'Dokumente konnten nicht geöffnet werden');throw e}finally{if(button){button.disabled=false;button.textContent=oldText||'Ladeliste & CMR öffnen'}}}

var CUSTOMER_ID_KEYS=['customerId','customerNo','customerNumber','customerAccount','kundennummer','customerCode'];
var CUSTOMER_NAME_KEYS=['customerName','customerDisplay','customerSearch'];
var CUSTOMER_OBJECT_KEYS=['customer','customerData','selectedCustomer'];
function customerIdValue(c){return q(c&&(c.id||c.customerId||c.a
```

### line~25020 pos=5058408
```text
s=state();s.documentShipmentId=id;try{sessionStorage.setItem('exporthub_document_return_v1',JSON.stringify({view:'shipmentview',shipmentId:id,reference:refOf(sh),at:Date.now()}))}catch(_){}if(typeof window.rc524OpenDocuments==='function')return window.rc524OpenDocuments(id);if(window.ExportHUBIndex240&&typeof window.ExportHUBIndex240.openDocuments==='function')return window.ExportHUBIndex240.openDocuments(id);return false}
function viewerPersist(){var v=viewerRead();return v&&v.persist?v.persist:v||null}
function viewerBack(){var v=viewerPersist(),s=state();viewerClearUrl();if(v){s.shipmentViewId=q(v.shipmentId||v.reference);try{sessionStorage.removeItem('exporthub_document_viewer_v1')}catch(_){}viewerMemory=null}if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentview','document-viewer-back');s.view='shipmentview';return render()}
function viewerCurrentDoc(){var v=viewerRead();if(!v)return null;return v.doc||v.persist&&v.persist.doc||v.persist||v}
async function viewerLoad(){var seq=++viewerLoadSeq,v=viewerPersist(),d=viewerCurrentDoc(),stage=document.getElementById('rc786DocumentStage');if(!v||!d||!stage)return false;try{var blob=await fetchDocBlob(Object.assign({},d,{reference:v.reference}));if(seq!==viewerLoadSeq||low(state().view)!=='documentviewer')return false;if(blob){viewerClearUrl();viewerObjectUrl=URL.createObjectURL(blob);stage.innerHTML='<iframe id="rc786DocumentFrame" title="'+esc(v.name||'Dokument')+'"></iframe>';var frame=document.getElementById('rc786DocumentFrame');frame.onload=function(){if(v.autoPrint===true){v.autoPrint=false;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){}setTimeout(function(){try{frame.contentWindow.focus();frame.contentWindow.print()}catch(e){console.warn('RC808 Drucken',e)}},180)}};frame.src=viewerObjectUrl;return false}if(d.url){stage.innerHTML='<iframe id="rc786DocumentFrame" src="'+esc(d.url)+'" title="'+esc(v.name||'Dokument')+'"></iframe>';return false}throw new Error('Für diese Datei ist kein vorhandener Dateiinhalt erreichbar.')}catch(e){if(seq!==viewerLoadSeq)return false;stage.innerHTML='<div class="rc786-viewer-error"><b>Datei konnte nicht geöffnet werden.</b><span>'+esc(q(e&&e.message||e))+'</span><button type="button" class="ghost" data-rc786-open-ref="1">Ref-Ordner öffnen</button></div>';return false}}
function renderViewer(){var v=viewerPersist(),d=viewerCurrentDoc(),root=document.getElementById('content');if(!root)return false;root.className='content rc786-document-viewer';root.setAttribute('data-exporthub-rendered-view','documentviewer');if(!v){root.innerHTML='<div class="rc786-viewer-shell"><div class="rc786-viewer-error"><b>Kein Dokument ausgewählt.</b><button type="button" class="btn" data-rc786-viewer-back="1">Zurück zur Sendung</button></div></div>';return false}var origin=v.source==='referenceFolder'?'Originaldatei aus Ref-Ordner':'Gespeicherte Datei aus der Sendung',meta=[q(v.documentType)||docType(v.name,''),origin,v.createdAt?fmtDate(v.createdAt):'',fmtSize(v.size)].filter(Boolean).join(' · ');root.innerHTML='<div class="rc786-viewer-shell"><header class="rc786-viewer-head"><div><button type="button" class="rc786-viewer-back" data-rc786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" 
```

### line~25023 pos=5060847
```text
ersist(),d=viewerCurrentDoc(),root=document.getElementById('content');if(!root)return false;root.className='content rc786-document-viewer';root.setAttribute('data-exporthub-rendered-view','documentviewer');if(!v){root.innerHTML='<div class="rc786-viewer-shell"><div class="rc786-viewer-error"><b>Kein Dokument ausgewählt.</b><button type="button" class="btn" data-rc786-viewer-back="1">Zurück zur Sendung</button></div></div>';return false}var origin=v.source==='referenceFolder'?'Originaldatei aus Ref-Ordner':'Gespeicherte Datei aus der Sendung',meta=[q(v.documentType)||docType(v.name,''),origin,v.createdAt?fmtDate(v.createdAt):'',fmtSize(v.size)].filter(Boolean).join(' · ');root.innerHTML='<div class="rc786-viewer-shell"><header class="rc786-viewer-head"><div><button type="button" class="rc786-viewer-back" data-rc786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" data-rc786-viewer-print="1">Drucken</button>':'')+'<button type="button" class="ghost" data-rc786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.contentWindow.focus();frame.contentWindow.print();return false}catch(_){}}var v=viewerPersist();if(v){v.autoPrint=true;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){} }viewerLoad();return false}
document.addEventListener('input',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('search',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('keydown',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch'&&e.key==='Enter'){e.preventDefault();return renderSearchResults(input.value)}},true);
document.addEventListener('click',function(e){var clearSearch=e.target&&e.target.closest&&e.target.closest('[data-rc911-search-clear]');if(clearSearch){e.preventDefault();shipmentSearchQuery='';var input=document.getElementById('rc807ShipmentSearch');if(input)input.value='';return renderSearchResults('')}var openSearch=e.target&&e.target.closest&&e.target.closest('[data-rc807-open-shipment]');if(openSearch){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();return open(openSearch.getAttribute('data-rc807-open-shipment'),'shipmentsearch')}var b=e.target&&e.target.closest&&e.target.closest('[data-rc776-back],[data-rc784-overview],[data-rc786-open-file],[data-rc786-download-file],[data-rc786-print-file],[data-rc776-documents],[data-rc786-open-ref],[data-rc786-viewer-back],[data-rc786-viewer-download],[data-rc786-viewer-print]');if(!b)return;if(b.hasAttribute('data-rc776-back')){e.preventDefault();return back()}if(b.hasAttribute('data-rc784-overview')){e.preventDefault();return overview()}if(b.hasAttribute('data-rc786-open-file')){e.preventDefault();return openDoc(b.ge
```

## PDF marker
needle='PDF'
count=203

### line~21629 pos=4379796
```text
ase64(buffer){var bytes=new Uint8Array(buffer),chunk=0x8000,out='';for(var i=0;i<bytes.length;i+=chunk)out+=String.fromCharCode.apply(null,bytes.subarray(i,Math.min(i+chunk,bytes.length)));return btoa(out)}
async function assertOfficialPdf(blob){if(!blob||blob.size<1000)throw new Error('POD-PDF ist leer.');var h=new Uint8Array(await blob.slice(0,5).arrayBuffer());if(h.length<5||h[0]!==0x25||h[1]!==0x50||h[2]!==0x44||h[3]!==0x46||h[4]!==0x2d)throw new Error('POD-Notfallsicherung akzeptiert nur die vollständige Ladeliste als PDF.');return true}
async function post(payload){var controller=typeof AbortController==='function'?new AbortController():null,timer=controller?setTimeout(function(){controller.abort()},90000):0,response;try{response=await fetch(ENDPOINT,{method:'POST',credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(payload),signal:controller&&controller.signal})}finally{if(timer)clearTimeout(timer)}var text='',body={};try{text=await response.text();body=text?JSON.parse(text):{}}catch(_){body={}}if(!response.ok){var e=new Error(body.message||body.error||('HTTP '+response.status));e.status=response.status;e.code=body.code||'';throw e}return body}
async function backupBlob(sh,out,options){options=options||{};if(!sh||!out||!out.blob)throw new Error('POD-PDF fehlt.');var k=key(sh);if(!k)throw new Error('Sendungsreferenz fehlt.');if(inflight[k])return inflight[k];if(saved(sh)&&!options.force)return{saved:true,alreadySaved:true,fileName:q(sh.podCloudBackupFileName),webUrl:q(sh.podCloudBackupWebUrl)};if(!retryAllowed(sh,options.force))return{saved:false,deferred:true};inflight[k]=(async function(){var blob=out.blob;await assertOfficialPdf(blob);if(blob.size>20*1024*1024)throw new Error('POD-PDF ist für die automatische Sicherung zu groß.');patch(sh,{podCloudBackupStatus:'uploading',podCloudBackupError:'',podCloudBackupAttemptAt:new Date().toISOString()});try{if(window.ExportHUBPODVault&&typeof window.ExportHUBPODVault.store==='function')await window.ExportHUBPODVault.store(sh,blob,q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf'),{pages:out.pages,documentKind:'signed-loadlist'})}catch(vaultError){console.warn('Lokale POD-Notfallsicherung',ref(sh),vaultError)}var pdfBase64=bytesBase64(await blob.arrayBuffer()),result=await post({token:token(sh),reference:ref(sh),shipmentId:sid(sh),confirmedAt:confirmedAt(sh),fileName:q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf'),pdfBase64:pdfBase64,source:q(options.source)||'automatic'});var values={podCloudBackupStatus:'saved',podCloudBackupAt:q(result.savedAt)||new Date().toISOString(),podCloudBackupFileName:q(result.fileName),podCloudBackupWebUrl:q(result.webUrl),podCloudBackupDriveItemId:q(result.driveItemId),podCloudBackupHash:q(result.hash),podCloudBackupError:'',podCloudBackupNextRetryAt:0};patch(sh,values);try{if(window.ExportHUBPODVault&&typeof window.ExportHUBPODVault.store==='function')await window.ExportHUBPODVault.store(sh,blob,q(out.name)||values.podCloudBackupFileName,{webUrl:values.podCloudBackupWebUrl,cloudFileName:values.podCloudBackupFileName,pages:out.pages,documentKind:'signed-loadlist'})}catch(_){}var persisted=save('POD automatisch in SharePoint gesichert');if(persisted&&typeof persisted.then==='function')try{await persisted}catch(_){}try{window.dispatchEvent(new CustomEvent('exporthub:pod-cloud-backup',{detail:{reference:ref(sh),saved:true,fileName:values.podCloudBackupFileName,webUrl:values.podCloudBackupWebUrl}}))}catch(_){}return Object.assign({saved:true},result)})
```

### line~24029 pos=4849472
```text
ss="muted">Alle echten PODs werden für den gewählten Zeitraum gemeinsam gesichert. Manuell hochgeladene PODs werden als POD_&lt;Referenz&gt;.pdf übernommen; digitale PODs bleiben vollständige unterschriebene Ladelisten.</p></div><button type="button" class="ghost rc422-pod-close" data-rc422-close aria-label="Schließen">×</button></div><div class="rc422-pod-grid"><label class="field">Von<input id="rc422PodFrom" type="text" inputmode="numeric" maxlength="10" placeholder="TT.MM.JJJJ" data-date-format="TT.MM.JJJJ"></label><label class="field">Bis<input id="rc422PodTo" type="text" inputmode="numeric" maxlength="10" placeholder="TT.MM.JJJJ" data-date-format="TT.MM.JJJJ"></label></div><div class="rc422-pod-summary"><div><span>Sendungen mit POD</span><strong id="rc422PodShipments">0</strong></div><div><span>POD-Dateien</span><strong id="rc422PodFiles">0</strong></div><div><span>Zeitraum</span><strong id="rc422PodPeriod" style="font-size:15px">–</strong></div></div><div class="rc551-pod-emergency"><div class="rc551-pod-emergency-grid"><div><span>SharePoint-Notfallsicherung</span><strong id="rc551PodCloud">Prüfung …</strong></div><div><span>Lokaler POD-Vault</span><strong id="rc551PodVault">Prüfung …</strong></div><div><span>Notfallordner auf diesem PC</span><strong id="rc551PodFolder">Nicht verbunden</strong></div></div><div class="rc551-pod-emergency-actions"><button type="button" class="ghost" id="rc551PodConnectFolder">Notfallordner verbinden</button><button type="button" class="ghost" id="rc551PodMirrorNow">Notfallsicherung jetzt prüfen</button><button type="button" class="ghost" id="rc551PodEmergencyPage">POD-Notfallseite öffnen</button></div><small>SharePoint ist die unabhängige Cloud-Sicherung. Der Notfallordner enthält vollständige unterschriebene Ladelisten als normale PDF-Dateien. Diese lassen sich bei einem ExportHUB-Ausfall direkt im Windows Explorer öffnen – die Website wird dafür nicht benötigt.</small></div><div id="rc422PodStatus" class="rc422-pod-status">Zeitraum auswählen.</div><div class="rc422-pod-progress"><i id="rc422PodProgress"></i></div><div class="rc422-pod-actions"><button type="button" class="ghost" data-rc422-close>Abbrechen</button><button type="button" class="btn" id="rc422PodDownload">POD-Sicherung herunterladen</button></div></section>';
 document.body.appendChild(m);
 m.addEventListener('click',function(e){if(e.target===m||e.target.closest('[data-rc422-close]'))close()});
 ['rc422PodFrom','rc422PodTo'].forEach(function(id){var el=m.querySelector('#'+id);el.addEventListener('input',function(){var masked=maskDate(el.value);if(el.value!==masked){el.value=masked;try{el.setSelectionRange(masked.length,masked.length)}catch(_){}}update()});el.addEventListener('change',update)});
 m.querySelector('#rc422PodDownload').addEventListener('click',downloadAll);
 m.querySelector('#rc551PodConnectFolder').addEventListener('click',async function(){var v=window.ExportHUBPODVault;try{if(!v)throw new Error('POD-Notfallvault nicht verfügbar');await v.connectFolder();setStatus('Notfallordner verbunden. Vorhandene PODs werden jetzt gespiegelt.','ok');await v.mirror(true);refreshEmergencyStatus()}catch(e){setStatus(q(e&&e.message||e),'bad')} });
 m.querySelector('#rc551PodMirrorNow').addEventListener('click',async function(){var v=window.ExportHUBPODVault,c=window.ExportHUBPODCloudBackup;try{setStatus('SharePoint und lokale Notfallsicherung werden geprüft …','');await syncManualCloudUploads(true);if(c&&typeof c.scan==='function')await c.scan(true);if(v&&typeof v.mirror==='function')await v.mirror(true);refreshEmergencyStatus();setSta
```

### line~24044 pos=4852745
```text
c function(){var v=window.ExportHUBPODVault,c=window.ExportHUBPODCloudBackup;try{setStatus('SharePoint und lokale Notfallsicherung werden geprüft …','');await syncManualCloudUploads(true);if(c&&typeof c.scan==='function')await c.scan(true);if(v&&typeof v.mirror==='function')await v.mirror(true);refreshEmergencyStatus();setStatus('POD-Notfallsicherung wurde geprüft und nachgesichert.','ok')}catch(e){setStatus('Notfallsicherung konnte nicht vollständig abgeschlossen werden: '+q(e&&e.message||e),'bad')} });
 m.querySelector('#rc551PodEmergencyPage').addEventListener('click',function(){window.open(podEmergencyPageUrl(),'_blank','noopener')});
 return m
}
function localISO(d){d=d||new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
function dateDefaults(){var now=new Date(),from=new Date(now.getFullYear(),now.getMonth(),1);return{from:localISO(from),to:localISO(now)}}
function setStatus(text,kind){var s=modal().querySelector('#rc422PodStatus');s.className='rc422-pod-status'+(kind?' '+kind:'');s.textContent=text}
async function refreshEmergencyStatus(){
 var m=document.getElementById('rc422PodBackupModal');if(!m)return false;var signed=filtered('',''),cloud=signed.filter(function(sh){return low(sh&&sh.podCloudBackupStatus)==='saved'&&!!q(sh&&sh.podCloudBackupFileName)}).length,vault=window.ExportHUBPODVault;var cloudEl=m.querySelector('#rc551PodCloud'),vaultEl=m.querySelector('#rc551PodVault'),folderEl=m.querySelector('#rc551PodFolder');if(cloudEl){cloudEl.textContent=cloud+' / '+signed.length+' PODs';cloudEl.style.color=cloud===signed.length&&signed.length?'#166534':(signed.length?'#991b1b':'')}
 if(vault){try{var count=await vault.count(),folder=await vault.folderInfo();if(vaultEl)vaultEl.textContent=count+' PDF'+(count===1?'':'s');if(folderEl){folderEl.textContent=folder.connected?(folder.name+(folder.writable?' · aktiv':' · Freigabe nötig')):'Nicht verbunden';folderEl.style.color=folder.connected&&folder.writable?'#166534':''}}catch(e){if(vaultEl)vaultEl.textContent='Fehler';if(folderEl)folderEl.textContent='Nicht verfügbar'}}else{if(vaultEl)vaultEl.textContent='Nicht verfügbar'}return true
}
function update(extra){
 var x=selection(),m=modal(),status=m.querySelector('#rc422PodStatus');
 m.querySelector('#rc422PodShipments').textContent=x.list.length;
 m.querySelector('#rc422PodFiles').textContent=x.files||0;
 m.querySelector('#rc422PodPeriod').textContent=(x.from?displayDate(x.from):'Anfang')+' bis '+(x.to?displayDate(x.to):'Heute');
 m.querySelector('#rc422PodDownload').disabled=!!x.error||!x.jobs.length||!!syncPromise;
 if(x.error){status.className='rc422-pod-status bad';status.textContent=x.error}
 else if(extra){status.className='rc422-pod-status '+(extra.kind||'');status.textContent=extra.text}
 else if(!x.list.length){status.className='rc422-pod-status';status.textContent='Im gewählten Zeitraum wurden keine echten PODs gefunden.'}
 else if(!x.jobs.length){status.className='rc422-pod-status bad';status.textContent='PODs wurden erkannt, aber es ist keine herunterladbare POD-Datei vorhanden. Bitte QR/POD-Status erneut synchronisieren.'}
 else{status.className='rc422-pod-status ok';status.textContent=x.list.length+' Sendung'+(x.list.length===1?'':'en')+' mit '+x.files+' POD-Datei'+(x.files===1?'':'en')+' bereit.'}
 try{localStorage.setItem('exporthub-pod-backup-from',x.from||'');localStorage.setItem('exporthub-pod-backup-to',x.to||'')}catch(_){}
 setTimeout(refreshEmergencyStatus,0);
 return x
}
async function syncManualCloudUploads(force){var cloud=window.ExportHUBPOD
```

### line~24102 pos=4860650
```text
{try{var r=await fetch(urls[i],{credentials:'same-origin',cache:'no-store',headers:{Accept:'image/png,image/jpeg,image/webp'}});if(!r.ok)continue;var bytes=new Uint8Array(await r.arrayBuffer()),ext=fileFormat(bytes);if(!/^(png|jpg|webp)$/.test(ext))continue;var mime=ext==='jpg'?'image/jpeg':'image/'+ext;if(applyInlineSignature(sh,'data:'+mime+';base64,'+bytesBase64(bytes)))return true}catch(_){}}
 return false
}
async function load(src){
 var bytes,ct='';
 if(/^data:/i.test(src.url))bytes=dataUrlBytes(src.url);
 else{var r=await fetch(src.url,{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/pdf,image/png,image/jpeg,image/webp,*/*'}});ct=low(r.headers&&r.headers.get&&r.headers.get('content-type'));if(!r.ok)throw new Error('HTTP '+r.status);bytes=new Uint8Array(await r.arrayBuffer());if(/application\/json|text\/html|text\/plain/.test(ct)&&!/application\/pdf/.test(ct))throw new Error('Server lieferte keine POD-Datei ('+(ct||'unbekannter Inhalt')+')')}
 if(!bytes||!bytes.length)throw new Error('Leere Datei');
 var ext=fileFormat(bytes);if(!ext)throw new Error('Ungültige oder nicht lesbare POD-Datei');
 return{bytes:bytes,ext:ext}
}
function qrSource(src){return /qr|pickup|signed-loadlist|unterschrieben|ladeliste.*unterschrift/.test(low([src&&src.kind,src&&src.name,src&&src.url].join(' ')))}
async function localSignedPod(sh){
 if(!await ensureInlineSignature(sh))throw new Error('Digitale Unterschrift ist nicht als Bild verfügbar');
 var api=window.ExportHUBIndex240;if(!api||typeof api.createPodBlob!=='function')throw new Error('Lokale POD-Erstellung ist nicht verfügbar');
 var out=await api.createPodBlob(sh),bytes=new Uint8Array(await out.blob.arrayBuffer()),ext=fileFormat(bytes);
 if(ext!=='pdf')throw new Error('Lokal erzeugter POD ist keine gültige PDF-Datei');
 return{bytes:bytes,ext:'pdf',name:q(out.name)||('POD_'+ref(sh)+'_Ladeliste_mit_Unterschrift.pdf')}
}
var crcTable=null;
function crc32(data){if(!crcTable){crcTable=new Uint32Array(256);for(var n=0;n<256;n++){var c=n;for(var k=0;k<8;k++)c=(c&1)?0xedb88320^(c>>>1):c>>>1;crcTable[n]=c>>>0}}var c=0xffffffff;for(var i=0;i<data.length;i++)c=crcTable[(c^data[i])&255]^(c>>>8);return(c^0xffffffff)>>>0}
function u16(n){return new Uint8Array([n&255,(n>>>8)&255])}
function u32(n){return new Uint8Array([n&255,(n>>>8)&255,(n>>>16)&255,(n>>>24)&255])}
function cat(parts){var len=parts.reduce(function(a,b){return a+b.length},0),out=new Uint8Array(len),p=0;parts.forEach(function(b){out.set(b,p);p+=b.length});return out}
function zip(entries){var local=[],central=[],offset=0,enc=new TextEncoder();entries.forEach(function(e){var name=enc.encode(e.name),data=e.data,crc=crc32(data),head=cat([u32(0x04034b50),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name]),block=cat([head,data]);local.push(block);central.push(cat([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]));offset+=block.length});var center=cat(central),body=cat(local),end=cat([u32(0x06054b50),u16(0),u16(0),u16(entries.length),u16(entries.length),u32(center.length),u32(body.length),u16(0)]);return new Blob([body,center,end],{type:'application/zip'})}
function saveBlob(blob,name){var u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u)},60000)}
function extension(src){var n=q(src.name),m=/\.(pdf|png|jpe?g|we
```

### line~24121 pos=4863360
```text
50),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name]),block=cat([head,data]);local.push(block);central.push(cat([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]));offset+=block.length});var center=cat(central),body=cat(local),end=cat([u32(0x06054b50),u16(0),u16(0),u16(entries.length),u16(entries.length),u32(center.length),u32(body.length),u16(0)]);return new Blob([body,center,end],{type:'application/zip'})}
function saveBlob(blob,name){var u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u)},60000)}
function extension(src){var n=q(src.name),m=/\.(pdf|png|jpe?g|webp)(?:$|[?#])/i.exec(n||src.url),ext=m?m[1].toLowerCase():'pdf';return ext==='jpeg'?'jpg':ext}
async function downloadAll(){
 if(syncPromise)await syncPromise;
 await syncServer(false);
 var x=update(),m=modal(),btn=m.querySelector('#rc422PodDownload'),status=m.querySelector('#rc422PodStatus'),bar=m.querySelector('#rc422PodProgress');
 if(x.error||!x.list.length)return false;
 btn.disabled=true;bar.style.width='0%';var entries=[],fail=[];
 for(var i=0;i<x.jobs.length;i++){
  var job=x.jobs[i],sh=job.sh,src=job.src,isManual=src&&src.kind==='manual-uploaded-pod';status.className='rc422-pod-status';status.textContent=(isManual?'Hochgeladener POD ':'Unterschriebene Ladeliste ')+(i+1)+' von '+x.jobs.length+' wird gesichert: '+ref(sh);bar.style.width=Math.round((i/x.jobs.length)*100)+'%';
  try{if(isManual){var loaded=await load(src);if(loaded.ext!=='pdf')throw new Error('Der hochgeladene POD ist keine PDF-Datei');entries.push({name:src.name,data:loaded.bytes})}else{var generated=await localSignedPod(sh),base='POD_'+safe(ref(sh))+'_Ladeliste_mit_Unterschrift';entries.push({name:safe(podDate(sh)+'_'+base)+'.pdf',data:generated.bytes})}}
  catch(error){fail.push(ref(sh)+' · '+q(error&&error.message||error))}
 }
 bar.style.width='100%';
 if(!entries.length){status.className='rc422-pod-status bad';status.textContent='Keine vollständige unterschriebene Ladeliste konnte als PDF erzeugt werden. '+(fail[0]||'POD-Daten prüfen.');btn.disabled=false;return false}
 var period=(x.from?displayDate(x.from):'Anfang')+' bis '+(x.to?displayDate(x.to):'Heute');
 var manualCount=x.jobs.filter(function(j){return j.src&&j.src.kind==='manual-uploaded-pod'}).length,signedCount=x.jobs.length-manualCount,manifest=['ExportHUB POD-Sicherung','Version: '+VERSION,'Zeitraum: '+period,'Erstellt: '+new Date().toLocaleString('de-DE'),'Gesicherte POD-Dateien: '+entries.length,'Davon manuell hochgeladen: '+manualCount,'Davon digital unterschriebene Ladelisten: '+signedCount,'Fehler: '+fail.length,'','Manuelle Uploads: POD_<Referenz>.pdf','Digitale PODs: vollständige Ladeliste mit Fahrerunterschrift',fail.length?'Nicht gesichert\n'+fail.join('\n'):'Alle verfügbaren POD-Dateien wurden im PDF-Format gesichert.'].join('\n');
 entries.push({name:'POD_Sicherung_Protokoll.txt',data:new TextEncoder().encode(manifest)});
 var archive=zip(entries);if(!archive||archive.size<100)throw new Error('ZIP-Datei konnte nicht korrekt erstellt werden');
 saveBlob(archive,'ExportHUB_POD_Ladelisten_'+(x.from||'Anfang')+'_bis_'+(x.to||'Heute')+'.zip');
 status.className=fail.length?'rc422-pod-status bad':'rc422-pod-status ok';status.textContent=(entries.length-1)+' POD-Datei'+(entries.length-1===1?'':'en')+' wurden gemeinsam i
```

### line~24125 pos=4863835
```text
gth),u16(entries.length),u32(center.length),u32(body.length),u16(0)]);return new Blob([body,center,end],{type:'application/zip'})}
function saveBlob(blob,name){var u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u)},60000)}
function extension(src){var n=q(src.name),m=/\.(pdf|png|jpe?g|webp)(?:$|[?#])/i.exec(n||src.url),ext=m?m[1].toLowerCase():'pdf';return ext==='jpeg'?'jpg':ext}
async function downloadAll(){
 if(syncPromise)await syncPromise;
 await syncServer(false);
 var x=update(),m=modal(),btn=m.querySelector('#rc422PodDownload'),status=m.querySelector('#rc422PodStatus'),bar=m.querySelector('#rc422PodProgress');
 if(x.error||!x.list.length)return false;
 btn.disabled=true;bar.style.width='0%';var entries=[],fail=[];
 for(var i=0;i<x.jobs.length;i++){
  var job=x.jobs[i],sh=job.sh,src=job.src,isManual=src&&src.kind==='manual-uploaded-pod';status.className='rc422-pod-status';status.textContent=(isManual?'Hochgeladener POD ':'Unterschriebene Ladeliste ')+(i+1)+' von '+x.jobs.length+' wird gesichert: '+ref(sh);bar.style.width=Math.round((i/x.jobs.length)*100)+'%';
  try{if(isManual){var loaded=await load(src);if(loaded.ext!=='pdf')throw new Error('Der hochgeladene POD ist keine PDF-Datei');entries.push({name:src.name,data:loaded.bytes})}else{var generated=await localSignedPod(sh),base='POD_'+safe(ref(sh))+'_Ladeliste_mit_Unterschrift';entries.push({name:safe(podDate(sh)+'_'+base)+'.pdf',data:generated.bytes})}}
  catch(error){fail.push(ref(sh)+' · '+q(error&&error.message||error))}
 }
 bar.style.width='100%';
 if(!entries.length){status.className='rc422-pod-status bad';status.textContent='Keine vollständige unterschriebene Ladeliste konnte als PDF erzeugt werden. '+(fail[0]||'POD-Daten prüfen.');btn.disabled=false;return false}
 var period=(x.from?displayDate(x.from):'Anfang')+' bis '+(x.to?displayDate(x.to):'Heute');
 var manualCount=x.jobs.filter(function(j){return j.src&&j.src.kind==='manual-uploaded-pod'}).length,signedCount=x.jobs.length-manualCount,manifest=['ExportHUB POD-Sicherung','Version: '+VERSION,'Zeitraum: '+period,'Erstellt: '+new Date().toLocaleString('de-DE'),'Gesicherte POD-Dateien: '+entries.length,'Davon manuell hochgeladen: '+manualCount,'Davon digital unterschriebene Ladelisten: '+signedCount,'Fehler: '+fail.length,'','Manuelle Uploads: POD_<Referenz>.pdf','Digitale PODs: vollständige Ladeliste mit Fahrerunterschrift',fail.length?'Nicht gesichert\n'+fail.join('\n'):'Alle verfügbaren POD-Dateien wurden im PDF-Format gesichert.'].join('\n');
 entries.push({name:'POD_Sicherung_Protokoll.txt',data:new TextEncoder().encode(manifest)});
 var archive=zip(entries);if(!archive||archive.size<100)throw new Error('ZIP-Datei konnte nicht korrekt erstellt werden');
 saveBlob(archive,'ExportHUB_POD_Ladelisten_'+(x.from||'Anfang')+'_bis_'+(x.to||'Heute')+'.zip');
 status.className=fail.length?'rc422-pod-status bad':'rc422-pod-status ok';status.textContent=(entries.length-1)+' POD-Datei'+(entries.length-1===1?'':'en')+' wurden gemeinsam im ZIP gesichert.'+(fail.length?' Nicht gesichert: '+fail.length+'. Details stehen im Protokoll.':'');btn.disabled=false;return true
}
document.addEventListener('click',function(e){var b=e.target&&e.target.closest&&e.target.closest('#podBackupBtn');if(!b)return;e.preventDefault();e.stopPropagation();open()},true);
document.addEventListener('keydown',function(e){if(e.key==='Escape')close()},true);
window.ExportHUBPODBackup=Object.freeze({version:VERSION,open:open,close:clo
```

### line~24127 pos=4864633
```text
.disabled=true;bar.style.width='0%';var entries=[],fail=[];
 for(var i=0;i<x.jobs.length;i++){
  var job=x.jobs[i],sh=job.sh,src=job.src,isManual=src&&src.kind==='manual-uploaded-pod';status.className='rc422-pod-status';status.textContent=(isManual?'Hochgeladener POD ':'Unterschriebene Ladeliste ')+(i+1)+' von '+x.jobs.length+' wird gesichert: '+ref(sh);bar.style.width=Math.round((i/x.jobs.length)*100)+'%';
  try{if(isManual){var loaded=await load(src);if(loaded.ext!=='pdf')throw new Error('Der hochgeladene POD ist keine PDF-Datei');entries.push({name:src.name,data:loaded.bytes})}else{var generated=await localSignedPod(sh),base='POD_'+safe(ref(sh))+'_Ladeliste_mit_Unterschrift';entries.push({name:safe(podDate(sh)+'_'+base)+'.pdf',data:generated.bytes})}}
  catch(error){fail.push(ref(sh)+' · '+q(error&&error.message||error))}
 }
 bar.style.width='100%';
 if(!entries.length){status.className='rc422-pod-status bad';status.textContent='Keine vollständige unterschriebene Ladeliste konnte als PDF erzeugt werden. '+(fail[0]||'POD-Daten prüfen.');btn.disabled=false;return false}
 var period=(x.from?displayDate(x.from):'Anfang')+' bis '+(x.to?displayDate(x.to):'Heute');
 var manualCount=x.jobs.filter(function(j){return j.src&&j.src.kind==='manual-uploaded-pod'}).length,signedCount=x.jobs.length-manualCount,manifest=['ExportHUB POD-Sicherung','Version: '+VERSION,'Zeitraum: '+period,'Erstellt: '+new Date().toLocaleString('de-DE'),'Gesicherte POD-Dateien: '+entries.length,'Davon manuell hochgeladen: '+manualCount,'Davon digital unterschriebene Ladelisten: '+signedCount,'Fehler: '+fail.length,'','Manuelle Uploads: POD_<Referenz>.pdf','Digitale PODs: vollständige Ladeliste mit Fahrerunterschrift',fail.length?'Nicht gesichert\n'+fail.join('\n'):'Alle verfügbaren POD-Dateien wurden im PDF-Format gesichert.'].join('\n');
 entries.push({name:'POD_Sicherung_Protokoll.txt',data:new TextEncoder().encode(manifest)});
 var archive=zip(entries);if(!archive||archive.size<100)throw new Error('ZIP-Datei konnte nicht korrekt erstellt werden');
 saveBlob(archive,'ExportHUB_POD_Ladelisten_'+(x.from||'Anfang')+'_bis_'+(x.to||'Heute')+'.zip');
 status.className=fail.length?'rc422-pod-status bad':'rc422-pod-status ok';status.textContent=(entries.length-1)+' POD-Datei'+(entries.length-1===1?'':'en')+' wurden gemeinsam im ZIP gesichert.'+(fail.length?' Nicht gesichert: '+fail.length+'. Details stehen im Protokoll.':'');btn.disabled=false;return true
}
document.addEventListener('click',function(e){var b=e.target&&e.target.closest&&e.target.closest('#podBackupBtn');if(!b)return;e.preventDefault();e.stopPropagation();open()},true);
document.addEventListener('keydown',function(e){if(e.key==='Escape')close()},true);
window.ExportHUBPODBackup=Object.freeze({version:VERSION,open:open,close:close,filter:filtered,refresh:function(){return syncServer(true)},download:downloadAll,selfTest:function(){var d=dateDefaults(),list=filtered(d.from,d.to),jobs=[];list.forEach(function(sh){jobs=jobs.concat(sourceList(sh))});return{periodSelection:true,mergedShipments:allShipments().length,signedShipments:list.length,files:jobs.length,azureSync:!!(window.ExportHUBPickupPOD&&typeof window.ExportHUBPickupPOD.syncAll==='function'),zip:true,manualUploadedPodSource:true,manualUploadedPodName:'POD_<REF>.pdf',dateFormat:'TT.MM.JJJJ',cloudBackup:!!window.ExportHUBPODCloudBackup,emergencyVault:!!window.ExportHUBPODVault,emergencyPage:'/pod-notfall.html'}}})
})();

</script>

<script id="rc430-canonical-theme-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_RC430_THEME__)return;
window.__
```

### line~25021 pos=5060494
```text
ener Dateiinhalt erreichbar.')}catch(e){if(seq!==viewerLoadSeq)return false;stage.innerHTML='<div class="rc786-viewer-error"><b>Datei konnte nicht geöffnet werden.</b><span>'+esc(q(e&&e.message||e))+'</span><button type="button" class="ghost" data-rc786-open-ref="1">Ref-Ordner öffnen</button></div>';return false}}
function renderViewer(){var v=viewerPersist(),d=viewerCurrentDoc(),root=document.getElementById('content');if(!root)return false;root.className='content rc786-document-viewer';root.setAttribute('data-exporthub-rendered-view','documentviewer');if(!v){root.innerHTML='<div class="rc786-viewer-shell"><div class="rc786-viewer-error"><b>Kein Dokument ausgewählt.</b><button type="button" class="btn" data-rc786-viewer-back="1">Zurück zur Sendung</button></div></div>';return false}var origin=v.source==='referenceFolder'?'Originaldatei aus Ref-Ordner':'Gespeicherte Datei aus der Sendung',meta=[q(v.documentType)||docType(v.name,''),origin,v.createdAt?fmtDate(v.createdAt):'',fmtSize(v.size)].filter(Boolean).join(' · ');root.innerHTML='<div class="rc786-viewer-shell"><header class="rc786-viewer-head"><div><button type="button" class="rc786-viewer-back" data-rc786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" data-rc786-viewer-print="1">Drucken</button>':'')+'<button type="button" class="ghost" data-rc786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.contentWindow.focus();frame.contentWindow.print();return false}catch(_){}}var v=viewerPersist();if(v){v.autoPrint=true;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){} }viewerLoad();return false}
document.addEventListener('input',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('search',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('keydown',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch'&&e.key==='Enter'){e.preventDefault();return renderSearchResults(input.value)}},true);
document.addEventListener('click',function(e){var clearSearch=e.target&&e.target.closest&&e.target.closest('[data-rc911-search-clear]');if(clearSearch){e.preventDefault();shipmentSearchQuery='';var input=document.getElementById('rc807ShipmentSearch');if(input)input.value='';return renderSearchResults('')}var openSearch=e.target&&e.target.closest&&e.target.closest('[data-rc807-open-shipment]');if(openSearch){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();return open(openSearch.getAttribute('data-rc807-open-shipment'),'shipmentsearch')}var b=e.target&&e.target.closest&&e.target.closest('[data-rc776-back],[data-rc784-overview],[data-rc786-open-file],[data-rc786-download-file],[data-rc786-print-file],[data-rc776-docume
```

## navigate functions
needle='function navigate'
count=4

### line~13581 pos=2189725
```text
;
 loginProgressTimer=native.setInterval(function(){var sec=Math.max(0,Math.floor((Date.now()-loginProgressStartedAt)/1000));if(lab)lab.textContent='Anmeldung läuft · '+sec+' s';if(txt)txt.textContent=sec<8?'Anmeldedaten werden geprüft …':sec<20?'Azure-Anmeldung antwortet noch …':'Serverantwort wird weiterhin erwartet …'},1000);return true
}
function stopLoginProgress(){if(loginProgressTimer){try{native.clearInterval(loginProgressTimer)}catch(_){ }loginProgressTimer=null}const bar=by('cleanProgressBar');if(bar){bar.classList.remove('clean-login-indeterminate');bar.style.transform='';bar.style.removeProperty('will-change')}return true}
function rc524StyleHealth(){
 const inline=document.getElementById('rc558-complete-design');
 if(inline){document.documentElement.removeAttribute('data-rc524-style-fallback');return Promise.resolve(true)}
 const app=by('app'),side=document.querySelector('.sidebar');
 let ok=false;try{ok=!!app&&!!side&&/gradient/i.test(getComputedStyle(side).backgroundImage||'')}catch(_){ok=false}
 return Promise.resolve(ok)
}
const MS_SWITCH_PARAM='msAccountSwitch';
const MS_SWITCH_FLOW_KEY='exporthub_rc549_ms_switch_active';
const MS_SWITCH_HINT_KEY='exporthub_rc549_ms_logout_hint';
function cleanReturnUrl(extra){
 const base=window.location.origin&&window.location.origin!=='null'?window.location.origin:document.baseURI;const u=new URL(LOGIN_RETURN||('/?v='+CACHE),base);u.hash='';u.searchParams.set('v',CACHE);if(extra)Object.keys(extra).forEach(function(k){u.searchParams.set(k,extra[k])});return u
}
function appAuthUrl(path,params){const base=window.location.origin&&window.location.origin!=='null'?window.location.origin:document.baseURI;const u=new URL(path,base);Object.keys(params||{}).forEach(function(k){u.searchParams.set(k,params[k])});return u.href}
function navigateMicrosoft(url){if(typeof window.__EXPORTHUB_MS_NAVIGATE__==='function')return window.__EXPORTHUB_MS_NAVIGATE__(url);location.replace(url)}
function microsoftSwitchStore(key,value){
 try{if(native.storageSet&&window.sessionStorage)native.storageSet.call(window.sessionStorage,key,String(value||''))}catch(_){}
 try{if(native.storageSet&&window.localStorage)native.storageSet.call(window.localStorage,key,String(value||''))}catch(_){}
}
function microsoftSwitchRead(key){
 let v='';try{if(native.storageGet&&window.sessionStorage)v=text(native.storageGet.call(window.sessionStorage,key))}catch(_){}
 try{if(!v&&native.storageGet&&window.localStorage)v=text(native.storageGet.call(window.localStorage,key))}catch(_){}
 return v
}
function microsoftSwitchRemove(key){
 try{if(native.storageRemove&&window.sessionStorage)native.storageRemove.call(window.sessionStorage,key)}catch(_){}
 try{if(native.storageRemove&&window.localStorage)native.storageRemove.call(window.localStorage,key)}catch(_){}
}
function clearMicrosoftSwitchState(){microsoftSwitchRemove(MS_SWITCH_FLOW_KEY);microsoftSwitchRemove(MS_SWITCH_HINT_KEY);runtime.msSwitchBusy=false}
function resetClientSessionForMicrosoftSwitch(){
 stopAuthenticatedJobs();clearTabSession();
 runtime.authToken='';runtime.passwordChangeTicket='';runtime.user=null;runtime.users=[];runtime.state=null;runtime.ready=false;runtime.loaded=false;runtime.loading=false;runtime.pendingPasswordLogin=null;runtime.passwordChangeRetry=false;runtime.loginRetryCredentials=null;runtime.sessionRefreshAttempted=false;
}
function accountSwitchDelay(ms){return new Promise(function(resolve){native.setTimeout(resolve,Math.max(20,Number(ms)||80))})}
async function readMicrosoftLogoutHint(){
 try{
  const res=await fetchWithTimeout(appAuthUrl('/.auth/me'
```

### line~20589 pos=4129144
```text
getFullYear()+'-'+z(d.getMonth()+1)+'-'+z(d.getDate())}
function todayISO(){return localISO(new Date())}
function weekday(iso){var d=iso?new Date(iso+'T12:00:00'):new Date();return ['Sonntag','Montag','Dienstag','Mittwoch','Donnerstag','Freitag','Samstag'][d.getDay()]}
function fmtDate(iso,withWeekday){if(!/^\d{4}-\d{2}-\d{2}$/.test(q(iso)))return'';try{return new Date(iso+'T12:00:00').toLocaleDateString('de-DE',withWeekday?{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'}:{day:'2-digit',month:'2-digit',year:'numeric'})}catch(_){return iso}}
function persist(reason){dashboardLocalChangeUntil=Date.now()+3500;try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')window.ExportHUBClean.queueSave(reason||'Dashboard/Aufgaben Planer gespeichert');else if(typeof window.save==='function')window.save()}catch(e){console.error('ExportHUB Dashboard/Planer speichern',e)}}
function accessRightId(id){id=low(id);if(id==='calculator')return'shippingcosts';if(id==='quiz')return'inspections';return id}
function canOpen(id){id=accessRightId(id);var u=currentUser();try{if(typeof window.canView==='function'&&typeof window.canRead==='function')return !!(window.canView(id)&&window.canRead(id)&&(id!=='rights'||typeof window.canManageRights!=='function'||window.canManageRights()))}catch(_){return false}var r=u&&u.rights&&u.rights[id]||{},level=low(r.level||r.access||'');if(/admin/.test(low(u&&u.role))||u&&u.globalAdmin)return true;return ['view','edit','admin'].indexOf(level)>=0||!!(r.read||r.visible||r.edit||r.admin||r.functionAdmin)}
function widgetAllowed(id){
 if(id==='tasks'||id==='shipments')return false;
 var right=WIDGET_RIGHTS[id];return !!right&&canOpen(right)
}
function availableModules(){return MODULES.filter(function(m){return canOpen(m.id)})}
function navigate(id){if(!canOpen(id)){alert('Dieser Bereich ist für deinen Benutzer nicht freigegeben.');return false}if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route(id,'dashboard');return false}
function directShipmentId(s){return q(s&&(s.id||s.shipmentId||s.ref||s.reference||s.shipmentRef))}
function directCustomerId(c){return q(c&&(c.id||c.customerId||c.account||c.customerNumber))}
function directCustomerNumber(c){return q(c&&(c.account||c.customerNumber||c.number))}
function directCustomerName(c){return q(c&&(c.name||c.customerName||c.company))}
function findDirectShipment(id){var key=q(id);return arr(state().shipments).find(function(s){return [s.id,s.shipmentId,s.ref,s.reference,s.shipmentRef].map(q).indexOf(key)>=0})||null}
function findDirectCustomer(id){var key=q(id);return arr(state().customers).find(function(c){return [c.id,c.customerId,c.account,c.customerNumber].map(q).indexOf(key)>=0})||null}
function directEntityLabel(link){if(!link)return'';if(link.type==='shipment'){var sh=findDirectShipment(link.id);return sh?(shipmentRef(sh)||directShipmentId(sh))+(shipmentCustomer(sh)?' · '+shipmentCustomer(sh):''):''}var c=findDirectCustomer(link.id);return c?([directCustomerNumber(c),directCustomerName(c)].filter(Boolean).join(' · ')||directCustomerId(c)):''}
function normalizeDirectLink(link){var type=low(link&&link.type),id=q(link&&link.id);if(type!=='shipment'&&type!=='customer'||!id)return null;if(type==='shipment'&&!canOpen('shipment'))return null;if(type==='customer'&&!canOpen('customerfolder'))return null;return{type:type,id:id}}
function clone(v){try{return JSON.parse(JSON.stringify(v))}catch(_){return v}}
function openDirectEntity(type,id){type=low(type);id=q(id);if(type==='shipment'){var sh=find
```

### line~21137 pos=4229818
```text
Id),ed=effectiveDate(t);return '<div class="i218-extra-row"><div><b>'+esc(t.title||t.name)+'</b><div class="muted">'+esc(t.owner||'Alle')+' · '+esc(groupName(t))+' · '+esc(ed?dateDe(ed):q(t.day||'Ohne Tag'))+'</div></div>'+(id?'<button class="soft" data-i218-open="'+esc(id)+'">Öffnen</button>':'')+'</div>'}).join('')+'</div>':'';return '<details class="card i218-group" data-i218-group="'+esc(name)+'" '+(isOpen?'open':'')+'><summary><div class="i218-group-title"><span class="i218-group-chevron">›</span><h3>'+esc(name)+'</h3></div><span class="pill orange">'+extra.length+'</span></summary><div class="i218-group-body">'+body+'</div></details>'}
function weekOverview(tasks,ws){return '<div class="i218-overview">'+DAYS.map(function(day){var date=dayDate(ws,day),h=holidayName(date),dayTasks=tasks.filter(function(t){return effectiveDate(t)===date}),chips=GROUPS.map(function(g){var n=dayTasks.filter(function(t){return groupName(t)===g}).length;return n?'<span class="i218-overview-chip">'+esc(g.replace('Aufträge kontrollieren','Kontrolle').replace('Zusatzaufgaben','Zusatz'))+' '+n+'</span>':''}).join('');return '<article class="i218-overview-day '+(h?'is-holiday':'')+'"><h4>'+esc(day)+'</h4><span class="i218-overview-date">'+esc(dateDe(date))+'</span>'+(h?'<span class="i218-holiday">'+esc(h)+'</span>':'')+'<strong class="i218-overview-count">'+dayTasks.length+'</strong><span class="muted">offene Aufgaben</span><div class="i218-overview-chips">'+chips+'</div></article>'}).join('')+'</div>'}
function setWeek(ws){var cur=weekStart(new Date());ws=weekStart(ws);if(ws<cur)ws=cur;st().taskPlannerWeekStart=ws;try{localStorage.setItem('exporthub:planner:week:rc874',ws)}catch(_){}render();return false}
function shiftWeek(n){return setWeek(localISO(addDays(mondayOf(selectedWeek()),n*7)))}
function navigateTasks(){if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('tasks','task-planner');try{if(typeof window.setView==='function')window.setView('tasks')}catch(_){}return false}
/* exporthub-rc923-planner-source-signature */
function plannerSourceSignatureRC923(ws){var s=st(),rows=[];(Array.isArray(s.tasks)?s.tasks:[]).forEach(function(t){if(!t||isTest(t))return;rows.push([q(t.id||t.taskId),q(t.status),q(t.title||t.name),q(t.area||t.category||t.section||t.type),q(t.owner||t.assignee),q(t.day),q(t.originalDay),q(t.plannedDate||t.dueDate||t.date||t.targetDate),q(t.originalDate||t.baseDate),q(t.linkedShipmentRef||t.shipmentRef||t.reference||t.ref),q(t.linkedCustomer||t.customerName),q(t.linkedAccount||t.customerAccount),t.manualMoved===true?'1':'0',t.holidayAutoMoved===true?'1':'0'].join('|'))});rows.sort();return ws+'§'+rows.join('~')+'§'+JSON.stringify(Array.isArray(s.holidays)?s.holidays:[])}
function render(){if(rendering||view()!=='planning')return false;ensureRC895PlannerStyle();var r=root();if(!r)return false;rendering=true;try{var ws=selectedWeek();try{if(window.ExportHUBTaskMasterRC848)window.ExportHUBTaskMasterRC848.ensureWeek(ws,true)}catch(_){}var sourceSignature=plannerSourceSignatureRC923(ws),mounted=!!r.querySelector(':scope>#index218Planner');if(mounted&&sourceSignature===lastSourceSignature)return false;lastSourceSignature=sourceSignature;var tasks=openTasksForWeek(ws),hcount=0;DAYS.forEach(function(d){if(holidayName(dayDate(ws,d)))hcount++});var moved=tasks.filter(function(t){return t.manualMoved||t.holidayAutoMoved}).length,conflicts=tasks.filter(function(t){var d=effectiveDate(t);return d&&!isWorkday(d)&&groupName(t)!=='ABD erstellen'&&groupName(t)!=='Anmelden'}).length,forecast=holid
```

### line~21340 pos=4268432
```text
n" data-index236-action="close-hint">×</button></div>'+(empty?'<div class="index236-hint-counts"><span>Alles aktuell</span></div>':'<div class="index236-hint-counts"><span class="is-alert">'+c.tasks+' offene Aufgabe'+(c.tasks===1?'':'n')+'</span><span class="is-alert">'+c.overdueTasks+' überfällig</span></div>')+'<div class="index236-hint-actions"><button class="btn" data-index236-action="open-center">Benachrichtigungen öffnen</button>'+(empty?'':'<button class="ghost" data-index236-action="open-tasks">Aufgaben</button>')+'</div></section>'}
function showHint(c,label,manual){removeHint();if(!manual&&!c.tasks)return;var host=document.createElement('div');host.id='index236NotificationHint';host.className='index236-hint-host';host.innerHTML=hintHtml(c,label,!!manual);document.body.appendChild(host);hintTimer=later(removeHint,manual?7000:10000)}
function checkSchedule(manual){var c=counts();if(manual){showHint(c,'Manuelle Prüfung',true);renderView();return false}var due=slotsDue(new Date());if(!due.length)return false;markDueSeen(due);var label=due[due.length-1].label;addHistory(due.map(function(s){return s.id}).join(','),label,c);persist('Benachrichtigung protokolliert');showHint(c,label,false);nativeNotify(c,label);renderView();return false}
function findShipment(id){var key=q(id),up=key.toUpperCase();return shipments().find(function(sh){return shipmentId(sh)===key||shipmentId(sh).toUpperCase()===up||shipmentRef(sh).toUpperCase()===up})||null}
function findTask(id){var key=q(id);return arr(main().tasks).find(function(t){return taskId(t)===key})||null}
function setViewRaw(view){var s=main();s.view=view;try{document.body.setAttribute('data-exporthub-view',view);document.body.setAttribute('data-current-view',view);document.body.setAttribute('data-eh-view',view)}catch(_){ }}
function navigate(view){try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route(view,'notifications')}catch(_){ }return false}
function openShipment(id){var sh=findShipment(id);if(!sh){alert('Sendung wurde nicht gefunden.');return false}var key=shipmentId(sh)||shipmentRef(sh);if(window.ExportHUBShipmentView&&typeof window.ExportHUBShipmentView.open==='function')return window.ExportHUBShipmentView.open(key,'notifications');return false}
function openTask(id){var t=findTask(id);if(!t){alert('Aufgabe wurde nicht gefunden.');return false}navigate('tasks');setTimeout(function(){try{if(typeof window.rc524OpenTaskEditor==='function')window.rc524OpenTaskEditor(id)}catch(_){ }},100);return false}
function completeTask(id){var t=findTask(id);if(!t)return false;var stamp=new Date().toISOString();t.status='Erledigt';t.done=true;t.completed=true;t.completedAt=stamp;t.doneAt=stamp;t.doneBy=userName();persist('Aufgabe erledigt');renderView();updateBadge();return false}
function setPickupDate(id,value){var sh=findShipment(id),v=iso(value);if(!sh||!v){alert('Bitte ein gültiges Abholdatum auswählen.');return false}sh.pickupDate=v;sh.plannedPickupDate=v;sh.dueDate=v;arr(main().tasks).forEach(function(t){var linked=(q(t.linkedShipmentId)===shipmentId(sh))||(taskRef(t)&&taskRef(t).toUpperCase()===shipmentRef(sh).toUpperCase());if(linked&&!isDone(t)){t.dueDate=v;t.date=v;t.plannedDate=v}});persist('Abholdatum geändert');renderView();updateBadge();return false}
function emailList(v){var out=[];function walk(x){if(x==null)return;if(Array.isArray(x)){x.forEach(walk);return}if(typeof x==='object'){Object.keys(x).forEach(function(k){walk(x[k])});return}var m=q(x).match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi)||[];m.forEach(function(e){if(
```

## viewchange
needle='viewchange'
count=57

### line~22292 pos=4498061
```text
(el);return !!el.dataset.rc373DateKind}
function write(kind,value){var sh=shipment();if(kind==='actual'){sh.actualPickupDate=value;sh.pickedUpAtDate=value}else{sh.pickupDate=value;sh.plannedPickupDate=value;sh.pickupPlannedDate=value}}
function sync(el,saveNow){if(!prepare(el))return false;var raw=q(el.value),formatted=mask(raw);if(raw!==formatted){el.value=formatted;try{el.setSelectionRange(formatted.length,formatted.length)}catch(_){}}var value=iso(formatted||el.dataset.dateIso);if(saveNow&&formatted&&!value){el.setCustomValidity('Bitte Datum als TT.MM.JJJJ eingeben.');return false}el.setCustomValidity('');if(value){el.dataset.dateIso=value;write(el.dataset.rc373DateKind,value)}else if(!formatted){el.dataset.dateIso='';write(el.dataset.rc373DateKind,'')}if(saveNow){if(value)el.value=display(value);queue(el.dataset.rc373DateKind==='actual'?'Tatsächliches Abholdatum gespeichert':'Geplantes Abholdatum gespeichert')}return true}
function patch(){dateInputs().forEach(function(el){prepare(el);var sh=shipment(),value=iso(el.dataset.rc373DateKind==='actual'?(sh.actualPickupDate||sh.pickedUpAtDate):(sh.plannedPickupDate||sh.pickupDate||sh.pickupPlannedDate));el.dataset.dateIso=value||'';if(document.activeElement!==el){var shown=value?display(value):mask(el.value);if(el.value!==shown)el.value=shown}});return true}
document.addEventListener('input',function(e){if(e.target&&kindOf(e.target))sync(e.target,false)},true);
document.addEventListener('change',function(e){if(e.target&&kindOf(e.target))sync(e.target,true)},true);
document.addEventListener('focusout',function(e){if(e.target&&kindOf(e.target))sync(e.target,true)},true);
document.addEventListener('focusin',function(e){if(e.target&&kindOf(e.target))prepare(e.target)},true);
['exporthub:ready','exporthub:rendered','exporthub:viewchange'].forEach(function(n){window.addEventListener(n,patch)});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',patch,{once:true});else patch();
window.ExportHUBPickupDate373=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',patch:patch,sync:sync,mask:mask,iso:iso,display:display,diagnostics:function(){return{inputs:dateInputs().length,values:dateInputs().map(function(el){return el.value}),format:'TT.MM.JJJJ',noObserver:true,noInterval:true}}});
})();
</script>
<style id="exporthub-rc373-shipment-style">
#content[data-view="shipment"],body[data-exporthub-view="shipment"] #content{display:block!important}
#rc573ShipmentShell{display:block!important;width:100%!important;min-width:0!important}
#rc363FixedShipmentLayout{container-type:inline-size!important;display:grid!important;grid-template-columns:1fr!important;gap:8px!important;width:100%!important;align-content:start!important;align-items:start!important}
.rc383-layout-pair{display:grid!important;grid-template-columns:repeat(2,minmax(0,1fr))!important;gap:8px!important;align-items:start!important;width:100%!important;min-width:0!important}
.rc383-column-stack{display:grid!important;grid-template-columns:1fr!important;gap:8px!important;align-content:start!important;align-items:start!important;width:100%!important;min-width:0!important}
.rc363-process-block{display:block!important;width:100%!important;min-width:0!important;margin:0!important;padding:10px!important;border:1px solid #cbdcf0!important;border-radius:16px!important;background:#fff!important;box-shadow:0 9px 24px rgba(15,35,65,.065)!important;box-sizing:border-box!important;overflow:visible!important}
.rc363-process-head{display:flex!important;align-items:center!important;gap:8px!i
```

### line~23183 pos=4615859
```text
over|freigeben|release|reparier|repair|anmelden|login/.test(all))return true;if(node.classList&&node.classList.contains('task-card'))return /erledig|offen|wieder/.test(all);return false}
function genericCommandStart(event){var node=actionNode(event);if(!node||node.disabled||node.getAttribute('aria-disabled')==='true'||node.closest('#nav')||node.hasAttribute('data-index321-route')||node.hasAttribute('data-index321-action')||node.closest('.brand')||node.hasAttribute('data-rc739-no-loading')||!commandLoadingAllowed(node))return;var now=Date.now();genericActionAt=now;var label=actionLabel(node);genericActionToken=loadingBarStart(label+' wird ausgeführt …',0);var token=genericActionToken;setTimeout(function(){if(loadingTask(token))loadingBarDone(token,label+' · Aktion ausgelöst')},650)}
function genericFileChange(event){var input=event&&event.target;if(!input||String(input.type||'').toLowerCase()!=='file'||!input.files||!input.files.length)return;var count=input.files.length,label=count===1?q(input.files[0].name)||'Datei':count+' Dateien';var token=loadingBarStart(label+' wird verarbeitet …',0);setTimeout(function(){if(loadingTask(token))loadingBarDone(token,'Dateiübernahme gestartet')},1000)}
window.ExportHUBLoadingBar=Object.freeze({start:loadingBarStart,step:loadingBarStep,done:loadingBarDone,fail:loadingBarFail,current:function(){return loadingBarLastToken},active:function(){return Object.keys(loadingBarTasks).length}});
window.ExportHUBOperationStatus=Object.freeze({start:operationStart,step:operationStep,done:operationDone,fail:operationFail,current:function(){return operationStatusSeq},active:function(){return !!operationStatusTimer}});
document.addEventListener('click',genericCommandStart,true);
document.addEventListener('change',genericFileChange,true);
['exporthub:viewchange','exporthub:rendered'].forEach(function(name){window.addEventListener(name,navigationLoadingDone)});
window.addEventListener('exporthub:ready',navigationLoadingDone);window.__EXPORTHUB_FINISH_NAV_LOADING__=navigationLoadingDone;
function state(){try{if(typeof window.__EXPORTHUB_GET_STATE__==='function'){var s=window.__EXPORTHUB_GET_STATE__();if(s)return s}}catch(_){}return window.state||(window.state={})}function shipment(){var s=state();s.shipment=s.shipment||{};s.shipment.rows=arr(s.shipment.rows);return s.shipment}function root(){return document.getElementById('content')}
function isShipmentView(){var s=state(),v=low(s.view||''),body=low(document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):'')),content=low(root()&&root().getAttribute('data-view'));if(v)return v==='shipment';if(body)return body==='shipment';if(content)return content==='shipment';return !!document.getElementById('rc573ShipmentShell')}
function rawQueue(reason){try{if(window.ExportHUBClean&&typeof window.ExportHUBClean.queueSave==='function')return window.ExportHUBClean.queueSave(reason||'Sendung aktualisiert')}catch(e){console.error('RC420 queueSave',e)}return false}
function repairKey(sh){return q(sh&&(sh.id||sh.shipmentId||sh.ref||sh.reference||sh.shipmentRef)).toUpperCase()}
function repairActive(sh){if(!sh)return false;var s=state(),wantedId=q(s.__rc844RepairShipmentId).toUpperCase(),wantedRef=q(s.__rc844RepairShipmentRef).toUpperCase(),globalWanted=q(window.__EXPORTHUB_RC844_REPAIR_SHIPMENT__).toUpperCase(),sid=q(sh.id||sh.shipmentId).toUpperCase(),sref=q(sh.ref||sh.reference||sh.shipmentRef||sh.referenceNumber).toUpperCase();return !!((wantedId&&sid===wantedId)||(wantedRef&&sref===wantedRef)||(globalWanted&&(sid===globalWanted||sref===globalWanted)))}
functio
```

### line~23512 pos=4726991
```text
-avis-value" data-rc897-avis-link>Noch kein Kundenlink</div></div></div><div class="rc897-avis-actions"><button type="button" class="btn" data-rc897-avis-action="toggle">Lieferavis aktivieren</button><button type="button" class="ghost" data-rc897-avis-action="copy">Link kopieren</button><button type="button" class="ghost" data-rc897-avis-action="open">Vorschau öffnen</button></div><div class="rc897-avis-help">Der Lieferavis ist Teil der Sendung. Der Kundenlink wird nur bei aktivem Lieferavis erzeugt und drei Arbeitstage nach der tatsächlichen Abholung automatisch deaktiviert.</div>';
 }
 if(panel.parentElement!==body)body.appendChild(panel);
 if(!window.__EXPORTHUB_RC897_AVIS_EVENTS__){
   window.__EXPORTHUB_RC897_AVIS_EVENTS__=true;
   document.addEventListener('click',function(ev){
     var btn=ev.target&&ev.target.closest&&ev.target.closest('#rc897LieferavisPanel [data-rc897-avis-action]');if(!btn)return;
     ev.preventDefault();ev.stopPropagation();
     var action=btn.getAttribute('data-rc897-avis-action'),api=rc897AvisApi(),sh=rc897AvisShipment();if(!api||!sh)return;
     if(action==='toggle'){
       var active=false;try{active=!!api.enabled(sh)}catch(_){}
       btn.disabled=true;
       Promise.resolve(api.toggle(!active)).catch(function(e){console.error('RC897 Lieferavis toggle',e)}).finally(function(){btn.disabled=false;updateRC897LieferavisPanel()});
       return
     }
     var url='';try{url=q(api.link(sh))}catch(_){}
     if(action==='copy'&&url){if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(url).catch(function(){prompt('Lieferavis-Link',url)});else prompt('Lieferavis-Link',url);return}
     if(action==='open'&&url){window.open(url,'_blank','noopener,noreferrer');return}
   },true);
   ['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:shipment-saved','exporthub:customer-avis-updated'].forEach(function(name){window.addEventListener(name,function(){requestAnimationFrame(updateRC897LieferavisPanel)})});
 }
 updateRC897LieferavisPanel();return true
}
function ensureRC894LieferavisButton(){var old=document.getElementById('lieferAvisBtn');if(old)old.remove();return !!document.getElementById('rc897LieferavisPanel')}
/* exporthub-rc899-no-voids-layout */
function applyRC899DocumentsAbd(documentsBody,abdDecisionBlock){
 if(!documentsBody||!abdDecisionBlock)return false;
 var main=documentsBody.querySelector(':scope > .rc899-doc-main');if(!main){main=document.createElement('div');main.className='rc899-doc-main';documentsBody.insertBefore(main,documentsBody.firstChild)}
 Array.from(documentsBody.children).forEach(function(node){if(node===main||node===abdDecisionBlock)return;main.appendChild(node)});
 if(abdDecisionBlock.parentElement!==documentsBody)documentsBody.appendChild(abdDecisionBlock);
 documentsBody.setAttribute('data-rc899-doc-abd','1');
 return true
}
function applyFixedBlocks(){
 var r=root(),shell=document.getElementById('rc573ShipmentShell')||r;if(!r||!shell)return 0;
 var layout=document.getElementById('rc363FixedShipmentLayout');
 if(!layout){layout=document.createElement('div');layout.id='rc363FixedShipmentLayout';var first=Array.from(r.children).find(function(el){return el.classList&&el.classList.contains('page-head')});if(first&&first.nextSibling)r.insertBefore(layout,first.nextSibling);else r.appendChild(layout)}
 function box(id,cls,parent){var el=document.getElementById(id);if(!el){el=document.createElement('div');el.id=id;el.className=cls}if(el.className!==cls)el.className=cls;if(el.parentElement!==parent)parent.appendChild(el);return el}
 var top=b
```

### line~23749 pos=4809437
```text
;window.addEventListener('change',captureShipmentField,true);document.addEventListener('focusin',function(e){if(isShipmentView()&&e.target&&e.target.closest&&e.target.closest('#rc363FixedShipmentLayout')){markEditing(e.target,12000);if(Date.now()-lastLockCheckAt>20000)refreshShipmentLock(false)}},true);document.addEventListener('input',function(e){markEditing(e.target,6500)},true);document.addEventListener('change',function(e){markEditing(e.target,9000)},true);document.addEventListener('input',function(e){if(e.target&&e.target.closest&&e.target.closest('#rc363WeightModal'))updateModalTotals();else onField(e)},false);document.addEventListener('change',onField,false);document.addEventListener('focusout',function(e){var el=e.target;if(!isShipmentView()||!el||!el.closest||!el.closest('.rc363-owned-row'))return;if(el.getAttribute('data-rc712-live-dirty')==='1')commitColliElement(el);markStowDirty();scheduleEditSave('Colli-Zeile gespeichert',4800);scheduleStowRender(1800,true)},true);document.addEventListener('input',queueShipmentEdit,false);document.addEventListener('change',queueShipmentEdit,false);document.addEventListener('blur',queueShipmentEdit,true);document.addEventListener('visibilitychange',function(){if(document.hidden)flushEditSave('Sendungseingabe vor App-Wechsel gespeichert',true);else refreshLockOnReturn()},true);window.addEventListener('focus',refreshLockOnReturn);window.addEventListener('pagehide',function(){flushEditSave('Sendungseingabe vor Verlassen gespeichert',true)},{capture:true});document.addEventListener('keydown',function(e){if(e.key!=='Escape')return;if(document.getElementById('rc363WeightModal')&&document.getElementById('rc363WeightModal').classList.contains('open'))closeWeight();closePackagingMenus()},true);patch()}
['exporthub:ready','exporthub:viewchange','exporthub:shipment-location-changed'].forEach(function(name){window.addEventListener(name,schedulePatch)});
window.addEventListener('exporthub:rendered',function(event){
 if(!isShipmentView())return;
 var detail=event&&event.detail||{},layout=document.getElementById('rc363FixedShipmentLayout');
 if(layout&&detail.view==='shipment')return;
 schedulePatch()
});window.addEventListener('exporthub:sync',function(event){if(event&&event.detail&&event.detail.remote===true)scheduleRemoteMerge(0)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
function refreshVisuals(){updateSummary();var active=document.activeElement,editing=!!(active&&/^(INPUT|SELECT|TEXTAREA)$/.test(active.tagName)&&active.closest&&active.closest('#rc573ColliCard'));if(rc710StowDirty){if(!editing)scheduleStowRender(120,true)}else if(!editing)renderStowPlan();updateQr();return true}

['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,function(){var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;nt(ensureRC894LieferavisButton,0)})});
var api=Object.freeze({version:VERSION,patch:patch,refreshVisuals:refreshVisuals,startNewShipment:startFreshShipment,openSavedShipment:openSavedExact,addRow:addRow,removeRow:removeRow,updateRow:updateRow,openWeight:openWeight,save:saveAction,printAll:printAll,openDocuments:openDocuments,mergeRemote:mergeRemote,mergeActiveRemote:mergeActiveRemote,renderStowPlan:renderStowPlan,activateQr:activateQr,diagnostics:function(){var r=root(),layout=document.getElementById('rc363FixedShipmentLayout'),stow=document.getElementById('rc380StowPlan');return{version:VERSION,shipmentView:isShipmentView(),rea
```

### line~23758 pos=4810406
```text
ender(1800,true)},true);document.addEventListener('input',queueShipmentEdit,false);document.addEventListener('change',queueShipmentEdit,false);document.addEventListener('blur',queueShipmentEdit,true);document.addEventListener('visibilitychange',function(){if(document.hidden)flushEditSave('Sendungseingabe vor App-Wechsel gespeichert',true);else refreshLockOnReturn()},true);window.addEventListener('focus',refreshLockOnReturn);window.addEventListener('pagehide',function(){flushEditSave('Sendungseingabe vor Verlassen gespeichert',true)},{capture:true});document.addEventListener('keydown',function(e){if(e.key!=='Escape')return;if(document.getElementById('rc363WeightModal')&&document.getElementById('rc363WeightModal').classList.contains('open'))closeWeight();closePackagingMenus()},true);patch()}
['exporthub:ready','exporthub:viewchange','exporthub:shipment-location-changed'].forEach(function(name){window.addEventListener(name,schedulePatch)});
window.addEventListener('exporthub:rendered',function(event){
 if(!isShipmentView())return;
 var detail=event&&event.detail||{},layout=document.getElementById('rc363FixedShipmentLayout');
 if(layout&&detail.view==='shipment')return;
 schedulePatch()
});window.addEventListener('exporthub:sync',function(event){if(event&&event.detail&&event.detail.remote===true)scheduleRemoteMerge(0)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
function refreshVisuals(){updateSummary();var active=document.activeElement,editing=!!(active&&/^(INPUT|SELECT|TEXTAREA)$/.test(active.tagName)&&active.closest&&active.closest('#rc573ColliCard'));if(rc710StowDirty){if(!editing)scheduleStowRender(120,true)}else if(!editing)renderStowPlan();updateQr();return true}

['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,function(){var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;nt(ensureRC894LieferavisButton,0)})});
var api=Object.freeze({version:VERSION,patch:patch,refreshVisuals:refreshVisuals,startNewShipment:startFreshShipment,openSavedShipment:openSavedExact,addRow:addRow,removeRow:removeRow,updateRow:updateRow,openWeight:openWeight,save:saveAction,printAll:printAll,openDocuments:openDocuments,mergeRemote:mergeRemote,mergeActiveRemote:mergeActiveRemote,renderStowPlan:renderStowPlan,activateQr:activateQr,diagnostics:function(){var r=root(),layout=document.getElementById('rc363FixedShipmentLayout'),stow=document.getElementById('rc380StowPlan');return{version:VERSION,shipmentView:isShipmentView(),readOnly:!!lockedShipment(),readOnlyStatus:lockedShipment()?lockText(lockedShipment()):'',rows:rowNodes().length,inputDirtyImmediate:true,remoteMergePending:remoteMergePending,customerChangeColliLock:!!customerChangeColliLock,activeShipmentField:activeShipmentField(),fixedBlocks:layout?layout.querySelectorAll('.rc363-process-block').length:0,blockOrder:layout?Array.from(layout.querySelectorAll('.rc363-process-block')).map(function(x){return x.id}):[],weightFields:document.querySelectorAll('input[data-rc363-weight="1"]').length,helperTexts:Array.from((r||document).querySelectorAll('.okbox,.badbox,.notice,.infobox,div,p')).filter(function(el){return /^(Anzahl\s*=\s*reale physische Anzahl|Referenzregel:|Dokumente:\s*Ladeliste wird als A4-Seite erzeugt)/i.test(q(el.textContent))}).length,hasMutationObserver:false,hasInterval:false,coalescedPatchTimer:true,stowDetachedFromColliInput:true,stowIdleOnly:true,singlePhaseSettle:true,processRetryCount:processRetryCount
```

### line~24211 pos=4869861
```text
Id('app'),content=document.getElementById('content');
 if(app)app.setAttribute('data-eh-theme',theme);
 if(content)content.setAttribute('data-eh-theme',theme);
 var select=document.getElementById('ehThemeSelect');
 if(select&&select.value!==theme)select.value=theme;
 if(persist)save(userName(),theme);
 requestAnimationFrame(function(){requestAnimationFrame(function(){root.classList.remove('eh-theme-changing')})});
 return theme;
}
function ensure(){
 var top=document.querySelector('.topbar');if(!top)return false;
 var wrap=document.getElementById('ehThemeSwitch');
 if(!wrap){
  wrap=document.createElement('div');wrap.id='ehThemeSwitch';wrap.className='eh-theme-switch ghost';wrap.title='Design auswählen';
  wrap.innerHTML='<span>Design</span><select id="ehThemeSelect" aria-label="Design auswählen"></select>';
  var lang=document.getElementById('languageSelect');
  if(lang&&lang.nextSibling)top.insertBefore(wrap,lang.nextSibling);else top.appendChild(wrap);
 }
 var select=document.getElementById('ehThemeSelect');
 if(select&&!select.dataset.rc446){
  select.innerHTML=THEMES.map(function(x){return'<option value="'+x[0]+'">'+x[1]+'</option>'}).join('');
  select.dataset.rc446='1';
  select.addEventListener('change',function(event){event.stopPropagation();apply(this.value,true)});
 }
 return true;
}
function refresh(){
 ensure();
 var user=userName();
 if(user!==currentUser||!document.body.getAttribute('data-eh-theme')){currentUser=user;apply(read(user),false)}
 var content=document.getElementById('content');if(content)content.setAttribute('data-eh-theme',valid(document.body.getAttribute('data-eh-theme')));
}
function boot(){refresh()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,refresh)});
window.addEventListener('storage',function(e){if(e&&e.key&&e.key.indexOf('exporthub-theme-choice')===0)refresh()});
window.ExportHUBThemeSwitcher=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',themes:THEMES.map(function(x){return{id:x[0],label:x[1]}}),apply:function(t){return apply(t,true)},current:function(){return valid(document.body&&document.body.getAttribute('data-eh-theme'))},refresh:refresh});
})();
</script>
<style id="rc510-menu-color-refresh">
/* RC567: Nur noch Funktions-Akzentfarben. Das Aussehen des Menüs kommt ausschließlich aus dem Theme-System. */
.nav button[data-index321-view]{--menu-accent:#38bdf8}.nav button[data-index321-view="planning"]{--menu-accent:#a78bfa}.nav button[data-index321-view="tasks"]{--menu-accent:#22c55e}.nav button[data-index321-view="notifications"]{--menu-accent:#f59e0b}.nav button[data-index321-view="shipment"]{--menu-accent:#0ea5e9}.nav button[data-index321-view="abd"]{--menu-accent:#ef4444}.nav button[data-index321-view="shipmentoverview"]{--menu-accent:#2563eb}.nav button[data-index321-view="cmr"]{--menu-accent:#14b8a6}.nav button[data-index321-view="warehouse"]{--menu-accent:#f97316}.nav button[data-index321-view="customers"]{--menu-accent:#8b5cf6}.nav button[data-index321-view="customerfolder"]{--menu-accent:#06b6d4}.nav button[data-index321-view="pallet"]{--menu-accent:#eab308}.nav button[data-index321-view="shippingcosts"]{--menu-accent:#10b981}.nav button[data-index321-view="customs"]{--menu-accent:#6366f1}.nav button[data-index321-view="sop"]{--menu-accent:#ec4899}.nav button[data-index321-view="academy"]{--menu-accent:#84cc16}.nav button[data-index321-view="inspections"]{--menu-accent:#f43f5e}.nav b
```

### line~24440 pos=4917396
```text
.length+'</div><div class="muted">Sendungen auf dieser Location</div>'+(d.list.length?'<div class="rc504-dashboard-list rc513-dashboard-location-list">'+items+'</div>':'<div class="rc504-dashboard-empty">Keine Sendung auf dieser Location.</div>')+'</section>'}
window.rc504WarehouseRefresh=function(){sync(true);return false};
window.rc504WarehouseMove=function(key){return openMove(key)};
window.rc504WarehouseCloseMove=function(){return closeMove()};
window.rc504WarehouseSaveMove=function(){return submitMove()};
window.rc504WarehouseOpenFirst=function(){var first=all().filter(active)[0];if(first)return openMove(skey(first));alert('Es gibt aktuell keine aktive Sendung im Lager.');return false};
window.ExportHUBWarehouse=Object.freeze({version:VERSION,locations:LOCS.slice(),render:render,sync:sync,register:register,collect:collect,qrMarkup:qrMarkup,urlFor:locationUrl,dashboardWidget:dashboardWidget,sortDashboardWidgets:sortDashboardWidgets,dashboardMeta:dashboardMeta,locationFor:locationName});
function warehouseBackgroundRelevant(){return currentView()==='warehouse'}
function schedule(){clearTimeout(timer);timer=setTimeout(function(){if(warehouseBackgroundRelevant())sync(false);paintNavIcon()},900)}
function targetedRegister(e){if(document.hidden||navigator.onLine===false)return false;var d=e&&e.detail||{},sh=d.shipment||all().find(function(x){return q(x.id||x.shipmentId)===q(d.shipmentId||d.id)||q(x.ref||x.reference).toUpperCase()===q(d.reference).toUpperCase()});if(!sh)return false;setTimeout(function(){register(sh,false)},1500);return true}
['exporthub:ready','exporthub:sync','exporthub:shipment-status-visible'].forEach(function(n){window.addEventListener(n,function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()})});window.addEventListener('exporthub:viewchange',function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()});window.addEventListener('focus',function(){if(warehouseBackgroundRelevant())schedule()});window.addEventListener('exporthub:shipment-saved',targetedRegister);window.addEventListener('exporthub:warehouse-updated',function(){if(currentView()==='dashboard'&&window.ExportHUBRC205&&typeof window.ExportHUBRC205.refreshDashboardIfChanged==='function')try{window.ExportHUBRC205.refreshDashboardIfChanged(false)}catch(_){}paintNavIcon()});setInterval(function(){if(document.visibilityState!=='hidden'&&warehouseBackgroundRelevant())sync(false)},120000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){paintNavIcon();if(warehouseBackgroundRelevant())schedule()},{once:true});else{paintNavIcon();if(warehouseBackgroundRelevant())schedule()}
})();
</script>
<style id="rc558-testportal-releasefix-style">
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #app{display:none!important;visibility:hidden!important;pointer-events:none!important}
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #login{position:fixed!important;inset:0!important;z-index:2147483600!important;background:var(--bg,#eef6fb)!important;overflow:auto!important}
@media(max-width:720px){
 html[data-exporthub-testportal="1"] .main{padding:4px 4px max(10px,env(safe-area-inset-bottom))!important}
 html[data-exporthub-testportal="1"] #content .card{padding:8px!important;border-radius:11px!important;margin-bottom:7px!important}
 html[data-exporthub-testportal="1"] #content :is(.grid2,.grid3,.grid4,.metrics){gap:6px!important}
 html[data-exporthub-testportal="1"] #content :is(input,select,textarea){min-height:34px!important;padding:5px 7px!important;font-size:12px!import
```

### line~24880 pos=5007730
```text
e(history.state||null,'',u.pathname+u.search+u.hash);mark()}catch(_){}}
 function ensureBadge(){var b=document.getElementById('rc561TestPortalBadge');if(!b){b=document.createElement('div');b.id='rc561TestPortalBadge';b.setAttribute('role','status');b.textContent='TESTSERVICE · '+VERSION+' · NICHT PRODUKTION';(document.body||document.documentElement).appendChild(b)}return b}
 function showRuntimeError(message){message=q(message);if(!message)return;var box=document.getElementById('rc561TestRuntimeError');if(!box){box=document.createElement('div');box.id='rc561TestRuntimeError';box.setAttribute('role','alert');(document.body||document.documentElement).appendChild(box)}box.textContent='TESTFEHLER: '+message;box.classList.add('show');clearTimeout(errorTimer);errorTimer=setTimeout(function(){box.classList.remove('show')},12000)}
 function bind(){mark();canonicalProbe();anchorTestRoute();ensureBadge();restoreUser();syncLoginMask();var login=document.getElementById('login');if(login&&window.MutationObserver)new MutationObserver(function(){syncLoginMask();restoreUser()}).observe(login,{attributes:true,attributeFilter:['class']});}
 document.addEventListener('click',function(e){var a=e.target&&e.target.closest&&e.target.closest('a[href]');if(!a||window.__EXPORTHUB_PICKUP_MODE__)return;try{var u=new URL(a.getAttribute('href'),location.href);if(u.origin===location.origin&&(u.pathname==='/'||/\/index\.html$/i.test(u.pathname))&&!/\.auth\//i.test(u.pathname)){e.preventDefault();location.assign('/test?v='+CACHE)}}catch(_){}},true);
 window.addEventListener('error',function(e){if(e&&e.message)showRuntimeError(e.message)});
 window.addEventListener('unhandledrejection',function(e){var r=e&&e.reason;showRuntimeError(r&&r.message||r)});
 ['exporthub:ready','exporthub:rendered','exporthub:viewchange','exporthub:sync'].forEach(function(n){window.addEventListener(n,function(){mark();anchorTestRoute();ensureBadge();syncLoginMask()})});
 window.addEventListener('pageshow',function(){mark();anchorTestRoute();ensureBadge()});window.addEventListener('popstate',function(){setTimeout(anchorTestRoute,0)});
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
</script>

<style id="rc776-shipment-view-style">
#content.rc776-shipment-view{max-width:1600px;margin:0 auto;padding:24px!important;min-width:0}
.rc776-page{display:grid;gap:18px;min-width:0}
.rc784-view-context{display:flex;align-items:center;gap:8px;flex-wrap:wrap;padding:2px 2px 0;color:var(--pc-muted,var(--eh-muted,#64748b));font-size:13px;font-weight:800}
.rc784-view-context button{appearance:none;border:0;background:transparent;color:var(--pc-primary,var(--eh-accent,#1d4ed8));font:inherit;font-weight:900;padding:4px 0;cursor:pointer;text-decoration:none}
.rc784-view-context button:hover{text-decoration:underline}.rc784-view-context-sep{opacity:.55}.rc784-view-context-current{color:var(--pc-text,var(--eh-text,#0f172a));font-weight:950}
.rc776-hero{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;padding:24px 26px;border:1px solid var(--pc-line,var(--eh-border,#cbd5e1));border-radius:var(--pc-radius-lg,24px);background:var(--pc-card-bg,var(--eh-surface,#fff));box-shadow:var(--pc-card-shadow,var(--eh-shadow,0 12px 32px rgba(15,23,42,.08)))}
.rc776-hero h1{margin:3px 0 5px;font-size:clamp(28px,4vw,42px);line-height:1.05;color:var(--pc-text,var(--eh-text,#0f172a))}
.rc776-kicker{font-size:11px;font-weight:950;letter-spacing:.12em;text-transform:uppercase;color:var(--pc-primary,var(--eh-accent,#1d4ed8))}
.rc776-hero-act
```

## currentView
needle='currentView'
count=72

### line~20808 pos=4184368
```text
||'Ohne Zuständigkeit')+'</small></div>'+statusPill(done,done?'Erledigt':'Offen')+'<div class="rc205-planner-actions"><input type="date" data-date-for="'+esc(id)+'" value="'+esc(suggested)+'"><button class="soft" data-rc205-action="planner-move" data-task-id="'+esc(id)+'">Verschieben</button><button class="ghost" data-rc205-action="planner-next" data-task-id="'+esc(id)+'" data-holiday="'+esc(holiday.date)+'">Nächster Arbeitstag</button></div></article>'}
function undatedRow(t,defaultDate){var id=q(t.id),ref=taskRef(t),customer=taskCustomer(t);return '<article class="rc205-planner-task"><div class="rc205-planner-main"><b>'+esc(ref||taskTitle(t))+'</b>'+(ref?'<span>'+esc(taskTitle(t))+'</span>':'')+(customer?'<small>'+esc(customer)+'</small>':'')+'<small>'+esc(taskOwner(t)||'Ohne Zuständigkeit')+'</small></div><span class="pill gray">Ohne Datum</span><div class="rc205-planner-actions"><input type="date" data-date-for="'+esc(id)+'" value="'+esc(defaultDate)+'"><button class="soft" data-rc205-action="planner-plan" data-task-id="'+esc(id)+'">Einplanen</button></div></article>'}
function renderPlanner(){return false}
function apply(view,force){if(applying)return;var v=low(view||currentView());if(v!=='dashboard')return;applying=true;try{refreshDashboardIfChanged(force===true)}catch(e){console.error('ExportHUB Dashboard',e)}finally{applying=false}}
function schedule(view,force){var n=window.ExportHUBClean&&window.ExportHUBClean.native||window;if(timer)n.clearTimeout(timer);timer=n.setTimeout(function(){timer=0;apply(view,force)},0)}
window.addEventListener('exporthub:ready',function(){schedule(currentView(),false)});
window.addEventListener('exporthub:viewchange',function(e){schedule(e&&e.detail&&e.detail.view,false)});
window.addEventListener('exporthub:sync',function(){var v=currentView();if(low(v)!=='dashboard'||Date.now()<dashboardLocalChangeUntil)return;refreshDashboardIfChanged(false)});
window.ExportHUBRC205={renderDashboard:renderDashboard,refreshDashboardIfChanged:refreshDashboardIfChanged,patchQuickAccess:patchQuickAccess,dashboardSignature:dashboardSignature,renderPlanner:renderPlanner,apply:apply,planRecurringTask:planRecurringTask,navigate:navigate,holidaysNRW:holidaysNRW,openDirectEntity:openDirectEntity,addDirectEntity:addDirectEntity,removeDirectEntity:removeDirectEntity};window.ExportHUBRC206=window.ExportHUBRC205;window.ExportHUBRC207=window.ExportHUBRC205;window.ExportHUBRC208=window.ExportHUBRC205;window.ExportHUBRC209=window.ExportHUBRC205;window.ExportHUBRC210=window.ExportHUBRC205;
})();

</script>
<!-- INDEX 208 AUFGABEN PLANER: VERGANGENE TAGE AUSBLENDEN + WIEDERKEHRENDE AUFGABEN -->
<!-- INDEX 209 DECKBLATT LAYOUT -->
<!-- INDEX 209 DECKBLATT LAYOUT ENDE -->
<!-- INDEX 210 SENDUNGSUEBERSICHT STATUSFARBEN + SORTIERUNG START -->
<style id="index210-overview-status-sort-style">
#content.rc524-overview-view .rc524-overview-controls{grid-template-columns:minmax(230px,1.35fr) minmax(170px,.75fr) minmax(150px,.6fr) minmax(145px,.55fr)!important}
#content.rc524-overview-view .rc485-status-filter{grid-column:1/-1!important;display:flex!important;align-items:center!important;gap:8px!important;flex-wrap:wrap!important;margin:0!important;padding:10px 12px!important;border:1px solid #cbd5e1!important;border-radius:14px!important;background:#f8fafc!important}
#content.rc524-overview-view .rc485-status-filter legend{padding:0 5px!important;font-size:12px!important;font-weight:950!important;color:#334155!important}
#content.rc524-overview-view .rc485-status-all{min-height:34px!important;padding:6px 11px!important}
#content.rc524-ove
```

### line~24349 pos=4898888
```text
ookedAt);changed=true}if(prepared&&!picked(x)&&q(x.status)!=='Vorbereitet'){x.status='Vorbereitet';x.processStatus='Vorbereitet';x.readyForPickup=true;changed=true;meaningful=true}if(!prepared&&!picked(x)&&low(loc)==='eingang'&&/vorbereitet/i.test(q(x.status||x.processStatus))){x.status='Bereit zur Abholung';x.processStatus='Bereit zur Abholung';x.readyForPickup=true;changed=true;meaningful=true}if(Array.isArray(rec.history)&&(meaningful||!arr(x.warehouseHistory).length)){var hist=JSON.stringify(rec.history);if(JSON.stringify(arr(x.warehouseHistory))!==hist){x.warehouseHistory=rec.history.slice();changed=true}}}});return changed}
async function mapLimit(list,limit,fn){var next=0,workers=[];async function worker(){while(next<list.length&&Date.now()>=apiUnavailableUntil){var i=next++;await fn(list[i],i)}}for(var w=0;w<Math.min(limit,list.length);w++)workers.push(worker());await Promise.all(workers)}
async function sync(force){if(syncing)return syncing;if(!auth()||Date.now()<apiUnavailableUntil)return false;if(!force&&Date.now()-lastSync<7000)return false;lastSync=Date.now();syncing=(async function(){var before=warehouseStateSignature(),touched=false,activeList=all().filter(active),pickedList=all().filter(picked);await mapLimit(activeList,2,function(sh){return register(sh,false)});await mapLimit(pickedList,2,function(sh){return q(sh.pickupToken)&&!q(sh.warehouseCollectedAt)?collect(sh):Promise.resolve(false)});if(Date.now()>=apiUnavailableUntil)try{var d=await api('list'),records=arr(d.records);records.forEach(function(r){if(applyRecord(r))touched=true})}catch(e){if(e&&e.code!=='LOCATION_API_COOLDOWN'&&Date.now()-apiFailureLoggedAt>30000){apiFailureLoggedAt=Date.now();console.warn('RC869 Lager synchronisieren',e)}}var changed=before!==warehouseStateSignature();if(changed&&currentView()==='warehouse')persist('Lagerlocations synchronisiert');if(changed||force){try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:changed,forced:!!force,touched:touched}}))}catch(_){}}if(currentView()==='warehouse'&&(changed||force))render(false);return changed})().finally(function(){syncing=null});return syncing}
function currentView(){var s=states()[0]||{};return low(s.view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):''))}
function zoneData(name){var list=all().filter(active).filter(function(sh){return low(locationName(sh))===low(name)}),p=list.reduce(function(n,sh){return n+palletCount(sh)},0),k=list.reduce(function(n,sh){return n+packageCount(sh)},0);return{list:list,pallets:p,packages:k}}
function occupiedCount(){return LOCS.filter(function(x){return zoneData(x[0]).list.length>0}).length}
function currentMove(){return all().find(function(x){return skey(x)===pendingMove})||null}
function moveOptions(selected){return LOCS.map(function(x){return '<option value="'+x[2]+'"'+(Number(selected)===x[2]?' selected':'')+'>'+esc(x[0])+' (Code '+x[2]+')</option>'}).join('')}
function openMove(key){pendingMove=q(key);render(false);return false}
function closeMove(){pendingMove='';render(false);return false}

/* exporthub-rc907-warehouse-optimistic-move */
function warehouseMoveSnapshotRC907(sh){
 return copies(sh).map(function(x){return{x:x,warehouseLocation:x.warehouseLocation,warehouseLocationCode:x.warehouseLocationCode,warehouseMovedBy:x.warehouseMovedBy,warehouseMovedAt:x.warehouseMovedAt}})
}
function warehouseApplyLocalMoveRC907(sh,code){
 var found=LOCS.find(function(x){return x[2]===Number(code)}),loc=found?found[0]:'Eingang',u=currentUser(),who=q(u&&(u.name||u.displayName||u.user||u.user
```

### line~24349 pos=4899121
```text
ereitet/i.test(q(x.status||x.processStatus))){x.status='Bereit zur Abholung';x.processStatus='Bereit zur Abholung';x.readyForPickup=true;changed=true;meaningful=true}if(Array.isArray(rec.history)&&(meaningful||!arr(x.warehouseHistory).length)){var hist=JSON.stringify(rec.history);if(JSON.stringify(arr(x.warehouseHistory))!==hist){x.warehouseHistory=rec.history.slice();changed=true}}}});return changed}
async function mapLimit(list,limit,fn){var next=0,workers=[];async function worker(){while(next<list.length&&Date.now()>=apiUnavailableUntil){var i=next++;await fn(list[i],i)}}for(var w=0;w<Math.min(limit,list.length);w++)workers.push(worker());await Promise.all(workers)}
async function sync(force){if(syncing)return syncing;if(!auth()||Date.now()<apiUnavailableUntil)return false;if(!force&&Date.now()-lastSync<7000)return false;lastSync=Date.now();syncing=(async function(){var before=warehouseStateSignature(),touched=false,activeList=all().filter(active),pickedList=all().filter(picked);await mapLimit(activeList,2,function(sh){return register(sh,false)});await mapLimit(pickedList,2,function(sh){return q(sh.pickupToken)&&!q(sh.warehouseCollectedAt)?collect(sh):Promise.resolve(false)});if(Date.now()>=apiUnavailableUntil)try{var d=await api('list'),records=arr(d.records);records.forEach(function(r){if(applyRecord(r))touched=true})}catch(e){if(e&&e.code!=='LOCATION_API_COOLDOWN'&&Date.now()-apiFailureLoggedAt>30000){apiFailureLoggedAt=Date.now();console.warn('RC869 Lager synchronisieren',e)}}var changed=before!==warehouseStateSignature();if(changed&&currentView()==='warehouse')persist('Lagerlocations synchronisiert');if(changed||force){try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:changed,forced:!!force,touched:touched}}))}catch(_){}}if(currentView()==='warehouse'&&(changed||force))render(false);return changed})().finally(function(){syncing=null});return syncing}
function currentView(){var s=states()[0]||{};return low(s.view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):''))}
function zoneData(name){var list=all().filter(active).filter(function(sh){return low(locationName(sh))===low(name)}),p=list.reduce(function(n,sh){return n+palletCount(sh)},0),k=list.reduce(function(n,sh){return n+packageCount(sh)},0);return{list:list,pallets:p,packages:k}}
function occupiedCount(){return LOCS.filter(function(x){return zoneData(x[0]).list.length>0}).length}
function currentMove(){return all().find(function(x){return skey(x)===pendingMove})||null}
function moveOptions(selected){return LOCS.map(function(x){return '<option value="'+x[2]+'"'+(Number(selected)===x[2]?' selected':'')+'>'+esc(x[0])+' (Code '+x[2]+')</option>'}).join('')}
function openMove(key){pendingMove=q(key);render(false);return false}
function closeMove(){pendingMove='';render(false);return false}

/* exporthub-rc907-warehouse-optimistic-move */
function warehouseMoveSnapshotRC907(sh){
 return copies(sh).map(function(x){return{x:x,warehouseLocation:x.warehouseLocation,warehouseLocationCode:x.warehouseLocationCode,warehouseMovedBy:x.warehouseMovedBy,warehouseMovedAt:x.warehouseMovedAt}})
}
function warehouseApplyLocalMoveRC907(sh,code){
 var found=LOCS.find(function(x){return x[2]===Number(code)}),loc=found?found[0]:'Eingang',u=currentUser(),who=q(u&&(u.name||u.displayName||u.user||u.username||u.login))||'Admin',at=new Date().toISOString();
 copies(sh).forEach(function(x){x.warehouseLocation=loc;x.warehouseLocationCode=Number(code);x.warehouseMovedBy=who;x.warehouseMovedAt=at});
 return{location:loc,code:Number(code)
```

### line~24350 pos=4899259
```text
hanged=true;meaningful=true}if(Array.isArray(rec.history)&&(meaningful||!arr(x.warehouseHistory).length)){var hist=JSON.stringify(rec.history);if(JSON.stringify(arr(x.warehouseHistory))!==hist){x.warehouseHistory=rec.history.slice();changed=true}}}});return changed}
async function mapLimit(list,limit,fn){var next=0,workers=[];async function worker(){while(next<list.length&&Date.now()>=apiUnavailableUntil){var i=next++;await fn(list[i],i)}}for(var w=0;w<Math.min(limit,list.length);w++)workers.push(worker());await Promise.all(workers)}
async function sync(force){if(syncing)return syncing;if(!auth()||Date.now()<apiUnavailableUntil)return false;if(!force&&Date.now()-lastSync<7000)return false;lastSync=Date.now();syncing=(async function(){var before=warehouseStateSignature(),touched=false,activeList=all().filter(active),pickedList=all().filter(picked);await mapLimit(activeList,2,function(sh){return register(sh,false)});await mapLimit(pickedList,2,function(sh){return q(sh.pickupToken)&&!q(sh.warehouseCollectedAt)?collect(sh):Promise.resolve(false)});if(Date.now()>=apiUnavailableUntil)try{var d=await api('list'),records=arr(d.records);records.forEach(function(r){if(applyRecord(r))touched=true})}catch(e){if(e&&e.code!=='LOCATION_API_COOLDOWN'&&Date.now()-apiFailureLoggedAt>30000){apiFailureLoggedAt=Date.now();console.warn('RC869 Lager synchronisieren',e)}}var changed=before!==warehouseStateSignature();if(changed&&currentView()==='warehouse')persist('Lagerlocations synchronisiert');if(changed||force){try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:changed,forced:!!force,touched:touched}}))}catch(_){}}if(currentView()==='warehouse'&&(changed||force))render(false);return changed})().finally(function(){syncing=null});return syncing}
function currentView(){var s=states()[0]||{};return low(s.view||document.body&&(document.body?document.body.getAttribute('data-exporthub-view'):''))}
function zoneData(name){var list=all().filter(active).filter(function(sh){return low(locationName(sh))===low(name)}),p=list.reduce(function(n,sh){return n+palletCount(sh)},0),k=list.reduce(function(n,sh){return n+packageCount(sh)},0);return{list:list,pallets:p,packages:k}}
function occupiedCount(){return LOCS.filter(function(x){return zoneData(x[0]).list.length>0}).length}
function currentMove(){return all().find(function(x){return skey(x)===pendingMove})||null}
function moveOptions(selected){return LOCS.map(function(x){return '<option value="'+x[2]+'"'+(Number(selected)===x[2]?' selected':'')+'>'+esc(x[0])+' (Code '+x[2]+')</option>'}).join('')}
function openMove(key){pendingMove=q(key);render(false);return false}
function closeMove(){pendingMove='';render(false);return false}

/* exporthub-rc907-warehouse-optimistic-move */
function warehouseMoveSnapshotRC907(sh){
 return copies(sh).map(function(x){return{x:x,warehouseLocation:x.warehouseLocation,warehouseLocationCode:x.warehouseLocationCode,warehouseMovedBy:x.warehouseMovedBy,warehouseMovedAt:x.warehouseMovedAt}})
}
function warehouseApplyLocalMoveRC907(sh,code){
 var found=LOCS.find(function(x){return x[2]===Number(code)}),loc=found?found[0]:'Eingang',u=currentUser(),who=q(u&&(u.name||u.displayName||u.user||u.username||u.login))||'Admin',at=new Date().toISOString();
 copies(sh).forEach(function(x){x.warehouseLocation=loc;x.warehouseLocationCode=Number(code);x.warehouseMovedBy=who;x.warehouseMovedAt=at});
 return{location:loc,code:Number(code),movedBy:who,movedAt:at}
}
function warehouseRestoreMoveRC907(snapshot){
 arr(snapshot).forEach(function(s){if(!s||!s.x)return;s.x.warehou
```

### line~24379 pos=4902001
```text
b-rc907-warehouse-optimistic-move */
function warehouseMoveSnapshotRC907(sh){
 return copies(sh).map(function(x){return{x:x,warehouseLocation:x.warehouseLocation,warehouseLocationCode:x.warehouseLocationCode,warehouseMovedBy:x.warehouseMovedBy,warehouseMovedAt:x.warehouseMovedAt}})
}
function warehouseApplyLocalMoveRC907(sh,code){
 var found=LOCS.find(function(x){return x[2]===Number(code)}),loc=found?found[0]:'Eingang',u=currentUser(),who=q(u&&(u.name||u.displayName||u.user||u.username||u.login))||'Admin',at=new Date().toISOString();
 copies(sh).forEach(function(x){x.warehouseLocation=loc;x.warehouseLocationCode=Number(code);x.warehouseMovedBy=who;x.warehouseMovedAt=at});
 return{location:loc,code:Number(code),movedBy:who,movedAt:at}
}
function warehouseRestoreMoveRC907(snapshot){
 arr(snapshot).forEach(function(s){if(!s||!s.x)return;s.x.warehouseLocation=s.warehouseLocation;s.x.warehouseLocationCode=s.warehouseLocationCode;s.x.warehouseMovedBy=s.warehouseMovedBy;s.x.warehouseMovedAt=s.warehouseMovedAt});
 return true
}
function warehouseZoneRefreshRC907(code){
 var zone=document.querySelector('[data-warehouse-drop-code="'+Number(code)+'"]');if(!zone)return false;
 var name=q(zone.getAttribute('data-warehouse-drop-name')),d=zoneData(name),count=zone.querySelector('.rc504-zone-count');
 if(count){var load=d.pallets>0&&d.packages>0?(d.pallets+' Paletten · '+d.packages+' Pakete'):d.pallets>0?(d.pallets+' Paletten'):d.packages>0?(d.packages+' Pakete'):'0 Colli';count.textContent=d.list.length+' Sendungen · '+load}
 var list=zone.querySelector('.rc504-zone-list');if(list&&!list.querySelector('.rc504-shipment'))list.innerHTML='<div class="rc504-zone-empty">Keine Sendung an dieser Location.</div>';
 return true
}
function warehouseMoveDomRC907(key,fromCode,toCode,local){
 if(currentView()!=='warehouse')return false;
 var card=document.querySelector('[data-warehouse-drag-key="'+CSS.escape(q(key))+'"]'),target=document.querySelector('[data-warehouse-drop-code="'+Number(toCode)+'"] .rc504-zone-list');
 if(!card||!target)return false;
 var empty=target.querySelector('.rc504-zone-empty');if(empty)empty.remove();
 target.appendChild(card);
 var badge=card.querySelector('.rc504-badge.loc');if(badge)badge.textContent=q(local&&local.location)||'—';
 var moved=card.querySelectorAll('.rc504-meta-chip b');if(moved[1])moved[1].textContent=q(local&&local.movedBy)||'—';if(moved[2])moved[2].textContent=fmt(local&&local.movedAt);
 warehouseZoneRefreshRC907(fromCode);warehouseZoneRefreshRC907(toCode);
 var modal=document.querySelector('.rc504-modal-backdrop');if(modal)modal.remove();
 return true
}

async function moveShipmentKey(key,code){
 if(!isAdmin()){alert('Für das manuelle Verschieben ist das Funktionsrecht Lager = Admin erforderlich.');return false}
 var sh=all().find(function(x){return skey(x)===q(key)});if(!sh)return false;
 code=Math.round(Number(code||0));if(code<0||code>8)return false;
 var fromCode=Number(sh.warehouseLocationCode||0);if(fromCode===code)return false;
 var tok=token(sh);if(!tok){alert('Location-Token fehlt.');return false}
 var snapshot=warehouseMoveSnapshotRC907(sh),local=warehouseApplyLocalMoveRC907(sh,code);
 pendingMove='';dragKey='';warehouseMoveDomRC907(key,fromCode,code,local);
 try{
  var result=await api('move',{method:'POST',token:tok,body:{token:tok,locationCode:code,colliCount:totalColliCount(sh),totalColli:totalColliCount(sh),expectedColliCount:totalColliCount(sh)}});
  if(result&&result.record)applyRecord(result.record);
  persist('Lagerlocation verschoben');
  try{window.dispatchEvent(new CustomEvent('exporthub:war
```

### line~24408 pos=4904021
```text
list');
 if(!card||!target)return false;
 var empty=target.querySelector('.rc504-zone-empty');if(empty)empty.remove();
 target.appendChild(card);
 var badge=card.querySelector('.rc504-badge.loc');if(badge)badge.textContent=q(local&&local.location)||'—';
 var moved=card.querySelectorAll('.rc504-meta-chip b');if(moved[1])moved[1].textContent=q(local&&local.movedBy)||'—';if(moved[2])moved[2].textContent=fmt(local&&local.movedAt);
 warehouseZoneRefreshRC907(fromCode);warehouseZoneRefreshRC907(toCode);
 var modal=document.querySelector('.rc504-modal-backdrop');if(modal)modal.remove();
 return true
}

async function moveShipmentKey(key,code){
 if(!isAdmin()){alert('Für das manuelle Verschieben ist das Funktionsrecht Lager = Admin erforderlich.');return false}
 var sh=all().find(function(x){return skey(x)===q(key)});if(!sh)return false;
 code=Math.round(Number(code||0));if(code<0||code>8)return false;
 var fromCode=Number(sh.warehouseLocationCode||0);if(fromCode===code)return false;
 var tok=token(sh);if(!tok){alert('Location-Token fehlt.');return false}
 var snapshot=warehouseMoveSnapshotRC907(sh),local=warehouseApplyLocalMoveRC907(sh,code);
 pendingMove='';dragKey='';warehouseMoveDomRC907(key,fromCode,code,local);
 try{
  var result=await api('move',{method:'POST',token:tok,body:{token:tok,locationCode:code,colliCount:totalColliCount(sh),totalColli:totalColliCount(sh),expectedColliCount:totalColliCount(sh)}});
  if(result&&result.record)applyRecord(result.record);
  persist('Lagerlocation verschoben');
  try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:true,optimistic:true,reference:sref(sh),locationCode:code}}))}catch(_){}
  setTimeout(function(){sync(false)},1400);
  return true
 }catch(e){
  warehouseRestoreMoveRC907(snapshot);
  if(currentView()==='warehouse')render(false);
  alert(e&&e.message||'Sendung konnte nicht verschoben werden.');
  return false
 }
}
async function submitMove(){var sh=currentMove(),sel=document.getElementById('rc504MoveLocation');if(!sh||!sel)return false;var btn=document.getElementById('rc504MoveSave');if(btn)btn.disabled=true;try{await moveShipmentKey(skey(sh),sel.value)}finally{if(btn)btn.disabled=false}return false}
function shipmentBadge(sh){var loc=locationName(sh)||'—';return '<span class="rc504-badge loc">'+esc(loc)+'</span>'}
function shipmentCard(sh){var ref=esc(sref(sh)||sid(sh)),cust=esc(customer(sh)),moved=q(sh.warehouseMovedBy),movedAt=q(sh.warehouseMovedAt),prepared=sh.warehousePrepared===true||/vorbereitet/i.test(q(sh.status||sh.processStatus)),admin=isAdmin(),load=warehouseLoadSummary(sh),dateClass=shipmentDateClass(sh),drag=admin?' data-warehouse-drag-key="'+esc(skey(sh))+'" title="Sendung auf eine andere Location ziehen"':'';return '<article class="rc504-shipment'+dateClass+'"'+drag+'><div class="rc504-shipment-top"><div><div class="rc504-shipment-ref">'+ref+'</div><div class="rc504-shipment-customer">'+cust+'</div></div></div><div class="rc664-shipment-badges">'+shipmentBadge(sh)+(prepared?'<span class="rc504-badge ready">Vorbereitet</span>':'')+'</div><div class="rc504-shipment-meta"><div class="rc504-meta-chip"><small>'+esc(load.label)+'</small><b>'+esc(load.value)+'</b></div><div class="rc504-meta-chip"><small>Verlader</small><b>'+esc(moved||'—')+'</b></div><div class="rc504-meta-chip"><small>Gebucht</small><b>'+esc(fmt(movedAt))+'</b></div></div></article>'}
function zone(name,key,code){var d=zoneData(name),cls=key==='eingang'?' eingang':key==='regal'?' regal':key==='vorregal'?' vorregal':'',loadLabel=d.pallets>0&&d.packages>0?(d.pallets+' Paletten 
```

### line~24437 pos=4916648
```text
rn x.group-y.group;if(x.group===1&&x.count!==y.count)return y.count-x.count;return x.code-y.code}),i=0;return ids.map(function(id){return /^warehouse-/.test(id)?sorted[i++]:id})}
function dashboardWidget(id){var meta=dashboardMeta(id),name=meta.name,d=meta.data,stateClass=name==='Eingang'?'eingang':(meta.count>0?'occupied':'empty'),items=d.list.map(function(sh){return '<div class="rc504-dashboard-item"><b>'+esc(sref(sh)||sid(sh))+'</b><span>'+esc(customer(sh))+'</span></div>'}).join('');return '<section class="card rc205-widget rc504-dashboard-location rc504-dashboard-location-'+stateClass+'" data-warehouse-dashboard-state="'+stateClass+'" data-rc205-action="navigate" data-view="warehouse"><h3>🏭 '+esc(name)+'</h3><div class="big">'+d.list.length+'</div><div class="muted">Sendungen auf dieser Location</div>'+(d.list.length?'<div class="rc504-dashboard-list rc513-dashboard-location-list">'+items+'</div>':'<div class="rc504-dashboard-empty">Keine Sendung auf dieser Location.</div>')+'</section>'}
window.rc504WarehouseRefresh=function(){sync(true);return false};
window.rc504WarehouseMove=function(key){return openMove(key)};
window.rc504WarehouseCloseMove=function(){return closeMove()};
window.rc504WarehouseSaveMove=function(){return submitMove()};
window.rc504WarehouseOpenFirst=function(){var first=all().filter(active)[0];if(first)return openMove(skey(first));alert('Es gibt aktuell keine aktive Sendung im Lager.');return false};
window.ExportHUBWarehouse=Object.freeze({version:VERSION,locations:LOCS.slice(),render:render,sync:sync,register:register,collect:collect,qrMarkup:qrMarkup,urlFor:locationUrl,dashboardWidget:dashboardWidget,sortDashboardWidgets:sortDashboardWidgets,dashboardMeta:dashboardMeta,locationFor:locationName});
function warehouseBackgroundRelevant(){return currentView()==='warehouse'}
function schedule(){clearTimeout(timer);timer=setTimeout(function(){if(warehouseBackgroundRelevant())sync(false);paintNavIcon()},900)}
function targetedRegister(e){if(document.hidden||navigator.onLine===false)return false;var d=e&&e.detail||{},sh=d.shipment||all().find(function(x){return q(x.id||x.shipmentId)===q(d.shipmentId||d.id)||q(x.ref||x.reference).toUpperCase()===q(d.reference).toUpperCase()});if(!sh)return false;setTimeout(function(){register(sh,false)},1500);return true}
['exporthub:ready','exporthub:sync','exporthub:shipment-status-visible'].forEach(function(n){window.addEventListener(n,function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()})});window.addEventListener('exporthub:viewchange',function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()});window.addEventListener('focus',function(){if(warehouseBackgroundRelevant())schedule()});window.addEventListener('exporthub:shipment-saved',targetedRegister);window.addEventListener('exporthub:warehouse-updated',function(){if(currentView()==='dashboard'&&window.ExportHUBRC205&&typeof window.ExportHUBRC205.refreshDashboardIfChanged==='function')try{window.ExportHUBRC205.refreshDashboardIfChanged(false)}catch(_){}paintNavIcon()});setInterval(function(){if(document.visibilityState!=='hidden'&&warehouseBackgroundRelevant())sync(false)},120000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){paintNavIcon();if(warehouseBackgroundRelevant())schedule()},{once:true});else{paintNavIcon();if(warehouseBackgroundRelevant())schedule()}
})();
</script>
<style id="rc558-testportal-releasefix-style">
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #app{display:none!important;visibility:hidden!important;poi
```

### line~24440 pos=4917711
```text
urn false};
window.rc504WarehouseMove=function(key){return openMove(key)};
window.rc504WarehouseCloseMove=function(){return closeMove()};
window.rc504WarehouseSaveMove=function(){return submitMove()};
window.rc504WarehouseOpenFirst=function(){var first=all().filter(active)[0];if(first)return openMove(skey(first));alert('Es gibt aktuell keine aktive Sendung im Lager.');return false};
window.ExportHUBWarehouse=Object.freeze({version:VERSION,locations:LOCS.slice(),render:render,sync:sync,register:register,collect:collect,qrMarkup:qrMarkup,urlFor:locationUrl,dashboardWidget:dashboardWidget,sortDashboardWidgets:sortDashboardWidgets,dashboardMeta:dashboardMeta,locationFor:locationName});
function warehouseBackgroundRelevant(){return currentView()==='warehouse'}
function schedule(){clearTimeout(timer);timer=setTimeout(function(){if(warehouseBackgroundRelevant())sync(false);paintNavIcon()},900)}
function targetedRegister(e){if(document.hidden||navigator.onLine===false)return false;var d=e&&e.detail||{},sh=d.shipment||all().find(function(x){return q(x.id||x.shipmentId)===q(d.shipmentId||d.id)||q(x.ref||x.reference).toUpperCase()===q(d.reference).toUpperCase()});if(!sh)return false;setTimeout(function(){register(sh,false)},1500);return true}
['exporthub:ready','exporthub:sync','exporthub:shipment-status-visible'].forEach(function(n){window.addEventListener(n,function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()})});window.addEventListener('exporthub:viewchange',function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()});window.addEventListener('focus',function(){if(warehouseBackgroundRelevant())schedule()});window.addEventListener('exporthub:shipment-saved',targetedRegister);window.addEventListener('exporthub:warehouse-updated',function(){if(currentView()==='dashboard'&&window.ExportHUBRC205&&typeof window.ExportHUBRC205.refreshDashboardIfChanged==='function')try{window.ExportHUBRC205.refreshDashboardIfChanged(false)}catch(_){}paintNavIcon()});setInterval(function(){if(document.visibilityState!=='hidden'&&warehouseBackgroundRelevant())sync(false)},120000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){paintNavIcon();if(warehouseBackgroundRelevant())schedule()},{once:true});else{paintNavIcon();if(warehouseBackgroundRelevant())schedule()}
})();
</script>
<style id="rc558-testportal-releasefix-style">
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #app{display:none!important;visibility:hidden!important;pointer-events:none!important}
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #login{position:fixed!important;inset:0!important;z-index:2147483600!important;background:var(--bg,#eef6fb)!important;overflow:auto!important}
@media(max-width:720px){
 html[data-exporthub-testportal="1"] .main{padding:4px 4px max(10px,env(safe-area-inset-bottom))!important}
 html[data-exporthub-testportal="1"] #content .card{padding:8px!important;border-radius:11px!important;margin-bottom:7px!important}
 html[data-exporthub-testportal="1"] #content :is(.grid2,.grid3,.grid4,.metrics){gap:6px!important}
 html[data-exporthub-testportal="1"] #content :is(input,select,textarea){min-height:34px!important;padding:5px 7px!important;font-size:12px!important;border-radius:8px!important}
 html[data-exporthub-testportal="1"] #content textarea{min-height:64px!important}
 html[data-exporthub-testportal="1"] #content :is(button,.btn,.ghost,.soft,.danger){min-height:34px!important;padding:5px 8px!important;font-size:11px!important;border-radius:8px!important}
 html[data
```

## setView
needle='setView'
count=54

### line~19454 pos=3077067
```text
tBefore)nav.insertBefore(cost,costBefore);
  Array.prototype.slice.call(nav.querySelectorAll('button,a')).forEach(function(b){if(b===cost)return;var meta=q(b.textContent)+' '+q(b.getAttribute('onclick'))+' '+q(b.dataset&&b.dataset.view);if(/UPS\s*(Preise|Versandkosten)|^Versandkosten$|calculator/i.test(meta))b.remove()});
 }
 var exam=buttons.find(function(b){return /^Prüfung(?:en)?$/i.test(q(b.textContent))||q(b.getAttribute('data-rc626-view'))==='inspections'||/setView\(['"]quiz|data-view=["']?quiz/i.test(q(b.getAttribute('onclick'))+' '+q(b.outerHTML))});
 if(navAllowed('quiz')){
  if(!exam){exam=makeNavButton('rc626NavInspections','Prüfungen',function(e){e&&e.preventDefault();return openInspections()});var ideas=buttons.find(function(b){return /Ideenpool/i.test(q(b.textContent))});if(ideas)nav.insertBefore(exam,ideas);else nav.appendChild(exam)}
  if(q(exam.textContent)!=='Prüfungen')exam.textContent='Prüfungen';exam.id=exam.id||'rc626NavInspections';exam.removeAttribute('onclick');exam.onclick=function(e){e&&e.preventDefault();e&&e.stopPropagation();return openInspections()};exam.setAttribute('data-rc626-view','inspections');exam.setAttribute('data-view','inspections');exam.setAttribute('data-index298-special-nav','1');exam.setAttribute('data-index298-nav-kind','inspections');exam.removeAttribute('data-index296-nav-group');var examBefore=Array.prototype.slice.call(nav.querySelectorAll('button,a')).find(function(b){return b!==exam&&/Ideenpool/i.test(q(b.textContent))});if(examBefore&&exam.nextElementSibling!==examBefore)nav.insertBefore(exam,examBefore);
  Array.prototype.slice.call(nav.querySelectorAll('button,a')).forEach(function(b){if(b===exam)return;var txt=q(b.textContent),meta=q(b.getAttribute('onclick'))+' '+q(b.outerHTML);if(/^Prüfung(?:en)?$/i.test(txt)||/setView\(['"]quiz|data-view=["']?quiz/i.test(meta))b.remove()});
 }
 var abd=buttons.find(function(b){return /^ABD\s*(erstellen|Anfrage)?$/i.test(q(b.textContent))||/setView\(['\"]abd/i.test(q(b.getAttribute('onclick')))});
 if(abd){abd.onclick=function(e){e&&e.preventDefault();e&&e.stopPropagation();return openAbd(false)};abd.setAttribute('data-rc626-view','abd')}
 return true;
}
function activateNav(kind){document.querySelectorAll('#nav button,#nav a').forEach(function(b){b.classList.toggle('active',q(b.getAttribute('data-rc626-view'))===kind)})}

/* ---------- Versandkosten ---------- */
function obj(v){return !!v&&typeof v==='object'&&!Array.isArray(v)}
function firstValue(o,keys){o=o||{};for(var i=0;i<keys.length;i++){var v=o[keys[i]];if(v!==undefined&&v!==null&&q(v)!=='')return q(v)}return''}
function extractPostal(v){var m=String(v||'').match(/\b\d{4,6}\b/);return m?m[0]:''}
function placeFromAddress(v){
 var text=String(v||'').replace(/\r/g,'\n').trim();if(!text)return'';
 var postal=extractPostal(text),tail='';
 if(postal){var at=text.lastIndexOf(postal);tail=q(text.slice(at+postal.length).replace(/^[\s,;|\-]+|[\s,;|\-]+$/g,''));if(tail){tail=q(tail.split(/[\n,;|]/)[0]);tail=q(tail.replace(/\b(?:Deutschland|Germany|Österreich|Austria|Belgien|Belgium|Frankreich|France|Niederlande|Netherlands|Polen|Poland|Schweiz|Switzerland|Italien|Italy|Spanien|Spain|Tschechien|Czechia|Dänemark|Denmark|Schweden|Sweden|Norwegen|Norway|Vereinigtes Königreich|United Kingdom)\b.*$/i,''));if(tail)return tail}}
 var lines=text.split(/[\n,;|]+/).map(q).filter(Boolean);for(var i=lines.length-1;i>=0;i--){var p=extractPostal(lines[i]);if(p){var rest=q(lines[i].slice(lines[i].lastIndexOf(p)+p.length));if(rest)return rest}}
 return lines.length?lines[lines.length-1]:''
}
function countryCo
```

### line~19456 pos=3077233
```text
ent)+' '+q(b.getAttribute('onclick'))+' '+q(b.dataset&&b.dataset.view);if(/UPS\s*(Preise|Versandkosten)|^Versandkosten$|calculator/i.test(meta))b.remove()});
 }
 var exam=buttons.find(function(b){return /^Prüfung(?:en)?$/i.test(q(b.textContent))||q(b.getAttribute('data-rc626-view'))==='inspections'||/setView\(['"]quiz|data-view=["']?quiz/i.test(q(b.getAttribute('onclick'))+' '+q(b.outerHTML))});
 if(navAllowed('quiz')){
  if(!exam){exam=makeNavButton('rc626NavInspections','Prüfungen',function(e){e&&e.preventDefault();return openInspections()});var ideas=buttons.find(function(b){return /Ideenpool/i.test(q(b.textContent))});if(ideas)nav.insertBefore(exam,ideas);else nav.appendChild(exam)}
  if(q(exam.textContent)!=='Prüfungen')exam.textContent='Prüfungen';exam.id=exam.id||'rc626NavInspections';exam.removeAttribute('onclick');exam.onclick=function(e){e&&e.preventDefault();e&&e.stopPropagation();return openInspections()};exam.setAttribute('data-rc626-view','inspections');exam.setAttribute('data-view','inspections');exam.setAttribute('data-index298-special-nav','1');exam.setAttribute('data-index298-nav-kind','inspections');exam.removeAttribute('data-index296-nav-group');var examBefore=Array.prototype.slice.call(nav.querySelectorAll('button,a')).find(function(b){return b!==exam&&/Ideenpool/i.test(q(b.textContent))});if(examBefore&&exam.nextElementSibling!==examBefore)nav.insertBefore(exam,examBefore);
  Array.prototype.slice.call(nav.querySelectorAll('button,a')).forEach(function(b){if(b===exam)return;var txt=q(b.textContent),meta=q(b.getAttribute('onclick'))+' '+q(b.outerHTML);if(/^Prüfung(?:en)?$/i.test(txt)||/setView\(['"]quiz|data-view=["']?quiz/i.test(meta))b.remove()});
 }
 var abd=buttons.find(function(b){return /^ABD\s*(erstellen|Anfrage)?$/i.test(q(b.textContent))||/setView\(['\"]abd/i.test(q(b.getAttribute('onclick')))});
 if(abd){abd.onclick=function(e){e&&e.preventDefault();e&&e.stopPropagation();return openAbd(false)};abd.setAttribute('data-rc626-view','abd')}
 return true;
}
function activateNav(kind){document.querySelectorAll('#nav button,#nav a').forEach(function(b){b.classList.toggle('active',q(b.getAttribute('data-rc626-view'))===kind)})}

/* ---------- Versandkosten ---------- */
function obj(v){return !!v&&typeof v==='object'&&!Array.isArray(v)}
function firstValue(o,keys){o=o||{};for(var i=0;i<keys.length;i++){var v=o[keys[i]];if(v!==undefined&&v!==null&&q(v)!=='')return q(v)}return''}
function extractPostal(v){var m=String(v||'').match(/\b\d{4,6}\b/);return m?m[0]:''}
function placeFromAddress(v){
 var text=String(v||'').replace(/\r/g,'\n').trim();if(!text)return'';
 var postal=extractPostal(text),tail='';
 if(postal){var at=text.lastIndexOf(postal);tail=q(text.slice(at+postal.length).replace(/^[\s,;|\-]+|[\s,;|\-]+$/g,''));if(tail){tail=q(tail.split(/[\n,;|]/)[0]);tail=q(tail.replace(/\b(?:Deutschland|Germany|Österreich|Austria|Belgien|Belgium|Frankreich|France|Niederlande|Netherlands|Polen|Poland|Schweiz|Switzerland|Italien|Italy|Spanien|Spain|Tschechien|Czechia|Dänemark|Denmark|Schweden|Sweden|Norwegen|Norway|Vereinigtes Königreich|United Kingdom)\b.*$/i,''));if(tail)return tail}}
 var lines=text.split(/[\n,;|]+/).map(q).filter(Boolean);for(var i=lines.length-1;i>=0;i--){var p=extractPostal(lines[i]);if(p){var rest=q(lines[i].slice(lines[i].lastIndexOf(p)+p.length));if(rest)return rest}}
 return lines.length?lines[lines.length-1]:''
}
function countryCode(v){
 var raw=q(v),x=low(raw).replace(/[._]/g,' ').replace(/\s+/g,' ').trim(),compact=x.replace(/[^a-z0-9äöüß]/g,'');
 var map={deutschland:'DE',germany:'DE',de:'DE
```

### line~21137 pos=4229997
```text
De(ed):q(t.day||'Ohne Tag'))+'</div></div>'+(id?'<button class="soft" data-i218-open="'+esc(id)+'">Öffnen</button>':'')+'</div>'}).join('')+'</div>':'';return '<details class="card i218-group" data-i218-group="'+esc(name)+'" '+(isOpen?'open':'')+'><summary><div class="i218-group-title"><span class="i218-group-chevron">›</span><h3>'+esc(name)+'</h3></div><span class="pill orange">'+extra.length+'</span></summary><div class="i218-group-body">'+body+'</div></details>'}
function weekOverview(tasks,ws){return '<div class="i218-overview">'+DAYS.map(function(day){var date=dayDate(ws,day),h=holidayName(date),dayTasks=tasks.filter(function(t){return effectiveDate(t)===date}),chips=GROUPS.map(function(g){var n=dayTasks.filter(function(t){return groupName(t)===g}).length;return n?'<span class="i218-overview-chip">'+esc(g.replace('Aufträge kontrollieren','Kontrolle').replace('Zusatzaufgaben','Zusatz'))+' '+n+'</span>':''}).join('');return '<article class="i218-overview-day '+(h?'is-holiday':'')+'"><h4>'+esc(day)+'</h4><span class="i218-overview-date">'+esc(dateDe(date))+'</span>'+(h?'<span class="i218-holiday">'+esc(h)+'</span>':'')+'<strong class="i218-overview-count">'+dayTasks.length+'</strong><span class="muted">offene Aufgaben</span><div class="i218-overview-chips">'+chips+'</div></article>'}).join('')+'</div>'}
function setWeek(ws){var cur=weekStart(new Date());ws=weekStart(ws);if(ws<cur)ws=cur;st().taskPlannerWeekStart=ws;try{localStorage.setItem('exporthub:planner:week:rc874',ws)}catch(_){}render();return false}
function shiftWeek(n){return setWeek(localISO(addDays(mondayOf(selectedWeek()),n*7)))}
function navigateTasks(){if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('tasks','task-planner');try{if(typeof window.setView==='function')window.setView('tasks')}catch(_){}return false}
/* exporthub-rc923-planner-source-signature */
function plannerSourceSignatureRC923(ws){var s=st(),rows=[];(Array.isArray(s.tasks)?s.tasks:[]).forEach(function(t){if(!t||isTest(t))return;rows.push([q(t.id||t.taskId),q(t.status),q(t.title||t.name),q(t.area||t.category||t.section||t.type),q(t.owner||t.assignee),q(t.day),q(t.originalDay),q(t.plannedDate||t.dueDate||t.date||t.targetDate),q(t.originalDate||t.baseDate),q(t.linkedShipmentRef||t.shipmentRef||t.reference||t.ref),q(t.linkedCustomer||t.customerName),q(t.linkedAccount||t.customerAccount),t.manualMoved===true?'1':'0',t.holidayAutoMoved===true?'1':'0'].join('|'))});rows.sort();return ws+'§'+rows.join('~')+'§'+JSON.stringify(Array.isArray(s.holidays)?s.holidays:[])}
function render(){if(rendering||view()!=='planning')return false;ensureRC895PlannerStyle();var r=root();if(!r)return false;rendering=true;try{var ws=selectedWeek();try{if(window.ExportHUBTaskMasterRC848)window.ExportHUBTaskMasterRC848.ensureWeek(ws,true)}catch(_){}var sourceSignature=plannerSourceSignatureRC923(ws),mounted=!!r.querySelector(':scope>#index218Planner');if(mounted&&sourceSignature===lastSourceSignature)return false;lastSourceSignature=sourceSignature;var tasks=openTasksForWeek(ws),hcount=0;DAYS.forEach(function(d){if(holidayName(dayDate(ws,d)))hcount++});var moved=tasks.filter(function(t){return t.manualMoved||t.holidayAutoMoved}).length,conflicts=tasks.filter(function(t){var d=effectiveDate(t);return d&&!isWorkday(d)&&groupName(t)!=='ABD erstellen'&&groupName(t)!=='Anmelden'}).length,forecast=holidayListFrom(ws),nextImpact=forecast.find(function(h){var d=parseDate(h.date);return d&&d.getDay()!==0&&d.getDay()!==6}),nextLabel=nextImpact?(nextImpact.name+' · in '+daysUntil(nex
```

### line~21137 pos=4230025
```text
+'</div></div>'+(id?'<button class="soft" data-i218-open="'+esc(id)+'">Öffnen</button>':'')+'</div>'}).join('')+'</div>':'';return '<details class="card i218-group" data-i218-group="'+esc(name)+'" '+(isOpen?'open':'')+'><summary><div class="i218-group-title"><span class="i218-group-chevron">›</span><h3>'+esc(name)+'</h3></div><span class="pill orange">'+extra.length+'</span></summary><div class="i218-group-body">'+body+'</div></details>'}
function weekOverview(tasks,ws){return '<div class="i218-overview">'+DAYS.map(function(day){var date=dayDate(ws,day),h=holidayName(date),dayTasks=tasks.filter(function(t){return effectiveDate(t)===date}),chips=GROUPS.map(function(g){var n=dayTasks.filter(function(t){return groupName(t)===g}).length;return n?'<span class="i218-overview-chip">'+esc(g.replace('Aufträge kontrollieren','Kontrolle').replace('Zusatzaufgaben','Zusatz'))+' '+n+'</span>':''}).join('');return '<article class="i218-overview-day '+(h?'is-holiday':'')+'"><h4>'+esc(day)+'</h4><span class="i218-overview-date">'+esc(dateDe(date))+'</span>'+(h?'<span class="i218-holiday">'+esc(h)+'</span>':'')+'<strong class="i218-overview-count">'+dayTasks.length+'</strong><span class="muted">offene Aufgaben</span><div class="i218-overview-chips">'+chips+'</div></article>'}).join('')+'</div>'}
function setWeek(ws){var cur=weekStart(new Date());ws=weekStart(ws);if(ws<cur)ws=cur;st().taskPlannerWeekStart=ws;try{localStorage.setItem('exporthub:planner:week:rc874',ws)}catch(_){}render();return false}
function shiftWeek(n){return setWeek(localISO(addDays(mondayOf(selectedWeek()),n*7)))}
function navigateTasks(){if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('tasks','task-planner');try{if(typeof window.setView==='function')window.setView('tasks')}catch(_){}return false}
/* exporthub-rc923-planner-source-signature */
function plannerSourceSignatureRC923(ws){var s=st(),rows=[];(Array.isArray(s.tasks)?s.tasks:[]).forEach(function(t){if(!t||isTest(t))return;rows.push([q(t.id||t.taskId),q(t.status),q(t.title||t.name),q(t.area||t.category||t.section||t.type),q(t.owner||t.assignee),q(t.day),q(t.originalDay),q(t.plannedDate||t.dueDate||t.date||t.targetDate),q(t.originalDate||t.baseDate),q(t.linkedShipmentRef||t.shipmentRef||t.reference||t.ref),q(t.linkedCustomer||t.customerName),q(t.linkedAccount||t.customerAccount),t.manualMoved===true?'1':'0',t.holidayAutoMoved===true?'1':'0'].join('|'))});rows.sort();return ws+'§'+rows.join('~')+'§'+JSON.stringify(Array.isArray(s.holidays)?s.holidays:[])}
function render(){if(rendering||view()!=='planning')return false;ensureRC895PlannerStyle();var r=root();if(!r)return false;rendering=true;try{var ws=selectedWeek();try{if(window.ExportHUBTaskMasterRC848)window.ExportHUBTaskMasterRC848.ensureWeek(ws,true)}catch(_){}var sourceSignature=plannerSourceSignatureRC923(ws),mounted=!!r.querySelector(':scope>#index218Planner');if(mounted&&sourceSignature===lastSourceSignature)return false;lastSourceSignature=sourceSignature;var tasks=openTasksForWeek(ws),hcount=0;DAYS.forEach(function(d){if(holidayName(dayDate(ws,d)))hcount++});var moved=tasks.filter(function(t){return t.manualMoved||t.holidayAutoMoved}).length,conflicts=tasks.filter(function(t){var d=effectiveDate(t);return d&&!isWorkday(d)&&groupName(t)!=='ABD erstellen'&&groupName(t)!=='Anmelden'}).length,forecast=holidayListFrom(ws),nextImpact=forecast.find(function(h){var d=parseDate(h.date);return d&&d.getDay()!==0&&d.getDay()!==6}),nextLabel=nextImpact?(nextImpact.name+' · in '+daysUntil(nextImpact.date)+' Tagen'):'kei
```

### line~21339 pos=4268217
```text
'+esc(text)+'</span><h3>'+esc(title)+'</h3><p>'+esc(new Date().toLocaleString('de-DE'))+' · die aktuelle Arbeit bleibt geöffnet</p></div><button class="index236-hint-close" type="button" aria-label="Hinweis schließen" data-index236-action="close-hint">×</button></div>'+(empty?'<div class="index236-hint-counts"><span>Alles aktuell</span></div>':'<div class="index236-hint-counts"><span class="is-alert">'+c.tasks+' offene Aufgabe'+(c.tasks===1?'':'n')+'</span><span class="is-alert">'+c.overdueTasks+' überfällig</span></div>')+'<div class="index236-hint-actions"><button class="btn" data-index236-action="open-center">Benachrichtigungen öffnen</button>'+(empty?'':'<button class="ghost" data-index236-action="open-tasks">Aufgaben</button>')+'</div></section>'}
function showHint(c,label,manual){removeHint();if(!manual&&!c.tasks)return;var host=document.createElement('div');host.id='index236NotificationHint';host.className='index236-hint-host';host.innerHTML=hintHtml(c,label,!!manual);document.body.appendChild(host);hintTimer=later(removeHint,manual?7000:10000)}
function checkSchedule(manual){var c=counts();if(manual){showHint(c,'Manuelle Prüfung',true);renderView();return false}var due=slotsDue(new Date());if(!due.length)return false;markDueSeen(due);var label=due[due.length-1].label;addHistory(due.map(function(s){return s.id}).join(','),label,c);persist('Benachrichtigung protokolliert');showHint(c,label,false);nativeNotify(c,label);renderView();return false}
function findShipment(id){var key=q(id),up=key.toUpperCase();return shipments().find(function(sh){return shipmentId(sh)===key||shipmentId(sh).toUpperCase()===up||shipmentRef(sh).toUpperCase()===up})||null}
function findTask(id){var key=q(id);return arr(main().tasks).find(function(t){return taskId(t)===key})||null}
function setViewRaw(view){var s=main();s.view=view;try{document.body.setAttribute('data-exporthub-view',view);document.body.setAttribute('data-current-view',view);document.body.setAttribute('data-eh-view',view)}catch(_){ }}
function navigate(view){try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route(view,'notifications')}catch(_){ }return false}
function openShipment(id){var sh=findShipment(id);if(!sh){alert('Sendung wurde nicht gefunden.');return false}var key=shipmentId(sh)||shipmentRef(sh);if(window.ExportHUBShipmentView&&typeof window.ExportHUBShipmentView.open==='function')return window.ExportHUBShipmentView.open(key,'notifications');return false}
function openTask(id){var t=findTask(id);if(!t){alert('Aufgabe wurde nicht gefunden.');return false}navigate('tasks');setTimeout(function(){try{if(typeof window.rc524OpenTaskEditor==='function')window.rc524OpenTaskEditor(id)}catch(_){ }},100);return false}
function completeTask(id){var t=findTask(id);if(!t)return false;var stamp=new Date().toISOString();t.status='Erledigt';t.done=true;t.completed=true;t.completedAt=stamp;t.doneAt=stamp;t.doneBy=userName();persist('Aufgabe erledigt');renderView();updateBadge();return false}
function setPickupDate(id,value){var sh=findShipment(id),v=iso(value);if(!sh||!v){alert('Bitte ein gültiges Abholdatum auswählen.');return false}sh.pickupDate=v;sh.plannedPickupDate=v;sh.dueDate=v;arr(main().tasks).forEach(function(t){var linked=(q(t.linkedShipmentId)===shipmentId(sh))||(taskRef(t)&&taskRef(t).toUpperCase()===shipmentRef(sh).toUpperCase());if(linked&&!isDone(t)){t.dueDate=v;t.date=v;t.plannedDate=v}});persist('Abholdatum geändert');renderView();updateBadge();return false}
function emailList(v){var out=[];function walk(x){if(x==null)retu
```

### line~21383 pos=4282361
```text
ext?esc(next.toLocaleString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})):'—')+'</strong></div><div><span>Signalton</span><strong>Deaktiviert</strong></div></div><div class="metrics index236-metrics"><div class="metric kpi-orange"><span>Offene Aufgaben</span><strong>'+tasks.length+'</strong></div><div class="metric kpi-red"><span>Überfällige Aufgaben</span><strong>'+late.length+'</strong></div><div class="metric kpi-blue"><span>Prüfzeiten</span><strong>3</strong><small>09:00 · 12:00 · 15:00</small></div></div><section class="card"><div class="index236-section-head"><div><h3>Offene Aufgaben</h3><p>Bis zur Erledigung bei jeder Prüfzeit. Sendungswarnungen werden hier bewusst nicht doppelt angezeigt.</p></div><button class="ghost" data-index236-action="open-tasks">Aufgaben öffnen</button></div><div class="index236-list">'+(tasks.length?tasks.slice().sort(function(a,b){return (taskDate(a)||'9999').localeCompare(taskDate(b)||'9999')}).map(taskCard).join(''):'<div class="index236-empty">Keine offenen Aufgaben.</div>')+'</div></section><section class="card index236-history-card"><h3>Erinnerungsverlauf</h3>'+historyHtml()+'</section></div>';var mounted=!!r.querySelector(':scope>#index236NotificationCenter'),active=document.activeElement,editing=!!(mounted&&active&&r.contains(active)&&/^(INPUT|SELECT|TEXTAREA)$/.test(active.tagName));if(force!==true&&editing){lastNotificationMarkup=html;notificationRenderSkips++;updateBadge();return false}if(force!==true&&mounted&&html===lastNotificationMarkup){notificationRenderSkips++;updateBadge();return false}var scrollTop=r.scrollTop||0;r.innerHTML=html;lastNotificationMarkup=html;notificationDomWrites++;if(scrollTop)r.scrollTop=scrollTop;updateBadge();return true}
function openView(){removeHint();setViewRaw('notifications');renderView();return false}
function updateBadge(){var total=openTasks().length,b=document.getElementById('index236NotifyBadge');if(b){try{b.remove()}catch(_){b.style.display='none'}}var shown=total>99?'99+':String(total);document.querySelectorAll('.nav button,[data-view="notifications"]').forEach(function(x){if(low(x.getAttribute('data-view')||x.dataset&&x.dataset.view||x.textContent)==='notifications'||/benachrichtigungen/.test(low(x.textContent))){x.setAttribute('data-index236-notification-count',shown);x.title=total+' offene Aufgaben-Erinnerung'+(total===1?'':'en');x.classList.toggle('index236-has-alerts',total>0)}})}
function enableBrowser(){if(!('Notification' in window)){alert('Dieser Browser unterstützt keine Systembenachrichtigungen.');renderView();return false}Notification.requestPermission().then(function(p){if(p==='granted'){var n=new Notification('ExportHUB Benachrichtigungen aktiviert',{body:'Erinnerungen erscheinen um 09:00, 12:00 und 15:00 Uhr, solange ExportHUB geöffnet ist.',silent:true,tag:'exporthub-enabled'});setTimeout(function(){try{n.close()}catch(_){ }},3500)}renderView()});return false}
function install(){if(installing)return;installing=true;try{window.rc451OpenNotifications=openView;window.rc451OpenOverview=function(){return navigate('shipmentoverview')};window.rc451OpenTaskList=function(){return navigate('tasks')}}finally{installing=false}}
function lock(){if(window.ExportHUBBuild){window.ExportHUBBuild.applyVersion();window.ExportHUBBuild.applyTitle()}}

document.addEventListener('click',function(e){var target=e.target&&e.target.closest&&e.target.closest('button,a');if(!target)return;var action=target.dataset&&target.dataset.index236Action,id=target.dataset&&target.dataset.id;if(!action)return;e.preventDefault();if
```

### line~21537 pos=4354924
```text
loadlist'},{force:true,source:'manual-download'}).then(function(result){if(result&&result.saved)message('POD-PDF wurde lokal heruntergeladen und in SharePoint gesichert.','ok')}).catch(function(error){message('POD wurde lokal heruntergeladen. Die SharePoint-Sicherung ist noch offen: '+q(error&&error.message||error),'bad')})}catch(e){console.error('RC352 POD-PDF',e);alert('Die unterschriebene Ladeliste konnte nicht als POD heruntergeladen werden.\n\n'+q(e&&e.message||e))}finally{pdfBusy=false;if(button){button.disabled=false;button.textContent=old}}return false}
function routeShipment(){var sh=currentSaved(),id=sh&&(sid(sh)||sref(sh)),ctx=documentReturnContext(),ref=sh&&sref(sh);if(ctx&&sh&&(q(ctx.shipmentId)===q(id)||q(ctx.reference).toUpperCase()===q(ref).toUpperCase())){states().forEach(function(st){st.shipmentViewId=id;st.selectedShipmentId=id;st.documentShipmentId=id});try{sessionStorage.removeItem('exporthub_document_return_v1')}catch(_){}try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentview','documents-return-view')}catch(_){}return false}states().forEach(function(st){if(sh){st.shipment=clone(sh);st.currentShipment=clone(sh);st.selectedShipment=clone(sh);st.activeShipmentId=id;st.selectedShipmentId=id;st.currentShipmentId=id;st.editingShipmentId=id;st.documentShipmentId=id}});try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipment','documents-return');states().forEach(function(st){st.view='shipment'});document.body.setAttribute('data-exporthub-view','shipment');document.body.setAttribute('data-current-view','shipment');document.body.setAttribute('data-eh-view','shipment');if(window.ExportHUBRC524&&typeof window.ExportHUBRC524.setView==='function')return window.ExportHUBRC524.setView('shipment')}catch(_){}return false}
function routeOverview(){try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentoverview','documents')}catch(_){}return false}
function patchSelector(selected){var sel=document.getElementById('rc576ShipmentSelect');if(!sel)return shipments().length;var keep=q(selected||sel.value),html=options(keep);if(sel.innerHTML!==html)sel.innerHTML=html;if(keep)sel.value=keep;var info=document.getElementById('index240DocumentStatus');if(info)info.textContent=shipments().length+' gespeicherte Sendung'+(shipments().length===1?'':'en')+' geladen';return shipments().length}
function setDocumentViewState(){states().forEach(function(st){st.view='cmr'});try{document.body.setAttribute('data-exporthub-view','cmr');document.body.setAttribute('data-current-view','cmr');document.body.setAttribute('data-eh-view','cmr')}catch(_){}}
function requestCentralDocumentRoute(id,source){var first=states()[0]||{},view=low((document.body?document.body.getAttribute('data-exporthub-view'):'')||(document.body?document.body.getAttribute('data-current-view'):'')||first.view||'');if(document.getElementById('rc576DocumentHub'))view='cmr';if(view==='cmr')return false;var selected=q(id);if(selected)states().forEach(function(st){st.documentShipmentId=selected;st.rc565SelectedShipmentId=selected;st.rc616SelectedShipmentId=selected});var nav=window.ExportHUBRC325;if(nav&&typeof nav.route==='function'){nav.route('cmr',source||'documents-api');return true}return false}
async function openHub(forceEmpty){if(opening)return false;if(requestCentralDocumentRoute('', 'documents-hub'))return false;setDocumentViewState();var sh=forceEmpty?null:currentSaved();return sh?o
```

### line~21537 pos=4354974
```text
then(function(result){if(result&&result.saved)message('POD-PDF wurde lokal heruntergeladen und in SharePoint gesichert.','ok')}).catch(function(error){message('POD wurde lokal heruntergeladen. Die SharePoint-Sicherung ist noch offen: '+q(error&&error.message||error),'bad')})}catch(e){console.error('RC352 POD-PDF',e);alert('Die unterschriebene Ladeliste konnte nicht als POD heruntergeladen werden.\n\n'+q(e&&e.message||e))}finally{pdfBusy=false;if(button){button.disabled=false;button.textContent=old}}return false}
function routeShipment(){var sh=currentSaved(),id=sh&&(sid(sh)||sref(sh)),ctx=documentReturnContext(),ref=sh&&sref(sh);if(ctx&&sh&&(q(ctx.shipmentId)===q(id)||q(ctx.reference).toUpperCase()===q(ref).toUpperCase())){states().forEach(function(st){st.shipmentViewId=id;st.selectedShipmentId=id;st.documentShipmentId=id});try{sessionStorage.removeItem('exporthub_document_return_v1')}catch(_){}try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentview','documents-return-view')}catch(_){}return false}states().forEach(function(st){if(sh){st.shipment=clone(sh);st.currentShipment=clone(sh);st.selectedShipment=clone(sh);st.activeShipmentId=id;st.selectedShipmentId=id;st.currentShipmentId=id;st.editingShipmentId=id;st.documentShipmentId=id}});try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipment','documents-return');states().forEach(function(st){st.view='shipment'});document.body.setAttribute('data-exporthub-view','shipment');document.body.setAttribute('data-current-view','shipment');document.body.setAttribute('data-eh-view','shipment');if(window.ExportHUBRC524&&typeof window.ExportHUBRC524.setView==='function')return window.ExportHUBRC524.setView('shipment')}catch(_){}return false}
function routeOverview(){try{if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentoverview','documents')}catch(_){}return false}
function patchSelector(selected){var sel=document.getElementById('rc576ShipmentSelect');if(!sel)return shipments().length;var keep=q(selected||sel.value),html=options(keep);if(sel.innerHTML!==html)sel.innerHTML=html;if(keep)sel.value=keep;var info=document.getElementById('index240DocumentStatus');if(info)info.textContent=shipments().length+' gespeicherte Sendung'+(shipments().length===1?'':'en')+' geladen';return shipments().length}
function setDocumentViewState(){states().forEach(function(st){st.view='cmr'});try{document.body.setAttribute('data-exporthub-view','cmr');document.body.setAttribute('data-current-view','cmr');document.body.setAttribute('data-eh-view','cmr')}catch(_){}}
function requestCentralDocumentRoute(id,source){var first=states()[0]||{},view=low((document.body?document.body.getAttribute('data-exporthub-view'):'')||(document.body?document.body.getAttribute('data-current-view'):'')||first.view||'');if(document.getElementById('rc576DocumentHub'))view='cmr';if(view==='cmr')return false;var selected=q(id);if(selected)states().forEach(function(st){st.documentShipmentId=selected;st.rc565SelectedShipmentId=selected;st.rc616SelectedShipmentId=selected});var nav=window.ExportHUBRC325;if(nav&&typeof nav.route==='function'){nav.route('cmr',source||'documents-api');return true}return false}
async function openHub(forceEmpty){if(opening)return false;if(requestCentralDocumentRoute('', 'documents-hub'))return false;setDocumentViewState();var sh=forceEmpty?null:currentSaved();return sh?openDocuments(sid(sh)||sref(sh)):renderHub('',null)
```

## search lowercase
needle='search'
count=179

### line~25027 pos=5061669
```text
786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" data-rc786-viewer-print="1">Drucken</button>':'')+'<button type="button" class="ghost" data-rc786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.contentWindow.focus();frame.contentWindow.print();return false}catch(_){}}var v=viewerPersist();if(v){v.autoPrint=true;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){} }viewerLoad();return false}
document.addEventListener('input',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('search',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('keydown',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch'&&e.key==='Enter'){e.preventDefault();return renderSearchResults(input.value)}},true);
document.addEventListener('click',function(e){var clearSearch=e.target&&e.target.closest&&e.target.closest('[data-rc911-search-clear]');if(clearSearch){e.preventDefault();shipmentSearchQuery='';var input=document.getElementById('rc807ShipmentSearch');if(input)input.value='';return renderSearchResults('')}var openSearch=e.target&&e.target.closest&&e.target.closest('[data-rc807-open-shipment]');if(openSearch){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();return open(openSearch.getAttribute('data-rc807-open-shipment'),'shipmentsearch')}var b=e.target&&e.target.closest&&e.target.closest('[data-rc776-back],[data-rc784-overview],[data-rc786-open-file],[data-rc786-download-file],[data-rc786-print-file],[data-rc776-documents],[data-rc786-open-ref],[data-rc786-viewer-back],[data-rc786-viewer-download],[data-rc786-viewer-print]');if(!b)return;if(b.hasAttribute('data-rc776-back')){e.preventDefault();return back()}if(b.hasAttribute('data-rc784-overview')){e.preventDefault();return overview()}if(b.hasAttribute('data-rc786-open-file')){e.preventDefault();return openDoc(b.getAttribute('data-rc786-open-file'))}if(b.hasAttribute('data-rc786-download-file')){e.preventDefault();return downloadDoc(b.getAttribute('data-rc786-download-file'))}if(b.hasAttribute('data-rc786-print-file')){e.preventDefault();return printDoc(b.getAttribute('data-rc786-print-file'))}if(b.hasAttribute('data-rc776-documents')){e.preventDefault();return openDocuments()}if(b.hasAttribute('data-rc786-open-ref')){e.preventDefault();return openReferenceFolder()}if(b.hasAttribute('data-rc786-viewer-back')){e.preventDefault();return viewerBack()}if(b.hasAttribute('data-rc786-viewer-download')){e.preventDefault();return viewerDownload()}if(b.hasAttribute('data-rc786-viewer-print')){e.preventDefault();return viewerPrint()}},true);
['exporthub:sync','exporthub:rendered'].forEach(function(name){window.addEventListener(name
```

### line~25027 pos=5062101
```text
c786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.contentWindow.focus();frame.contentWindow.print();return false}catch(_){}}var v=viewerPersist();if(v){v.autoPrint=true;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){} }viewerLoad();return false}
document.addEventListener('input',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('search',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('keydown',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch'&&e.key==='Enter'){e.preventDefault();return renderSearchResults(input.value)}},true);
document.addEventListener('click',function(e){var clearSearch=e.target&&e.target.closest&&e.target.closest('[data-rc911-search-clear]');if(clearSearch){e.preventDefault();shipmentSearchQuery='';var input=document.getElementById('rc807ShipmentSearch');if(input)input.value='';return renderSearchResults('')}var openSearch=e.target&&e.target.closest&&e.target.closest('[data-rc807-open-shipment]');if(openSearch){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();return open(openSearch.getAttribute('data-rc807-open-shipment'),'shipmentsearch')}var b=e.target&&e.target.closest&&e.target.closest('[data-rc776-back],[data-rc784-overview],[data-rc786-open-file],[data-rc786-download-file],[data-rc786-print-file],[data-rc776-documents],[data-rc786-open-ref],[data-rc786-viewer-back],[data-rc786-viewer-download],[data-rc786-viewer-print]');if(!b)return;if(b.hasAttribute('data-rc776-back')){e.preventDefault();return back()}if(b.hasAttribute('data-rc784-overview')){e.preventDefault();return overview()}if(b.hasAttribute('data-rc786-open-file')){e.preventDefault();return openDoc(b.getAttribute('data-rc786-open-file'))}if(b.hasAttribute('data-rc786-download-file')){e.preventDefault();return downloadDoc(b.getAttribute('data-rc786-download-file'))}if(b.hasAttribute('data-rc786-print-file')){e.preventDefault();return printDoc(b.getAttribute('data-rc786-print-file'))}if(b.hasAttribute('data-rc776-documents')){e.preventDefault();return openDocuments()}if(b.hasAttribute('data-rc786-open-ref')){e.preventDefault();return openReferenceFolder()}if(b.hasAttribute('data-rc786-viewer-back')){e.preventDefault();return viewerBack()}if(b.hasAttribute('data-rc786-viewer-download')){e.preventDefault();return viewerDownload()}if(b.hasAttribute('data-rc786-viewer-print')){e.preventDefault();return viewerPrint()}},true);
['exporthub:sync','exporthub:rendered'].forEach(function(name){window.addEventListener(name,function(){if(rc843ViewGuard)rc843ScheduleViewGuard(name==='exporthub:sync'?30:80)})});
document.addEventListener('click',function(e){if(!rc843ViewGuard)return;var t=e.target;if(!t||!t.closest)return;var explicit=t.closest('#nav button,.brand,[data-index321-route],[data-index321-view],[data-rc776-back],[data-rc784-overview],[data-rc776-documents],[data-rc786-open-ref]');if(explicit)rc843ReleaseViewGuard('user-navigation')},true
```

### line~25030 pos=5064014
```text
open-file],[data-rc786-download-file],[data-rc786-print-file],[data-rc776-documents],[data-rc786-open-ref],[data-rc786-viewer-back],[data-rc786-viewer-download],[data-rc786-viewer-print]');if(!b)return;if(b.hasAttribute('data-rc776-back')){e.preventDefault();return back()}if(b.hasAttribute('data-rc784-overview')){e.preventDefault();return overview()}if(b.hasAttribute('data-rc786-open-file')){e.preventDefault();return openDoc(b.getAttribute('data-rc786-open-file'))}if(b.hasAttribute('data-rc786-download-file')){e.preventDefault();return downloadDoc(b.getAttribute('data-rc786-download-file'))}if(b.hasAttribute('data-rc786-print-file')){e.preventDefault();return printDoc(b.getAttribute('data-rc786-print-file'))}if(b.hasAttribute('data-rc776-documents')){e.preventDefault();return openDocuments()}if(b.hasAttribute('data-rc786-open-ref')){e.preventDefault();return openReferenceFolder()}if(b.hasAttribute('data-rc786-viewer-back')){e.preventDefault();return viewerBack()}if(b.hasAttribute('data-rc786-viewer-download')){e.preventDefault();return viewerDownload()}if(b.hasAttribute('data-rc786-viewer-print')){e.preventDefault();return viewerPrint()}},true);
['exporthub:sync','exporthub:rendered'].forEach(function(name){window.addEventListener(name,function(){if(rc843ViewGuard)rc843ScheduleViewGuard(name==='exporthub:sync'?30:80)})});
document.addEventListener('click',function(e){if(!rc843ViewGuard)return;var t=e.target;if(!t||!t.closest)return;var explicit=t.closest('#nav button,.brand,[data-index321-route],[data-index321-view],[data-rc776-back],[data-rc784-overview],[data-rc776-documents],[data-rc786-open-ref]');if(explicit)rc843ReleaseViewGuard('user-navigation')},true);
window.ExportHUBShipmentView=Object.freeze({version:'RC844',open:open,render:render,renderSearch:renderSearch,search:renderSearchResults,back:back,overview:overview,find:find,openFile:openDoc,downloadFile:downloadDoc,printFile:printDoc,openDocuments:openDocuments,refreshReferenceFiles:function(){var sh=currentShipmentForDocs();if(!sh)return false;delete referenceCache[refOf(sh)];return loadReferenceDocs(sh,collectDocs(sh).map(normalizeFallback))},diagnostics:function(){return{view:low(state().view),shipmentId:q(state().shipmentViewId),readOnly:true,referenceApi:true,searchQuery:shipmentSearchQuery,searchResults:shipmentSearchResults(shipmentSearchQuery).length,inputs:document.querySelectorAll('#content.rc776-shipment-view input,#content.rc776-shipment-view select,#content.rc776-shipment-view textarea').length,saveButtons:Array.from(document.querySelectorAll('#content.rc776-shipment-view button')).filter(function(b){return /speichern|save/i.test(q(b.textContent))}).length}}});
window.ExportHUBDocumentViewer=Object.freeze({version:'RC844',render:renderViewer,back:viewerBack,download:viewerDownload,print:viewerPrint,diagnostics:function(){var v=viewerPersist();return{view:low(state().view),reference:v&&v.reference||'',name:v&&v.name||'',itemId:v&&v.itemId||'',storedFileOnly:true}}});
})();
</script>

<!-- EXPORTHUB_TESTSERVICE_FILE_BEGIN path=api/location-booking/index.js encoding=base64 sha256=
```

### line~25030 pos=5064477
```text
le'))}if(b.hasAttribute('data-rc786-download-file')){e.preventDefault();return downloadDoc(b.getAttribute('data-rc786-download-file'))}if(b.hasAttribute('data-rc786-print-file')){e.preventDefault();return printDoc(b.getAttribute('data-rc786-print-file'))}if(b.hasAttribute('data-rc776-documents')){e.preventDefault();return openDocuments()}if(b.hasAttribute('data-rc786-open-ref')){e.preventDefault();return openReferenceFolder()}if(b.hasAttribute('data-rc786-viewer-back')){e.preventDefault();return viewerBack()}if(b.hasAttribute('data-rc786-viewer-download')){e.preventDefault();return viewerDownload()}if(b.hasAttribute('data-rc786-viewer-print')){e.preventDefault();return viewerPrint()}},true);
['exporthub:sync','exporthub:rendered'].forEach(function(name){window.addEventListener(name,function(){if(rc843ViewGuard)rc843ScheduleViewGuard(name==='exporthub:sync'?30:80)})});
document.addEventListener('click',function(e){if(!rc843ViewGuard)return;var t=e.target;if(!t||!t.closest)return;var explicit=t.closest('#nav button,.brand,[data-index321-route],[data-index321-view],[data-rc776-back],[data-rc784-overview],[data-rc776-documents],[data-rc786-open-ref]');if(explicit)rc843ReleaseViewGuard('user-navigation')},true);
window.ExportHUBShipmentView=Object.freeze({version:'RC844',open:open,render:render,renderSearch:renderSearch,search:renderSearchResults,back:back,overview:overview,find:find,openFile:openDoc,downloadFile:downloadDoc,printFile:printDoc,openDocuments:openDocuments,refreshReferenceFiles:function(){var sh=currentShipmentForDocs();if(!sh)return false;delete referenceCache[refOf(sh)];return loadReferenceDocs(sh,collectDocs(sh).map(normalizeFallback))},diagnostics:function(){return{view:low(state().view),shipmentId:q(state().shipmentViewId),readOnly:true,referenceApi:true,searchQuery:shipmentSearchQuery,searchResults:shipmentSearchResults(shipmentSearchQuery).length,inputs:document.querySelectorAll('#content.rc776-shipment-view input,#content.rc776-shipment-view select,#content.rc776-shipment-view textarea').length,saveButtons:Array.from(document.querySelectorAll('#content.rc776-shipment-view button')).filter(function(b){return /speichern|save/i.test(q(b.textContent))}).length}}});
window.ExportHUBDocumentViewer=Object.freeze({version:'RC844',render:renderViewer,back:viewerBack,download:viewerDownload,print:viewerPrint,diagnostics:function(){var v=viewerPersist();return{view:low(state().view),reference:v&&v.reference||'',name:v&&v.name||'',itemId:v&&v.itemId||'',storedFileOnly:true}}});
})();
</script>

<!-- EXPORTHUB_TESTSERVICE_FILE_BEGIN path=api/location-booking/index.js encoding=base64 sha256=
```

### line~25030 pos=5064509
```text
786-download-file')){e.preventDefault();return downloadDoc(b.getAttribute('data-rc786-download-file'))}if(b.hasAttribute('data-rc786-print-file')){e.preventDefault();return printDoc(b.getAttribute('data-rc786-print-file'))}if(b.hasAttribute('data-rc776-documents')){e.preventDefault();return openDocuments()}if(b.hasAttribute('data-rc786-open-ref')){e.preventDefault();return openReferenceFolder()}if(b.hasAttribute('data-rc786-viewer-back')){e.preventDefault();return viewerBack()}if(b.hasAttribute('data-rc786-viewer-download')){e.preventDefault();return viewerDownload()}if(b.hasAttribute('data-rc786-viewer-print')){e.preventDefault();return viewerPrint()}},true);
['exporthub:sync','exporthub:rendered'].forEach(function(name){window.addEventListener(name,function(){if(rc843ViewGuard)rc843ScheduleViewGuard(name==='exporthub:sync'?30:80)})});
document.addEventListener('click',function(e){if(!rc843ViewGuard)return;var t=e.target;if(!t||!t.closest)return;var explicit=t.closest('#nav button,.brand,[data-index321-route],[data-index321-view],[data-rc776-back],[data-rc784-overview],[data-rc776-documents],[data-rc786-open-ref]');if(explicit)rc843ReleaseViewGuard('user-navigation')},true);
window.ExportHUBShipmentView=Object.freeze({version:'RC844',open:open,render:render,renderSearch:renderSearch,search:renderSearchResults,back:back,overview:overview,find:find,openFile:openDoc,downloadFile:downloadDoc,printFile:printDoc,openDocuments:openDocuments,refreshReferenceFiles:function(){var sh=currentShipmentForDocs();if(!sh)return false;delete referenceCache[refOf(sh)];return loadReferenceDocs(sh,collectDocs(sh).map(normalizeFallback))},diagnostics:function(){return{view:low(state().view),shipmentId:q(state().shipmentViewId),readOnly:true,referenceApi:true,searchQuery:shipmentSearchQuery,searchResults:shipmentSearchResults(shipmentSearchQuery).length,inputs:document.querySelectorAll('#content.rc776-shipment-view input,#content.rc776-shipment-view select,#content.rc776-shipment-view textarea').length,saveButtons:Array.from(document.querySelectorAll('#content.rc776-shipment-view button')).filter(function(b){return /speichern|save/i.test(q(b.textContent))}).length}}});
window.ExportHUBDocumentViewer=Object.freeze({version:'RC844',render:renderViewer,back:viewerBack,download:viewerDownload,print:viewerPrint,diagnostics:function(){var v=viewerPersist();return{view:low(state().view),reference:v&&v.reference||'',name:v&&v.name||'',itemId:v&&v.itemId||'',storedFileOnly:true}}});
})();
</script>

<!-- EXPORTHUB_TESTSERVICE_FILE_BEGIN path=api/location-booking/index.js encoding=base64 sha256=
```

### line~27011 pos=5369498
```text
flow-x:hidden!important;touch-action:pan-y!important;overscroll-behavior:contain!important;-webkit-overflow-scrolling:touch!important;padding:0 2px calc(90px + env(safe-area-inset-bottom))!important;scrollbar-gutter:stable!important}
  body .sidebar>.profile{flex:0 0 auto!important;margin-top:8px!important}
  body #nav>[data-index308-group]:not([hidden]){position:relative!important;display:block!important;visibility:visible!important;flex:none!important}
  body #nav>button[data-index321-view]:not([hidden]){position:relative!important;z-index:2202!important;display:flex!important;visibility:visible!important;pointer-events:auto!important;touch-action:manipulation!important;user-select:none!important;-webkit-user-select:none!important;min-height:52px!important;flex:none!important}
  body #nav>button[data-index321-view][hidden],body #nav>button[data-index308-view][hidden],body #nav>button[data-index305-view][hidden],body #nav>[data-index308-group][hidden],body #nav>[data-index305-group][hidden]{display:none!important;visibility:hidden!important;pointer-events:none!important}
  body #nav>button[data-index321-view]:active{transform:scale(.985)!important;background:rgba(255,255,255,.16)!important}
  body .main{display:block!important;width:100%!important;min-width:0!important;min-height:100dvh!important;margin:0!important;padding:10px!important;background:transparent!important}
  body .topbar{display:flex!important;position:sticky!important;top:6px!important;z-index:1000!important;width:100%!important;min-height:58px!important;margin:0 0 12px!important;padding:8px!important;border-radius:17px!important;background:rgba(255,255,255,.94)!important}
  body .eh-menu-btn{display:inline-grid!important;visibility:visible!important;opacity:1!important;flex:0 0 42px!important}
  body .search{order:10!important;flex:1 0 100%!important;width:100%!important}
  body #content{display:block!important;visibility:visible!important;opacity:1!important;width:100%!important;min-height:calc(100dvh - 82px)!important}
  body #content>*{max-width:100%!important}
  body.eh-sidebar-open{overflow:hidden!important}
  body.eh-sidebar-open::after{content:"";position:fixed;inset:0;background:rgba(2,12,27,.46);z-index:1100;pointer-events:none!important}
  body:not(.eh-sidebar-open) #content input,body:not(.eh-sidebar-open) #content select,body:not(.eh-sidebar-open) #content textarea,body:not(.eh-sidebar-open) #content button,body:not(.eh-sidebar-open) #content a,body:not(.eh-sidebar-open) #content label{pointer-events:auto!important;touch-action:manipulation!important}
  body:not(.eh-sidebar-open) #content input,body:not(.eh-sidebar-open) #content textarea{user-select:text!important;-webkit-user-select:text!important}
  body:not(.eh-sidebar-open) .main,body:not(.eh-sidebar-open) #content{position:relative!important;z-index:auto!important}
  body .grid2,body .grid3,body .grid4,body .metrics,body .split,body .folder-header,body .miniGrid,body .summaryGrid,body .rc147-week-grid,body .rc150-week-grid,body .rc138-week-grid,body .task-grid{grid-template-columns:minmax(0,1fr)!important;min-width:0!important}
  body .card,body .metric{min-width:0!important;max-width:100%!important;overflow-wrap:anywhere!important}body .table-wrap{min-width:0!important;max-width:100%!important;overflow-x:auto!important}
  body #rc626Shipping{width:100%!important;max-width:100%!important;min-width:0!important;box-sizing:border-box!important;overflow-x:hidden!important}
  body #rc626Shipping .rc501-shipping-layout,body #rc626Shipping .rc501-form,body #rc626Shipping .rc501-field-group,body #rc626Shipp
```

### line~27040 pos=5375120
```text
ant;padding-right:0!important;overflow-x:hidden!important}
  body #content>*,body[data-eh-theme] #content>*{width:100%!important;max-width:100%!important;min-width:0!important;box-sizing:border-box!important}
  body .sidebar,body[data-eh-theme] .sidebar,body[data-eh-theme][data-eh-theme] .sidebar{position:fixed!important;left:0!important;right:auto!important;top:0!important;bottom:0!important;width:min(320px,88vw)!important;max-width:88vw!important;height:100dvh!important;transform:translateX(-105%)!important;margin:0!important}
  body.eh-sidebar-open .sidebar,body[data-eh-theme].eh-sidebar-open .sidebar,body.eh-sidebar-open[data-eh-theme] .sidebar{transform:translateX(0)!important}
  body .topbar,body[data-eh-theme] .topbar,body[data-eh-theme][data-eh-theme] .topbar{display:grid!important;grid-template-columns:44px minmax(0,1fr)!important;align-items:start!important;gap:7px!important;width:100%!important;max-width:100%!important;min-width:0!important;margin:0 0 10px!important;padding:7px!important;border-radius:14px!important;overflow:visible!important}
  body #ehMenuBtn,body .eh-menu-btn,body[data-eh-theme] #ehMenuBtn{display:inline-grid!important;grid-column:1!important;grid-row:1!important;place-items:center!important;width:44px!important;min-width:44px!important;max-width:44px!important;height:44px!important;min-height:44px!important;padding:0!important;margin:0!important;justify-self:start!important;align-self:start!important}
  body .eh-topbar-main,body[data-eh-theme] .eh-topbar-main,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-main{grid-column:2!important;grid-row:1!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;gap:6px!important;width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-main .search,body[data-eh-theme] .eh-topbar-main .search{order:1!important;width:100%!important;max-width:100%!important;min-width:0!important;margin:0!important;padding:7px 9px!important}
  body .eh-topbar-environment,body[data-eh-theme] .eh-topbar-environment{order:2!important;display:block!important;width:100%!important;max-width:100%!important;min-width:0!important;text-align:left!important}
  body .eh-testservice-ribbon,html[data-exporthub-testportal="1"] .eh-testservice-ribbon{display:flex!important;flex-wrap:wrap!important;justify-content:flex-start!important;gap:3px 7px!important;width:100%!important;max-width:100%!important;min-width:0!important;white-space:normal!important;overflow-wrap:anywhere!important;line-height:1.2!important;padding:6px 8px!important;font-size:10px!important}
  body .eh-testservice-ribbon small{white-space:normal!important;overflow-wrap:anywhere!important;font-size:9px!important}
  body .eh-topbar-tools,body[data-eh-theme] .eh-topbar-tools,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-tools{grid-column:1/-1!important;grid-row:2!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;gap:6px!important;width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-options,body[data-eh-theme] .eh-topbar-options,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-options{display:grid!important;grid-template-columns:minmax(0,1fr) minmax(0,1fr)!important;gap:6px!important;width:100%!important;min-width:0!important}
  body .eh-topbar-options>*,body[data-eh-theme] .eh-topbar-options>*{width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-actions,body[data-eh-theme] .eh-topbar-actions,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-actions{display:grid!impor
```

### line~27040 pos=5375164
```text
den!important}
  body #content>*,body[data-eh-theme] #content>*{width:100%!important;max-width:100%!important;min-width:0!important;box-sizing:border-box!important}
  body .sidebar,body[data-eh-theme] .sidebar,body[data-eh-theme][data-eh-theme] .sidebar{position:fixed!important;left:0!important;right:auto!important;top:0!important;bottom:0!important;width:min(320px,88vw)!important;max-width:88vw!important;height:100dvh!important;transform:translateX(-105%)!important;margin:0!important}
  body.eh-sidebar-open .sidebar,body[data-eh-theme].eh-sidebar-open .sidebar,body.eh-sidebar-open[data-eh-theme] .sidebar{transform:translateX(0)!important}
  body .topbar,body[data-eh-theme] .topbar,body[data-eh-theme][data-eh-theme] .topbar{display:grid!important;grid-template-columns:44px minmax(0,1fr)!important;align-items:start!important;gap:7px!important;width:100%!important;max-width:100%!important;min-width:0!important;margin:0 0 10px!important;padding:7px!important;border-radius:14px!important;overflow:visible!important}
  body #ehMenuBtn,body .eh-menu-btn,body[data-eh-theme] #ehMenuBtn{display:inline-grid!important;grid-column:1!important;grid-row:1!important;place-items:center!important;width:44px!important;min-width:44px!important;max-width:44px!important;height:44px!important;min-height:44px!important;padding:0!important;margin:0!important;justify-self:start!important;align-self:start!important}
  body .eh-topbar-main,body[data-eh-theme] .eh-topbar-main,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-main{grid-column:2!important;grid-row:1!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;gap:6px!important;width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-main .search,body[data-eh-theme] .eh-topbar-main .search{order:1!important;width:100%!important;max-width:100%!important;min-width:0!important;margin:0!important;padding:7px 9px!important}
  body .eh-topbar-environment,body[data-eh-theme] .eh-topbar-environment{order:2!important;display:block!important;width:100%!important;max-width:100%!important;min-width:0!important;text-align:left!important}
  body .eh-testservice-ribbon,html[data-exporthub-testportal="1"] .eh-testservice-ribbon{display:flex!important;flex-wrap:wrap!important;justify-content:flex-start!important;gap:3px 7px!important;width:100%!important;max-width:100%!important;min-width:0!important;white-space:normal!important;overflow-wrap:anywhere!important;line-height:1.2!important;padding:6px 8px!important;font-size:10px!important}
  body .eh-testservice-ribbon small{white-space:normal!important;overflow-wrap:anywhere!important;font-size:9px!important}
  body .eh-topbar-tools,body[data-eh-theme] .eh-topbar-tools,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-tools{grid-column:1/-1!important;grid-row:2!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;gap:6px!important;width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-options,body[data-eh-theme] .eh-topbar-options,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-options{display:grid!important;grid-template-columns:minmax(0,1fr) minmax(0,1fr)!important;gap:6px!important;width:100%!important;min-width:0!important}
  body .eh-topbar-options>*,body[data-eh-theme] .eh-topbar-options>*{width:100%!important;max-width:100%!important;min-width:0!important}
  body .eh-topbar-actions,body[data-eh-theme] .eh-topbar-actions,body[data-eh-theme][data-eh-theme] .topbar .eh-topbar-actions{display:grid!important;grid-template-columns:repeat(2,minmax(0
```

## Suche
needle='Suche'
count=37

### line~13240 pos=1842728
```text
sches Zeitfenster kann die Abholung verschieben.\\\"},{\\\"title\\\":\\\"Collis erfassen\\\",\\\"text\\\":\\\"Collis eintragen:\\\\n1WPA = Einwegpalette\\\\nEUPA = Europalette\\\\nOTHE = Paket\\\\nEs kann angepasst werden, was wirklich vorhanden ist.\\\",\\\"warning\\\":\\\"Nur tats\\u00e4chliche Packst\\u00fccke erfassen.\\\"},{\\\"title\\\":\\\"Stapelbarkeit pr\\u00fcfen\\\",\\\"text\\\":\\\"Die Ware ist nie stapelbar. Die entsprechenden Haken/Angaben so setzen, dass keine Stapelbarkeit angegeben wird.\\\",\\\"warning\\\":\\\"Falsche Stapelbarkeit kann zu Transportsch\\u00e4den oder falscher Planung f\\u00fchren.\\\"},{\\\"title\\\":\\\"Bemerkung mit Referenznummer und ATR-Nummer erfassen\\\",\\\"text\\\":\\\"In das Feld Bemerkung die Referenznummer aus der Ladeliste UND zus\\u00e4tzlich die ATR-Nummer eintragen.\\\",\\\"warning\\\":\\\"Bei BSH T\\u00fcrkei darf die ATR-Nummer in der Bemerkung nicht fehlen.\\\"},{\\\"title\\\":\\\"Lieferavis \\u00f6ffnen\\\",\\\"text\\\":\\\"Unten rechts das Lieferavis \\u00f6ffnen.\\\",\\\"warning\\\":\\\"Ohne Lieferavis ist die Anmeldung nicht vollst\\u00e4ndig.\\\"},{\\\"title\\\":\\\"Lieferavis anhand Rechnung erfassen\\\",\\\"text\\\":\\\"Beim Lieferavis die Rechnung verwenden und NICHT den Lieferschein. Alle Artikel und Liefermengen aus der Rechnung erfassen.\\\",\\\"warning\\\":\\\"Bei BSH T\\u00fcrkei ist die Rechnung die Grundlage f\\u00fcr das Lieferavis, nicht der Lieferschein.\\\"},{\\\"title\\\":\\\"Doppelte Artikel zusammenfassen\\\",\\\"text\\\":\\\"Wenn ein Artikel doppelt vorkommt, die Mengen zusammenfassen und eine DNC-Nummer eintragen.\\\",\\\"warning\\\":\\\"Keine doppelten Mengen erfassen.\\\"},{\\\"title\\\":\\\"Kundenartikelnummer suchen\\\",\\\"text\\\":\\\"Mit der Kundenartikelnummer suchen. STRG + F kann zur Suche verwendet werden.\\\",\\\"warning\\\":\\\"Nicht mit falscher Artikelnummer anmelden.\\\"},{\\\"title\\\":\\\"Sichern und schlie\\u00dfen\\\",\\\"text\\\":\\\"Auf \\u201eSichern und Schlie\\u00dfen\\u201c klicken.\\\",\\\"warning\\\":\\\"Nicht schlie\\u00dfen, bevor alle Artikel gespeichert sind.\\\"},{\\\"title\\\":\\\"Sendungsnummer \\u00fcbernehmen\\\",\\\"text\\\":\\\"Die angezeigte Sendungsnummer in die Ladeliste unter Kundenreferenz eintragen.\\\",\\\"warning\\\":\\\"Ohne Kundenreferenz fehlt die Zuordnung in der Ladeliste.\\\"},{\\\"title\\\":\\\"Dokumente an Kunden senden\\\",\\\"text\\\":\\\"Zum Schluss Ladeliste, Lieferscheine, Rechnung und ATR an den Kunden schicken.\\\",\\\"warning\\\":\\\"Rechnung und ATR bei BSH T\\u00fcrkei nicht vergessen.\\\"}]},{\\\"id\\\":\\\"uploaded-ohare-3006108901\\\",\\\"type\\\":\\\"hochgeladen\\\",\\\"title\\\":\\\"O\\u2019Hare Components \\u00b7 Kunden-SOP\\\",\\\"customerName\\\":\\\"O\\u2019Hare Components\\\",\\\"account\\\":\\\"3006108901\\\",\\\"country\\\":\\\"Irland / EU\\\",\\\"portal\\\":\\\"Exportprozess / Rechnung\\\",\\\"source\\\":\\\"ExportHUB Kunden-SOP aus RC23/RC42\\\",\\\"tags\\\":[\\\"O\\u2019Hare\\\",\\\"OHare\\\",\\\"3006108901\\\",\\\"Wave geschlossen\\\",\\\"Rechnung\\\",\\\"EU\\\"],\\\"purpose\\\":\\\"O\\u2019Hare-Prozess: Wave geschlossen, Rechnung pr\\u00fcfen und EU-Prozess sauber dokumentieren.\\\",\\\"requirements\\\":[\\\"Kunde O\\u2019Hare gew\\u00e4hlt\\\",\\\"Kundennummer 3006108901 gepr\\u00fcft\\\",\\\"Wave geschlossen\\\",\\\"Rechnung gepr\\u00fcft\\\"],\\\"docs\\\":[\\\"Ladeliste\\\",\\\"Lieferscheine\\\",\\\"Rechnung\\\",\\\"CMR falls Spedition\\\"],\\\"steps\\\":[{\\\"title\\\":\\\"Kunde und Kundennummer pr\\u00fcfen\\\",\\\"text\\\":\\\"O\\u2019Hare Components ausw\\u00e4hlen und Ku
```

### line~13240 pos=2110883
```text
.trim(),Q(r[1]).trim())})}catch(e){try{if(typeof DEEP_TRANSLATION_ROWS!=='undefined'&&Array.isArray(DEEP_TRANSLATION_ROWS))DEEP_TRANSLATION_ROWS.forEach(function(r){if(r&&r[0]&&r[1])pairs.set(Q(r[0]).trim(),Q(r[1]).trim())})}catch(x){}}\\nvar extra={\\n'Start':'Home','Aufgaben':'Tasks','Urlaub & Vertretung':'Vacation & Cover','Sendung erstellen':'Create Shipment','ABD erstellen':'Create Export Declaration Request','Sendungs\u00fcbersicht':'Shipment Overview','Ladeliste & CMR':'Loading List & CMR','Palettenkonto':'Pallet Account','Kunden & Dokumente':'Customers & Documents','Kundenordner':'Customer Folder','UPS Preise':'UPS Prices','Zollwissen':'Customs Knowledge','SOP starten':'Start SOP','Academy Logistik':'Logistics Academy','Pr\u00fcfung':'Assessment','Ideenpool':'Idea Pool','Update ExportHUB':'Update ExportHUB','Benutzer & Rechte':'Users & Permissions','Sammelmodus':'Collection Mode','Archiv':'Archive',\\n'Sprache':'Language','Deutsch':'German','Englisch':'English','Benutzername':'Username','Passwort':'Password','Anmelden':'Sign in','Abmelden':'Sign out','Speichern':'Save','Bearbeiten':'Edit','L\u00f6schen':'Delete','Schlie\u00dfen':'Close','Abbrechen':'Cancel','Zur\u00fcck':'Back','Weiter':'Next','\u00d6ffnen':'Open','Drucken':'Print','Herunterladen':'Download','Hochladen':'Upload','Best\u00e4tigen':'Confirm','Erledigt':'Completed','Offen':'Open','\u00dcberf\u00e4llig':'Overdue','Heute':'Today','Morgen':'Tomorrow','Datum':'Date','Uhrzeit':'Time','Status':'Status','Aktion':'Action','Aktionen':'Actions','Kunde':'Customer','Spedition':'Carrier','Referenz':'Reference','Gewicht':'Weight','Anzahl':'Quantity','Verpackung':'Packaging','H\u00f6he cm':'Height cm','L\u00e4nge cm':'Length cm','Breite cm':'Width cm','Hinweis':'Note','Notiz':'Note','Beschreibung':'Description','Suche':'Search','Filter':'Filter','Alle':'All','Keine':'None','Ja':'Yes','Nein':'No','Pflichtfeld':'Required field','Fehler':'Error','Warnung':'Warning','Erfolg':'Success',\\n'Sendung speichern + Aufgaben pr\u00fcfen':'Save Shipment + Check Tasks','Dokumente direkt aus der Sendungsvorschau':'Documents Directly from Shipment Preview','Gesamtausgabe drucken':'Print Complete Document Set','Deckblatt':'Cover Sheet','Seite 1 drucken':'Print Page 1','Seite 2 drucken':'Print Page 2','CMR 4\u00d7':'CMR 4\u00d7','Ladeliste + Stauplan':'Loading List + Load Plan','Stauplan':'Load Plan','Incoterm / Hinweis':'Incoterm / Note','Abholdatum':'Collection Date','Tats\u00e4chliches Abholdatum':'Actual Collection Date','Kennzeichen':'Vehicle Registration','Verlader':'Loader','Warenbeschreibung f\u00fcr CMR Feld 9':'Goods Description for CMR Box 9','Neue Zeile':'Add Row','Collis':'Packages','Tats\u00e4chliche LDM':'Actual Loading Metres','Palettenh\u00f6he manuell eingeben':'Enter Pallet Height Manually','Die H\u00f6he der Palette wird manuell erfasst und bleibt gespeichert.':'The pallet height is entered manually and remains saved.',\\n'Teamdaten synchronisiert':'Team Data Synchronized','Teamdaten werden synchronisiert':'Synchronizing Team Data','Azure-Speicher bereit':'Team Storage Ready','Team-Speicher pr\u00fcfen':'Check Team Storage','Offline \u2013 nicht dauerhaft gespeichert':'Offline \u2013 Saved Locally','Aktueller gemeinsamer Datenstand':'Current Shared Data State','Teamdaten jetzt synchronisieren':'Synchronize Team Data Now','Jetzt':'Now','Microsoft-Konto':'Microsoft Account','ExportHUB-Benutzer':'ExportHUB User','nicht angemeldet':'not signed in','Mit Microsoft-Konto anmelden':'Sign in with Microsoft Account','Microsoft-Konto wechseln':'Switch Microsoft Account','Microsoft-Konto
```

### line~13240 pos=2124504
```text
tNode:function(n){return n.nodeValue&&n.nodeValue.trim()&&!skip(n.parentElement)?NodeFilter.FILTER_ACCEPT:NodeFilter.FILTER_REJECT}});while(walker.nextNode())translateNode(walker.currentNode);bind()}finally{applying=false;applyCount++;lastApplyMs=Math.max(0,((window.performance&&performance.now)?performance.now():Date.now())-t0);reconnectObserver()}}\\nfunction flush(){scheduled=0;if(applying)return;var roots=Array.from(pendingRoots).filter(function(r){return r&&r.isConnected});pendingRoots.clear();if(!roots.length)return;var compact=[];roots.forEach(function(r){if(compact.some(function(x){return x===r||x.contains&&x.contains(r)}))return;compact=compact.filter(function(x){return !(r.contains&&r.contains(x))});compact.push(r)});if(compact.length>8||compact.some(function(r){return r===document.body||r===document.documentElement}))apply(document.body);else compact.forEach(apply)}\\nfunction schedule(root){if(applying)return;root=root&&root.nodeType===1?root:document.body;pendingRoots.add(root);if(!scheduled)scheduled=setTimeout(flush,120)}\\nfunction persistLanguage(v){try{localStorage.setItem(KEY,v)}catch(e){}try{if(typeof state==='object'&&state){state.language=v;if(typeof STORAGE_KEY!=='undefined')localStorage.setItem(STORAGE_KEY,JSON.stringify(state))}}catch(e){}}\\nfunction setLang(v){v=v==='en'?'en':'de';runtimeLang=v;persistLanguage(v);try{if(typeof render==='function'&&typeof currentUser!=='undefined'&&currentUser)render()}catch(e){}apply(document.body)}\\nfunction ui(key){var d={language:'Sprache',loginEmpty:'Die Login-Felder bleiben leer.',username:'Benutzername',password:'Passwort',login:'Anmelden',firstLogin:'Erstanmeldung: Bitte neues Passwort setzen.',newPassword:'Neues Passwort',repeatPassword:'Passwort wiederholen',savePassword:'Passwort speichern',search:'Suche oder Befehl: update exporthub',backup:'Backup',import:'Import',print:'Drucken',logout:'Abmelden',offlineMode:'Offline-Modus',notSaved:'Noch nicht gespeichert',saved:'Gespeichert: ',collectMode:'Sammelmodus'},e={language:'Language',loginEmpty:'Login fields remain empty.',username:'Username',password:'Password',login:'Sign in',firstLogin:'First login: please set a new password.',newPassword:'New password',repeatPassword:'Repeat password',savePassword:'Save password',search:'Search or command: update exporthub',backup:'Backup',import:'Import',print:'Print',logout:'Sign out',offlineMode:'Offline mode',notSaved:'Not saved yet',saved:'Saved: ',collectMode:'Collection mode'};return (lang()==='en'?e:d)[key]||key}\\nfunction nav(id,label){var m={start:'Home',dashboard:'Dashboard',tasks:'Tasks',vacation:'Vacation & Cover',shipment:'Create Shipment',abd:'Create Export Declaration Request',shipmentoverview:'Shipment Overview',cmr:'Loading List & CMR',pallet:'Pallet Account',customers:'Customers & Documents',customerfolder:'Customer Folder',calculator:'UPS Prices',customs:'Customs Knowledge',sop:'Start SOP',academy:'Logistics Academy',quiz:'Assessment',ideas:'Idea Pool',update:'Update ExportHUB',rights:'Users & Permissions',teamfile:'Collection Mode',archive:'Archive'};return lang()==='en'?(m[id]||exact(label)):label}\\ntry{if(typeof LANGS!=='undefined'){Object.keys(LANGS).forEach(function(k){if(k!=='de'&&k!=='en')delete LANGS[k]})}}catch(e){}\\ntry{currentLang=lang;setLanguage=setLang;trUI=ui;trNav=nav;applyLanguage=function(){apply(document.body)};deepTranslateText=function(v){return lang()==='en'?composite(v):v};applyDeepTranslations=function(root){apply(root||document.body)}}catch(e){window.currentLang=lang;window.setLanguage=setLang;window.trUI=ui;window.trNav=nav;window.
```

### line~14349 pos=2296161
```text
eady','exporthub:viewchange','exporthub:resume'].forEach(function(name){window.addEventListener(name,function(){schedulePersistentFilterRestore(45)})});
window.addEventListener('pageshow',function(event){if(event.persisted&&runtime.authToken&&runtime.user){runtime.ready=true;runtime.loaded=true;unlockRestoredApplication();schedulePersistentFilterRestore(35)}},true);

function authoritativeShipmentSyncReason(reason){return /QR-Abholung und POD verbindlich synchronisiert|QR\/POD.*synchron|POD.*(?:Cloud|Sicherung|Backup|synchron)|Abholung.*(?:Server|synchron)|pickup.*(?:server|sync)|Lagerlocations synchronisiert|Lager.*synchron|warehouse.*sync/i.test(String(reason||''))}
function shipmentFlushGuardNeeded(reason){if(authoritativeShipmentSyncReason(reason))return false;var currentState=null;try{currentState=window.__EXPORTHUB_GET_STATE__&&window.__EXPORTHUB_GET_STATE__()}catch(_){currentState=runtime.state}var view=lower(currentState&&currentState.view||'');return view==='shipment'||/Sendungseingabe|Sendung aktualisiert|Sendung gespeichert|Kunde ausgewählt|Standort ausgewählt|Colli-Zeile|Einzelgewichte|Adressprüfung|Zolldokumentprüfung/i.test(String(reason||''))}
async function ensureShipmentFlushAllowed(reason){if(!shipmentFlushGuardNeeded(reason))return true;var guard=window.__EXPORTHUB_ENSURE_SHIPMENT_WRITE_ALLOWED__;if(typeof guard!=='function')return true;try{return (await guard(reason))!==false}catch(error){console.warn('Sendungssperre konnte vor Azure-Speicherung nicht bestätigt werden',error);return false}}
function queueSaveDelay(reason){var r=String(reason||'');if(/Status und QR-Aktivierung|Lagerlocations synchronisiert|Lager.*synchron|warehouse.*sync/i.test(r))return 220;if(/Eingabe aktualisiert|Auswahl aktualisiert|Sendungseingabe/i.test(r))return 650;if(/Filter|Suche|Sort|Planer|Benachrichtigung/i.test(r))return 350;return 0}
function shouldPersistFilterStateForSave(reason){var r=String(reason||'');if(currentPersistentFilterView()==='shipment')return false;return /Filter|Suche|Sort|Planer|Benachrichtigung|Dashboard-Einstellungen|Jahr im Aufgaben Planer/i.test(r)}
function persistentStateEqual(baseState,currentState){
 const base=isObject(baseState)?baseState:{},current=isObject(currentState)?currentState:{},keys=new Set(Object.keys(base).concat(Object.keys(current)));
 for(const key of keys){if(key==='users'||isLocalOnlyStateKey(key))continue;if(!sameValue(base[key],current[key]))return false}return true
}
function hasPersistentChanges(baseState,currentState,baseUsers,currentUsers){
 if(!runtime.lastSnapshot)return true;if(!persistentStateEqual(baseState,currentState))return true;return !sameValue(Array.isArray(baseUsers)?baseUsers:[],Array.isArray(currentUsers)?currentUsers:[])
}
function finishNoChangeSave(reason,generation){
 runtime.pendingSave=false;runtime.dirty=false;runtime.lastSavedGeneration=Math.max(Number(runtime.lastSavedGeneration||0),Number(generation||runtime.changeGeneration||0));runtime.lastNoChangeAt=new Date().toISOString();runtime.lastNoChangeReason=String(reason||'');runtime.noChangeSaveSkips=Number(runtime.noChangeSaveSkips||0)+1;
 if(Number(runtime.pendingOperationGeneration||0)<=Number(generation||0)){runtime.pendingOperationId='';runtime.pendingOperationGeneration=0}
 const info=by('saveInfo');if(info&&/Speicher|Azure|vorgemerkt/i.test(String(info.textContent||'')))info.textContent='Keine Änderung · keine Azure-Speicherung nötig';return true
}
function azureNetworkFailure(error){var code=String(error&&error.code||''),statusCode=Number(error&&error.status||0);return ['API_UNREACHABLE','API_TIMEOUT','ST
```

### line~14350 pos=2296375
```text
ersisted&&runtime.authToken&&runtime.user){runtime.ready=true;runtime.loaded=true;unlockRestoredApplication();schedulePersistentFilterRestore(35)}},true);

function authoritativeShipmentSyncReason(reason){return /QR-Abholung und POD verbindlich synchronisiert|QR\/POD.*synchron|POD.*(?:Cloud|Sicherung|Backup|synchron)|Abholung.*(?:Server|synchron)|pickup.*(?:server|sync)|Lagerlocations synchronisiert|Lager.*synchron|warehouse.*sync/i.test(String(reason||''))}
function shipmentFlushGuardNeeded(reason){if(authoritativeShipmentSyncReason(reason))return false;var currentState=null;try{currentState=window.__EXPORTHUB_GET_STATE__&&window.__EXPORTHUB_GET_STATE__()}catch(_){currentState=runtime.state}var view=lower(currentState&&currentState.view||'');return view==='shipment'||/Sendungseingabe|Sendung aktualisiert|Sendung gespeichert|Kunde ausgewählt|Standort ausgewählt|Colli-Zeile|Einzelgewichte|Adressprüfung|Zolldokumentprüfung/i.test(String(reason||''))}
async function ensureShipmentFlushAllowed(reason){if(!shipmentFlushGuardNeeded(reason))return true;var guard=window.__EXPORTHUB_ENSURE_SHIPMENT_WRITE_ALLOWED__;if(typeof guard!=='function')return true;try{return (await guard(reason))!==false}catch(error){console.warn('Sendungssperre konnte vor Azure-Speicherung nicht bestätigt werden',error);return false}}
function queueSaveDelay(reason){var r=String(reason||'');if(/Status und QR-Aktivierung|Lagerlocations synchronisiert|Lager.*synchron|warehouse.*sync/i.test(r))return 220;if(/Eingabe aktualisiert|Auswahl aktualisiert|Sendungseingabe/i.test(r))return 650;if(/Filter|Suche|Sort|Planer|Benachrichtigung/i.test(r))return 350;return 0}
function shouldPersistFilterStateForSave(reason){var r=String(reason||'');if(currentPersistentFilterView()==='shipment')return false;return /Filter|Suche|Sort|Planer|Benachrichtigung|Dashboard-Einstellungen|Jahr im Aufgaben Planer/i.test(r)}
function persistentStateEqual(baseState,currentState){
 const base=isObject(baseState)?baseState:{},current=isObject(currentState)?currentState:{},keys=new Set(Object.keys(base).concat(Object.keys(current)));
 for(const key of keys){if(key==='users'||isLocalOnlyStateKey(key))continue;if(!sameValue(base[key],current[key]))return false}return true
}
function hasPersistentChanges(baseState,currentState,baseUsers,currentUsers){
 if(!runtime.lastSnapshot)return true;if(!persistentStateEqual(baseState,currentState))return true;return !sameValue(Array.isArray(baseUsers)?baseUsers:[],Array.isArray(currentUsers)?currentUsers:[])
}
function finishNoChangeSave(reason,generation){
 runtime.pendingSave=false;runtime.dirty=false;runtime.lastSavedGeneration=Math.max(Number(runtime.lastSavedGeneration||0),Number(generation||runtime.changeGeneration||0));runtime.lastNoChangeAt=new Date().toISOString();runtime.lastNoChangeReason=String(reason||'');runtime.noChangeSaveSkips=Number(runtime.noChangeSaveSkips||0)+1;
 if(Number(runtime.pendingOperationGeneration||0)<=Number(generation||0)){runtime.pendingOperationId='';runtime.pendingOperationGeneration=0}
 const info=by('saveInfo');if(info&&/Speicher|Azure|vorgemerkt/i.test(String(info.textContent||'')))info.textContent='Keine Änderung · keine Azure-Speicherung nötig';return true
}
function azureNetworkFailure(error){var code=String(error&&error.code||''),statusCode=Number(error&&error.status||0);return ['API_UNREACHABLE','API_TIMEOUT','STATE_API_UNREACHABLE'].indexOf(code)>=0||[408,425,429,500,502,503,504].indexOf(statusCode)>=0}
function noteAzureNetworkFailure(error){if(!azureNetworkFailure(error))return 0;runtime.networkFailureCount=Math.min(5,N
```

### line~14681 pos=2346093
```text
:'';delete this.dataset.rc542DateInternal},true)
}
function prepareDateAction(ev){var t=ev&&ev.target;if(t&&t.closest&&t.closest('input[data-date-format]'))return;var restore=[];document.querySelectorAll('input[data-date-format]').forEach(function(i){var x=isoDate(i.value||i.dataset.dateIso);if(Q(i.value)&&!/^\d{4}-\d{2}-\d{2}$/.test(x))return;i.dataset.dateIso=x||'';restore.push([i,x]);i.dataset.rc542DateInternal='1';i.value=x||''});if(restore.length)setTimeout(function(){restore.forEach(function(pair){var i=pair[0],x=pair[1];if(!i.isConnected)return;i.value=x?deDate(x):'';delete i.dataset.rc542DateInternal})},0)}
function formatVisibleDates(){
 var r=root();document.querySelectorAll('input[type="date"],input[data-date-format],input[placeholder*="TT-MM-JJJJ"]').forEach(bindVisibleDate);if(!r)return;
 var walker=document.createTreeWalker(r,NodeFilter.SHOW_TEXT,{acceptNode:function(n){var p=n.parentElement;if(!p||/^(SCRIPT|STYLE|TEXTAREA|INPUT|OPTION)$/i.test(p.tagName))return NodeFilter.FILTER_REJECT;return /\b\d{4}-\d{2}-\d{2}\b/.test(n.nodeValue||'')?NodeFilter.FILTER_ACCEPT:NodeFilter.FILTER_REJECT}}),n;
 while((n=walker.nextNode()))n.nodeValue=n.nodeValue.replace(/\b(\d{4})-(\d{2})-(\d{2})\b/g,'$3-$2-$1');
}
function upgradeSelect(select){
 if(!select||select.dataset.rc542Auto==='1'||select.hidden)return;
 var label=select.closest('label'),txt=Q(label&&label.childNodes[0]&&label.childNodes[0].textContent||label&&label.textContent||'');
 if(!/(Kunde|Spedition|Abgleichspartner|Empfänger|Empfaenger)/i.test(txt))return;
 var id='rc542-list-'+Math.random().toString(36).slice(2),input=document.createElement('input'),dl=document.createElement('datalist');dl.id=id;input.setAttribute('list',id);input.className=select.className;input.placeholder='Name eingeben – automatische Suche';input.value=(select.options[select.selectedIndex]||{}).text||'';
 Array.from(select.options).forEach(function(o){var x=document.createElement('option');x.value=Q(o.textContent);x.dataset.value=o.value;dl.appendChild(x)});
 function sync(){var opt=Array.from(select.options).find(function(o){return L(o.textContent)===L(input.value)||L(o.value)===L(input.value)});if(opt){select.value=opt.value;input.value=Q(opt.textContent);select.dispatchEvent(new Event('change',{bubbles:true}))}}
 input.addEventListener('change',sync);input.addEventListener('blur',sync);select.dataset.rc542Auto='1';select.style.display='none';select.parentNode.insertBefore(input,select);select.parentNode.insertBefore(dl,select.nextSibling)
}
function upgradeSearches(){var r=root();if(r)r.querySelectorAll('select').forEach(upgradeSelect)}
function findCustomer(v){var k=L(v);return A(S().customers).find(function(c){return [c.id,c.account,c.customerNumber,c.kundennummer,c.name].some(function(x){return L(x)===k})})||null}
function shipmentId(sh){return Q(sh&&(sh.id||sh.shipmentId||sh.ref))}
function shipmentRef(sh){return Q(sh&&(sh.ref||sh.reference||sh.shipmentRef))}
function isPersisted(sh){var id=shipmentId(sh),ref=shipmentRef(sh);return A(S().shipments).some(function(x){return(id&&shipmentId(x)===id)||(ref&&shipmentRef(x)===ref)})}
function currentShipment(){var s=S(),id=Q(s.activeShipmentId);if(id&&id!=='current'){var found=A(s.shipments).find(function(x){return shipmentId(x)===id||shipmentRef(x)===id});if(found)return found}return s.shipment||(s.shipment={})}
function findShipment(id){var k=Q(id);return A(S().shipments).find(function(x){return shipmentId(x)===k||shipmentRef(x)===k})||null}
function customerForShipment(sh){return findCustomer(sh.customerId||sh.customerAccount||sh.customerNumber||
```

### line~14713 pos=2359690
```text
nd(Number(x.difference)):null;return '<div><div><b>'+E(x.number||x.id)+'</b> · Abgleich '+E(deDate(date))+' · '+E(x.status||'Confirmed')+'</div>'+(system!==null&&actual!==null?'<small>Systemstand: <b>'+system+'</b> · Ist-Stand: <b>'+actual+'</b> · Differenz: <b>'+(diff>0?'+':'')+diff+'</b></small>':'<small>'+E(x.balanceText||'Historischer Palettenausgleich')+'</small>')+'<div class="toolbar"><button class="ghost" onclick="return rc542PalletReconciliationReport(\''+E(x.id)+'\')">Abgleichsreport öffnen</button>'+(L(x.status)==='confirmed'&&window.canAdmin('pallet')?'<button class="danger" onclick="return rc542CancelSettlement(\''+E(x.id)+'\')">Abgleich stornieren</button>':'')+'</div></div>'}
function palletHtml542(){var ps=parties(),key=Q(S().rc542PalletParty||((ps[0]||{}).party||{}).key),entry=ps.find(function(x){return x.party.key===key})||ps[0],b=entry?balanceData(entry):{inTotal:0,outTotal:0,net:0,text:'Saldo 0 – ausgeglichen'},settlements=A(S().palletSettlements).filter(function(x){return !entry||x.partyKey===entry.party.key});if(entry)S().rc542PalletParty=entry.party.key;var today=isoDate(new Date().toISOString()),monthStart=new Date(),monthStartIso;monthStart.setDate(1);monthStartIso=isoDate(monthStart.toISOString());return '<div class="rc542-page"><div class="rc542-head"><div><span class="rc542-kicker">Palettenprozess</span><h1>Palettenkonto und Palettenabgleich</h1><p>Europaletten werden über das Kundenkonto oder die hinterlegte Spedition geführt. Jeder Abgleich speichert den tatsächlichen Stand und das zugehörige Datum.</p></div></div><section class="card"><label class="field">Kunde oder Spedition<input id="rc542PalletPartySearch" list="rc542PalletParties" value="'+E(entry?entry.party.label+': '+entry.party.name:'')+'" placeholder="Name eingeben – automatische Suche"><datalist id="rc542PalletParties">'+ps.map(function(x){return '<option value="'+E(x.party.label+': '+x.party.name)+'" data-key="'+E(x.party.key)+'"></option>'}).join('')+'</datalist></label><button class="ghost" onclick="return rc542ChoosePalletParty()">Konto öffnen</button>'+(entry?'<div class="rc542-account-banner"><span>Kontoart: '+E(entry.party.label)+'</span><strong>'+E(entry.party.name)+'</strong></div><div class="rc542-metrics"><div><span>Eingang</span><strong>'+b.inTotal+'</strong></div><div><span>Ausgang</span><strong>'+b.outTotal+'</strong></div><div><span>Aktueller Systemstand</span><strong>'+b.net+'</strong><small>'+E(b.text)+'</small></div></div><h2>Manuelle Buchung</h2><div class="rc542-grid3"><label class="field">Anzahl<input id="rc542PalCount" type="number" min="1" step="1"></label><label class="field">Buchungsdatum<input id="rc542PalDate" value="'+E(deDate(today))+'" placeholder="TT-MM-JJJJ"></label><label class="field">Sendungsreferenz<input id="rc542PalRef" placeholder="bei Ausgang Pflicht"></label></div><div class="rc542-segments"><button class="active" id="rc542PalIn" onclick="return rc542PalDirection(\'Eingang\')">Eingang</button><button id="rc542PalOut" onclick="return rc542PalDirection(\'Ausgang\')">Ausgang</button></div><label class="field">Bemerkung optional<input id="rc542PalNote"></label><button class="btn" onclick="return rc542AddPalletBooking()">Buchung speichern</button><h2>Buchungen</h2><div class="rc542-table-wrap"><table class="rc542-table"><thead><tr><th>Datum</th><th>Referenz</th><th>Richtung</th><th>Anzahl</th><th>Status</th><th></th></tr></thead><tbody>'+entry.bookings.slice().reverse().map(function(x){return '<tr><td>'+E(deDate(x.date||x.createdAt))+'</td><td>'+E(x.shipmentRef||x.ref||'')+'</td><td>'+E(x.direction||x.type||'
```

### line~15769 pos=2829391
```text
 data-rc871-diag-page="prev" '+(page<=0?'disabled':'')+'>← Zurück</button><span>Seite '+(page+1)+' / '+pages+'</span><button type="button" class="ghost" data-rc871-diag-page="next" '+(page>=pages-1?'disabled':'')+'>Weiter →</button>'}
function refreshVisible(){renderStats();renderList();updateStatusOnly()}
function render(){var root=document.getElementById('content');if(!root)return false;if(!isGlobal()){root.innerHTML='<div class="badbox">Die Fehlerdiagnose ist nur für globale Administratoren freigegeben.</div>';return false}root.className='content rc871-diagnostics-view';root.setAttribute('data-exporthub-rendered-view','diagnostics');root.setAttribute('data-rc871-diagnostics-view','1');root.innerHTML='<div class="rc871-diag-page" data-rc871-diagnostics-view="1"><div class="page-head rc871-diag-head"><div><span class="rc871-diag-kicker">GLOBAL ADMIN · '+(isTestservice()?'TESTSERVICE':'PRODUKTION')+'</span><h2>Fehlerdiagnose</h2><p>Statischer Diagnose-Snapshot. Neue Hintergrundereignisse verändern diese Ansicht nicht; neue Cloud-Daten werden ausschließlich über „Aktualisieren“ übernommen.</p></div><div id="rc871DiagStatus" class="rc871-diag-status">'+esc(statusText())+'</div></div><section class="card"><div id="rc871DiagSummary" class="rc871-diag-summary"></div></section><section class="card rc871-diag-controls"><div class="rc871-diag-toolbar"><label class="rc871-diag-field">Anzeige<select id="rc871DiagFilter"><option value="problems">Fehler + Warnungen</option><option value="errors">Nur Fehler</option><option value="warnings">Nur Warnungen</option><option value="network">Netzwerk/API</option><option value="performance">Leistung</option><option value="render">Neu-Render</option><option value="all">Alle Ereignisse</option></select></label><label class="rc871-diag-field">Suche<input id="rc871DiagSearch" type="search" autocomplete="off" placeholder="Meldung, Bereich oder Benutzer"></label><div class="rc871-diag-actions"><button type="button" class="btn" data-rc871-diag="refresh">Aktualisieren</button><button type="button" class="ghost" data-rc871-diag="export">JSON herunterladen</button><button type="button" class="ghost danger" data-rc871-diag="clear">Protokoll löschen</button></div></div><div class="rc871-diag-meta"><span id="rc871DiagMeta"></span><span>Keine automatische Listen- oder Cloud-Aktualisierung</span></div></section><section class="card"><div class="rc871-diag-table-head"><span>Zeit</span><span>Status</span><span>Bereich</span><span>Meldung</span></div><div id="rc871DiagList" class="rc871-diag-list"></div><div id="rc871DiagPager" class="rc871-diag-pager"></div></section></div>';var list=document.getElementById('rc871DiagList');if(list)list.innerHTML='<div class="rc871-diag-empty">Diagnosedaten werden vorbereitet …</div>';setTimeout(function(){if(currentView()!=='diagnostics')return;takeSnapshot();refreshVisible()},0);return false}
function download(){var c=cloud(),data={exportedAt:new Date().toISOString(),version:q(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version||'RC882'),cloudStatus:c&&typeof c.status==='function'?c.status():{},records:sourceRecords()},blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json;charset=utf-8'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='ExportHUB_Fehlerdiagnose_'+new Date().toISOString().slice(0,10)+'.json';document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(u)},30000)}
async function refresh(){if(loading)return false;loading=true;var btn=document.querySelector('[data-rc871-diag="refresh"]');if(btn){b
```

## data-search
needle='data-search'
count=0

## RC945 Colli marker
needle='exporthub-rc945-compact-stable-colli-layout'
count=1

### line~23457 pos=4713873
```text
ght:auto!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-area{padding:9px!important;margin:0!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-head{gap:6px!important;margin-bottom:8px!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-head h2{font-size:15px!important;line-height:1.2!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-head p{font-size:10px!important;line-height:1.3!important;margin:3px 0 0!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-segments{display:grid!important;grid-template-columns:1fr 1fr!important;gap:6px!important}#rc363BlockDocuments #rc363BlockAbdDecision .rc543-segments button{min-height:34px!important;padding:6px 8px!important;font-size:11px!important}#rc383TopPair.rc894-full-stack .rc363-process-head{margin-bottom:8px!important}#rc383TopPair.rc894-full-stack .rc363-process-head h2{font-size:18px!important}#rc896ShipmentFieldGrid{display:grid!important;grid-template-columns:repeat(6,minmax(0,1fr))!important;gap:10px 12px!important;align-items:start!important;width:100%!important}#rc896ShipmentFieldGrid>*{min-width:0!important;width:100%!important;margin:0!important}#rc896ShipmentFieldGrid [data-rc896-field="reference"]{grid-column:span 2!important;order:10}#rc896ShipmentFieldGrid [data-rc896-field="pickup"]{grid-column:span 2!important;order:20}#rc896ShipmentFieldGrid [data-rc896-field="carrier"]{grid-column:span 2!important;order:30}#rc896ShipmentFieldGrid [data-rc896-field="description"]{grid-column:span 4!important;order:40}#rc896ShipmentFieldGrid [data-rc896-field="remark"]{grid-column:1/-1!important;order:50}#rc363BlockShipment #rc896ShipmentFieldGrid textarea{min-height:88px!important;height:88px!important}#rc363BlockShipment #rc896ShipmentFieldGrid .field{margin:0!important}/* exporthub-rc945-compact-stable-colli-layout */#rc363BlockColli{align-self:start!important;height:auto!important;min-width:0!important}#rc363BlockColli #rc573ColliCard{height:auto!important;min-width:0!important}#rc363BlockColli #rc573ColliCard #rowsBox{display:grid!important;gap:6px!important;align-content:start!important;overflow-anchor:none!important}#rc363BlockColli #rc573ColliCard .rc363-owned-row{padding:6px 7px!important;border-radius:12px!important;height:auto!important;min-height:0!important;overflow-anchor:none!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid{display:grid!important;grid-template-columns:repeat(12,minmax(0,1fr))!important;grid-template-rows:auto auto!important;gap:5px 6px!important;align-items:end!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid>[data-rc363-field]{padding:0!important;gap:3px!important;min-width:0!important;align-self:end!important;font-size:10px!important;line-height:1.15!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="type"]{grid-area:1 / 1 / 2 / 5!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="count"]{grid-area:1 / 5 / 2 / 7!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="weight"]{grid-area:1 / 7 / 2 / 10!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="ldm"]{grid-area:1 / 10 / 2 / 13!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="l"]{grid-area:2 / 1 / 3 / 4!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="w"]{grid-area:2 / 4 / 3 / 7!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="h"]{grid-area:2 / 7 / 3 / 10!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="action"]{grid-area:2 / 10 / 3 / 13!important;align-self:end!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="action"]>span{dis
```

## Colli card
needle='rc573ColliCard'
count=140

### line~23457 pos=4718304
```text
kColli #rc573ColliCard .rc344-summary{display:grid!important;grid-template-columns:repeat(4,minmax(0,1fr))!important;gap:4px!important;margin:8px 0 0!important;padding:4px!important;border:1px solid #dbe8f2!important;border-radius:12px!important;background:#f8fbfe!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox{display:flex!important;flex-direction:column!important;justify-content:center!important;align-items:flex-start!important;min-width:0!important;min-height:48px!important;padding:5px 7px!important;margin:0!important;border:0!important;border-right:1px solid #e3edf4!important;border-radius:7px!important;background:transparent!important;box-shadow:none!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:last-child{border-right:0!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox span{font-size:8px!important;line-height:1.1!important;font-weight:750!important;color:#60778a!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important;max-width:100%!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox strong{display:block!important;font-size:12px!important;line-height:1.15!important;font-weight:900!important;color:#17324d!important;white-space:nowrap!important;margin-top:2px!important}@media(max-width:620px){#rc363BlockColli #rc573ColliCard .rc344-summary{grid-template-columns:repeat(2,minmax(0,1fr))!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:nth-child(2){border-right:0!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:nth-child(-n+2){border-bottom:1px solid #e3edf4!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid{grid-template-columns:1fr!important;grid-template-rows:none!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid>[data-rc363-field]{grid-column:1!important;grid-row:auto!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="action"]>span{display:block!important}}#rc845AvisEditHost,#rc845AvisReadHost,#rc890LieferAvisBtn,#rc891LieferAvisBtn,#rc892LieferAvisBtn,#rc894LieferAvisBtn,#lieferAvisBtn{display:none!important}@media(max-width:900px){#rc896ShipmentFieldGrid{grid-template-columns:1fr!important}#rc896ShipmentFieldGrid [data-rc896-field]{grid-column:1/-1!important}}#rc897LieferavisPanel{display:grid;gap:9px!important;margin-top:12px!important;padding:11px 12px!important;border:1px solid #f2c46d!important;border-radius:14px!important;background:linear-gradient(180deg,#fffdf7,#fff9eb)!important}#rc897LieferavisPanel .rc897-avis-head{display:flex!important;align-items:center!important;justify-content:space-between!important;gap:8px!important;font-size:14px!important;color:#17324d!important}#rc897LieferavisPanel .rc897-avis-head strong{font-size:14px!important}#rc897LieferavisPanel .rc897-avis-summary{display:grid;grid-template-columns:minmax(90px,.55fr) minmax(100px,.65fr) minmax(0,1.8fr);gap:8px;align-items:stretch}#rc897LieferavisPanel .rc897-avis-box{border:1px solid #d7e5ef;border-radius:10px;background:#fff;padding:8px 10px;min-width:0}#rc897LieferavisPanel .rc897-avis-label{font-size:11px;font-weight:800;letter-spacing:.04em;text-transform:uppercase;color:#657c8d;margin-bottom:5px}#rc897LieferavisPanel .rc897-avis-value{font-size:14px;font-weight:750;color:#17324d;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}#rc897LieferavisPanel .rc897-avis-status[data-active="1"]{color:#087a5a}#rc897LieferavisPanel .rc897-avis-status[data-active="0"]{color:#9a5510}#rc897LieferavisPanel .rc897-avis-actions{display:flex;flex-wrap:wrap;gap:
```

### line~23457 pos=4718422
```text
ap:4px!important;margin:8px 0 0!important;padding:4px!important;border:1px solid #dbe8f2!important;border-radius:12px!important;background:#f8fbfe!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox{display:flex!important;flex-direction:column!important;justify-content:center!important;align-items:flex-start!important;min-width:0!important;min-height:48px!important;padding:5px 7px!important;margin:0!important;border:0!important;border-right:1px solid #e3edf4!important;border-radius:7px!important;background:transparent!important;box-shadow:none!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:last-child{border-right:0!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox span{font-size:8px!important;line-height:1.1!important;font-weight:750!important;color:#60778a!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important;max-width:100%!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox strong{display:block!important;font-size:12px!important;line-height:1.15!important;font-weight:900!important;color:#17324d!important;white-space:nowrap!important;margin-top:2px!important}@media(max-width:620px){#rc363BlockColli #rc573ColliCard .rc344-summary{grid-template-columns:repeat(2,minmax(0,1fr))!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:nth-child(2){border-right:0!important}#rc363BlockColli #rc573ColliCard .rc344-summary .summaryBox:nth-child(-n+2){border-bottom:1px solid #e3edf4!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid{grid-template-columns:1fr!important;grid-template-rows:none!important}#rc363BlockColli #rc573ColliCard .rc363-colli-grid>[data-rc363-field]{grid-column:1!important;grid-row:auto!important}#rc363BlockColli #rc573ColliCard [data-rc363-field="action"]>span{display:block!important}}#rc845AvisEditHost,#rc845AvisReadHost,#rc890LieferAvisBtn,#rc891LieferAvisBtn,#rc892LieferAvisBtn,#rc894LieferAvisBtn,#lieferAvisBtn{display:none!important}@media(max-width:900px){#rc896ShipmentFieldGrid{grid-template-columns:1fr!important}#rc896ShipmentFieldGrid [data-rc896-field]{grid-column:1/-1!important}}#rc897LieferavisPanel{display:grid;gap:9px!important;margin-top:12px!important;padding:11px 12px!important;border:1px solid #f2c46d!important;border-radius:14px!important;background:linear-gradient(180deg,#fffdf7,#fff9eb)!important}#rc897LieferavisPanel .rc897-avis-head{display:flex!important;align-items:center!important;justify-content:space-between!important;gap:8px!important;font-size:14px!important;color:#17324d!important}#rc897LieferavisPanel .rc897-avis-head strong{font-size:14px!important}#rc897LieferavisPanel .rc897-avis-summary{display:grid;grid-template-columns:minmax(90px,.55fr) minmax(100px,.65fr) minmax(0,1.8fr);gap:8px;align-items:stretch}#rc897LieferavisPanel .rc897-avis-box{border:1px solid #d7e5ef;border-radius:10px;background:#fff;padding:8px 10px;min-width:0}#rc897LieferavisPanel .rc897-avis-label{font-size:11px;font-weight:800;letter-spacing:.04em;text-transform:uppercase;color:#657c8d;margin-bottom:5px}#rc897LieferavisPanel .rc897-avis-value{font-size:14px;font-weight:750;color:#17324d;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}#rc897LieferavisPanel .rc897-avis-status[data-active="1"]{color:#087a5a}#rc897LieferavisPanel .rc897-avis-status[data-active="0"]{color:#9a5510}#rc897LieferavisPanel .rc897-avis-actions{display:flex;flex-wrap:wrap;gap:7px;align-items:center;justify-content:flex-end}#rc897LieferavisPanel .rc897-avis-actions button{min-height:34px!impor
```

### line~23559 pos=4731664
```text
lockMail','8','Mailbereich',false),
     actions=processBlock('rc363BlockActions','9','Speichern & Ausgabe',false);
 stow.hidden=false;stow.removeAttribute('hidden');
 [customer,shipmentBlock,colli,documents].forEach(function(x){top.appendChild(x)});
 if(left&&left.parentNode)left.remove();if(right&&right.parentNode)right.remove();top.className='rc894-full-stack';
 [top,stow,mailBlock,actions].forEach(function(x){layout.appendChild(x)});
 var customerBody=customer.querySelector('.rc363-process-body'),
     shipmentBody=shipmentBlock.querySelector('.rc363-process-body'),
     colliBody=colli.querySelector('.rc363-process-body'),
     documentsBody=documents.querySelector('.rc363-process-body'),
     stowBody=stow.querySelector('.rc363-process-body'),
     abdDecisionBody=abdDecisionBlock.querySelector('.rc363-process-body'),
     mailBody=mailBlock.querySelector('.rc363-process-body'),
     actionsBody=actions.querySelector('.rc363-process-body');
 var directLegacyGrids=Array.from(r.querySelectorAll('.grid2')).filter(function(grid){return !grid.closest('#rc363FixedShipmentLayout')}),baseCard=null,renderedColli=null;
 directLegacyGrids.some(function(grid){baseCard=Array.from(grid.children).find(function(el){return el.matches&&el.matches('.card')&&/Kunde\s*&\s*Grunddaten/i.test(q(el.textContent))})||null;return!!baseCard});
 var globalRows=document.getElementById('rowsBox');renderedColli=globalRows&&globalRows.closest('.card,section,article');
 if(!renderedColli||!renderedColli.querySelector('input,select'))renderedColli=Array.from(r.querySelectorAll('.card,section,article')).find(function(el){return !el.classList.contains('rc363-process-block')&&/Collis|Lademeter/i.test(heading(el))&&!!el.querySelector('input,select')})||null;
 var existingColli=colliBody.querySelector('#rc573ColliCard'),validExisting=existingColli&&!!existingColli.querySelector('#rowsBox,[data-rc375-rows],.rc344-colli-row,input,select');
 if(existingColli&&!validExisting){existingColli.removeAttribute('id');if(!q(existingColli.textContent)||/^Kunde\s*&\s*Grunddaten$/i.test(q(existingColli.textContent)))existingColli.remove();existingColli=null}
 var colliCard=renderedColli||existingColli||canonicalColliCard();if(colliCard){var duplicate=document.getElementById('rc573ColliCard');if(duplicate&&duplicate!==colliCard)duplicate.removeAttribute('id');colliCard.id='rc573ColliCard'}if(existingColli&&renderedColli&&existingColli!==renderedColli)existingColli.remove();
 if(baseCard){Array.from(baseCard.children).forEach(function(node){var lt=low(node.textContent);if(node.matches('h1,h2,h3,h4'))return;if(/lieferscheine|dncs|lieferschein-datei/.test(lt)){take(documentsBody,node);return}if(/^verlader\b/.test(lt)){node.remove();return}if(/sendungsreferenz|geplantes abholdatum|tatsachliches abholdatum|spedition|kennzeichen|incoterm|drittland|zolldokumente/.test(lt)){take(shipmentBody,node);return}if(node.classList&&node.classList.contains('toolbar')){node.remove();return}if(node.classList&&node.classList.contains('notice')&&!/lieferschein-datei/.test(lt)){node.remove();return}take(customerBody,node)});if(baseCard.parentNode)baseCard.remove()}
 take(colliBody,colliCard);
 if(colliCard){Array.from(colliCard.children).forEach(function(node){var text=low(node.textContent);if(node.matches&&node.matches('label.field')&&/warenbeschreibung|bemerkung/.test(text)){take(shipmentBody,node)}})}
 ensureStowCard(stowBody);
 take(abdDecisionBody,document.getElementById('rc543AbdDecision'));
 var canonicalMailCard=canonicalMail();take(mailBody,canonicalMailCard);take(actionsBody,document.getElementBy
```

### line~23561 pos=4732131
```text
r.querySelector('.rc363-process-body'),
     shipmentBody=shipmentBlock.querySelector('.rc363-process-body'),
     colliBody=colli.querySelector('.rc363-process-body'),
     documentsBody=documents.querySelector('.rc363-process-body'),
     stowBody=stow.querySelector('.rc363-process-body'),
     abdDecisionBody=abdDecisionBlock.querySelector('.rc363-process-body'),
     mailBody=mailBlock.querySelector('.rc363-process-body'),
     actionsBody=actions.querySelector('.rc363-process-body');
 var directLegacyGrids=Array.from(r.querySelectorAll('.grid2')).filter(function(grid){return !grid.closest('#rc363FixedShipmentLayout')}),baseCard=null,renderedColli=null;
 directLegacyGrids.some(function(grid){baseCard=Array.from(grid.children).find(function(el){return el.matches&&el.matches('.card')&&/Kunde\s*&\s*Grunddaten/i.test(q(el.textContent))})||null;return!!baseCard});
 var globalRows=document.getElementById('rowsBox');renderedColli=globalRows&&globalRows.closest('.card,section,article');
 if(!renderedColli||!renderedColli.querySelector('input,select'))renderedColli=Array.from(r.querySelectorAll('.card,section,article')).find(function(el){return !el.classList.contains('rc363-process-block')&&/Collis|Lademeter/i.test(heading(el))&&!!el.querySelector('input,select')})||null;
 var existingColli=colliBody.querySelector('#rc573ColliCard'),validExisting=existingColli&&!!existingColli.querySelector('#rowsBox,[data-rc375-rows],.rc344-colli-row,input,select');
 if(existingColli&&!validExisting){existingColli.removeAttribute('id');if(!q(existingColli.textContent)||/^Kunde\s*&\s*Grunddaten$/i.test(q(existingColli.textContent)))existingColli.remove();existingColli=null}
 var colliCard=renderedColli||existingColli||canonicalColliCard();if(colliCard){var duplicate=document.getElementById('rc573ColliCard');if(duplicate&&duplicate!==colliCard)duplicate.removeAttribute('id');colliCard.id='rc573ColliCard'}if(existingColli&&renderedColli&&existingColli!==renderedColli)existingColli.remove();
 if(baseCard){Array.from(baseCard.children).forEach(function(node){var lt=low(node.textContent);if(node.matches('h1,h2,h3,h4'))return;if(/lieferscheine|dncs|lieferschein-datei/.test(lt)){take(documentsBody,node);return}if(/^verlader\b/.test(lt)){node.remove();return}if(/sendungsreferenz|geplantes abholdatum|tatsachliches abholdatum|spedition|kennzeichen|incoterm|drittland|zolldokumente/.test(lt)){take(shipmentBody,node);return}if(node.classList&&node.classList.contains('toolbar')){node.remove();return}if(node.classList&&node.classList.contains('notice')&&!/lieferschein-datei/.test(lt)){node.remove();return}take(customerBody,node)});if(baseCard.parentNode)baseCard.remove()}
 take(colliBody,colliCard);
 if(colliCard){Array.from(colliCard.children).forEach(function(node){var text=low(node.textContent);if(node.matches&&node.matches('label.field')&&/warenbeschreibung|bemerkung/.test(text)){take(shipmentBody,node)}})}
 ensureStowCard(stowBody);
 take(abdDecisionBody,document.getElementById('rc543AbdDecision'));
 var canonicalMailCard=canonicalMail();take(mailBody,canonicalMailCard);take(actionsBody,document.getElementById('rc543PrintStatus'));take(actionsBody,document.getElementById('rc363ShipmentActions'));
 if(colliCard){Array.from(colliCard.children).forEach(function(node){var text=low(node.textContent);if(node.matches&&node.matches('.badbox,.okbox,.notice,.infobox')&&/sendung kann|bitte erfassen|abgeschlossen/.test(text))take(actionsBody,node)})}
 Array.from(shipmentBody.querySelectorAll('label,.field,.form-group,.input-group')).forEach(function(node){if(/^verlader\b/.test(
```

### line~23561 pos=4732230
```text
ss-body'),
     colliBody=colli.querySelector('.rc363-process-body'),
     documentsBody=documents.querySelector('.rc363-process-body'),
     stowBody=stow.querySelector('.rc363-process-body'),
     abdDecisionBody=abdDecisionBlock.querySelector('.rc363-process-body'),
     mailBody=mailBlock.querySelector('.rc363-process-body'),
     actionsBody=actions.querySelector('.rc363-process-body');
 var directLegacyGrids=Array.from(r.querySelectorAll('.grid2')).filter(function(grid){return !grid.closest('#rc363FixedShipmentLayout')}),baseCard=null,renderedColli=null;
 directLegacyGrids.some(function(grid){baseCard=Array.from(grid.children).find(function(el){return el.matches&&el.matches('.card')&&/Kunde\s*&\s*Grunddaten/i.test(q(el.textContent))})||null;return!!baseCard});
 var globalRows=document.getElementById('rowsBox');renderedColli=globalRows&&globalRows.closest('.card,section,article');
 if(!renderedColli||!renderedColli.querySelector('input,select'))renderedColli=Array.from(r.querySelectorAll('.card,section,article')).find(function(el){return !el.classList.contains('rc363-process-block')&&/Collis|Lademeter/i.test(heading(el))&&!!el.querySelector('input,select')})||null;
 var existingColli=colliBody.querySelector('#rc573ColliCard'),validExisting=existingColli&&!!existingColli.querySelector('#rowsBox,[data-rc375-rows],.rc344-colli-row,input,select');
 if(existingColli&&!validExisting){existingColli.removeAttribute('id');if(!q(existingColli.textContent)||/^Kunde\s*&\s*Grunddaten$/i.test(q(existingColli.textContent)))existingColli.remove();existingColli=null}
 var colliCard=renderedColli||existingColli||canonicalColliCard();if(colliCard){var duplicate=document.getElementById('rc573ColliCard');if(duplicate&&duplicate!==colliCard)duplicate.removeAttribute('id');colliCard.id='rc573ColliCard'}if(existingColli&&renderedColli&&existingColli!==renderedColli)existingColli.remove();
 if(baseCard){Array.from(baseCard.children).forEach(function(node){var lt=low(node.textContent);if(node.matches('h1,h2,h3,h4'))return;if(/lieferscheine|dncs|lieferschein-datei/.test(lt)){take(documentsBody,node);return}if(/^verlader\b/.test(lt)){node.remove();return}if(/sendungsreferenz|geplantes abholdatum|tatsachliches abholdatum|spedition|kennzeichen|incoterm|drittland|zolldokumente/.test(lt)){take(shipmentBody,node);return}if(node.classList&&node.classList.contains('toolbar')){node.remove();return}if(node.classList&&node.classList.contains('notice')&&!/lieferschein-datei/.test(lt)){node.remove();return}take(customerBody,node)});if(baseCard.parentNode)baseCard.remove()}
 take(colliBody,colliCard);
 if(colliCard){Array.from(colliCard.children).forEach(function(node){var text=low(node.textContent);if(node.matches&&node.matches('label.field')&&/warenbeschreibung|bemerkung/.test(text)){take(shipmentBody,node)}})}
 ensureStowCard(stowBody);
 take(abdDecisionBody,document.getElementById('rc543AbdDecision'));
 var canonicalMailCard=canonicalMail();take(mailBody,canonicalMailCard);take(actionsBody,document.getElementById('rc543PrintStatus'));take(actionsBody,document.getElementById('rc363ShipmentActions'));
 if(colliCard){Array.from(colliCard.children).forEach(function(node){var text=low(node.textContent);if(node.matches&&node.matches('.badbox,.okbox,.notice,.infobox')&&/sendung kann|bitte erfassen|abgeschlossen/.test(text))take(actionsBody,node)})}
 Array.from(shipmentBody.querySelectorAll('label,.field,.form-group,.input-group')).forEach(function(node){if(/^verlader\b/.test(low(node.textContent)))node.remove()});placeWorkflowButtons(customerBody,shipmentBody,documentsBody
```

### line~23756 pos=4810239
```text
ed-row'))return;if(el.getAttribute('data-rc712-live-dirty')==='1')commitColliElement(el);markStowDirty();scheduleEditSave('Colli-Zeile gespeichert',4800);scheduleStowRender(1800,true)},true);document.addEventListener('input',queueShipmentEdit,false);document.addEventListener('change',queueShipmentEdit,false);document.addEventListener('blur',queueShipmentEdit,true);document.addEventListener('visibilitychange',function(){if(document.hidden)flushEditSave('Sendungseingabe vor App-Wechsel gespeichert',true);else refreshLockOnReturn()},true);window.addEventListener('focus',refreshLockOnReturn);window.addEventListener('pagehide',function(){flushEditSave('Sendungseingabe vor Verlassen gespeichert',true)},{capture:true});document.addEventListener('keydown',function(e){if(e.key!=='Escape')return;if(document.getElementById('rc363WeightModal')&&document.getElementById('rc363WeightModal').classList.contains('open'))closeWeight();closePackagingMenus()},true);patch()}
['exporthub:ready','exporthub:viewchange','exporthub:shipment-location-changed'].forEach(function(name){window.addEventListener(name,schedulePatch)});
window.addEventListener('exporthub:rendered',function(event){
 if(!isShipmentView())return;
 var detail=event&&event.detail||{},layout=document.getElementById('rc363FixedShipmentLayout');
 if(layout&&detail.view==='shipment')return;
 schedulePatch()
});window.addEventListener('exporthub:sync',function(event){if(event&&event.detail&&event.detail.remote===true)scheduleRemoteMerge(0)});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
function refreshVisuals(){updateSummary();var active=document.activeElement,editing=!!(active&&/^(INPUT|SELECT|TEXTAREA)$/.test(active.tagName)&&active.closest&&active.closest('#rc573ColliCard'));if(rc710StowDirty){if(!editing)scheduleStowRender(120,true)}else if(!editing)renderStowPlan();updateQr();return true}

['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,function(){var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;nt(ensureRC894LieferavisButton,0)})});
var api=Object.freeze({version:VERSION,patch:patch,refreshVisuals:refreshVisuals,startNewShipment:startFreshShipment,openSavedShipment:openSavedExact,addRow:addRow,removeRow:removeRow,updateRow:updateRow,openWeight:openWeight,save:saveAction,printAll:printAll,openDocuments:openDocuments,mergeRemote:mergeRemote,mergeActiveRemote:mergeActiveRemote,renderStowPlan:renderStowPlan,activateQr:activateQr,diagnostics:function(){var r=root(),layout=document.getElementById('rc363FixedShipmentLayout'),stow=document.getElementById('rc380StowPlan');return{version:VERSION,shipmentView:isShipmentView(),readOnly:!!lockedShipment(),readOnlyStatus:lockedShipment()?lockText(lockedShipment()):'',rows:rowNodes().length,inputDirtyImmediate:true,remoteMergePending:remoteMergePending,customerChangeColliLock:!!customerChangeColliLock,activeShipmentField:activeShipmentField(),fixedBlocks:layout?layout.querySelectorAll('.rc363-process-block').length:0,blockOrder:layout?Array.from(layout.querySelectorAll('.rc363-process-block')).map(function(x){return x.id}):[],weightFields:document.querySelectorAll('input[data-rc363-weight="1"]').length,helperTexts:Array.from((r||document).querySelectorAll('.okbox,.badbox,.notice,.infobox,div,p')).filter(function(el){return /^(Anzahl\s*=\s*reale physische Anzahl|Referenzregel:|Dokumente:\s*Ladeliste wird als A4-Seite erzeugt)/i.test(q(el.textContent))}).length,hasMutatio
```

### line~24783 pos=4977778
```text
play:block;width:74px;height:3px;margin-top:12px;background:linear-gradient(90deg,#b38a45,#ead9ae);border-radius:999px}
body[data-eh-theme="boardroom-elite"] :is(.card,.metric,.folder-section,.rc205-widget){border-color:#dfd2bb!important}
body[data-eh-theme="strategic-graphite"] :is(.card,.metric,.folder-section,.rc205-widget){border-left:3px solid color-mix(in srgb,var(--pc-primary) 46%,var(--pc-line))!important}
body[data-eh-theme="strategic-graphite"] .topbar{border-left:4px solid var(--pc-primary)!important}
/* RC813 – component semantics and complete theme inheritance */
body[data-eh-theme] #content .rc255-today-tile{background:var(--pc-surface)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #content .rc255-today-tile:hover{background:var(--pc-table-hover)!important;border-color:var(--pc-line-strong)!important;box-shadow:var(--pc-shadow-soft)!important}
body[data-eh-theme] #content .rc255-today-tile b{color:var(--pc-title)!important}body[data-eh-theme] #content .rc255-today-tile small{color:var(--pc-muted)!important}
body[data-eh-theme] #rc363FixedShipmentLayout .rc363-process-head{border-color:var(--pc-line)!important}
body[data-eh-theme] #rc363FixedShipmentLayout .rc363-process-number{background:var(--pc-primary-soft)!important;color:var(--pc-primary)!important;border:1px solid color-mix(in srgb,var(--pc-primary) 24%,var(--pc-line))!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(.rc363-process-head h2,.rc363-actions-copy h3,.rc363-qr-copy b,.rc396-graphic-head b){color:var(--pc-title)!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(.rc363-actions-copy p,.rc363-qr-copy,.rc396-axis,.rc396-column-head small){color:var(--pc-muted)!important}
body[data-eh-theme] #rc573ColliCard .rc363-owned-row{background:var(--pc-surface-2)!important;border-color:var(--pc-line-strong)!important;box-shadow:var(--pc-shadow-soft)!important}
body[data-eh-theme] #rc573ColliCard input[data-rc363-weight="1"]{background:var(--pc-field)!important;color:var(--pc-text)!important;border-color:var(--pc-line-strong)!important;box-shadow:none!important}
body[data-eh-theme] #rc363FixedShipmentLayout .field:focus-within{background:var(--pc-primary-soft)!important;border-color:color-mix(in srgb,var(--pc-focus) 42%,var(--pc-line))!important;box-shadow:0 0 0 3px color-mix(in srgb,var(--pc-focus) 14%,transparent)!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(input,select,textarea):focus{border-color:var(--pc-focus)!important;background:var(--pc-field)!important;box-shadow:0 0 0 3px color-mix(in srgb,var(--pc-focus) 18%,transparent)!important}
body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-card{background:var(--pc-surface-2)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-card h3{color:var(--pc-title)!important}body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-customer,body[data-eh-theme] #rc363BlockCustomer .rc628-customer-empty{color:var(--pc-muted)!important}
body[data-eh-theme] #content .rc544-user{background:var(--pc-surface)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #content .rc544-user:hover{background:var(--pc-table-hover)!important;border-color:var(--pc-line-strong)!important}
body[data-eh-theme] #content .rc544-user.active{background:var(--pc-primary-soft)!important;border-color:var(--pc-primary)!important;bo
```

### line~24784 pos=4977961
```text
-section,.rc205-widget){border-color:#dfd2bb!important}
body[data-eh-theme="strategic-graphite"] :is(.card,.metric,.folder-section,.rc205-widget){border-left:3px solid color-mix(in srgb,var(--pc-primary) 46%,var(--pc-line))!important}
body[data-eh-theme="strategic-graphite"] .topbar{border-left:4px solid var(--pc-primary)!important}
/* RC813 – component semantics and complete theme inheritance */
body[data-eh-theme] #content .rc255-today-tile{background:var(--pc-surface)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #content .rc255-today-tile:hover{background:var(--pc-table-hover)!important;border-color:var(--pc-line-strong)!important;box-shadow:var(--pc-shadow-soft)!important}
body[data-eh-theme] #content .rc255-today-tile b{color:var(--pc-title)!important}body[data-eh-theme] #content .rc255-today-tile small{color:var(--pc-muted)!important}
body[data-eh-theme] #rc363FixedShipmentLayout .rc363-process-head{border-color:var(--pc-line)!important}
body[data-eh-theme] #rc363FixedShipmentLayout .rc363-process-number{background:var(--pc-primary-soft)!important;color:var(--pc-primary)!important;border:1px solid color-mix(in srgb,var(--pc-primary) 24%,var(--pc-line))!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(.rc363-process-head h2,.rc363-actions-copy h3,.rc363-qr-copy b,.rc396-graphic-head b){color:var(--pc-title)!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(.rc363-actions-copy p,.rc363-qr-copy,.rc396-axis,.rc396-column-head small){color:var(--pc-muted)!important}
body[data-eh-theme] #rc573ColliCard .rc363-owned-row{background:var(--pc-surface-2)!important;border-color:var(--pc-line-strong)!important;box-shadow:var(--pc-shadow-soft)!important}
body[data-eh-theme] #rc573ColliCard input[data-rc363-weight="1"]{background:var(--pc-field)!important;color:var(--pc-text)!important;border-color:var(--pc-line-strong)!important;box-shadow:none!important}
body[data-eh-theme] #rc363FixedShipmentLayout .field:focus-within{background:var(--pc-primary-soft)!important;border-color:color-mix(in srgb,var(--pc-focus) 42%,var(--pc-line))!important;box-shadow:0 0 0 3px color-mix(in srgb,var(--pc-focus) 14%,transparent)!important}
body[data-eh-theme] #rc363FixedShipmentLayout :is(input,select,textarea):focus{border-color:var(--pc-focus)!important;background:var(--pc-field)!important;box-shadow:0 0 0 3px color-mix(in srgb,var(--pc-focus) 18%,transparent)!important}
body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-card{background:var(--pc-surface-2)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-card h3{color:var(--pc-title)!important}body[data-eh-theme] #rc363BlockCustomer .rc628-customer-context-customer,body[data-eh-theme] #rc363BlockCustomer .rc628-customer-empty{color:var(--pc-muted)!important}
body[data-eh-theme] #content .rc544-user{background:var(--pc-surface)!important;color:var(--pc-text)!important;border-color:var(--pc-line)!important;box-shadow:none!important}
body[data-eh-theme] #content .rc544-user:hover{background:var(--pc-table-hover)!important;border-color:var(--pc-line-strong)!important}
body[data-eh-theme] #content .rc544-user.active{background:var(--pc-primary-soft)!important;border-color:var(--pc-primary)!important;box-shadow:0 0 0 2px color-mix(in srgb,var(--pc-primary) 16%,transparent)!important}
body[data-eh-theme] #content .rc544-user :is(b,strong){color:var(--pc-title)!important}body[data-eh-
```

## RC946 task pointer
needle='rc946TaskPointer'
count=13

### line~21153 pos=4236666
```text
==='open-all')return setAllGroups(true);if(a==='close-all')return setAllGroups(false);if(a==='prev')return shiftWeek(-1);if(a==='next')return shiftWeek(1);if(a==='holidays')return autoPlanHolidays()}if(b.hasAttribute('data-i218-open')){e.preventDefault();return openEditor(b.getAttribute('data-i218-open'))}if(b.hasAttribute('data-i218-reset')){e.preventDefault();return resetTask(b.getAttribute('data-i218-reset'))}if(b.hasAttribute('data-i218-move')){e.preventDefault();var id=b.getAttribute('data-i218-move'),inp=document.querySelector('[data-i218-date="'+CSS.escape(id)+'"]');return moveTask(id,inp&&inp.value)}},true);
document.addEventListener('toggle',function(e){var el=e.target;if(el&&el.matches&&el.matches('#index218Planner details.i218-group')){rememberGroup(el.getAttribute('data-i218-group')||'',el.open);hydrateGroup(el)}},true);
document.addEventListener('change',function(e){if(e.target&&e.target.hasAttribute('data-i218-week'))setWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&
```

### line~21154 pos=4236842
```text
urn autoPlanHolidays()}if(b.hasAttribute('data-i218-open')){e.preventDefault();return openEditor(b.getAttribute('data-i218-open'))}if(b.hasAttribute('data-i218-reset')){e.preventDefault();return resetTask(b.getAttribute('data-i218-reset'))}if(b.hasAttribute('data-i218-move')){e.preventDefault();var id=b.getAttribute('data-i218-move'),inp=document.querySelector('[data-i218-date="'+CSS.escape(id)+'"]');return moveTask(id,inp&&inp.value)}},true);
document.addEventListener('toggle',function(e){var el=e.target;if(el&&el.matches&&el.matches('#index218Planner details.i218-group')){rememberGroup(el.getAttribute('data-i218-group')||'',el.open);hydrateGroup(el)}},true);
document.addEventListener('change',function(e){if(e.target&&e.target.hasAttribute('data-i218-week'))setWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}}
```

### line~21154 pos=4237185
```text
ument.querySelector('[data-i218-date="'+CSS.escape(id)+'"]');return moveTask(id,inp&&inp.value)}},true);
document.addEventListener('toggle',function(e){var el=e.target;if(el&&el.matches&&el.matches('#index218Planner details.i218-group')){rememberGroup(el.getAttribute('data-i218-group')||'',el.open);hydrateGroup(el)}},true);
document.addEventListener('change',function(e){if(e.target&&e.target.hasAttribute('data-i218-week'))setWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MOD
```

### line~21155 pos=4237458
```text
-i218-group')||'',el.open);hydrateGroup(el)}},true);
document.addEventListener('change',function(e){if(e.target&&e.target.hasAttribute('data-i218-week'))setWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function arr(v){r
```

### line~21155 pos=4237554
```text
(e){if(e.target&&e.target.hasAttribute('data-i218-week'))setWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function arr(v){return Array.isArray(v)?v:[]}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window._
```

### line~21155 pos=4237613
```text
tWeek(e.target.value)},true);
var rc946TaskPointer=null;
function rc946TaskPointerDayAt(e){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY));return el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function arr(v){return Array.isArray(v)?v:[]}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.state||(window.state={}))}
```

### line~21156 pos=4237781
```text
el&&el.closest?el.closest('[data-i218-drop-date]'):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function arr(v){return Array.isArray(v)?v:[]}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.state||(window.state={}))}catch(_){return window.state||(window.state={})}}
function later(fn,ms){var n=window.ExportHUBClean&&window.ExportHUBClean.native;return(n&&n.setTimeout?n.setTimeout:wi
```

### line~21156 pos=4237830
```text
):null}
function rc946TaskPointerClear(){var p=rc946TaskPointer;if(p&&p.card){p.card.classList.remove('i218-dragging');p.card.style.cursor='';p.card.style.userSelect=''}document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){x.classList.remove('drag-over')});rc946TaskPointer=null}
document.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-i218-drag]');if(!card)return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var id=card.getAttribute('data-i218-drag');if(!id)return;rc946TaskPointer={pointerId:e.pointerId,id:id,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}},true);
document.addEventListener('pointermove',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;p.card.classList.add('i218-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();var day=rc946TaskPointerDayAt(e);document.querySelectorAll('#index218Planner .drag-over').forEach(function(x){if(x!==day)x.classList.remove('drag-over')});if(day)day.classList.add('drag-over')},{capture:true,passive:false});
document.addEventListener('pointerup',function(e){var p=rc946TaskPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,id=p.id,day=active?rc946TaskPointerDayAt(e):null;if(active)e.preventDefault();rc946TaskPointerClear();if(active&&id&&day)moveTask(id,day.getAttribute('data-i218-drop-date'),day)},true);
document.addEventListener('pointercancel',function(e){var p=rc946TaskPointer;if(p&&e.pointerId===p.pointerId)rc946TaskPointerClear()},true);
function boot(){schedule()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange','exporthub:sync','exporthub:tasks-updated'].forEach(function(n){window.addEventListener(n,schedule)});
window.ExportHUBIndex218=Object.freeze({version:VERSION,render:render,autoPlanHolidays:autoPlanHolidays,moveTask:moveTask,setWeek:setWeek,holidaysNRW:holidaysNRW,selfTest:function(){var ws=selectedWeek(),tasks=openTasksForWeek(ws),picks=tasks.filter(function(t){return groupName(t)==='Picken'});return{ok:picks.length>0&&GROUPS.length===5,weekStart:ws,open:tasks.length,picks:picks.length,groups:GROUPS.slice(),master:window.ExportHUBTaskMasterRC848&&window.ExportHUBTaskMasterRC848.selfTest()}}});
})();
</script>
<!-- INDEX 218 AUTHORITATIVE MOVABLE TASK PLANNER END -->
<!-- INDEX 232 COLLI + MAIL TEMPLATE + LOADLIST 2 + CONFIRMED POD BACKUP START -->
<script id="index232-authoritative-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_INDEX232__)return;window.__EXPORTHUB_INDEX232__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB';
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}
function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function arr(v){return Array.isArray(v)?v:[]}
function state(){try{return window.__EXPORTHUB_GET_STATE__?window.__EXPORTHUB_GET_STATE__():(window.state||(window.state={}))}catch(_){return window.state||(window.state={})}}
function later(fn,ms){var n=window.ExportHUBClean&&window.ExportHUBClean.native;return(n&&n.setTimeout?n.setTimeout:window.setTimeout)(fn,ms||0)}
function direct(grid,
```

## warehouse bind
needle='bindWarehouseDnD'
count=2

### line~24420 pos=4908272
```text
</section>'}
function moveModal(){var sh=currentMove();if(!sh||!isAdmin())return '';return '<div class="rc504-modal-backdrop" onclick="if(event.target===this)rc504WarehouseCloseMove()"><div class="rc504-modal"><h3>Sendung manuell verschieben</h3><p>Als Admin kannst du die Sendung direkt zwischen Eingang und den Ausgangslocations verschieben.</p><div class="rc504-modal-info"><b>'+esc(sref(sh)||sid(sh))+' · '+esc(customer(sh))+'</b><span>Aktuelle Location: '+esc(locationName(sh))+' · '+palletCount(sh)+' Paletten</span></div><div class="rc504-modal-grid"><div class="field"><label for="rc504MoveLocation">Neue Location</label><select id="rc504MoveLocation">'+moveOptions(Number(sh.warehouseLocationCode||0))+'</select></div><div class="field"><label>Hinweis</label><input type="text" value="Manuelle Admin-Umbuchung" disabled></div></div><div class="rc504-modal-note">Code 0 = Eingang · 1–5 = Lane 1–5 · 6 = Regal · 7 = Vor dem Regal · 8 = DAF</div><div class="rc504-modal-actions"><button class="ghost" onclick="return rc504WarehouseCloseMove()">Abbrechen</button><button class="rc504-shift-btn primary" id="rc504MoveSave" onclick="return rc504WarehouseSaveMove()">Verschieben</button></div></div></div>'}
function rc946WarehousePointerZone(e,root){var el=document.elementFromPoint(Number(e.clientX),Number(e.clientY)),zone=el&&el.closest?el.closest('[data-warehouse-drop-code]'):null;return zone&&root.contains(zone)?zone:null}
function rc946WarehouseClearPointer(root){var p=dragPointer;if(p&&p.card){p.card.classList.remove('rc513-dragging');p.card.style.cursor='';p.card.style.userSelect=''}if(root&&root.querySelectorAll)root.querySelectorAll('.rc513-drop-target').forEach(function(x){x.classList.remove('rc513-drop-target')});dragPointer=null;dragKey='';stopWarehouseDragScroll()}
function bindWarehouseDnD(root){if(!root||root.__rc513WarehouseDnD)return;root.__rc513WarehouseDnD=true;root.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-warehouse-drag-key]');if(!card||!isAdmin())return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var key=q(card.getAttribute('data-warehouse-drag-key'));if(!key)return;dragPointer={pointerId:e.pointerId,key:key,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}});root.addEventListener('pointermove',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;dragKey=p.key;p.card.classList.add('rc513-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();updateWarehouseDragScroll(e);var zone=rc946WarehousePointerZone(e,root);root.querySelectorAll('.rc513-drop-target').forEach(function(x){if(x!==zone)x.classList.remove('rc513-drop-target')});if(zone)zone.classList.add('rc513-drop-target')},{passive:false});root.addEventListener('pointerup',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,key=p.key,zone=active?rc946WarehousePointerZone(e,root):null;if(active)e.preventDefault();rc946WarehouseClearPointer(root);if(active&&key&&zone)moveShipmentKey(key,Number(zone.getAttribute('data-warehouse-drop-code')||0))});root.addEventListener('pointercancel',function(e){var p=dragPointer;if(p&&e.pointerId===p.pointerId)rc946WarehouseClearPointer(root)})}
function stopWarehouseDragScroll(){dragScrollSpeed=0;if(dragScrollFrame){cancelAnimationFrame
```

### line~24425 pos=4913352
```text
tor('svg');if(svg){svg.setAttribute('fill','none');svg.setAttribute('stroke','currentColor');svg.setAttribute('stroke-width','2');svg.setAttribute('stroke-linecap','round');svg.setAttribute('stroke-linejoin','round')}}return true}
function render(doSync){var root=document.getElementById('content');if(!root)return false;root.className='content rc504-warehouse-view';var activeList=all().filter(active),pallets=activeList.reduce(function(n,sh){return n+palletCount(sh)},0),prepared=activeList.filter(function(sh){return sh.warehousePrepared===true}).length,eingang=zoneData('Eingang').list.length;root.innerHTML='<div class="rc504-warehouse"><div class="rc504-warehouse-head"><div><h2>🏭 Lager & Locations</h2><p>Aktuelle Position aller noch nicht abgeholten Sendungen. Ausgangslocations zeigen direkt, welche Sendungen dort stehen. Admins können Sendungen zusätzlich manuell verschieben.</p></div><div class="rc504-warehouse-actions"><button class="ghost" onclick="return rc504WarehouseRefresh()">Aktualisieren</button>'+(isAdmin()?'<button class="ghost" onclick="return rc504WarehouseOpenFirst()">Manuell verschieben</button>':'')+'</div></div><div class="rc504-warehouse-summary"><div class="rc504-warehouse-kpi"><span>Sendungen im Lager</span><strong>'+activeList.length+'</strong></div><div class="rc504-warehouse-kpi"><span>Belegte Locations</span><strong>'+occupiedCount()+'</strong></div><div class="rc504-warehouse-kpi"><span>Im Eingang</span><strong>'+eingang+'</strong></div><div class="rc504-warehouse-kpi"><span>Vorbereitet</span><strong>'+prepared+'</strong></div></div><div class="rc504-layout">'+LOCS.map(function(x){return zone(x[0],x[1],x[2])}).join('')+'</div><p class="rc504-sync">Location-Codes: 0 Eingang · 1–5 Lane 1–5 · 6 Regal · 7 Vor dem Regal · 8 DAF</p></div>'+moveModal();bindWarehouseDnD(root);if(doSync===true)setTimeout(function(){sync(false)},650);paintNavIcon();return false}
function locationUrl(sh){var tok=token(sh);if(!tok)return'';try{var u=new URL('/location.html',location.origin);u.searchParams.set('location',tok);var cache=window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.cache;if(cache)u.searchParams.set('v',String(cache));return u.href}catch(_){return''}}
function qrMarkup(sh){if(!sh||cancelled(sh))return'';var tok=token(sh),url=locationUrl(sh),svg='';if(!tok||!url)return'';try{if(typeof window.RC569QrSvg==='function')svg=window.RC569QrSvg(url)}catch(_){}if(!svg)return'';return '<div class="index351-qr-box rc504-location-qr-box" data-location-token="'+esc(tok)+'">'+svg+'<span>QR-Location</span></div>'}
function dashboardMeta(id){var map={'warehouse-eingang':['Eingang',0],'warehouse-lane1':['Lane 1',1],'warehouse-lane2':['Lane 2',2],'warehouse-lane3':['Lane 3',3],'warehouse-lane4':['Lane 4',4],'warehouse-lane5':['Lane 5',5],'warehouse-regal':['Regal',6],'warehouse-vorregal':['Vor dem Regal',7],'warehouse-daf':['DAF',8]},entry=map[id]||map['warehouse-eingang'],name=entry[0],code=entry[1],d=zoneData(name),count=d.list.length,group=name==='Eingang'?0:(count>0?1:2);return{id:id,name:name,code:code,count:count,group:group,data:d}}
function sortDashboardWidgets(ids){ids=arr(ids).slice();var sorted=ids.filter(function(id){return /^warehouse-/.test(id)}).sort(function(a,b){var x=dashboardMeta(a),y=dashboardMeta(b);if(x.group!==y.group)return x.group-y.group;if(x.group===1&&x.count!==y.count)return y.count-x.count;return x.code-y.code}),i=0;return ids.map(function(id){return /^warehouse-/.test(id)?sorted[i++]:id})}
function dashboardWidget(id){var meta=dashboardMeta(id),name=meta.name,d=meta.data,stateClass=name==='Eingang'?'eingang':(met
```

## warehouse move
needle='moveShipmentKey('
count=3

### line~24391 pos=4902839
```text
x)return;s.x.warehouseLocation=s.warehouseLocation;s.x.warehouseLocationCode=s.warehouseLocationCode;s.x.warehouseMovedBy=s.warehouseMovedBy;s.x.warehouseMovedAt=s.warehouseMovedAt});
 return true
}
function warehouseZoneRefreshRC907(code){
 var zone=document.querySelector('[data-warehouse-drop-code="'+Number(code)+'"]');if(!zone)return false;
 var name=q(zone.getAttribute('data-warehouse-drop-name')),d=zoneData(name),count=zone.querySelector('.rc504-zone-count');
 if(count){var load=d.pallets>0&&d.packages>0?(d.pallets+' Paletten · '+d.packages+' Pakete'):d.pallets>0?(d.pallets+' Paletten'):d.packages>0?(d.packages+' Pakete'):'0 Colli';count.textContent=d.list.length+' Sendungen · '+load}
 var list=zone.querySelector('.rc504-zone-list');if(list&&!list.querySelector('.rc504-shipment'))list.innerHTML='<div class="rc504-zone-empty">Keine Sendung an dieser Location.</div>';
 return true
}
function warehouseMoveDomRC907(key,fromCode,toCode,local){
 if(currentView()!=='warehouse')return false;
 var card=document.querySelector('[data-warehouse-drag-key="'+CSS.escape(q(key))+'"]'),target=document.querySelector('[data-warehouse-drop-code="'+Number(toCode)+'"] .rc504-zone-list');
 if(!card||!target)return false;
 var empty=target.querySelector('.rc504-zone-empty');if(empty)empty.remove();
 target.appendChild(card);
 var badge=card.querySelector('.rc504-badge.loc');if(badge)badge.textContent=q(local&&local.location)||'—';
 var moved=card.querySelectorAll('.rc504-meta-chip b');if(moved[1])moved[1].textContent=q(local&&local.movedBy)||'—';if(moved[2])moved[2].textContent=fmt(local&&local.movedAt);
 warehouseZoneRefreshRC907(fromCode);warehouseZoneRefreshRC907(toCode);
 var modal=document.querySelector('.rc504-modal-backdrop');if(modal)modal.remove();
 return true
}

async function moveShipmentKey(key,code){
 if(!isAdmin()){alert('Für das manuelle Verschieben ist das Funktionsrecht Lager = Admin erforderlich.');return false}
 var sh=all().find(function(x){return skey(x)===q(key)});if(!sh)return false;
 code=Math.round(Number(code||0));if(code<0||code>8)return false;
 var fromCode=Number(sh.warehouseLocationCode||0);if(fromCode===code)return false;
 var tok=token(sh);if(!tok){alert('Location-Token fehlt.');return false}
 var snapshot=warehouseMoveSnapshotRC907(sh),local=warehouseApplyLocalMoveRC907(sh,code);
 pendingMove='';dragKey='';warehouseMoveDomRC907(key,fromCode,code,local);
 try{
  var result=await api('move',{method:'POST',token:tok,body:{token:tok,locationCode:code,colliCount:totalColliCount(sh),totalColli:totalColliCount(sh),expectedColliCount:totalColliCount(sh)}});
  if(result&&result.record)applyRecord(result.record);
  persist('Lagerlocation verschoben');
  try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:true,optimistic:true,reference:sref(sh),locationCode:code}}))}catch(_){}
  setTimeout(function(){sync(false)},1400);
  return true
 }catch(e){
  warehouseRestoreMoveRC907(snapshot);
  if(currentView()==='warehouse')render(false);
  alert(e&&e.message||'Sendung konnte nicht verschoben werden.');
  return false
 }
}
async function submitMove(){var sh=currentMove(),sel=document.getElementById('rc504MoveLocation');if(!sh||!sel)return false;var btn=document.getElementById('rc504MoveSave');if(btn)btn.disabled=true;try{await moveShipmentKey(skey(sh),sel.value)}finally{if(btn)btn.disabled=false}return false}
function shipmentBadge(sh){var loc=locationName(sh)||'—';return '<span class="rc504-badge loc">'+esc(loc)+'</span>'}
function shipmentCard(sh){var ref=esc(sref(sh)||sid(sh)),cust=esc(customer(sh)),
```

### line~24413 pos=4904358
```text
tent=q(local&&local.movedBy)||'—';if(moved[2])moved[2].textContent=fmt(local&&local.movedAt);
 warehouseZoneRefreshRC907(fromCode);warehouseZoneRefreshRC907(toCode);
 var modal=document.querySelector('.rc504-modal-backdrop');if(modal)modal.remove();
 return true
}

async function moveShipmentKey(key,code){
 if(!isAdmin()){alert('Für das manuelle Verschieben ist das Funktionsrecht Lager = Admin erforderlich.');return false}
 var sh=all().find(function(x){return skey(x)===q(key)});if(!sh)return false;
 code=Math.round(Number(code||0));if(code<0||code>8)return false;
 var fromCode=Number(sh.warehouseLocationCode||0);if(fromCode===code)return false;
 var tok=token(sh);if(!tok){alert('Location-Token fehlt.');return false}
 var snapshot=warehouseMoveSnapshotRC907(sh),local=warehouseApplyLocalMoveRC907(sh,code);
 pendingMove='';dragKey='';warehouseMoveDomRC907(key,fromCode,code,local);
 try{
  var result=await api('move',{method:'POST',token:tok,body:{token:tok,locationCode:code,colliCount:totalColliCount(sh),totalColli:totalColliCount(sh),expectedColliCount:totalColliCount(sh)}});
  if(result&&result.record)applyRecord(result.record);
  persist('Lagerlocation verschoben');
  try{window.dispatchEvent(new CustomEvent('exporthub:warehouse-updated',{detail:{changed:true,optimistic:true,reference:sref(sh),locationCode:code}}))}catch(_){}
  setTimeout(function(){sync(false)},1400);
  return true
 }catch(e){
  warehouseRestoreMoveRC907(snapshot);
  if(currentView()==='warehouse')render(false);
  alert(e&&e.message||'Sendung konnte nicht verschoben werden.');
  return false
 }
}
async function submitMove(){var sh=currentMove(),sel=document.getElementById('rc504MoveLocation');if(!sh||!sel)return false;var btn=document.getElementById('rc504MoveSave');if(btn)btn.disabled=true;try{await moveShipmentKey(skey(sh),sel.value)}finally{if(btn)btn.disabled=false}return false}
function shipmentBadge(sh){var loc=locationName(sh)||'—';return '<span class="rc504-badge loc">'+esc(loc)+'</span>'}
function shipmentCard(sh){var ref=esc(sref(sh)||sid(sh)),cust=esc(customer(sh)),moved=q(sh.warehouseMovedBy),movedAt=q(sh.warehouseMovedAt),prepared=sh.warehousePrepared===true||/vorbereitet/i.test(q(sh.status||sh.processStatus)),admin=isAdmin(),load=warehouseLoadSummary(sh),dateClass=shipmentDateClass(sh),drag=admin?' data-warehouse-drag-key="'+esc(skey(sh))+'" title="Sendung auf eine andere Location ziehen"':'';return '<article class="rc504-shipment'+dateClass+'"'+drag+'><div class="rc504-shipment-top"><div><div class="rc504-shipment-ref">'+ref+'</div><div class="rc504-shipment-customer">'+cust+'</div></div></div><div class="rc664-shipment-badges">'+shipmentBadge(sh)+(prepared?'<span class="rc504-badge ready">Vorbereitet</span>':'')+'</div><div class="rc504-shipment-meta"><div class="rc504-meta-chip"><small>'+esc(load.label)+'</small><b>'+esc(load.value)+'</b></div><div class="rc504-meta-chip"><small>Verlader</small><b>'+esc(moved||'—')+'</b></div><div class="rc504-meta-chip"><small>Gebucht</small><b>'+esc(fmt(movedAt))+'</b></div></div></article>'}
function zone(name,key,code){var d=zoneData(name),cls=key==='eingang'?' eingang':key==='regal'?' regal':key==='vorregal'?' vorregal':'',loadLabel=d.pallets>0&&d.packages>0?(d.pallets+' Paletten · '+d.packages+' Pakete'):d.pallets>0?(d.pallets+' Paletten'):d.packages>0?(d.packages+' Pakete'):'0 Colli';return '<section class="rc504-zone '+esc(key)+cls+'" data-warehouse-drop-code="'+code+'" data-warehouse-drop-name="'+esc(name)+'"><div class="rc504-zone-head"><div class="rc504-zone-title"><span class="rc504-zone-code">'+(code===
```

### line~24420 pos=4909763
```text
r;if(p&&p.card){p.card.classList.remove('rc513-dragging');p.card.style.cursor='';p.card.style.userSelect=''}if(root&&root.querySelectorAll)root.querySelectorAll('.rc513-drop-target').forEach(function(x){x.classList.remove('rc513-drop-target')});dragPointer=null;dragKey='';stopWarehouseDragScroll()}
function bindWarehouseDnD(root){if(!root||root.__rc513WarehouseDnD)return;root.__rc513WarehouseDnD=true;root.addEventListener('pointerdown',function(e){if(e.button!=null&&e.button!==0)return;var card=e.target&&e.target.closest&&e.target.closest('[data-warehouse-drag-key]');if(!card||!isAdmin())return;if(e.target&&e.target.closest&&e.target.closest('button,input,select,textarea,a,label'))return;var key=q(card.getAttribute('data-warehouse-drag-key'));if(!key)return;dragPointer={pointerId:e.pointerId,key:key,card:card,x:Number(e.clientX),y:Number(e.clientY),active:false}});root.addEventListener('pointermove',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var dx=Number(e.clientX)-p.x,dy=Number(e.clientY)-p.y;if(!p.active){if(dx*dx+dy*dy<9)return;p.active=true;dragKey=p.key;p.card.classList.add('rc513-dragging');p.card.style.cursor='grabbing';p.card.style.userSelect='none';try{p.card.setPointerCapture(e.pointerId)}catch(_){}}e.preventDefault();updateWarehouseDragScroll(e);var zone=rc946WarehousePointerZone(e,root);root.querySelectorAll('.rc513-drop-target').forEach(function(x){if(x!==zone)x.classList.remove('rc513-drop-target')});if(zone)zone.classList.add('rc513-drop-target')},{passive:false});root.addEventListener('pointerup',function(e){var p=dragPointer;if(!p||e.pointerId!==p.pointerId)return;var active=p.active,key=p.key,zone=active?rc946WarehousePointerZone(e,root):null;if(active)e.preventDefault();rc946WarehouseClearPointer(root);if(active&&key&&zone)moveShipmentKey(key,Number(zone.getAttribute('data-warehouse-drop-code')||0))});root.addEventListener('pointercancel',function(e){var p=dragPointer;if(p&&e.pointerId===p.pointerId)rc946WarehouseClearPointer(root)})}
function stopWarehouseDragScroll(){dragScrollSpeed=0;if(dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function warehouseDragScrollTick(){dragScrollFrame=0;if(!dragKey||!dragScrollSpeed)return;window.scrollBy(0,dragScrollSpeed);dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick)}
function updateWarehouseDragScroll(e){if(!dragKey||!e)return stopWarehouseDragScroll();var h=Math.max(1,window.innerHeight||document.documentElement.clientHeight||1),edge=Math.max(80,Math.min(150,Math.round(h*.16))),y=Number(e.clientY),speed=0;if(Number.isFinite(y)){if(y<edge)speed=-Math.max(5,Math.round((edge-y)/edge*28));else if(y>h-edge)speed=Math.max(5,Math.round((y-(h-edge))/edge*28))}dragScrollSpeed=speed;if(speed&&!dragScrollFrame)dragScrollFrame=requestAnimationFrame(warehouseDragScrollTick);if(!speed&&dragScrollFrame){cancelAnimationFrame(dragScrollFrame);dragScrollFrame=0}}
function paintNavIcon(){var b=document.querySelector('#nav>button[data-index321-view="warehouse"],#nav button[data-view="warehouse"]');if(!b)return false;var icon=b.querySelector('.index308-nav-icon');if(icon){icon.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 21h18M4 21V8l7-4v17M11 5l9 3v13"></path><rect x="6" y="11" width="2" height="2"></rect><rect x="6" y="15" width="2" height="2"></rect><rect x="14" y="10" width="2" height="2"></rect><rect x="18" y="10" width="2" height="2"></rect><rect x="14" y="14" width="2" height="2"></rect><rect x="18" y="14" width="2" height="2"></rect></svg>';icon.style.fontSize='';var svg=icon.querySelector('svg');
```

## busy/loader
needle='busy'
count=74

### line~21820 pos=4408717
```text
chert und die Liste neu geladen.',false)}catch(e){setStatus((e&&e.message)||'Verladecode konnte nicht gespeichert werden.',true)}finally{busy=false;if(btn&&btn.isConnected)btn.disabled=false}return false};
window.rc727CreateLoaderPin=async function(){if(busy)return false;var name=q((document.getElementById('rc727NewLoaderName')||{}).value),pin=q((document.getElementById('rc727NewLoaderPin')||{}).value);if(!validate(name,pin))return false;busy=true;try{var data=await serverCall('create',{name:name,pin:pin,code:pin,loaderPin:pin,active:true}),list=responsePins(data);if(list===null){var created=responseSinglePin(data)||{id:q(data&&data.id)||('loader-'+Date.now()),name:name,pin:pin,active:true};mergeOptimistic(created)}patchCards();busy=false;await refresh(true);patchCards();setStatus('Verlader '+name+' wurde mit persönlichem Verladecode angelegt und vom Server neu geladen.',false)}catch(e){setStatus((e&&e.message)||'Verlader konnte nicht angelegt werden.',true)}finally{busy=false}return false};
window.rc727DeleteLoaderPin=async function(btn){if(busy)return false;var row=rowFromButton(btn);if(!row)return false;if(!confirm('Verladecode für '+row.name+' wirklich löschen?'))return false;busy=true;btn.disabled=true;try{await serverCall('delete',{id:row.id,name:row.name});optimisticPins=optimisticPins.filter(function(x){return q(x.id)!==q(row.id)&&q(x.name).toLowerCase()!==q(row.name).toLowerCase()});if(Array.isArray(serverPins))serverPins=serverPins.filter(function(x){return q(x.id)!==q(row.id)&&q(x.name).toLowerCase()!==q(row.name).toLowerCase()});busy=false;await refresh(true);patchCards();setStatus('Verladecode für '+row.name+' wurde gelöscht und die Serverliste neu geladen.',false)}catch(e){setStatus((e&&e.message)||'Verladecode konnte nicht gelöscht werden.',true)}finally{busy=false;if(btn&&btn.isConnected)btn.disabled=false}return false};
function patch(){migrate();positionCard();patchCards();if(q(state().view)==='rights')refresh(false);try{window.ExportHUBBuild.applyVersion();window.ExportHUBBuild.applyTitle()}catch(_){} }
function schedule(){if(timer)return;timer=setTimeout(function(){timer=0;patch()},80)}
window.rc544SaveGlobalPin=function(){refresh(true);return false};window.rc542SaveGlobalPin=window.rc544SaveGlobalPin;
function boot(){patch();setTimeout(function(){patch();refresh(false)},250)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:sync','exporthub:rendered','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,schedule)});
window.ExportHUBIndex250=Object.freeze({version:VERSION,migration:MIGRATION,refresh:function(){return refresh(true)},patch:patch,selfTest:function(){return{version:VERSION,serverValidated:true,adminApi:API,generalPinRemoved:!q((state().settings||{}).qrPin),serverPinsLoaded:Array.isArray(serverPins),count:Array.isArray(serverPins)?serverPins.length:0}}});
})();
</script>
<!-- INDEX 250 PERSONAL LOADER PINS AND FREE PLATE INPUT END -->
<!-- INDEX 255 AUTHORITATIVE SHIPMENT STATUS + TODAY DASHBOARD START -->
<style id="index255-authoritative-status-style">
#content .rc524-shipment-card.index255-canonical-status-pod,
#content .rc524-shipment-card.index255-canonical-status-abgeholt{border-left:7px solid #16a34a!important}
#content .index255-canonical-pill-pod,
#content .index255-canonical-pill-abgeholt{background:#dcfce7!important;color:#166534!important}
.index255-recovery-note{margin:9px 0 0;padding:9px 11px;border-radius:12px;font-size:12px;font-weight:850;background:#eff6ff;color:#1e3a8a}
.i
```

### line~23152 pos=4602717
```text
ColliCard .rc682-packaging-column>strong{display:block!important;margin:0 0 3px!important;padding:1px 3px 7px!important;border-bottom:1px solid #dbe7f5!important;color:#08245d!important;font-size:13px!important;line-height:1.2!important;font-weight:950!important;background:#f8fbff!important}
#rc573ColliCard .rc682-packaging-option{width:100%!important;min-height:34px!important;padding:6px 8px!important;border:1px solid transparent!important;border-radius:8px!important;background:#fff!important;color:#1e293b!important;text-align:left!important;font-size:12.5px!important;line-height:1.2!important;font-weight:800!important;cursor:pointer!important;white-space:normal!important;overflow-wrap:anywhere!important}
#rc573ColliCard .rc682-packaging-option:hover{border-color:#93c5fd!important;background:#eff6ff!important;color:#0b4b91!important}
#rc573ColliCard .rc682-packaging-option[aria-pressed="true"]{border-color:#2563eb!important;background:#dbeafe!important;color:#08245d!important}
#rc573ColliCard .rc682-packaging-empty{padding:7px 8px!important;color:#94a3b8!important;font-size:11px!important;font-weight:750!important}
@media(max-width:720px){#rc573ColliCard .rc682-packaging-menu{width:min(650px,calc(100vw - 14px))!important;left:0!important;max-height:58vh!important;overflow:auto!important}#rc573ColliCard .rc682-packaging-columns{grid-template-columns:minmax(120px,.85fr) minmax(215px,1.35fr) minmax(120px,.85fr)!important;min-width:480px!important}}
</style>
<script id="exporthub-rc373-shipment-controller">
(function(){
'use strict';
if(window.__EXPORTHUB_RC374_SHIPMENT_OWNER__)return;
window.__EXPORTHUB_RC374_SHIPMENT_OWNER__=true;
if(window.__EXPORTHUB_PICKUP_MODE__)return;
var VERSION=(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',installed=false,busy=false,persistenceSave=null,weightIndex=-1,patchTimer=0,processRetryTimer=0,processRetryCount=0,qrPromise=null,editSaveTimer=0,editSaveReason='',remoteMergeTimer=0,remoteMergePending=false,localFieldDraft={},localFieldDraftShipment='',localRowsDirty=false,localCustomerDirty=false,fullPatchPending=false,deferredPatchTimer=0,editingCustomerLock=null,customerChangeColliLock=null,lockSyncPromise=null,lastLockCheckAt=0;
var PACK=[{name:'Einweg Palette',ldm:0.4,l:120,w:80,h:110},{name:'Euro Palette',ldm:0.4,l:120,w:80,h:110},{name:'Plastic Palette',ldm:0.5897,l:122,w:116,h:''},{name:'Gestapelte Einweg Palette',ldm:0.2,l:120,w:80,h:220},{name:'Gestapelte Euro Palette',ldm:0.2,l:120,w:80,h:220},{name:'Gestapelte Industrie Palette',ldm:0.25,l:120,w:100,h:220},{name:'Industrie Palette',ldm:0.5,l:120,w:100,h:110},{name:'Paletten gestell',ldm:0.45,l:120,w:90,h:200},{name:'Holzbox',ldm:0.6,l:'',w:'',h:''},{name:'Düsseldorfer Palette',ldm:0.2,l:80,w:60,h:60},{name:'E0',ldm:0.03,l:30,w:20,h:11},{name:'E1',ldm:0.03,l:39,w:20,h:20},{name:'E2',ldm:0.06,l:46,w:33,h:16},{name:'E3',ldm:0.06,l:43,w:31,h:31},{name:'E4',ldm:0.12,l:66,w:45,h:32},{name:'E5',ldm:0.12,l:66,w:45,h:43},{name:'Umschlag',ldm:0,l:15,w:5,h:1},{name:'E6',ldm:0.0156,l:25,w:15,h:5}];
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}function arr(v){return Array.isArray(v)?v:[]}function clone(v){try{return JSON.parse(JSON.stringify(v))}catch(_){return Object.assign({},v||{})}}function num(v){var x=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(x)?x:0}function round(v){return Math.round((num(v)+Number.EPSILON)*100)/100}
function currentCreator(){var u=null;try{if(typeof window.__EXPORTHUB_GET
```

### line~23157 pos=4605419
```text
',h:''},{name:'Düsseldorfer Palette',ldm:0.2,l:80,w:60,h:60},{name:'E0',ldm:0.03,l:30,w:20,h:11},{name:'E1',ldm:0.03,l:39,w:20,h:20},{name:'E2',ldm:0.06,l:46,w:33,h:16},{name:'E3',ldm:0.06,l:43,w:31,h:31},{name:'E4',ldm:0.12,l:66,w:45,h:32},{name:'E5',ldm:0.12,l:66,w:45,h:43},{name:'Umschlag',ldm:0,l:15,w:5,h:1},{name:'E6',ldm:0.0156,l:25,w:15,h:5}];
function q(v){return String(v==null?'':v).replace(/\s+/g,' ').trim()}function low(v){return q(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}function arr(v){return Array.isArray(v)?v:[]}function clone(v){try{return JSON.parse(JSON.stringify(v))}catch(_){return Object.assign({},v||{})}}function num(v){var x=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(x)?x:0}function round(v){return Math.round((num(v)+Number.EPSILON)*100)/100}
function currentCreator(){var u=null;try{if(typeof window.__EXPORTHUB_GET_CURRENT_USER__==='function')u=window.__EXPORTHUB_GET_CURRENT_USER__()}catch(_){}try{if(!u&&window.currentUser)u=window.currentUser}catch(_){}u=u||{};var name=q(u.displayName||u.name||u.fullName||u.user||u.username||u.login||u.email),id=q(u.id||u.userId||u.username||u.login||u.user||u.email||name);if(!name){var n=document.getElementById('profileName')||document.getElementById('brandUserLine');name=q(n&&n.textContent).replace(/^ExportHUB-Benutzer:\s*/i,'')}return{id:id||name,name:name||id}}
var loadingBarSeq=0,loadingBarHideTimer=0,loadingBarTasks=Object.create(null),loadingBarVisible=false,loadingBarText='',loadingBarLastToken=0;
function ensureLoadingBar(){var wrap=document.getElementById('rc680LoadingBar');if(!wrap){wrap=document.createElement('div');wrap.id='rc680LoadingBar';wrap.setAttribute('role','progressbar');wrap.setAttribute('aria-label','ExportHUB lädt');wrap.setAttribute('aria-busy','false');wrap.style.cssText='position:fixed;left:0;right:0;top:0;height:6px;z-index:2147483646;display:none;pointer-events:none;background:rgba(8,36,93,.12);overflow:hidden';var bar=document.createElement('div');bar.setAttribute('data-rc680-loading-fill','1');bar.style.cssText='height:100%;width:38%;transform:translateX(-120%);background:linear-gradient(90deg,#2563eb,#05b8ff,#2563eb);box-shadow:0 0 12px rgba(5,184,255,.65);will-change:transform,width;transition:width .18s ease';wrap.appendChild(bar);(document.body||document.documentElement).appendChild(wrap)}var label=document.getElementById('rc739LoadingStatus');if(!label){label=document.createElement('div');label.id='rc739LoadingStatus';label.setAttribute('role','status');label.setAttribute('aria-live','polite');label.setAttribute('aria-atomic','true');label.style.cssText='position:fixed;left:50%;top:10px;transform:translateX(-50%);z-index:2147483645;display:none;max-width:min(620px,calc(100vw - 24px));padding:7px 12px;border-radius:999px;background:#08245d;color:#fff;box-shadow:0 8px 24px rgba(2,20,55,.22);font:800 12px/1.25 Segoe UI,Arial,sans-serif;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;pointer-events:none';(document.body||document.documentElement).appendChild(label)}var style=document.getElementById('rc680LoadingBarStyle');if(!style){style=document.createElement('style');style.id='rc680LoadingBarStyle';style.textContent='@keyframes rc680LoadingSweep{0%{transform:translateX(-120%)}55%{transform:translateX(115%)}100%{transform:translateX(260%)}}#rc680LoadingBar[data-active="1"] [data-rc680-loading-fill="1"]{animation:rc680LoadingSweep 1.05s cubic-bezier(.4,0,.2,1) infinite}@media(max-width:760px){#rc739LoadingStatus{top:8px!important;max-width:calc(100vw - 18px)!important;font-size:11px!impo
```

### line~23160 pos=4608091
```text
0px;transform:translateX(-50%);z-index:2147483645;display:none;max-width:min(620px,calc(100vw - 24px));padding:7px 12px;border-radius:999px;background:#08245d;color:#fff;box-shadow:0 8px 24px rgba(2,20,55,.22);font:800 12px/1.25 Segoe UI,Arial,sans-serif;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;pointer-events:none';(document.body||document.documentElement).appendChild(label)}var style=document.getElementById('rc680LoadingBarStyle');if(!style){style=document.createElement('style');style.id='rc680LoadingBarStyle';style.textContent='@keyframes rc680LoadingSweep{0%{transform:translateX(-120%)}55%{transform:translateX(115%)}100%{transform:translateX(260%)}}#rc680LoadingBar[data-active="1"] [data-rc680-loading-fill="1"]{animation:rc680LoadingSweep 1.05s cubic-bezier(.4,0,.2,1) infinite}@media(max-width:760px){#rc739LoadingStatus{top:8px!important;max-width:calc(100vw - 18px)!important;font-size:11px!important;padding:6px 10px!important}}@media(prefers-reduced-motion:reduce){#rc680LoadingBar[data-active="1"] [data-rc680-loading-fill="1"]{animation-duration:2s}}';(document.head||document.documentElement).appendChild(style)}return wrap}
function loadingTask(token){return loadingBarTasks[token]||null}
function activeLoadingTask(){var keys=Object.keys(loadingBarTasks).map(Number).sort(function(a,b){return b-a});for(var i=0;i<keys.length;i++){var task=loadingBarTasks[keys[i]];if(task&&task.visible!==false)return task}return null}
function loadingBarPaintActive(){var wrap=ensureLoadingBar(),fill=wrap.querySelector('[data-rc680-loading-fill="1"]'),label=document.getElementById('rc739LoadingStatus'),task=activeLoadingTask();if(!task){return false}loadingBarText=q(task.text)||'ExportHUB arbeitet …';wrap.setAttribute('aria-label',loadingBarText);wrap.setAttribute('aria-busy','true');wrap.style.display='block';wrap.setAttribute('data-active','1');if(fill){fill.style.width='38%';fill.style.transform='translateX(-120%)';fill.style.background='linear-gradient(90deg,#2563eb,#05b8ff,#2563eb)'}if(label){label.textContent='⏳ '+loadingBarText;label.style.background='#08245d';label.style.display='block'}loadingBarVisible=true;return true}
function loadingBarStart(text,delay){var token=++loadingBarSeq,wait=Math.max(0,Number(delay)||0),task={token:token,text:q(text)||'ExportHUB arbeitet …',visible:wait<=0,startedAt:Date.now(),showTimer:0,fallbackTimer:0};loadingBarTasks[token]=task;loadingBarLastToken=token;if(loadingBarHideTimer){clearTimeout(loadingBarHideTimer);loadingBarHideTimer=0}if(wait>0)task.showTimer=setTimeout(function(){var current=loadingTask(token);if(!current)return;current.showTimer=0;current.visible=true;loadingBarPaintActive()},wait);else loadingBarPaintActive();task.fallbackTimer=setTimeout(function(){if(loadingTask(token))loadingBarDone(token,'Ladeanzeige automatisch beendet')},45000);return token}
function loadingBarStep(token,text){var task=loadingTask(token);if(!task)return false;task.text=q(text)||task.text;if(task.visible!==false)loadingBarPaintActive();return true}
function loadingBarDone(token,text,failed){var task=loadingTask(token);if(!task)return false;if(task.showTimer)clearTimeout(task.showTimer);if(task.fallbackTimer)clearTimeout(task.fallbackTimer);delete loadingBarTasks[token];var next=activeLoadingTask();if(next){loadingBarPaintActive();return true}var wrap=ensureLoadingBar(),fill=wrap.querySelector('[data-rc680-loading-fill="1"]'),label=document.getElementById('rc739LoadingStatus'),message=q(text)||(failed?'Vorgang nicht abgeschlossen':'Erledigt');wrap.removeAttribute('data-active');wrap.setAttribute('aria-bus
```

### line~23163 pos=4609888
```text
ia-busy','true');wrap.style.display='block';wrap.setAttribute('data-active','1');if(fill){fill.style.width='38%';fill.style.transform='translateX(-120%)';fill.style.background='linear-gradient(90deg,#2563eb,#05b8ff,#2563eb)'}if(label){label.textContent='⏳ '+loadingBarText;label.style.background='#08245d';label.style.display='block'}loadingBarVisible=true;return true}
function loadingBarStart(text,delay){var token=++loadingBarSeq,wait=Math.max(0,Number(delay)||0),task={token:token,text:q(text)||'ExportHUB arbeitet …',visible:wait<=0,startedAt:Date.now(),showTimer:0,fallbackTimer:0};loadingBarTasks[token]=task;loadingBarLastToken=token;if(loadingBarHideTimer){clearTimeout(loadingBarHideTimer);loadingBarHideTimer=0}if(wait>0)task.showTimer=setTimeout(function(){var current=loadingTask(token);if(!current)return;current.showTimer=0;current.visible=true;loadingBarPaintActive()},wait);else loadingBarPaintActive();task.fallbackTimer=setTimeout(function(){if(loadingTask(token))loadingBarDone(token,'Ladeanzeige automatisch beendet')},45000);return token}
function loadingBarStep(token,text){var task=loadingTask(token);if(!task)return false;task.text=q(text)||task.text;if(task.visible!==false)loadingBarPaintActive();return true}
function loadingBarDone(token,text,failed){var task=loadingTask(token);if(!task)return false;if(task.showTimer)clearTimeout(task.showTimer);if(task.fallbackTimer)clearTimeout(task.fallbackTimer);delete loadingBarTasks[token];var next=activeLoadingTask();if(next){loadingBarPaintActive();return true}var wrap=ensureLoadingBar(),fill=wrap.querySelector('[data-rc680-loading-fill="1"]'),label=document.getElementById('rc739LoadingStatus'),message=q(text)||(failed?'Vorgang nicht abgeschlossen':'Erledigt');wrap.removeAttribute('data-active');wrap.setAttribute('aria-busy','false');wrap.style.display='block';if(fill){fill.style.width='100%';fill.style.transform='translateX(0)';fill.style.background=failed?'#dc2626':'#16a34a'}if(label){label.textContent=(failed?'⚠ ':'✓ ')+message;label.style.background=failed?'#991b1b':'#166534';label.style.display='block'}loadingBarVisible=false;if(loadingBarHideTimer)clearTimeout(loadingBarHideTimer);loadingBarHideTimer=setTimeout(function(){if(activeLoadingTask())return loadingBarPaintActive();wrap.style.display='none';if(label)label.style.display='none';if(fill){fill.style.width='38%';fill.style.background='linear-gradient(90deg,#2563eb,#05b8ff,#2563eb)'}},failed?3200:1300);return true}
function loadingBarFail(token,text){return loadingBarDone(token,text||'Vorgang nicht abgeschlossen',true)}
function navigationLoadingDone(){return false}
var operationStatusSeq=0,operationStatusTimer=0,operationStatusHideTimer=0,operationStatusStartedAt=0,operationStatusText='',operationLoadingToken=0;
function operationRender(token){if(token!==operationStatusSeq)return false;var seconds=Math.max(0,Math.floor((Date.now()-operationStatusStartedAt)/1000));loadingBarStep(operationLoadingToken,operationStatusText+(seconds>0?' · '+seconds+' s':''));return true}
function operationStart(text){operationStatusSeq++;operationStatusStartedAt=Date.now();operationStatusText=q(text)||'ExportHUB arbeitet …';if(operationStatusTimer)clearInterval(operationStatusTimer);if(operationStatusHideTimer)clearTimeout(operationStatusHideTimer);operationLoadingToken=loadingBarStart(operationStatusText,0);operationRender(operationStatusSeq);var token=operationStatusSeq;operationStatusTimer=setInterval(function(){operationRender(token)},1000);operationStatusHideTimer=setTimeout(function(){if(token!==operationStatusSeq)return;if(operationStatus
```

### line~23742 pos=4796851
```text
mer){clearTimeout(processRetryTimer);processRetryTimer=0}return false}var ready=!!document.getElementById('rc543MailArea')&&!!document.getElementById('rc543AbdDecision')&&!!document.getElementById('rc543PrintStatus');if(ready){processRetryCount=0;if(processRetryTimer){clearTimeout(processRetryTimer);processRetryTimer=0}return true}try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.postProcess==='function')window.ExportHUBRC543.postProcess()}catch(e){console.error('RC399 Prozessbereiche',e)}ready=!!document.getElementById('rc543MailArea')&&!!document.getElementById('rc543AbdDecision')&&!!document.getElementById('rc543PrintStatus');if(!ready&&processRetryCount<12&&!processRetryTimer){processRetryCount++;var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;processRetryTimer=nt(function(){processRetryTimer=0;schedulePatch()},Math.min(600,80+processRetryCount*35))}return ready}
function safePatchDuringEdit(){enforceEditingCustomerLock();var block=document.getElementById('rc363BlockShipment'),body=block&&block.querySelector('.rc363-process-body');applyRC894ShipmentFieldGrid(body);ensureRC894LieferavisButton();updateSummary();updateQr();updateConfirmationButtons();return true}
function deferFullPatch(delay){if(!isShipmentView())return false;fullPatchPending=false;safePatchDuringEdit();return true}
function schedulePatch(){if(!isShipmentView())return false;var layout=document.getElementById('rc363FixedShipmentLayout');if(layout||editLockActive()){safePatchDuringEdit();return true}if(patchTimer)return true;var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;patchTimer=nt(function(){patchTimer=0;patch()},16);return true}
function patch(){if(busy){safePatchDuringEdit();return true}if(!isShipmentView())return false;var r=root();if(!r)return false;if(editLockActive()){safePatchDuringEdit();return true}busy=true;try{enforceEditingCustomerLock();var colliLocked=enforceCustomerChangeColli();cleanupOldActions();var fresh=enforceFreshDraft();if(!fresh&&!colliLocked)syncRows();enforceCustomerChangeColli();normalizeRowsDom();reindex();ensureProcessCards();canonicalMail();ensureActions();removeHints();clearChoosePrompts();applyFixedBlocks();patchSavedOpenControls();unlockEditableShipmentForm();prepareCanonicalShipmentFields();restoreLocalFieldDraft();enforceEditingCustomerLock();try{if(window.ExportHUBIndex289&&typeof window.ExportHUBIndex289.patch==='function')window.ExportHUBIndex289.patch()}catch(e){console.error('RC413 Standortauswahl',e)}enforceCustomerChangeColli();if(isFreshDraft())enforceFreshDraft();else enforceEditingCustomerLock();syncReferenceField(ensureReference(shipment()));updateSummary();updateQr();updateConfirmationButtons();unlockEditableShipmentForm();return true}finally{busy=false}}
function click(event){var t=event.target,viewOpen=t&&t.closest&&t.closest('[data-rc776-view-shipment]');if(viewOpen){var viewId=q(viewOpen.getAttribute('data-rc776-view-shipment')||viewOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();if(window.ExportHUBShipmentView&&typeof window.ExportHUBShipmentView.open==='function')return window.ExportHUBShipmentView.open(viewId,low(state().view)||'shipmentoverview');return false}var exactOpen=t&&t.closest&&t.closest('[data-rc410-open-shipment]');if(exactOpen){var exactId=q(exactOpen.getAttribute('data-rc410-open-shipment')||exactOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmed
```

### line~23742 pos=4797012
```text
cision')&&!!document.getElementById('rc543PrintStatus');if(ready){processRetryCount=0;if(processRetryTimer){clearTimeout(processRetryTimer);processRetryTimer=0}return true}try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.postProcess==='function')window.ExportHUBRC543.postProcess()}catch(e){console.error('RC399 Prozessbereiche',e)}ready=!!document.getElementById('rc543MailArea')&&!!document.getElementById('rc543AbdDecision')&&!!document.getElementById('rc543PrintStatus');if(!ready&&processRetryCount<12&&!processRetryTimer){processRetryCount++;var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;processRetryTimer=nt(function(){processRetryTimer=0;schedulePatch()},Math.min(600,80+processRetryCount*35))}return ready}
function safePatchDuringEdit(){enforceEditingCustomerLock();var block=document.getElementById('rc363BlockShipment'),body=block&&block.querySelector('.rc363-process-body');applyRC894ShipmentFieldGrid(body);ensureRC894LieferavisButton();updateSummary();updateQr();updateConfirmationButtons();return true}
function deferFullPatch(delay){if(!isShipmentView())return false;fullPatchPending=false;safePatchDuringEdit();return true}
function schedulePatch(){if(!isShipmentView())return false;var layout=document.getElementById('rc363FixedShipmentLayout');if(layout||editLockActive()){safePatchDuringEdit();return true}if(patchTimer)return true;var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;patchTimer=nt(function(){patchTimer=0;patch()},16);return true}
function patch(){if(busy){safePatchDuringEdit();return true}if(!isShipmentView())return false;var r=root();if(!r)return false;if(editLockActive()){safePatchDuringEdit();return true}busy=true;try{enforceEditingCustomerLock();var colliLocked=enforceCustomerChangeColli();cleanupOldActions();var fresh=enforceFreshDraft();if(!fresh&&!colliLocked)syncRows();enforceCustomerChangeColli();normalizeRowsDom();reindex();ensureProcessCards();canonicalMail();ensureActions();removeHints();clearChoosePrompts();applyFixedBlocks();patchSavedOpenControls();unlockEditableShipmentForm();prepareCanonicalShipmentFields();restoreLocalFieldDraft();enforceEditingCustomerLock();try{if(window.ExportHUBIndex289&&typeof window.ExportHUBIndex289.patch==='function')window.ExportHUBIndex289.patch()}catch(e){console.error('RC413 Standortauswahl',e)}enforceCustomerChangeColli();if(isFreshDraft())enforceFreshDraft();else enforceEditingCustomerLock();syncReferenceField(ensureReference(shipment()));updateSummary();updateQr();updateConfirmationButtons();unlockEditableShipmentForm();return true}finally{busy=false}}
function click(event){var t=event.target,viewOpen=t&&t.closest&&t.closest('[data-rc776-view-shipment]');if(viewOpen){var viewId=q(viewOpen.getAttribute('data-rc776-view-shipment')||viewOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();if(window.ExportHUBShipmentView&&typeof window.ExportHUBShipmentView.open==='function')return window.ExportHUBShipmentView.open(viewId,low(state().view)||'shipmentoverview');return false}var exactOpen=t&&t.closest&&t.closest('[data-rc410-open-shipment]');if(exactOpen){var exactId=q(exactOpen.getAttribute('data-rc410-open-shipment')||exactOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();return openSavedExact(exactId)}var overviewEdit=t&&t.closest&&t.closest('[data-rc409-edit-shipment="1"]');if(overviewEdit){var chosen=findSaved
```

### line~23742 pos=4797911
```text
BlockShipment'),body=block&&block.querySelector('.rc363-process-body');applyRC894ShipmentFieldGrid(body);ensureRC894LieferavisButton();updateSummary();updateQr();updateConfirmationButtons();return true}
function deferFullPatch(delay){if(!isShipmentView())return false;fullPatchPending=false;safePatchDuringEdit();return true}
function schedulePatch(){if(!isShipmentView())return false;var layout=document.getElementById('rc363FixedShipmentLayout');if(layout||editLockActive()){safePatchDuringEdit();return true}if(patchTimer)return true;var nt=window.ExportHUBClean&&window.ExportHUBClean.native&&window.ExportHUBClean.native.setTimeout||window.setTimeout;patchTimer=nt(function(){patchTimer=0;patch()},16);return true}
function patch(){if(busy){safePatchDuringEdit();return true}if(!isShipmentView())return false;var r=root();if(!r)return false;if(editLockActive()){safePatchDuringEdit();return true}busy=true;try{enforceEditingCustomerLock();var colliLocked=enforceCustomerChangeColli();cleanupOldActions();var fresh=enforceFreshDraft();if(!fresh&&!colliLocked)syncRows();enforceCustomerChangeColli();normalizeRowsDom();reindex();ensureProcessCards();canonicalMail();ensureActions();removeHints();clearChoosePrompts();applyFixedBlocks();patchSavedOpenControls();unlockEditableShipmentForm();prepareCanonicalShipmentFields();restoreLocalFieldDraft();enforceEditingCustomerLock();try{if(window.ExportHUBIndex289&&typeof window.ExportHUBIndex289.patch==='function')window.ExportHUBIndex289.patch()}catch(e){console.error('RC413 Standortauswahl',e)}enforceCustomerChangeColli();if(isFreshDraft())enforceFreshDraft();else enforceEditingCustomerLock();syncReferenceField(ensureReference(shipment()));updateSummary();updateQr();updateConfirmationButtons();unlockEditableShipmentForm();return true}finally{busy=false}}
function click(event){var t=event.target,viewOpen=t&&t.closest&&t.closest('[data-rc776-view-shipment]');if(viewOpen){var viewId=q(viewOpen.getAttribute('data-rc776-view-shipment')||viewOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();if(window.ExportHUBShipmentView&&typeof window.ExportHUBShipmentView.open==='function')return window.ExportHUBShipmentView.open(viewId,low(state().view)||'shipmentoverview');return false}var exactOpen=t&&t.closest&&t.closest('[data-rc410-open-shipment]');if(exactOpen){var exactId=q(exactOpen.getAttribute('data-rc410-open-shipment')||exactOpen.getAttribute('data-id'));event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();return openSavedExact(exactId)}var overviewEdit=t&&t.closest&&t.closest('[data-rc409-edit-shipment="1"]');if(overviewEdit){var chosen=findSavedExact(overviewEdit.getAttribute('data-id')||overviewEdit.dataset&&overviewEdit.dataset.id);event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();return openSavedExact(chosen||overviewEdit.getAttribute('data-id'))}if(!isShipmentView())return;var packagingPicker=t&&t.closest&&t.closest('.rc682-packaging-picker');if(!packagingPicker)closePackagingMenus();var packagingOption=t&&t.closest&&t.closest('[data-rc682-packaging-option]');if(packagingOption){event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();return choosePackaging(packagingOption)}var packagingToggle=t&&t.closest&&t.closest('[data-rc682-packaging-toggle]');if(packagingToggle){event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();return togglePackagingMenu(packagingToggle)}var savedControl=t&&t.closest&&t.closest('#rc573SavedCard button,#rc573SavedCard 
```

## loading
needle='loading'
count=221

### line~24210 pos=4869746
```text
root.setAttribute('data-eh-theme',theme);
 body.setAttribute('data-eh-theme',theme);
 var app=document.getElementById('app'),content=document.getElementById('content');
 if(app)app.setAttribute('data-eh-theme',theme);
 if(content)content.setAttribute('data-eh-theme',theme);
 var select=document.getElementById('ehThemeSelect');
 if(select&&select.value!==theme)select.value=theme;
 if(persist)save(userName(),theme);
 requestAnimationFrame(function(){requestAnimationFrame(function(){root.classList.remove('eh-theme-changing')})});
 return theme;
}
function ensure(){
 var top=document.querySelector('.topbar');if(!top)return false;
 var wrap=document.getElementById('ehThemeSwitch');
 if(!wrap){
  wrap=document.createElement('div');wrap.id='ehThemeSwitch';wrap.className='eh-theme-switch ghost';wrap.title='Design auswählen';
  wrap.innerHTML='<span>Design</span><select id="ehThemeSelect" aria-label="Design auswählen"></select>';
  var lang=document.getElementById('languageSelect');
  if(lang&&lang.nextSibling)top.insertBefore(wrap,lang.nextSibling);else top.appendChild(wrap);
 }
 var select=document.getElementById('ehThemeSelect');
 if(select&&!select.dataset.rc446){
  select.innerHTML=THEMES.map(function(x){return'<option value="'+x[0]+'">'+x[1]+'</option>'}).join('');
  select.dataset.rc446='1';
  select.addEventListener('change',function(event){event.stopPropagation();apply(this.value,true)});
 }
 return true;
}
function refresh(){
 ensure();
 var user=userName();
 if(user!==currentUser||!document.body.getAttribute('data-eh-theme')){currentUser=user;apply(read(user),false)}
 var content=document.getElementById('content');if(content)content.setAttribute('data-eh-theme',valid(document.body.getAttribute('data-eh-theme')));
}
function boot(){refresh()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
['exporthub:ready','exporthub:viewchange'].forEach(function(name){window.addEventListener(name,refresh)});
window.addEventListener('storage',function(e){if(e&&e.key&&e.key.indexOf('exporthub-theme-choice')===0)refresh()});
window.ExportHUBThemeSwitcher=Object.freeze({version:(window.EXPORTHUB_BUILD&&window.EXPORTHUB_BUILD.version)||'ExportHUB',themes:THEMES.map(function(x){return{id:x[0],label:x[1]}}),apply:function(t){return apply(t,true)},current:function(){return valid(document.body&&document.body.getAttribute('data-eh-theme'))},refresh:refresh});
})();
</script>
<style id="rc510-menu-color-refresh">
/* RC567: Nur noch Funktions-Akzentfarben. Das Aussehen des Menüs kommt ausschließlich aus dem Theme-System. */
.nav button[data-index321-view]{--menu-accent:#38bdf8}.nav button[data-index321-view="planning"]{--menu-accent:#a78bfa}.nav button[data-index321-view="tasks"]{--menu-accent:#22c55e}.nav button[data-index321-view="notifications"]{--menu-accent:#f59e0b}.nav button[data-index321-view="shipment"]{--menu-accent:#0ea5e9}.nav button[data-index321-view="abd"]{--menu-accent:#ef4444}.nav button[data-index321-view="shipmentoverview"]{--menu-accent:#2563eb}.nav button[data-index321-view="cmr"]{--menu-accent:#14b8a6}.nav button[data-index321-view="warehouse"]{--menu-accent:#f97316}.nav button[data-index321-view="customers"]{--menu-accent:#8b5cf6}.nav button[data-index321-view="customerfolder"]{--menu-accent:#06b6d4}.nav button[data-index321-view="pallet"]{--menu-accent:#eab308}.nav button[data-index321-view="shippingcosts"]{--menu-accent:#10b981}.nav button[data-index321-view="customs"]{--menu-accent:#6366f1}.nav button[data-index321-view="sop"]{--menu-accent:#ec4899}.nav button[data-index3
```

### line~24440 pos=4918057
```text
tive Sendung im Lager.');return false};
window.ExportHUBWarehouse=Object.freeze({version:VERSION,locations:LOCS.slice(),render:render,sync:sync,register:register,collect:collect,qrMarkup:qrMarkup,urlFor:locationUrl,dashboardWidget:dashboardWidget,sortDashboardWidgets:sortDashboardWidgets,dashboardMeta:dashboardMeta,locationFor:locationName});
function warehouseBackgroundRelevant(){return currentView()==='warehouse'}
function schedule(){clearTimeout(timer);timer=setTimeout(function(){if(warehouseBackgroundRelevant())sync(false);paintNavIcon()},900)}
function targetedRegister(e){if(document.hidden||navigator.onLine===false)return false;var d=e&&e.detail||{},sh=d.shipment||all().find(function(x){return q(x.id||x.shipmentId)===q(d.shipmentId||d.id)||q(x.ref||x.reference).toUpperCase()===q(d.reference).toUpperCase()});if(!sh)return false;setTimeout(function(){register(sh,false)},1500);return true}
['exporthub:ready','exporthub:sync','exporthub:shipment-status-visible'].forEach(function(n){window.addEventListener(n,function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()})});window.addEventListener('exporthub:viewchange',function(){if(warehouseBackgroundRelevant())schedule();else paintNavIcon()});window.addEventListener('focus',function(){if(warehouseBackgroundRelevant())schedule()});window.addEventListener('exporthub:shipment-saved',targetedRegister);window.addEventListener('exporthub:warehouse-updated',function(){if(currentView()==='dashboard'&&window.ExportHUBRC205&&typeof window.ExportHUBRC205.refreshDashboardIfChanged==='function')try{window.ExportHUBRC205.refreshDashboardIfChanged(false)}catch(_){}paintNavIcon()});setInterval(function(){if(document.visibilityState!=='hidden'&&warehouseBackgroundRelevant())sync(false)},120000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){paintNavIcon();if(warehouseBackgroundRelevant())schedule()},{once:true});else{paintNavIcon();if(warehouseBackgroundRelevant())schedule()}
})();
</script>
<style id="rc558-testportal-releasefix-style">
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #app{display:none!important;visibility:hidden!important;pointer-events:none!important}
html[data-exporthub-testportal="1"][data-rc558-login-visible="1"] #login{position:fixed!important;inset:0!important;z-index:2147483600!important;background:var(--bg,#eef6fb)!important;overflow:auto!important}
@media(max-width:720px){
 html[data-exporthub-testportal="1"] .main{padding:4px 4px max(10px,env(safe-area-inset-bottom))!important}
 html[data-exporthub-testportal="1"] #content .card{padding:8px!important;border-radius:11px!important;margin-bottom:7px!important}
 html[data-exporthub-testportal="1"] #content :is(.grid2,.grid3,.grid4,.metrics){gap:6px!important}
 html[data-exporthub-testportal="1"] #content :is(input,select,textarea){min-height:34px!important;padding:5px 7px!important;font-size:12px!important;border-radius:8px!important}
 html[data-exporthub-testportal="1"] #content textarea{min-height:64px!important}
 html[data-exporthub-testportal="1"] #content :is(button,.btn,.ghost,.soft,.danger){min-height:34px!important;padding:5px 8px!important;font-size:11px!important;border-radius:8px!important}
 html[data-exporthub-testportal="1"] #content .field{gap:2px!important;font-size:10px!important}
 html[data-exporthub-testportal="1"] #content .field>small{font-size:9px!important;line-height:1.15!important}
 html[data-exporthub-testportal="1"] #content :is(table,th,td){font-size:10.5px!important}
 html[data-exporthub-testportal="1"] #content :is(th,td){
```

### line~24882 pos=5008071
```text
ntElement).appendChild(b)}return b}
 function showRuntimeError(message){message=q(message);if(!message)return;var box=document.getElementById('rc561TestRuntimeError');if(!box){box=document.createElement('div');box.id='rc561TestRuntimeError';box.setAttribute('role','alert');(document.body||document.documentElement).appendChild(box)}box.textContent='TESTFEHLER: '+message;box.classList.add('show');clearTimeout(errorTimer);errorTimer=setTimeout(function(){box.classList.remove('show')},12000)}
 function bind(){mark();canonicalProbe();anchorTestRoute();ensureBadge();restoreUser();syncLoginMask();var login=document.getElementById('login');if(login&&window.MutationObserver)new MutationObserver(function(){syncLoginMask();restoreUser()}).observe(login,{attributes:true,attributeFilter:['class']});}
 document.addEventListener('click',function(e){var a=e.target&&e.target.closest&&e.target.closest('a[href]');if(!a||window.__EXPORTHUB_PICKUP_MODE__)return;try{var u=new URL(a.getAttribute('href'),location.href);if(u.origin===location.origin&&(u.pathname==='/'||/\/index\.html$/i.test(u.pathname))&&!/\.auth\//i.test(u.pathname)){e.preventDefault();location.assign('/test?v='+CACHE)}}catch(_){}},true);
 window.addEventListener('error',function(e){if(e&&e.message)showRuntimeError(e.message)});
 window.addEventListener('unhandledrejection',function(e){var r=e&&e.reason;showRuntimeError(r&&r.message||r)});
 ['exporthub:ready','exporthub:rendered','exporthub:viewchange','exporthub:sync'].forEach(function(n){window.addEventListener(n,function(){mark();anchorTestRoute();ensureBadge();syncLoginMask()})});
 window.addEventListener('pageshow',function(){mark();anchorTestRoute();ensureBadge()});window.addEventListener('popstate',function(){setTimeout(anchorTestRoute,0)});
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
</script>

<style id="rc776-shipment-view-style">
#content.rc776-shipment-view{max-width:1600px;margin:0 auto;padding:24px!important;min-width:0}
.rc776-page{display:grid;gap:18px;min-width:0}
.rc784-view-context{display:flex;align-items:center;gap:8px;flex-wrap:wrap;padding:2px 2px 0;color:var(--pc-muted,var(--eh-muted,#64748b));font-size:13px;font-weight:800}
.rc784-view-context button{appearance:none;border:0;background:transparent;color:var(--pc-primary,var(--eh-accent,#1d4ed8));font:inherit;font-weight:900;padding:4px 0;cursor:pointer;text-decoration:none}
.rc784-view-context button:hover{text-decoration:underline}.rc784-view-context-sep{opacity:.55}.rc784-view-context-current{color:var(--pc-text,var(--eh-text,#0f172a));font-weight:950}
.rc776-hero{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;padding:24px 26px;border:1px solid var(--pc-line,var(--eh-border,#cbd5e1));border-radius:var(--pc-radius-lg,24px);background:var(--pc-card-bg,var(--eh-surface,#fff));box-shadow:var(--pc-card-shadow,var(--eh-shadow,0 12px 32px rgba(15,23,42,.08)))}
.rc776-hero h1{margin:3px 0 5px;font-size:clamp(28px,4vw,42px);line-height:1.05;color:var(--pc-text,var(--eh-text,#0f172a))}
.rc776-kicker{font-size:11px;font-weight:950;letter-spacing:.12em;text-transform:uppercase;color:var(--pc-primary,var(--eh-accent,#1d4ed8))}
.rc776-hero-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}
.rc776-status{display:inline-flex;align-items:center;gap:7px;border-radius:999px;padding:8px 13px;font-size:13px;font-weight:950;border:1px solid var(--eh-border,#cbd5e1);background:var(--eh-surface-2,#f8fafc);color:var(--pc-text,var(--eh-text,#0f172a))}
.rc776-readonly{dis
```

### line~24913 pos=5018088
```text
lign:center;font-weight:800}@media(max-width:1050px){.rc807-result{grid-template-columns:1fr 1fr}.rc807-result-docs,.rc807-result .toolbar{grid-column:1/-1}.rc807-result .toolbar{margin:0}}@media(max-width:650px){.rc807-search-head{display:block}.rc807-search-box{grid-template-columns:1fr}.rc807-result{grid-template-columns:1fr}.rc807-result-docs,.rc807-result .toolbar{grid-column:auto}.rc807-result .toolbar button{width:100%}}

.rc786-doc-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;margin-bottom:14px}.rc786-doc-head h2{margin:0 0 3px}.rc786-doc-head small{color:var(--pc-muted,var(--eh-muted,#64748b));font-weight:800}.rc786-doc-head-actions{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}
.rc786-doc-source{margin:0 0 9px;padding:7px 9px;border-radius:var(--pc-control-radius,12px);background:var(--pc-detail-bg,var(--eh-surface-2,#f8fafc));color:var(--pc-muted,var(--eh-muted,#64748b));font-size:11px;font-weight:850}
.rc786-doc-row{display:grid;grid-template-columns:38px minmax(0,1fr) auto;gap:11px;align-items:center;padding:12px 13px;border:1px solid var(--pc-line,var(--eh-border,#dbe4ef));border-radius:var(--pc-control-radius,14px);background:var(--pc-detail-bg,var(--eh-surface-2,#f8fafc));min-width:0}.rc786-doc-icon{display:grid;place-items:center;width:36px;height:36px;border-radius:10px;background:var(--pc-primary-soft,#eef2ff);font-size:18px}.rc786-doc-main{min-width:0}.rc786-doc-main b{display:block;color:var(--pc-text,var(--eh-text,#0f172a));font-size:14px;overflow-wrap:anywhere}.rc786-doc-main small{display:block;margin-top:3px;color:var(--pc-muted,var(--eh-muted,#64748b));font-size:11px;font-weight:750;overflow-wrap:anywhere}.rc786-doc-actions{display:flex;gap:6px;flex-wrap:wrap;justify-content:flex-end}.rc786-doc-loading{padding:15px;border:1px dashed var(--pc-line,var(--eh-border,#cbd5e1));border-radius:var(--pc-control-radius,14px);color:var(--pc-muted,var(--eh-muted,#64748b));background:var(--pc-detail-bg,var(--eh-surface-2,#f8fafc));font-weight:800}
#content.rc786-document-viewer{padding:16px!important;min-width:0}.rc786-viewer-shell{min-height:calc(100vh - 150px);display:grid;grid-template-rows:auto minmax(520px,1fr);gap:12px}.rc786-viewer-head{position:sticky;top:0;z-index:4;display:flex;align-items:flex-start;justify-content:space-between;gap:18px;padding:15px 17px;border:1px solid var(--pc-line,var(--eh-border,#cbd5e1));border-radius:var(--pc-card-radius,18px);background:var(--pc-card-bg,var(--eh-surface,#fff));box-shadow:var(--pc-card-shadow,var(--eh-shadow,0 10px 28px rgba(15,23,42,.08)))}.rc786-viewer-back{appearance:none;border:0;background:transparent;color:var(--pc-primary,var(--eh-accent,#1d4ed8));padding:0;font:inherit;font-size:13px;font-weight:950;cursor:pointer}.rc786-viewer-back:hover{text-decoration:underline}.rc786-viewer-head h1{margin:7px 0 3px;color:var(--pc-text,var(--eh-text,#0f172a));font-size:20px;overflow-wrap:anywhere}.rc786-viewer-meta{color:var(--pc-muted,var(--eh-muted,#64748b));font-size:12px;font-weight:800}.rc786-viewer-actions{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}.rc786-viewer-stage{min-width:0;min-height:520px;overflow:hidden;border:1px solid var(--pc-line,var(--eh-border,#cbd5e1));border-radius:var(--pc-card-radius,18px);background:#fff}.rc786-viewer-stage iframe{display:block;width:100%;height:calc(100vh - 245px);min-height:620px;border:0;background:#fff}.rc786-viewer-error{display:grid;gap:10px;justify-items:start;align-content:center;min-height:320px;padding:30px;color:var(--pc-text,var(--eh-text,#0f172a));backgr
```

### line~24943 pos=5027491
```text
rn best}
function rows(sh){var candidates=[sh&&sh.rows,sh&&sh.colli,sh&&sh.collis,sh&&sh.packages,sh&&sh.packagingRows,sh&&sh.shipmentRows],best=[];candidates.forEach(function(x){if(arr(x).length>best.length)best=arr(x)});return best}
function num(v){var n=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(n)?n:0}function fmt(v,max){return num(v).toLocaleString('de-DE',{maximumFractionDigits:max==null?2:max})}
function fmtDate(v){v=q(v);if(!v)return'–';var d=new Date(v);if(!Number.isNaN(d.getTime()))return d.toLocaleString('de-DE',{day:'2-digit',month:'2-digit',year:'numeric',hour:/T|\d:\d/.test(v)?'2-digit':undefined,minute:/T|\d:\d/.test(v)?'2-digit':undefined}).replace(', 00:00','');var m=v.match(/^(\d{4})-(\d{2})-(\d{2})/);return m?m[3]+'.'+m[2]+'.'+m[1]:v}
function customer(sh){if(obj(sh&&sh.customer))return sh.customer;if(obj(sh&&sh.customerData))return sh.customerData;var s=state(),id=q(sh&&(sh.customerId||sh.customerAccount||sh.customerNumber)),name=low(sh&&sh.customerName);return arr(s.customers).find(function(c){return id&&[c.id,c.customerId,c.account,c.customerNumber].some(function(v){return q(v)===id})||name&&low(c.name)===name})||{}}
function status(sh){try{if(window.ExportHUBRC543&&typeof window.ExportHUBRC543.computeShipmentStatus==='function'){var st=q(window.ExportHUBRC543.computeShipmentStatus(sh));if(st)return st}}catch(_){}return q(sh&&(sh.status||sh.processStatus||sh.pickupStatus||sh.podStatus))||'Offen'}
function totals(sh){var rs=rows(sh),count=num(sh.totalColli)||rs.reduce(function(a,r){return a+num(r.count||r.qty||r.anzahl)},0),weight=num(sh.totalWeight)||rs.reduce(function(a,r){return a+num(r.weight||r.gewicht)},0),ldm=num(sh.totalLdm||sh.ldmTotal)||rs.reduce(function(a,r){var c=num(r.count||r.qty||r.anzahl)||1,v=num(r.ldm||r.loadingMeter);return a+(v*(c>0?c:1))},0);return{count:count,weight:weight,ldm:ldm}}
function field(label,value,wide){return '<div class="rc776-field '+(wide?'rc776-wide':'')+'"><span>'+esc(label)+'</span><strong>'+esc(q(value)||'–')+'</strong></div>'}
function fileUrl(f){if(!f)return'';if(typeof f==='string'){if(/^data:|^blob:|^https?:|^\/api\//i.test(f))return f;return''}var u=q(f.data||f.dataUrl||f.url||f.downloadUrl||f.contentUrl||f.blobUrl||f.href);if(u)return u;var base=q(f.base64||f.content||f.fileData);if(!base)return'';if(/^data:/i.test(base))return base;return'data:'+(q(f.type||f.mimeType)||'application/pdf')+';base64,'+base.replace(/^base64,/i,'')}
function fileName(f,fallback){if(typeof f==='string')return f;return q(f&&(f.name||f.filename||f.fileName||f.title||f.number))||fallback||''}
var GROUPS=[['deliveryFiles','Lieferscheine'],['deliveryNotesFiles','Lieferscheine'],['abdFiles','ABD'],['documents','Dokumente'],['generatedDocuments','Erzeugte Dokumente'],['files','Weitere Dateien'],['attachments','Anhänge'],['invoiceFiles','Rechnungen'],['mailAttachments','Mail-Anhänge'],['podFiles','POD']];
function generatedType(f){return q(f&&(f.type||f.documentType||f.docType||f.kind))}
function documentFallback(field,label,index,f){if(field==='generatedDocuments')return generatedType(f);return label+' '+(index+1)}
function collectDocs(sh){var out=[],seen={};GROUPS.forEach(function(g){arr(sh&&sh[g[0]]).forEach(function(f,i){var url=fileUrl(f),fallback=documentFallback(g[0],g[1],i,f),name=fileName(f,fallback);if(g[0]==='generatedDocuments'&&!url)return;if(!name)return;var key=g[0]+'|'+name+'|'+url;if(seen[key])return;seen[key]=1;out.push({group:g[1],field:g[0],index:i,name:name,file:f,url:url})})});var s=state(),sid=idOf(sh),ref=refOf(sh);arr(s.abdRequests).forEach(func
```

### line~24997 pos=5044646
```text
 (Altdaten)'}
function render(){var s=state(),sh=find(s.shipmentViewId||s.selectedShipmentId||s.activeShipmentId),root=document.getElementById('content');if(!root)return false;root.className='content rc776-shipment-view';root.setAttribute('data-exporthub-rendered-view','shipmentview');if(!sh){root.innerHTML='<div class="rc776-page"><section class="rc776-card"><h2>Sendung nicht gefunden</h2><p class="muted">Die ausgewählte Sendung ist im aktuellen Teamstand nicht mehr vorhanden.</p><button type="button" class="btn" data-rc776-back="1">Zurück</button></section></div>';return false}var c=customer(sh),t=totals(sh),rs=rows(sh),fallback=collectDocs(sh).map(normalizeFallback),id=idOf(sh)||refOf(sh),ref=refOf(sh)||id,st=status(sh),location=obj(sh.locationData)?sh.locationData:obj(sh.location)?sh.location:{},address=q(sh.recipientAddress||sh.destinationAddress||sh.deliveryAddress||location.address||c.address),actual=q(sh.actualPickupDate||sh.pickedUpAtDate||sh.pickedUpAt||sh.pickupConfirmedAt),avis=sh.customerAvisEnabled===true||sh.avisEnabled===true?'Aktiv':'Aus',abd=q(sh.abdStatus||sh.customsStatus),pod=podEvidence(sh),stow=obj(sh.stowPlan)&&arr(sh.stowPlan.placements).length?'Vorhanden':'–',reminderHtml=window.ExportHUBIndex236&&typeof window.ExportHUBIndex236.reminderButtons==='function'?window.ExportHUBIndex236.reminderButtons(sh):'';window.__EXPORTHUB_RC776_VIEW_DOCS__=fallback;window.__EXPORTHUB_RC776_VIEW_SHIPMENT__=sh;
var rowHtml=rs.length?'<div class="rc776-table-wrap"><table class="rc776-table"><thead><tr><th>Verpackung</th><th>Anzahl</th><th>Maße L × B × H</th><th>Gewicht</th><th>LDM</th></tr></thead><tbody>'+rs.map(function(r){var count=num(r.count||r.qty||r.anzahl),dims=[r.l||r.length,r.w||r.width,r.h||r.height].map(q).filter(Boolean).join(' × '),ldm=num(r.ldm||r.loadingMeter);return '<tr><td><b>'+esc(r.type||r.packaging||r.verpackung||'–')+'</b></td><td>'+esc(fmt(count,0))+'</td><td>'+esc(dims||'–')+'</td><td>'+esc(fmt(r.weight||r.gewicht,2))+' kg</td><td>'+esc(fmt(ldm*(count||1),3))+'</td></tr>'}).join('')+'</tbody></table></div>':'<div class="rc776-empty">Keine Colli-Zeilen gespeichert.</div>';
var podHtml=pod?'<div class="rc776-doc"><div><b>Unterschriebener POD / Abliefernachweis</b><small>Digitaler oder hochgeladener POD vorhanden</small></div><div class="toolbar"><button type="button" class="btn" data-id="'+esc(id)+'" data-rc628-official-pod-open="1">POD öffnen</button></div></div>':'<div class="rc776-empty">Noch kein echter POD vorhanden.</div>';
var sourceView=low(s.shipmentViewReturnView),sourceLabel=sourceView==='shipmentsearch'?'Sendungsansicht':sourceView==='customerfolder'?'Kundenordner':sourceView==='notifications'?'Benachrichtigungen':'Sendungsübersicht';root.innerHTML='<div class="rc776-page" data-shipment="'+esc(id)+'"><nav class="rc784-view-context" aria-label="Sendungsnavigation"><button type="button" data-rc784-overview="1">'+esc(sourceLabel)+'</button><span class="rc784-view-context-sep">›</span><span class="rc784-view-context-current">Sendungsansicht · '+esc(ref)+'</span></nav><section class="rc776-hero"><div><span class="rc776-kicker">Sendungsansicht · nur lesen</span><h1>'+esc(ref)+'</h1><div class="toolbar"><span class="rc776-status">'+esc(st)+'</span><span class="rc776-readonly">Keine Bearbeitung</span></div></div><div class="rc776-hero-actions"><button type="button" class="ghost" data-rc776-back="1">← Zurück</button><button type="button" class="btn" data-rc776-documents="1">Ladeliste &amp; CMR</button>'+(pod?'<button type="button" class="ghost" data-id="'+esc(id)+'" data-rc628-official-pod-open="1">POD
```

### line~24999 pos=5049951
```text
|sh.created))+field('Letzte Änderung',fmtDate(sh.updatedAt||sh._syncUpdatedAt||sh.modifiedAt))+field('Stauplan',stow)+field('Warenbeschreibung',q(sh.goodsDescription||sh.description),true)+field('Bemerkung',q(sh.comments||sh.notes||sh.remark),true)+field('Notiz',q(sh.overviewNote||sh.shipmentOverviewNote),true)+'</div></section><section class="rc776-card"><h2>Prozessstatus</h2><div class="rc776-fields">'+field('Sendungsstatus',st)+field('ABD',abd||'–')+field('Lieferavis',avis)+field('POD',pod?'Vorhanden':'Nicht vorhanden')+field('Abholung',actual?'Bestätigt':'Noch nicht bestätigt')+field('Abgeschlossen',sh.completed===true||sh.completionConfirmed===true||q(sh.completedAt)?'Ja':'Nein')+'</div></section></div><div id="rc845AvisReadHost" class="rc844-avis-host" data-rc845-avis-host="readonly" data-rc849-shipment-id="'+esc(id)+'" data-rc849-shipment-ref="'+esc(ref)+'"></div>'+(reminderHtml?'<section class="rc776-card rc820-reminder-card"><h2>Abholerinnerung</h2><p class="muted">Für diese überfällige Sendung kannst du direkt eine professionelle Erinnerung an Kunde oder Spedition öffnen. Nicht hinterlegte Empfänger sind deaktiviert.</p>'+reminderHtml+'</section>':'')+'<section class="rc776-card"><h2>Colli, Maße, Gewicht &amp; LDM</h2>'+rowHtml+'</section><div class="rc776-grid"><section class="rc776-card" id="rc786ReferenceFilesCard"><div class="rc786-doc-head"><div><h2 id="rc786ReferenceFilesTitle">📎 Dateien &amp; Dokumente</h2><small>Vorhandene Originaldateien · Ref-Ordner '+esc(ref)+'</small></div><div class="rc786-doc-head-actions"><button type="button" class="ghost" data-rc786-open-ref="1">Ref-Ordner öffnen</button><button type="button" class="ghost" data-rc776-documents="1">Ladeliste &amp; CMR</button></div></div><div id="rc786ReferenceFilesPanel"><div class="rc786-doc-loading">Vorhandene Dateien aus dem Ref-Ordner werden geladen …</div></div></section><section class="rc776-card"><h2>POD</h2>'+podHtml+'</section></div><section class="rc776-card"><h2>Statusverlauf</h2><p class="rc808-history-note">Nur tatsächliche Statuswechsel der Sendung. Technische RC543-Synchronisierungstexte werden nicht angezeigt.</p>'+historyHtml(sh)+'</section></div>';loadReferenceDocs(sh,fallback);try{if(window.ExportHUBCustomerAvis706&&typeof window.ExportHUBCustomerAvis706.render==='function')window.ExportHUBCustomerAvis706.render()}catch(e){console.error('RC845 Lieferavis in Sendungsansicht',e)}return false}
function open(value,source){var sh=find(value);if(!sh){alert('Die gespeicherte Sendung wurde nicht gefunden.');return false}var s=state(),cur=low(s.view||(document.body?document.body.getAttribute('data-exporthub-view'):'')||'shipmentoverview');s.shipmentViewId=idOf(sh)||refOf(sh);if(cur!=='shipmentview'&&cur!=='documentviewer')s.shipmentViewReturnView=source&&source!=='shipmentview'?source:cur;rc843ArmViewGuard(sh,s.shipmentViewReturnView||source||cur);try{if(typeof window.__EXPORTHUB_BIND_STATE__==='function')window.__EXPORTHUB_BIND_STATE__(s)}catch(_){}if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route('shipmentview','shipment-view-open');s.view='shipmentview';return render()}
function back(){var s=state(),target=low(s.shipmentViewReturnView)||'shipmentoverview';rc843ReleaseViewGuard('back');if(target==='shipmentview'||target==='documentviewer')target='shipmentsearch';if(window.ExportHUBRC325&&typeof window.ExportHUBRC325.route==='function')return window.ExportHUBRC325.route(target,'shipment-view-back');return false}
function overview(){var target=low(state().shipmentViewReturnView)||'shipmento
```

### line~25021 pos=5060434
```text
turn false}throw new Error('Für diese Datei ist kein vorhandener Dateiinhalt erreichbar.')}catch(e){if(seq!==viewerLoadSeq)return false;stage.innerHTML='<div class="rc786-viewer-error"><b>Datei konnte nicht geöffnet werden.</b><span>'+esc(q(e&&e.message||e))+'</span><button type="button" class="ghost" data-rc786-open-ref="1">Ref-Ordner öffnen</button></div>';return false}}
function renderViewer(){var v=viewerPersist(),d=viewerCurrentDoc(),root=document.getElementById('content');if(!root)return false;root.className='content rc786-document-viewer';root.setAttribute('data-exporthub-rendered-view','documentviewer');if(!v){root.innerHTML='<div class="rc786-viewer-shell"><div class="rc786-viewer-error"><b>Kein Dokument ausgewählt.</b><button type="button" class="btn" data-rc786-viewer-back="1">Zurück zur Sendung</button></div></div>';return false}var origin=v.source==='referenceFolder'?'Originaldatei aus Ref-Ordner':'Gespeicherte Datei aus der Sendung',meta=[q(v.documentType)||docType(v.name,''),origin,v.createdAt?fmtDate(v.createdAt):'',fmtSize(v.size)].filter(Boolean).join(' · ');root.innerHTML='<div class="rc786-viewer-shell"><header class="rc786-viewer-head"><div><button type="button" class="rc786-viewer-back" data-rc786-viewer-back="1">← Zurück zur Sendung '+esc(v.reference||'')+'</button><h1>'+esc(v.name||'Dokument')+'</h1><div class="rc786-viewer-meta">'+esc(meta)+'</div></div><div class="rc786-viewer-actions"><button type="button" class="ghost" data-rc786-viewer-download="1">Download</button>'+(isPrintable(d)?'<button type="button" class="ghost" data-rc786-viewer-print="1">Drucken</button>':'')+'<button type="button" class="ghost" data-rc786-viewer-back="1">Schließen</button></div></header><main id="rc786DocumentStage" class="rc786-viewer-stage"><div class="rc786-doc-loading">Vorhandene Datei wird geladen – es wird keine neue PDF erzeugt …</div></main></div>';viewerLoad();return false}
function viewerDownload(){var d=viewerCurrentDoc(),v=viewerPersist();return d?downloadDocObject(Object.assign({},d,{reference:v&&v.reference})):false}
function viewerPrint(){var frame=document.getElementById('rc786DocumentFrame');if(frame){try{frame.contentWindow.focus();frame.contentWindow.print();return false}catch(_){}}var v=viewerPersist();if(v){v.autoPrint=true;try{sessionStorage.setItem('exporthub_document_viewer_v1',JSON.stringify(v))}catch(_){} }viewerLoad();return false}
document.addEventListener('input',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('search',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch')return renderSearchResults(input.value)},true);
document.addEventListener('keydown',function(e){var input=e.target;if(input&&input.id==='rc807ShipmentSearch'&&e.key==='Enter'){e.preventDefault();return renderSearchResults(input.value)}},true);
document.addEventListener('click',function(e){var clearSearch=e.target&&e.target.closest&&e.target.closest('[data-rc911-search-clear]');if(clearSearch){e.preventDefault();shipmentSearchQuery='';var input=document.getElementById('rc807ShipmentSearch');if(input)input.value='';return renderSearchResults('')}var openSearch=e.target&&e.target.closest&&e.target.closest('[data-rc807-open-shipment]');if(openSearch){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();return open(openSearch.getAttribute('data-rc807-open-shipment'),'shipmentsearch')}var b=e.target&&e.target.closest&&e.target.closest('[data-rc776-back],[data-rc784-overview],[data-rc786-open-file],[data-rc7
```

## Interesting function inventory
15413: persistShipment
15426: patchShipment
15487: syncField
15526: customerFolderSaveButton
15527: ensureSingleCustomerFolderSave
15587: saveCustomerFolder
15623: afterRender
15718: reloadLatest
15751: currentView
15765: renderStats
15767: renderList
15769: render
15770: download
15772: loadDetails
17089: allShipments
17092: persisted
17098: sameShipment
17101: save
17115: shipmentLocation
17117: colliNumber
17118: rowColli
17119: colliCount
17123: samePalletShipment
17160: syncSnapshot
17162: syncOne
17163: syncAll
17165: downloadUrl
17167: syncVisible
17168: overviewVisible
17171: scheduleVisibleSync
17337: shipment
17338: isShipment
18331: shipment
18332: inShipment
18334: save
18366: stabilizeShipment
18367: uploadLabel
18403: installUploadFunctions
18416: buildLoadingRows
18593: shipmentCustomer
18597: persistSelectors
18598: applyShipmentDefault
18740: saveDraft
18814: shipment
18819: isShipmentView
18835: saveQuiet
19290: shipment
19295: updateSaved
19299: afterSave
19425: persist
19427: currentView
19431: shipmentRecipientCountry
19433: shipments
19435: findShipment
19439: navAllowed
19440: makeNavButton
19441: ensureNav
19460: activateNav
19508: shipmentKey
19509: shipmentScore
19510: activeShipmentSource
19516: activeShipmentRoute
19520: shipmentRows
19521: shipmentLoad
19523: syncCostFromShipment
19566: loadCostFromShipment
19579: saveCost
19631: renderShipping
19782: ihkLoadPdfLib
19783: ihkPdfPageLines
19784: ihkReadPdf
19785: ihkCleanPdfLine
19791: ihkPdfStatus
19792: ihkImportPdf
19797: ihkPdfEditorHtml
19798: ihkSavePdfDraft
19808: ihkOwnReviewHtml
19809: ihkShowReview
19810: ihkCloseReview
19812: renderInspections
19827: ihkDownload
19832: ihkPrintReport
19846: newColliRow
19853: colliDetails
19858: setAbdCustomerSearch
19864: abdPdfFiles
19865: latestAbdPdf
19867: shipmentForAbd
19868: linkAbdDocumentToShipment
19869: saveAbdInternal
19870: saveAbd
19875: uploadCompletedAbd
19880: prefillFromShipment
19881: customerSearchOptions
19882: colliTypeOptions
19886: renderAbd
19890: renderCurrent
19922: shipmentScore
19923: shipments
19924: findShipment
19931: persist
19932: refreshOverview
19936: officialPodShipment
19943: downloadOfficialPod
19949: openResetShipment
19950: repairShipment
19953: patchOverview
20139: patchOverviewCards
20194: sync
20576: currentView
20581: persist
20589: navigate
20590: directShipmentId
20594: findDirectShipment
20606: dashboardLayout
20641: dashboardIsTestShipmentRC818
20644: shipmentDone
20645: shipmentRef
20646: shipmentCustomer
20647: shipmentPickup
20649: dashboardFindShipment
20650: dashboardShipmentEuroPallets
20665: dashboardShipmentTruth
20666: dashboardTodayShipmentsWidget
20667: dashboardShipmentWidget
20681: shipmentSig
20697: dashboardMasonryMobile
20698: dashboardMasonryMeasure
20708: dashboardMasonrySchedule
20713: dashboardMasonryBind
20768: renderDashboard
20803: renderPlanner
21007: view
21076: persist
21134: weekOverview
21137: navigateTasks
21140: render
21178: tagColli
21216: printLoad2
21217: downloadLoad2
21313: shipmentId
21314: shipmentRef
21315: shipmentCustomer
21316: shipmentPickup
21317: shipments
21318: overdueShipments
21321: loadLocalNotificationCenter
21324: persist
21337: findShipment
21339: setViewRaw
21340: navigate
21341: openShipment
21348: reminderShipmentDetails
21380: shipmentCard
21382: renderView
21383: openView
21426: shipmentRows
21428: docShipmentCopy
21430: shipments
21433: currentSaved
21453: sameShipment
21454: fingerprint
21460: loadHtml
21468: renderHub
21479: prepareShipment
21490: printDocuments
21495: multiPagePdf
21496: downloadBlob
21504: renderer
21517: stripQrForPdf
21532: createPdf
21534: downloadDocument
21535: syncPod
21536: downloadPod
21537: routeShipment
21538: routeOverview
21540: setDocumentViewState
21568: validPdfBlob
21574: shipments
21588: mirrorShipment
21614: shipments
21617: save
21620: saved
21625: uiBusy
21627: assertOfficialPdf
21726: prepareEditorForSave
21731: afterCustomerSave
21735: linkCurrentShipment
21863: matchesShipmentKey
21864: shipmentGroups
21865: allShipments
21868: shipmentScore
21870: shipmentCopies
21871: currentShipment
21887: sanitizeShipment
21893: persist
21904: hasLoad2
21939: syncStatus
22002: allShipments
22004: shipmentScore
22005: currentShipment
22006: shipmentCopies
22007: findShipment
22009: shipmentForDocument
22010: customerForShipment
22017: savedLocationId
22018: restoreSavedLocation
22025: persistLocations
22026: saveLocationsOnly
22031: renderRecipientAddress
22035: restoreColli
22044: patchShipmentLocationSelect
22174: currentShipment
22182: persist
22188: syncRemote
22252: currentShipment
22275: shipment
22276: isShipment
22286: sync
23157: ensureLoadingBar
23158: loadingTask
23159: activeLoadingTask
23160: loadingBarPaintActive
23161: loadingBarStart
23162: loadingBarStep
23163: loadingBarDone
23164: loadingBarFail
23165: navigationLoadingDone
23167: operationRender
23176: commandLoadingAllowed
23185: shipment
23186: isShipmentView
23198: shipmentReadOnly
23199: lockedShipment
23201: restoreLockedShipmentBaseline
23203: lockShipmentForm
23204: cancelPendingEditSave
23205: needsLockSync
23206: refreshShipmentLock
23208: activeShipmentField
23211: scheduleEditSave
23212: flushEditSave
23218: syncReferenceField
23218: currentSaved
23220: startFreshShipment
23250: colliLockMatches
23251: setRowsOnShipment
23254: captureCustomerChangeColli
23255: enforceCustomerChangeColli
23256: restoreCustomerChangeColli
23259: syncRows
23285: commitColliElement
23291: saveWeight
23298: savedFlag
23332: scheduleStowRender
23360: chooseStowLayout
23400: renderStowPlan
23432: printStow
23449: canonicalColliCard
23460: applyRC894ShipmentFieldGrid
23471: rc897AvisShipment
23580: shipmentFieldLabel
23581: shipmentFieldKey
23582: writeShipmentField
23583: editShipmentKey
23588: applyCanonicalShipmentField
23589: syncVisibleShipmentFields
23590: captureShipmentField
23591: prepareCanonicalShipmentFields
23593: completeShipmentRows
23594: shipmentRowsValid
23595: newestSavedForSave
23596: paintRowsForSave
23597: reconcileLatestSavedBeforeSave
23598: restorePersistedCustomerForSave
23599: saveAction
23600: selectSaved
23601: printAll
23611: resolveShipmentCustomer
23622: shipmentLocationIdentity
23640: applyShipmentLocationIdentity
23659: sameEditingShipment
23678: findSavedExact
23679: savedFromControl
23680: bindEditingShipment
23695: patchSavedOpenControls
23696: unlockEditableShipmentForm
23697: commitEditingToSaved
23698: savedFieldValue
23699: paintEditingShipment
23705: openSavedExact
23722: openRepairShipment
23727: installExactLoadShipment
23734: syncRemoteVisibleFields
23745: queueShipmentEdit
23978: shipmentScore
23983: allShipments
24061: syncManualCloudUploads
24062: syncServer
24089: load
24111: saveBlob
24113: downloadAll
24162: save
24324: totalColliCount
24325: warehouseLoadSummary
24332: persist
24339: shipmentDateClass
24349: sync
24350: currentView
24391: moveShipmentKey
24414: shipmentBadge
24415: shipmentCard
24424: paintNavIcon
24425: render
24871: syncLoginMask
24927: rc843ReleaseViewGuard
24928: rc843ArmViewGuard
24929: rc843ViewDomOk
24930: rc843EnsureView
24931: rc843ScheduleViewGuard
24935: allShipments
24936: savedShipments
24959: isPrintable
24970: loadReferenceDocs
24972: shipmentSearchStamp
24973: shipmentSearchText
24976: shipmentSearchHayRC911
24981: shipmentSearchResults
24991: shipmentSearchResultHtml
24992: renderSearchResults
24993: renderSearch
24995: shipmentViewCreator
24996: render
25002: overview
25004: currentShipmentForDocs
25005: viewerStore
25006: viewerRead
25007: viewerClearUrl
25008: viewerContextFor
25009: routeViewer
25011: downloadDocObject
25013: downloadDoc
25014: printDoc
25017: viewerPersist
25018: viewerBack
25019: viewerCurrentDoc
25020: viewerLoad
25021: renderViewer
25022: viewerDownload
25023: viewerPrint

## Event listener counts
addEventListener\(['\"]resize['\"]: 2 lines=[15801, 20728]
addEventListener\(['\"]scroll['\"]: 0 lines=[]
addEventListener\(['\"]input['\"]: 25 lines=[2540, 2669, 2669, 2669, 2669, 2669, 2816, 14340, 14668, 15037, 15775, 18602, 18897, 19636, 20218, 20422, 21715, 22288, 23748, 23748, 23748, 23748, 24032, 24872, 25024]
addEventListener\(['\"]change['\"]: 37 lines=[13240, 14341, 14669, 14684, 15025, 15037, 15774, 18438, 18601, 18907, 19300, 19636, 20219, 20423, 20757, 21149, 21951, 21957, 22051, 22186, 22289, 23182, 23406, 23426, 23748, 23748, 23748, 23748, 24032, 24199]
addEventListener\(['\"]exporthub:viewchange['\"]: 16 lines=[47, 239, 2542, 10172, 13240, 13240, 14224, 14797, 14833, 15037, 15171, 15296, 17176, 20807, 21392, 24440]
addEventListener\(['\"]exporthub:sync['\"]: 9 lines=[85, 2543, 14797, 15037, 15171, 18640, 20808, 21392, 23755]