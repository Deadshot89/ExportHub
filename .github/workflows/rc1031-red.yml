name: RC1031 Diagnose RED
on:
  push:
    branches:
      - fix/rc1031-diagnostics-lieferavis-storage
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: RC1031 Diagnose-Zieltests
        run: node --test test/rc1031-diagnostics-lieferavis-storage.test.mjs test/rc1031-pickup-status-410-backoff.test.mjs
